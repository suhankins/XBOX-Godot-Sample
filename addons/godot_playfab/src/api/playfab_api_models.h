// PlayFab API binding file.
#ifndef GODOT_PLAYFAB_API_MODELS_H
#define GODOT_PLAYFAB_API_MODELS_H
#include <string>
#include <vector>
#include <godot_cpp/classes/object.hpp>
#include <godot_cpp/variant/array.hpp>
#include <godot_cpp/variant/dictionary.hpp>
#include <godot_cpp/variant/string.hpp>
#include <godot_cpp/variant/variant.hpp>
#include "playfab_api_helpers.h"
#include <playfab/core/PFAuthentication.h>
#include <playfab/core/PFEvents.h>
#include <playfab/core/PFTypes.h>
#include <playfab/services/PFAccountManagement.h>
#include <playfab/services/PFCatalog.h>
#include <playfab/services/PFCloudScript.h>
#include <playfab/services/PFData.h>
#include <playfab/services/PFExperimentation.h>
#include <playfab/services/PFFriends.h>
#include <playfab/services/PFGroups.h>
#include <playfab/services/PFInventory.h>
#include <playfab/services/PFLocalization.h>
#include <playfab/services/PFPlayerDataManagement.h>
#include <playfab/services/PFProfiles.h>
#include <playfab/services/PFStatistics.h>
#include <playfab/services/PFTitleDataManagement.h>
#include <playfab/services/PFTypes.h>

#ifdef CONNECT_DEFERRED
#undef CONNECT_DEFERRED
#endif

namespace godot { namespace playfab_api {
Variant to_variant_PFStringDictionaryEntry(const PFStringDictionaryEntry *p_value);
Variant to_variant_PFAccountManagementAddOrUpdateContactEmailRequest(const PFAccountManagementAddOrUpdateContactEmailRequest *p_value);
Variant to_variant_PFAccountManagementBattleNetAccountPlayFabIdPair(const PFAccountManagementBattleNetAccountPlayFabIdPair *p_value);
Variant to_variant_PFAccountManagementClientLinkBattleNetAccountRequest(const PFAccountManagementClientLinkBattleNetAccountRequest *p_value);
Variant to_variant_PFAccountManagementClientLinkNintendoServiceAccountRequest(const PFAccountManagementClientLinkNintendoServiceAccountRequest *p_value);
Variant to_variant_PFAccountManagementClientLinkPSNAccountRequest(const PFAccountManagementClientLinkPSNAccountRequest *p_value);
Variant to_variant_PFAccountManagementClientLinkXboxAccountRequest(const PFAccountManagementClientLinkXboxAccountRequest *p_value);
Variant to_variant_PFAccountManagementClientUnlinkBattleNetAccountRequest(const PFAccountManagementClientUnlinkBattleNetAccountRequest *p_value);
Variant to_variant_PFAccountManagementClientUnlinkNintendoServiceAccountRequest(const PFAccountManagementClientUnlinkNintendoServiceAccountRequest *p_value);
Variant to_variant_PFAccountManagementClientUnlinkPSNAccountRequest(const PFAccountManagementClientUnlinkPSNAccountRequest *p_value);
Variant to_variant_PFAccountManagementClientUnlinkXboxAccountRequest(const PFAccountManagementClientUnlinkXboxAccountRequest *p_value);
Variant to_variant_PFAccountManagementClientUpdateAvatarUrlRequest(const PFAccountManagementClientUpdateAvatarUrlRequest *p_value);
Variant to_variant_PFAccountManagementFacebookInstantGamesPlayFabIdPair(const PFAccountManagementFacebookInstantGamesPlayFabIdPair *p_value);
Variant to_variant_PFAccountManagementFacebookPlayFabIdPair(const PFAccountManagementFacebookPlayFabIdPair *p_value);
Variant to_variant_PFAccountManagementGameCenterPlayFabIdPair(const PFAccountManagementGameCenterPlayFabIdPair *p_value);
Variant to_variant_PFAccountManagementGetAccountInfoRequest(const PFAccountManagementGetAccountInfoRequest *p_value);
Variant to_variant_PFUserAndroidDeviceInfo(const PFUserAndroidDeviceInfo *p_value);
Variant to_variant_PFUserAppleIdInfo(const PFUserAppleIdInfo *p_value);
Variant to_variant_PFUserBattleNetInfo(const PFUserBattleNetInfo *p_value);
Variant to_variant_PFUserCustomIdInfo(const PFUserCustomIdInfo *p_value);
Variant to_variant_PFUserFacebookInfo(const PFUserFacebookInfo *p_value);
Variant to_variant_PFUserFacebookInstantGamesIdInfo(const PFUserFacebookInstantGamesIdInfo *p_value);
Variant to_variant_PFUserGameCenterInfo(const PFUserGameCenterInfo *p_value);
Variant to_variant_PFUserGoogleInfo(const PFUserGoogleInfo *p_value);
Variant to_variant_PFUserGooglePlayGamesInfo(const PFUserGooglePlayGamesInfo *p_value);
Variant to_variant_PFUserIosDeviceInfo(const PFUserIosDeviceInfo *p_value);
Variant to_variant_PFUserKongregateInfo(const PFUserKongregateInfo *p_value);
Variant to_variant_PFUserNintendoSwitchAccountIdInfo(const PFUserNintendoSwitchAccountIdInfo *p_value);
Variant to_variant_PFUserNintendoSwitchDeviceIdInfo(const PFUserNintendoSwitchDeviceIdInfo *p_value);
Variant to_variant_PFUserOpenIdInfo(const PFUserOpenIdInfo *p_value);
Variant to_variant_PFUserPrivateAccountInfo(const PFUserPrivateAccountInfo *p_value);
Variant to_variant_PFUserPsnInfo(const PFUserPsnInfo *p_value);
Variant to_variant_PFUserServerCustomIdInfo(const PFUserServerCustomIdInfo *p_value);
Variant to_variant_PFUserSteamInfo(const PFUserSteamInfo *p_value);
Variant to_variant_PFEntityKey(const PFEntityKey *p_value);
Variant to_variant_PFUserTitleInfo(const PFUserTitleInfo *p_value);
Variant to_variant_PFUserTwitchInfo(const PFUserTwitchInfo *p_value);
Variant to_variant_PFUserXboxInfo(const PFUserXboxInfo *p_value);
Variant to_variant_PFUserAccountInfo(const PFUserAccountInfo *p_value);
Variant to_variant_PFAccountManagementGetAccountInfoResult(const PFAccountManagementGetAccountInfoResult *p_value);
Variant to_variant_PFAccountManagementGetPlayFabIDsFromBattleNetAccountIdsRequest(const PFAccountManagementGetPlayFabIDsFromBattleNetAccountIdsRequest *p_value);
Variant to_variant_PFAccountManagementGetPlayFabIDsFromBattleNetAccountIdsResult(const PFAccountManagementGetPlayFabIDsFromBattleNetAccountIdsResult *p_value);
Variant to_variant_PFAccountManagementGetPlayFabIDsFromFacebookIDsRequest(const PFAccountManagementGetPlayFabIDsFromFacebookIDsRequest *p_value);
Variant to_variant_PFAccountManagementGetPlayFabIDsFromFacebookIDsResult(const PFAccountManagementGetPlayFabIDsFromFacebookIDsResult *p_value);
Variant to_variant_PFAccountManagementGetPlayFabIDsFromFacebookInstantGamesIdsRequest(const PFAccountManagementGetPlayFabIDsFromFacebookInstantGamesIdsRequest *p_value);
Variant to_variant_PFAccountManagementGetPlayFabIDsFromFacebookInstantGamesIdsResult(const PFAccountManagementGetPlayFabIDsFromFacebookInstantGamesIdsResult *p_value);
Variant to_variant_PFAccountManagementGetPlayFabIDsFromGameCenterIDsRequest(const PFAccountManagementGetPlayFabIDsFromGameCenterIDsRequest *p_value);
Variant to_variant_PFAccountManagementGetPlayFabIDsFromGameCenterIDsResult(const PFAccountManagementGetPlayFabIDsFromGameCenterIDsResult *p_value);
Variant to_variant_PFAccountManagementGetPlayFabIDsFromGoogleIDsRequest(const PFAccountManagementGetPlayFabIDsFromGoogleIDsRequest *p_value);
Variant to_variant_PFAccountManagementGooglePlayFabIdPair(const PFAccountManagementGooglePlayFabIdPair *p_value);
Variant to_variant_PFAccountManagementGetPlayFabIDsFromGoogleIDsResult(const PFAccountManagementGetPlayFabIDsFromGoogleIDsResult *p_value);
Variant to_variant_PFAccountManagementGetPlayFabIDsFromGooglePlayGamesPlayerIDsRequest(const PFAccountManagementGetPlayFabIDsFromGooglePlayGamesPlayerIDsRequest *p_value);
Variant to_variant_PFAccountManagementGooglePlayGamesPlayFabIdPair(const PFAccountManagementGooglePlayGamesPlayFabIdPair *p_value);
Variant to_variant_PFAccountManagementGetPlayFabIDsFromGooglePlayGamesPlayerIDsResult(const PFAccountManagementGetPlayFabIDsFromGooglePlayGamesPlayerIDsResult *p_value);
Variant to_variant_PFAccountManagementGetPlayFabIDsFromKongregateIDsRequest(const PFAccountManagementGetPlayFabIDsFromKongregateIDsRequest *p_value);
Variant to_variant_PFAccountManagementKongregatePlayFabIdPair(const PFAccountManagementKongregatePlayFabIdPair *p_value);
Variant to_variant_PFAccountManagementGetPlayFabIDsFromKongregateIDsResult(const PFAccountManagementGetPlayFabIDsFromKongregateIDsResult *p_value);
Variant to_variant_PFAccountManagementGetPlayFabIDsFromNintendoServiceAccountIdsRequest(const PFAccountManagementGetPlayFabIDsFromNintendoServiceAccountIdsRequest *p_value);
Variant to_variant_PFAccountManagementNintendoServiceAccountPlayFabIdPair(const PFAccountManagementNintendoServiceAccountPlayFabIdPair *p_value);
Variant to_variant_PFAccountManagementGetPlayFabIDsFromNintendoServiceAccountIdsResult(const PFAccountManagementGetPlayFabIDsFromNintendoServiceAccountIdsResult *p_value);
Variant to_variant_PFAccountManagementGetPlayFabIDsFromNintendoSwitchDeviceIdsRequest(const PFAccountManagementGetPlayFabIDsFromNintendoSwitchDeviceIdsRequest *p_value);
Variant to_variant_PFAccountManagementNintendoSwitchPlayFabIdPair(const PFAccountManagementNintendoSwitchPlayFabIdPair *p_value);
Variant to_variant_PFAccountManagementGetPlayFabIDsFromNintendoSwitchDeviceIdsResult(const PFAccountManagementGetPlayFabIDsFromNintendoSwitchDeviceIdsResult *p_value);
Variant to_variant_PFAccountManagementGetPlayFabIDsFromPSNAccountIDsRequest(const PFAccountManagementGetPlayFabIDsFromPSNAccountIDsRequest *p_value);
Variant to_variant_PFAccountManagementPSNAccountPlayFabIdPair(const PFAccountManagementPSNAccountPlayFabIdPair *p_value);
Variant to_variant_PFAccountManagementGetPlayFabIDsFromPSNAccountIDsResult(const PFAccountManagementGetPlayFabIDsFromPSNAccountIDsResult *p_value);
Variant to_variant_PFAccountManagementGetPlayFabIDsFromPSNOnlineIDsRequest(const PFAccountManagementGetPlayFabIDsFromPSNOnlineIDsRequest *p_value);
Variant to_variant_PFAccountManagementPSNOnlinePlayFabIdPair(const PFAccountManagementPSNOnlinePlayFabIdPair *p_value);
Variant to_variant_PFAccountManagementGetPlayFabIDsFromPSNOnlineIDsResult(const PFAccountManagementGetPlayFabIDsFromPSNOnlineIDsResult *p_value);
Variant to_variant_PFAccountManagementGetPlayFabIDsFromSteamIDsRequest(const PFAccountManagementGetPlayFabIDsFromSteamIDsRequest *p_value);
Variant to_variant_PFAccountManagementSteamPlayFabIdPair(const PFAccountManagementSteamPlayFabIdPair *p_value);
Variant to_variant_PFAccountManagementGetPlayFabIDsFromSteamIDsResult(const PFAccountManagementGetPlayFabIDsFromSteamIDsResult *p_value);
Variant to_variant_PFAccountManagementGetPlayFabIDsFromSteamNamesRequest(const PFAccountManagementGetPlayFabIDsFromSteamNamesRequest *p_value);
Variant to_variant_PFAccountManagementSteamNamePlayFabIdPair(const PFAccountManagementSteamNamePlayFabIdPair *p_value);
Variant to_variant_PFAccountManagementGetPlayFabIDsFromSteamNamesResult(const PFAccountManagementGetPlayFabIDsFromSteamNamesResult *p_value);
Variant to_variant_PFAccountManagementGetPlayFabIDsFromTwitchIDsRequest(const PFAccountManagementGetPlayFabIDsFromTwitchIDsRequest *p_value);
Variant to_variant_PFAccountManagementTwitchPlayFabIdPair(const PFAccountManagementTwitchPlayFabIdPair *p_value);
Variant to_variant_PFAccountManagementGetPlayFabIDsFromTwitchIDsResult(const PFAccountManagementGetPlayFabIDsFromTwitchIDsResult *p_value);
Variant to_variant_PFAccountManagementGetPlayFabIDsFromXboxLiveIDsRequest(const PFAccountManagementGetPlayFabIDsFromXboxLiveIDsRequest *p_value);
Variant to_variant_PFAccountManagementXboxLiveAccountPlayFabIdPair(const PFAccountManagementXboxLiveAccountPlayFabIdPair *p_value);
Variant to_variant_PFAccountManagementGetPlayFabIDsFromXboxLiveIDsResult(const PFAccountManagementGetPlayFabIDsFromXboxLiveIDsResult *p_value);
Variant to_variant_PFPlayerProfileViewConstraints(const PFPlayerProfileViewConstraints *p_value);
Variant to_variant_PFGetPlayerCombinedInfoRequestParams(const PFGetPlayerCombinedInfoRequestParams *p_value);
Variant to_variant_PFAccountManagementGetPlayerCombinedInfoRequest(const PFAccountManagementGetPlayerCombinedInfoRequest *p_value);
Variant to_variant_PFItemInstance(const PFItemInstance *p_value);
Variant to_variant_PFCharacterInventory(const PFCharacterInventory *p_value);
Variant to_variant_PFCharacterResult(const PFCharacterResult *p_value);
Variant to_variant_PFAdCampaignAttributionModel(const PFAdCampaignAttributionModel *p_value);
Variant to_variant_PFContactEmailInfoModel(const PFContactEmailInfoModel *p_value);
Variant to_variant_PFLinkedPlatformAccountModel(const PFLinkedPlatformAccountModel *p_value);
Variant to_variant_PFLocationModel(const PFLocationModel *p_value);
Variant to_variant_PFSubscriptionModel(const PFSubscriptionModel *p_value);
Variant to_variant_PFMembershipModel(const PFMembershipModel *p_value);
Variant to_variant_PFPushNotificationRegistrationModel(const PFPushNotificationRegistrationModel *p_value);
Variant to_variant_PFStatisticModel(const PFStatisticModel *p_value);
Variant to_variant_PFTagModel(const PFTagModel *p_value);
Variant to_variant_PFValueToDateModel(const PFValueToDateModel *p_value);
Variant to_variant_PFPlayerProfileModel(const PFPlayerProfileModel *p_value);
Variant to_variant_PFStatisticValue(const PFStatisticValue *p_value);
Variant to_variant_PFUserDataRecord(const PFUserDataRecord *p_value);
Variant to_variant_PFUserDataRecordDictionaryEntry(const PFUserDataRecordDictionaryEntry *p_value);
Variant to_variant_PFInt32DictionaryEntry(const PFInt32DictionaryEntry *p_value);
Variant to_variant_PFVirtualCurrencyRechargeTime(const PFVirtualCurrencyRechargeTime *p_value);
Variant to_variant_PFVirtualCurrencyRechargeTimeDictionaryEntry(const PFVirtualCurrencyRechargeTimeDictionaryEntry *p_value);
Variant to_variant_PFGetPlayerCombinedInfoResultPayload(const PFGetPlayerCombinedInfoResultPayload *p_value);
Variant to_variant_PFAccountManagementGetPlayerCombinedInfoResult(const PFAccountManagementGetPlayerCombinedInfoResult *p_value);
Variant to_variant_PFAccountManagementGetPlayerProfileRequest(const PFAccountManagementGetPlayerProfileRequest *p_value);
Variant to_variant_PFAccountManagementGetPlayerProfileResult(const PFAccountManagementGetPlayerProfileResult *p_value);
Variant to_variant_PFEntityLineage(const PFEntityLineage *p_value);
Variant to_variant_PFEntityLineageDictionaryEntry(const PFEntityLineageDictionaryEntry *p_value);
Variant to_variant_PFAccountManagementGetTitlePlayersFromProviderIDsResponse(const PFAccountManagementGetTitlePlayersFromProviderIDsResponse *p_value);
Variant to_variant_PFAccountManagementGetTitlePlayersFromXboxLiveIDsRequest(const PFAccountManagementGetTitlePlayersFromXboxLiveIDsRequest *p_value);
Variant to_variant_PFAccountManagementLinkCustomIDRequest(const PFAccountManagementLinkCustomIDRequest *p_value);
Variant to_variant_PFAccountManagementLinkOpenIdConnectRequest(const PFAccountManagementLinkOpenIdConnectRequest *p_value);
Variant to_variant_PFAccountManagementLinkSteamAccountRequest(const PFAccountManagementLinkSteamAccountRequest *p_value);
Variant to_variant_PFAccountManagementRemoveContactEmailRequest(const PFAccountManagementRemoveContactEmailRequest *p_value);
Variant to_variant_PFAccountManagementReportPlayerClientRequest(const PFAccountManagementReportPlayerClientRequest *p_value);
Variant to_variant_PFAccountManagementReportPlayerClientResult(const PFAccountManagementReportPlayerClientResult *p_value);
Variant to_variant_PFAccountManagementSetDisplayNameRequest(const PFAccountManagementSetDisplayNameRequest *p_value);
Variant to_variant_PFAccountManagementSetDisplayNameResponse(const PFAccountManagementSetDisplayNameResponse *p_value);
Variant to_variant_PFAccountManagementUnlinkCustomIDRequest(const PFAccountManagementUnlinkCustomIDRequest *p_value);
Variant to_variant_PFAccountManagementUnlinkOpenIdConnectRequest(const PFAccountManagementUnlinkOpenIdConnectRequest *p_value);
Variant to_variant_PFAccountManagementUnlinkSteamAccountRequest(const PFAccountManagementUnlinkSteamAccountRequest *p_value);
Variant to_variant_PFAccountManagementUpdateUserTitleDisplayNameRequest(const PFAccountManagementUpdateUserTitleDisplayNameRequest *p_value);
Variant to_variant_PFAccountManagementUpdateUserTitleDisplayNameResult(const PFAccountManagementUpdateUserTitleDisplayNameResult *p_value);
Variant to_variant_PFCatalogCatalogAlternateId(const PFCatalogCatalogAlternateId *p_value);
Variant to_variant_PFCatalogCatalogSpecificConfig(const PFCatalogCatalogSpecificConfig *p_value);
Variant to_variant_PFCatalogDeepLinkFormat(const PFCatalogDeepLinkFormat *p_value);
Variant to_variant_PFCatalogDisplayPropertyIndexInfo(const PFCatalogDisplayPropertyIndexInfo *p_value);
Variant to_variant_PFCatalogFileConfig(const PFCatalogFileConfig *p_value);
Variant to_variant_PFCatalogImageConfig(const PFCatalogImageConfig *p_value);
Variant to_variant_PFCatalogCategoryRatingConfig(const PFCatalogCategoryRatingConfig *p_value);
Variant to_variant_PFCatalogReviewConfig(const PFCatalogReviewConfig *p_value);
Variant to_variant_PFCatalogCatalogConfig(const PFCatalogCatalogConfig *p_value);
Variant to_variant_PFCatalogContent(const PFCatalogContent *p_value);
Variant to_variant_PFCatalogDeepLink(const PFCatalogDeepLink *p_value);
Variant to_variant_PFJsonObject(const PFJsonObject *p_value);
Variant to_variant_PFCatalogImage(const PFCatalogImage *p_value);
Variant to_variant_PFCatalogCatalogPriceAmount(const PFCatalogCatalogPriceAmount *p_value);
Variant to_variant_PFCatalogCatalogPrice(const PFCatalogCatalogPrice *p_value);
Variant to_variant_PFCatalogCatalogPriceOptions(const PFCatalogCatalogPriceOptions *p_value);
Variant to_variant_PFCatalogCatalogItemReference(const PFCatalogCatalogItemReference *p_value);
Variant to_variant_PFCatalogKeywordSet(const PFCatalogKeywordSet *p_value);
Variant to_variant_PFCatalogKeywordSetDictionaryEntry(const PFCatalogKeywordSetDictionaryEntry *p_value);
Variant to_variant_PFCatalogModerationState(const PFCatalogModerationState *p_value);
Variant to_variant_PFCatalogRating(const PFCatalogRating *p_value);
Variant to_variant_PFCatalogRealMoneyPriceDetails(const PFCatalogRealMoneyPriceDetails *p_value);
Variant to_variant_PFCatalogFilterOptions(const PFCatalogFilterOptions *p_value);
Variant to_variant_PFCatalogPermissions(const PFCatalogPermissions *p_value);
Variant to_variant_PFCatalogCatalogPriceAmountOverride(const PFCatalogCatalogPriceAmountOverride *p_value);
Variant to_variant_PFCatalogCatalogPriceOverride(const PFCatalogCatalogPriceOverride *p_value);
Variant to_variant_PFCatalogCatalogPriceOptionsOverride(const PFCatalogCatalogPriceOptionsOverride *p_value);
Variant to_variant_PFCatalogStoreDetails(const PFCatalogStoreDetails *p_value);
Variant to_variant_PFCatalogCatalogItem(const PFCatalogCatalogItem *p_value);
Variant to_variant_PFCatalogCreateDraftItemRequest(const PFCatalogCreateDraftItemRequest *p_value);
Variant to_variant_PFCatalogCreateDraftItemResponse(const PFCatalogCreateDraftItemResponse *p_value);
Variant to_variant_PFCatalogUploadInfo(const PFCatalogUploadInfo *p_value);
Variant to_variant_PFCatalogCreateUploadUrlsRequest(const PFCatalogCreateUploadUrlsRequest *p_value);
Variant to_variant_PFCatalogUploadUrlMetadata(const PFCatalogUploadUrlMetadata *p_value);
Variant to_variant_PFCatalogCreateUploadUrlsResponse(const PFCatalogCreateUploadUrlsResponse *p_value);
Variant to_variant_PFCatalogDeleteEntityItemReviewsRequest(const PFCatalogDeleteEntityItemReviewsRequest *p_value);
Variant to_variant_PFCatalogDeleteItemRequest(const PFCatalogDeleteItemRequest *p_value);
Variant to_variant_PFCatalogGetCatalogConfigRequest(const PFCatalogGetCatalogConfigRequest *p_value);
Variant to_variant_PFCatalogGetCatalogConfigResponse(const PFCatalogGetCatalogConfigResponse *p_value);
Variant to_variant_PFCatalogGetDraftItemRequest(const PFCatalogGetDraftItemRequest *p_value);
Variant to_variant_PFCatalogGetDraftItemResponse(const PFCatalogGetDraftItemResponse *p_value);
Variant to_variant_PFCatalogGetDraftItemsRequest(const PFCatalogGetDraftItemsRequest *p_value);
Variant to_variant_PFCatalogGetDraftItemsResponse(const PFCatalogGetDraftItemsResponse *p_value);
Variant to_variant_PFCatalogGetEntityDraftItemsRequest(const PFCatalogGetEntityDraftItemsRequest *p_value);
Variant to_variant_PFCatalogGetEntityDraftItemsResponse(const PFCatalogGetEntityDraftItemsResponse *p_value);
Variant to_variant_PFCatalogGetEntityItemReviewRequest(const PFCatalogGetEntityItemReviewRequest *p_value);
Variant to_variant_PFCatalogReview(const PFCatalogReview *p_value);
Variant to_variant_PFCatalogGetEntityItemReviewResponse(const PFCatalogGetEntityItemReviewResponse *p_value);
Variant to_variant_PFCatalogGetItemContainersRequest(const PFCatalogGetItemContainersRequest *p_value);
Variant to_variant_PFCatalogGetItemContainersResponse(const PFCatalogGetItemContainersResponse *p_value);
Variant to_variant_PFCatalogGetItemModerationStateRequest(const PFCatalogGetItemModerationStateRequest *p_value);
Variant to_variant_PFCatalogGetItemModerationStateResponse(const PFCatalogGetItemModerationStateResponse *p_value);
Variant to_variant_PFCatalogGetItemPublishStatusRequest(const PFCatalogGetItemPublishStatusRequest *p_value);
Variant to_variant_PFCatalogGetItemPublishStatusResponse(const PFCatalogGetItemPublishStatusResponse *p_value);
Variant to_variant_PFCatalogGetItemRequest(const PFCatalogGetItemRequest *p_value);
Variant to_variant_PFCatalogGetItemResponse(const PFCatalogGetItemResponse *p_value);
Variant to_variant_PFCatalogGetItemReviewSummaryRequest(const PFCatalogGetItemReviewSummaryRequest *p_value);
Variant to_variant_PFCatalogGetItemReviewSummaryResponse(const PFCatalogGetItemReviewSummaryResponse *p_value);
Variant to_variant_PFCatalogGetItemReviewsRequest(const PFCatalogGetItemReviewsRequest *p_value);
Variant to_variant_PFCatalogGetItemReviewsResponse(const PFCatalogGetItemReviewsResponse *p_value);
Variant to_variant_PFCatalogGetItemsRequest(const PFCatalogGetItemsRequest *p_value);
Variant to_variant_PFCatalogGetItemsResponse(const PFCatalogGetItemsResponse *p_value);
Variant to_variant_PFCatalogPublishDraftItemRequest(const PFCatalogPublishDraftItemRequest *p_value);
Variant to_variant_PFCatalogReportItemRequest(const PFCatalogReportItemRequest *p_value);
Variant to_variant_PFCatalogReportItemReviewRequest(const PFCatalogReportItemReviewRequest *p_value);
Variant to_variant_PFCatalogReviewItemRequest(const PFCatalogReviewItemRequest *p_value);
Variant to_variant_PFCatalogReviewTakedown(const PFCatalogReviewTakedown *p_value);
Variant to_variant_PFCatalogStoreReference(const PFCatalogStoreReference *p_value);
Variant to_variant_PFCatalogSearchItemsRequest(const PFCatalogSearchItemsRequest *p_value);
Variant to_variant_PFCatalogSearchItemsResponse(const PFCatalogSearchItemsResponse *p_value);
Variant to_variant_PFCatalogSetItemModerationStateRequest(const PFCatalogSetItemModerationStateRequest *p_value);
Variant to_variant_PFCatalogSubmitItemReviewVoteRequest(const PFCatalogSubmitItemReviewVoteRequest *p_value);
Variant to_variant_PFCatalogTakedownItemReviewsRequest(const PFCatalogTakedownItemReviewsRequest *p_value);
Variant to_variant_PFCatalogUpdateCatalogConfigRequest(const PFCatalogUpdateCatalogConfigRequest *p_value);
Variant to_variant_PFCatalogUpdateDraftItemRequest(const PFCatalogUpdateDraftItemRequest *p_value);
Variant to_variant_PFCatalogUpdateDraftItemResponse(const PFCatalogUpdateDraftItemResponse *p_value);
Variant to_variant_PFCloudScriptExecuteCloudScriptRequest(const PFCloudScriptExecuteCloudScriptRequest *p_value);
Variant to_variant_PFCloudScriptScriptExecutionError(const PFCloudScriptScriptExecutionError *p_value);
Variant to_variant_PFCloudScriptLogStatement(const PFCloudScriptLogStatement *p_value);
Variant to_variant_PFCloudScriptExecuteCloudScriptResult(const PFCloudScriptExecuteCloudScriptResult *p_value);
Variant to_variant_PFCloudScriptExecuteEntityCloudScriptRequest(const PFCloudScriptExecuteEntityCloudScriptRequest *p_value);
Variant to_variant_PFCloudScriptExecuteFunctionRequest(const PFCloudScriptExecuteFunctionRequest *p_value);
Variant to_variant_PFCloudScriptFunctionExecutionError(const PFCloudScriptFunctionExecutionError *p_value);
Variant to_variant_PFCloudScriptExecuteFunctionResult(const PFCloudScriptExecuteFunctionResult *p_value);
Variant to_variant_PFDataAbortFileUploadsRequest(const PFDataAbortFileUploadsRequest *p_value);
Variant to_variant_PFDataAbortFileUploadsResponse(const PFDataAbortFileUploadsResponse *p_value);
Variant to_variant_PFDataDeleteFilesRequest(const PFDataDeleteFilesRequest *p_value);
Variant to_variant_PFDataDeleteFilesResponse(const PFDataDeleteFilesResponse *p_value);
Variant to_variant_PFDataFinalizeFileUploadsRequest(const PFDataFinalizeFileUploadsRequest *p_value);
Variant to_variant_PFDataGetFileMetadata(const PFDataGetFileMetadata *p_value);
Variant to_variant_PFDataGetFileMetadataDictionaryEntry(const PFDataGetFileMetadataDictionaryEntry *p_value);
Variant to_variant_PFDataFinalizeFileUploadsResponse(const PFDataFinalizeFileUploadsResponse *p_value);
Variant to_variant_PFDataGetFilesRequest(const PFDataGetFilesRequest *p_value);
Variant to_variant_PFDataGetFilesResponse(const PFDataGetFilesResponse *p_value);
Variant to_variant_PFDataGetObjectsRequest(const PFDataGetObjectsRequest *p_value);
Variant to_variant_PFDataObjectResult(const PFDataObjectResult *p_value);
Variant to_variant_PFDataObjectResultDictionaryEntry(const PFDataObjectResultDictionaryEntry *p_value);
Variant to_variant_PFDataGetObjectsResponse(const PFDataGetObjectsResponse *p_value);
Variant to_variant_PFDataInitiateFileUploadMetadata(const PFDataInitiateFileUploadMetadata *p_value);
Variant to_variant_PFDataInitiateFileUploadsRequest(const PFDataInitiateFileUploadsRequest *p_value);
Variant to_variant_PFDataInitiateFileUploadsResponse(const PFDataInitiateFileUploadsResponse *p_value);
Variant to_variant_PFDataSetObject(const PFDataSetObject *p_value);
Variant to_variant_PFDataSetObjectInfo(const PFDataSetObjectInfo *p_value);
Variant to_variant_PFDataSetObjectsRequest(const PFDataSetObjectsRequest *p_value);
Variant to_variant_PFDataSetObjectsResponse(const PFDataSetObjectsResponse *p_value);
Variant to_variant_PFEntityKeyDictionaryEntry(const PFEntityKeyDictionaryEntry *p_value);
Variant to_variant_PFEntityStatisticValue(const PFEntityStatisticValue *p_value);
Variant to_variant_PFEntityStatisticValueDictionaryEntry(const PFEntityStatisticValueDictionaryEntry *p_value);
Variant to_variant_PFExperimentationGetTreatmentAssignmentRequest(const PFExperimentationGetTreatmentAssignmentRequest *p_value);
Variant to_variant_PFVariable(const PFVariable *p_value);
Variant to_variant_PFTreatmentAssignment(const PFTreatmentAssignment *p_value);
Variant to_variant_PFExperimentationGetTreatmentAssignmentResult(const PFExperimentationGetTreatmentAssignmentResult *p_value);
Variant to_variant_PFFriendsAddFriendResult(const PFFriendsAddFriendResult *p_value);
Variant to_variant_PFFriendsClientAddFriendRequest(const PFFriendsClientAddFriendRequest *p_value);
Variant to_variant_PFFriendsClientGetFriendsListRequest(const PFFriendsClientGetFriendsListRequest *p_value);
Variant to_variant_PFFriendsClientRemoveFriendRequest(const PFFriendsClientRemoveFriendRequest *p_value);
Variant to_variant_PFFriendsClientSetFriendTagsRequest(const PFFriendsClientSetFriendTagsRequest *p_value);
Variant to_variant_PFFriendsFriendInfo(const PFFriendsFriendInfo *p_value);
Variant to_variant_PFFriendsGetFriendsListResult(const PFFriendsGetFriendsListResult *p_value);
Variant to_variant_PFGroupsAcceptGroupApplicationRequest(const PFGroupsAcceptGroupApplicationRequest *p_value);
Variant to_variant_PFGroupsAcceptGroupInvitationRequest(const PFGroupsAcceptGroupInvitationRequest *p_value);
Variant to_variant_PFGroupsAddMembersRequest(const PFGroupsAddMembersRequest *p_value);
Variant to_variant_PFGroupsApplyToGroupRequest(const PFGroupsApplyToGroupRequest *p_value);
Variant to_variant_PFGroupsEntityWithLineage(const PFGroupsEntityWithLineage *p_value);
Variant to_variant_PFGroupsApplyToGroupResponse(const PFGroupsApplyToGroupResponse *p_value);
Variant to_variant_PFGroupsBlockEntityRequest(const PFGroupsBlockEntityRequest *p_value);
Variant to_variant_PFGroupsChangeMemberRoleRequest(const PFGroupsChangeMemberRoleRequest *p_value);
Variant to_variant_PFGroupsCreateGroupRequest(const PFGroupsCreateGroupRequest *p_value);
Variant to_variant_PFGroupsCreateGroupResponse(const PFGroupsCreateGroupResponse *p_value);
Variant to_variant_PFGroupsCreateGroupRoleRequest(const PFGroupsCreateGroupRoleRequest *p_value);
Variant to_variant_PFGroupsCreateGroupRoleResponse(const PFGroupsCreateGroupRoleResponse *p_value);
Variant to_variant_PFGroupsDeleteGroupRequest(const PFGroupsDeleteGroupRequest *p_value);
Variant to_variant_PFGroupsDeleteRoleRequest(const PFGroupsDeleteRoleRequest *p_value);
Variant to_variant_PFGroupsEntityMemberRole(const PFGroupsEntityMemberRole *p_value);
Variant to_variant_PFGroupsGetGroupRequest(const PFGroupsGetGroupRequest *p_value);
Variant to_variant_PFGroupsGetGroupResponse(const PFGroupsGetGroupResponse *p_value);
Variant to_variant_PFGroupsGroupApplication(const PFGroupsGroupApplication *p_value);
Variant to_variant_PFGroupsGroupBlock(const PFGroupsGroupBlock *p_value);
Variant to_variant_PFGroupsGroupInvitation(const PFGroupsGroupInvitation *p_value);
Variant to_variant_PFGroupsGroupRole(const PFGroupsGroupRole *p_value);
Variant to_variant_PFGroupsGroupWithRoles(const PFGroupsGroupWithRoles *p_value);
Variant to_variant_PFGroupsInviteToGroupRequest(const PFGroupsInviteToGroupRequest *p_value);
Variant to_variant_PFGroupsInviteToGroupResponse(const PFGroupsInviteToGroupResponse *p_value);
Variant to_variant_PFGroupsIsMemberRequest(const PFGroupsIsMemberRequest *p_value);
Variant to_variant_PFGroupsIsMemberResponse(const PFGroupsIsMemberResponse *p_value);
Variant to_variant_PFGroupsListGroupApplicationsRequest(const PFGroupsListGroupApplicationsRequest *p_value);
Variant to_variant_PFGroupsListGroupApplicationsResponse(const PFGroupsListGroupApplicationsResponse *p_value);
Variant to_variant_PFGroupsListGroupBlocksRequest(const PFGroupsListGroupBlocksRequest *p_value);
Variant to_variant_PFGroupsListGroupBlocksResponse(const PFGroupsListGroupBlocksResponse *p_value);
Variant to_variant_PFGroupsListGroupInvitationsRequest(const PFGroupsListGroupInvitationsRequest *p_value);
Variant to_variant_PFGroupsListGroupInvitationsResponse(const PFGroupsListGroupInvitationsResponse *p_value);
Variant to_variant_PFGroupsListGroupMembersRequest(const PFGroupsListGroupMembersRequest *p_value);
Variant to_variant_PFGroupsListGroupMembersResponse(const PFGroupsListGroupMembersResponse *p_value);
Variant to_variant_PFGroupsListMembershipOpportunitiesRequest(const PFGroupsListMembershipOpportunitiesRequest *p_value);
Variant to_variant_PFGroupsListMembershipOpportunitiesResponse(const PFGroupsListMembershipOpportunitiesResponse *p_value);
Variant to_variant_PFGroupsListMembershipRequest(const PFGroupsListMembershipRequest *p_value);
Variant to_variant_PFGroupsListMembershipResponse(const PFGroupsListMembershipResponse *p_value);
Variant to_variant_PFGroupsRemoveGroupApplicationRequest(const PFGroupsRemoveGroupApplicationRequest *p_value);
Variant to_variant_PFGroupsRemoveGroupInvitationRequest(const PFGroupsRemoveGroupInvitationRequest *p_value);
Variant to_variant_PFGroupsRemoveMembersRequest(const PFGroupsRemoveMembersRequest *p_value);
Variant to_variant_PFGroupsUnblockEntityRequest(const PFGroupsUnblockEntityRequest *p_value);
Variant to_variant_PFGroupsUpdateGroupRequest(const PFGroupsUpdateGroupRequest *p_value);
Variant to_variant_PFGroupsUpdateGroupResponse(const PFGroupsUpdateGroupResponse *p_value);
Variant to_variant_PFGroupsUpdateGroupRoleRequest(const PFGroupsUpdateGroupRoleRequest *p_value);
Variant to_variant_PFGroupsUpdateGroupRoleResponse(const PFGroupsUpdateGroupRoleResponse *p_value);
Variant to_variant_PFInventoryAlternateId(const PFInventoryAlternateId *p_value);
Variant to_variant_PFInventoryInventoryItemReference(const PFInventoryInventoryItemReference *p_value);
Variant to_variant_PFInventoryInitialValues(const PFInventoryInitialValues *p_value);
Variant to_variant_PFInventoryAddInventoryItemsOperation(const PFInventoryAddInventoryItemsOperation *p_value);
Variant to_variant_PFInventoryAddInventoryItemsRequest(const PFInventoryAddInventoryItemsRequest *p_value);
Variant to_variant_PFInventoryAddInventoryItemsResponse(const PFInventoryAddInventoryItemsResponse *p_value);
Variant to_variant_PFInventoryDeleteInventoryCollectionRequest(const PFInventoryDeleteInventoryCollectionRequest *p_value);
Variant to_variant_PFInventoryDeleteInventoryItemsOperation(const PFInventoryDeleteInventoryItemsOperation *p_value);
Variant to_variant_PFInventoryDeleteInventoryItemsRequest(const PFInventoryDeleteInventoryItemsRequest *p_value);
Variant to_variant_PFInventoryDeleteInventoryItemsResponse(const PFInventoryDeleteInventoryItemsResponse *p_value);
Variant to_variant_PFInventoryPurchasePriceAmount(const PFInventoryPurchasePriceAmount *p_value);
Variant to_variant_PFInventoryPurchaseInventoryItemsOperation(const PFInventoryPurchaseInventoryItemsOperation *p_value);
Variant to_variant_PFInventorySubtractInventoryItemsOperation(const PFInventorySubtractInventoryItemsOperation *p_value);
Variant to_variant_PFInventoryTransferInventoryItemsOperation(const PFInventoryTransferInventoryItemsOperation *p_value);
Variant to_variant_PFInventoryInventoryItem(const PFInventoryInventoryItem *p_value);
Variant to_variant_PFInventoryUpdateInventoryItemsOperation(const PFInventoryUpdateInventoryItemsOperation *p_value);
Variant to_variant_PFInventoryInventoryOperation(const PFInventoryInventoryOperation *p_value);
Variant to_variant_PFInventoryExecuteInventoryOperationsRequest(const PFInventoryExecuteInventoryOperationsRequest *p_value);
Variant to_variant_PFInventoryExecuteInventoryOperationsResponse(const PFInventoryExecuteInventoryOperationsResponse *p_value);
Variant to_variant_PFInventoryExecuteTransferOperationsRequest(const PFInventoryExecuteTransferOperationsRequest *p_value);
Variant to_variant_PFInventoryExecuteTransferOperationsResponse(const PFInventoryExecuteTransferOperationsResponse *p_value);
Variant to_variant_PFInventoryGetInventoryCollectionIdsRequest(const PFInventoryGetInventoryCollectionIdsRequest *p_value);
Variant to_variant_PFInventoryGetInventoryCollectionIdsResponse(const PFInventoryGetInventoryCollectionIdsResponse *p_value);
Variant to_variant_PFInventoryGetInventoryItemsRequest(const PFInventoryGetInventoryItemsRequest *p_value);
Variant to_variant_PFInventoryGetInventoryItemsResponse(const PFInventoryGetInventoryItemsResponse *p_value);
Variant to_variant_PFInventoryGetInventoryOperationStatusRequest(const PFInventoryGetInventoryOperationStatusRequest *p_value);
Variant to_variant_PFInventoryGetInventoryOperationStatusResponse(const PFInventoryGetInventoryOperationStatusResponse *p_value);
Variant to_variant_PFInventoryGetTransactionHistoryRequest(const PFInventoryGetTransactionHistoryRequest *p_value);
Variant to_variant_PFInventoryTransactionClawbackDetails(const PFInventoryTransactionClawbackDetails *p_value);
Variant to_variant_PFInventoryTransactionOperation(const PFInventoryTransactionOperation *p_value);
Variant to_variant_PFInventoryTransactionPurchaseDetails(const PFInventoryTransactionPurchaseDetails *p_value);
Variant to_variant_PFInventoryTransactionRedeemDetails(const PFInventoryTransactionRedeemDetails *p_value);
Variant to_variant_PFInventoryTransactionTransferDetails(const PFInventoryTransactionTransferDetails *p_value);
Variant to_variant_PFInventoryTransaction(const PFInventoryTransaction *p_value);
Variant to_variant_PFInventoryGetTransactionHistoryResponse(const PFInventoryGetTransactionHistoryResponse *p_value);
Variant to_variant_PFInventoryGooglePlayProductPurchase(const PFInventoryGooglePlayProductPurchase *p_value);
Variant to_variant_PFInventoryPurchaseInventoryItemsRequest(const PFInventoryPurchaseInventoryItemsRequest *p_value);
Variant to_variant_PFInventoryPurchaseInventoryItemsResponse(const PFInventoryPurchaseInventoryItemsResponse *p_value);
Variant to_variant_PFInventoryRedeemAppleAppStoreInventoryItemsRequest(const PFInventoryRedeemAppleAppStoreInventoryItemsRequest *p_value);
Variant to_variant_PFInventoryRedemptionFailure(const PFInventoryRedemptionFailure *p_value);
Variant to_variant_PFInventoryRedemptionSuccess(const PFInventoryRedemptionSuccess *p_value);
Variant to_variant_PFInventoryRedeemAppleAppStoreInventoryItemsResponse(const PFInventoryRedeemAppleAppStoreInventoryItemsResponse *p_value);
Variant to_variant_PFInventoryRedeemGooglePlayInventoryItemsRequest(const PFInventoryRedeemGooglePlayInventoryItemsRequest *p_value);
Variant to_variant_PFInventoryRedeemGooglePlayInventoryItemsResponse(const PFInventoryRedeemGooglePlayInventoryItemsResponse *p_value);
Variant to_variant_PFInventoryRedeemMicrosoftStoreInventoryItemsRequest(const PFInventoryRedeemMicrosoftStoreInventoryItemsRequest *p_value);
Variant to_variant_PFInventoryRedeemMicrosoftStoreInventoryItemsResponse(const PFInventoryRedeemMicrosoftStoreInventoryItemsResponse *p_value);
Variant to_variant_PFInventoryRedeemNintendoEShopInventoryItemsRequest(const PFInventoryRedeemNintendoEShopInventoryItemsRequest *p_value);
Variant to_variant_PFInventoryRedeemNintendoEShopInventoryItemsResponse(const PFInventoryRedeemNintendoEShopInventoryItemsResponse *p_value);
Variant to_variant_PFInventoryRedeemPlayStationStoreInventoryItemsRequest(const PFInventoryRedeemPlayStationStoreInventoryItemsRequest *p_value);
Variant to_variant_PFInventoryRedeemPlayStationStoreInventoryItemsResponse(const PFInventoryRedeemPlayStationStoreInventoryItemsResponse *p_value);
Variant to_variant_PFInventoryRedeemSteamInventoryItemsRequest(const PFInventoryRedeemSteamInventoryItemsRequest *p_value);
Variant to_variant_PFInventoryRedeemSteamInventoryItemsResponse(const PFInventoryRedeemSteamInventoryItemsResponse *p_value);
Variant to_variant_PFInventorySubtractInventoryItemsRequest(const PFInventorySubtractInventoryItemsRequest *p_value);
Variant to_variant_PFInventorySubtractInventoryItemsResponse(const PFInventorySubtractInventoryItemsResponse *p_value);
Variant to_variant_PFInventoryTransferInventoryItemsRequest(const PFInventoryTransferInventoryItemsRequest *p_value);
Variant to_variant_PFInventoryTransferInventoryItemsResponse(const PFInventoryTransferInventoryItemsResponse *p_value);
Variant to_variant_PFInventoryUpdateInventoryItemsRequest(const PFInventoryUpdateInventoryItemsRequest *p_value);
Variant to_variant_PFInventoryUpdateInventoryItemsResponse(const PFInventoryUpdateInventoryItemsResponse *p_value);
Variant to_variant_PFLocalizationGetLanguageListRequest(const PFLocalizationGetLanguageListRequest *p_value);
Variant to_variant_PFLocalizationGetLanguageListResponse(const PFLocalizationGetLanguageListResponse *p_value);
Variant to_variant_PFPlayerDataManagementClientDeletePlayerCustomPropertiesRequest(const PFPlayerDataManagementClientDeletePlayerCustomPropertiesRequest *p_value);
Variant to_variant_PFPlayerDataManagementDeletedPropertyDetails(const PFPlayerDataManagementDeletedPropertyDetails *p_value);
Variant to_variant_PFPlayerDataManagementClientDeletePlayerCustomPropertiesResult(const PFPlayerDataManagementClientDeletePlayerCustomPropertiesResult *p_value);
Variant to_variant_PFPlayerDataManagementClientGetPlayerCustomPropertyRequest(const PFPlayerDataManagementClientGetPlayerCustomPropertyRequest *p_value);
Variant to_variant_PFPlayerDataManagementCustomPropertyDetails(const PFPlayerDataManagementCustomPropertyDetails *p_value);
Variant to_variant_PFPlayerDataManagementClientGetPlayerCustomPropertyResult(const PFPlayerDataManagementClientGetPlayerCustomPropertyResult *p_value);
Variant to_variant_PFPlayerDataManagementClientGetUserDataResult(const PFPlayerDataManagementClientGetUserDataResult *p_value);
Variant to_variant_PFPlayerDataManagementClientListPlayerCustomPropertiesResult(const PFPlayerDataManagementClientListPlayerCustomPropertiesResult *p_value);
Variant to_variant_PFPlayerDataManagementUpdateProperty(const PFPlayerDataManagementUpdateProperty *p_value);
Variant to_variant_PFPlayerDataManagementClientUpdatePlayerCustomPropertiesRequest(const PFPlayerDataManagementClientUpdatePlayerCustomPropertiesRequest *p_value);
Variant to_variant_PFPlayerDataManagementClientUpdatePlayerCustomPropertiesResult(const PFPlayerDataManagementClientUpdatePlayerCustomPropertiesResult *p_value);
Variant to_variant_PFPlayerDataManagementClientUpdateUserDataRequest(const PFPlayerDataManagementClientUpdateUserDataRequest *p_value);
Variant to_variant_PFPlayerDataManagementGetUserDataRequest(const PFPlayerDataManagementGetUserDataRequest *p_value);
Variant to_variant_PFPlayerDataManagementUpdateUserDataResult(const PFPlayerDataManagementUpdateUserDataResult *p_value);
Variant to_variant_PFProfilesEntityDataObject(const PFProfilesEntityDataObject *p_value);
Variant to_variant_PFProfilesEntityDataObjectDictionaryEntry(const PFProfilesEntityDataObjectDictionaryEntry *p_value);
Variant to_variant_PFProfilesEntityPermissionStatement(const PFProfilesEntityPermissionStatement *p_value);
Variant to_variant_PFProfilesEntityProfileFileMetadata(const PFProfilesEntityProfileFileMetadata *p_value);
Variant to_variant_PFProfilesEntityProfileFileMetadataDictionaryEntry(const PFProfilesEntityProfileFileMetadataDictionaryEntry *p_value);
Variant to_variant_PFProfilesEntityProfileBody(const PFProfilesEntityProfileBody *p_value);
Variant to_variant_PFProfilesGetEntityProfileRequest(const PFProfilesGetEntityProfileRequest *p_value);
Variant to_variant_PFProfilesGetEntityProfileResponse(const PFProfilesGetEntityProfileResponse *p_value);
Variant to_variant_PFProfilesGetEntityProfilesRequest(const PFProfilesGetEntityProfilesRequest *p_value);
Variant to_variant_PFProfilesGetEntityProfilesResponse(const PFProfilesGetEntityProfilesResponse *p_value);
Variant to_variant_PFProfilesGetTitlePlayersFromMasterPlayerAccountIdsRequest(const PFProfilesGetTitlePlayersFromMasterPlayerAccountIdsRequest *p_value);
Variant to_variant_PFProfilesGetTitlePlayersFromMasterPlayerAccountIdsResponse(const PFProfilesGetTitlePlayersFromMasterPlayerAccountIdsResponse *p_value);
Variant to_variant_PFProfilesSetEntityProfilePolicyRequest(const PFProfilesSetEntityProfilePolicyRequest *p_value);
Variant to_variant_PFProfilesSetEntityProfilePolicyResponse(const PFProfilesSetEntityProfilePolicyResponse *p_value);
Variant to_variant_PFProfilesSetProfileLanguageRequest(const PFProfilesSetProfileLanguageRequest *p_value);
Variant to_variant_PFProfilesSetProfileLanguageResponse(const PFProfilesSetProfileLanguageResponse *p_value);
Variant to_variant_PFStatisticsStatisticColumn(const PFStatisticsStatisticColumn *p_value);
Variant to_variant_PFStatisticsStatisticsUpdateEventConfig(const PFStatisticsStatisticsUpdateEventConfig *p_value);
Variant to_variant_PFStatisticsStatisticsEventEmissionConfig(const PFStatisticsStatisticsEventEmissionConfig *p_value);
Variant to_variant_PFVersionConfiguration(const PFVersionConfiguration *p_value);
Variant to_variant_PFStatisticsCreateStatisticDefinitionRequest(const PFStatisticsCreateStatisticDefinitionRequest *p_value);
Variant to_variant_PFStatisticsDeleteStatisticDefinitionRequest(const PFStatisticsDeleteStatisticDefinitionRequest *p_value);
Variant to_variant_PFStatisticsStatisticDelete(const PFStatisticsStatisticDelete *p_value);
Variant to_variant_PFStatisticsDeleteStatisticsRequest(const PFStatisticsDeleteStatisticsRequest *p_value);
Variant to_variant_PFStatisticsDeleteStatisticsResponse(const PFStatisticsDeleteStatisticsResponse *p_value);
Variant to_variant_PFStatisticsEntityStatisticValue(const PFStatisticsEntityStatisticValue *p_value);
Variant to_variant_PFStatisticsEntityStatisticValueDictionaryEntry(const PFStatisticsEntityStatisticValueDictionaryEntry *p_value);
Variant to_variant_PFStatisticsEntityStatistics(const PFStatisticsEntityStatistics *p_value);
Variant to_variant_PFStatisticsGetStatisticDefinitionRequest(const PFStatisticsGetStatisticDefinitionRequest *p_value);
Variant to_variant_PFStatisticsGetStatisticDefinitionResponse(const PFStatisticsGetStatisticDefinitionResponse *p_value);
Variant to_variant_PFStatisticsGetStatisticsForEntitiesRequest(const PFStatisticsGetStatisticsForEntitiesRequest *p_value);
Variant to_variant_PFStatisticsStatisticColumnCollection(const PFStatisticsStatisticColumnCollection *p_value);
Variant to_variant_PFStatisticsStatisticColumnCollectionDictionaryEntry(const PFStatisticsStatisticColumnCollectionDictionaryEntry *p_value);
Variant to_variant_PFStatisticsGetStatisticsForEntitiesResponse(const PFStatisticsGetStatisticsForEntitiesResponse *p_value);
Variant to_variant_PFStatisticsGetStatisticsRequest(const PFStatisticsGetStatisticsRequest *p_value);
Variant to_variant_PFStatisticsGetStatisticsResponse(const PFStatisticsGetStatisticsResponse *p_value);
Variant to_variant_PFStatisticsIncrementStatisticVersionRequest(const PFStatisticsIncrementStatisticVersionRequest *p_value);
Variant to_variant_PFStatisticsIncrementStatisticVersionResponse(const PFStatisticsIncrementStatisticVersionResponse *p_value);
Variant to_variant_PFStatisticsListStatisticDefinitionsRequest(const PFStatisticsListStatisticDefinitionsRequest *p_value);
Variant to_variant_PFStatisticsStatisticDefinition(const PFStatisticsStatisticDefinition *p_value);
Variant to_variant_PFStatisticsListStatisticDefinitionsResponse(const PFStatisticsListStatisticDefinitionsResponse *p_value);
Variant to_variant_PFStatisticsStatisticUpdate(const PFStatisticsStatisticUpdate *p_value);
Variant to_variant_PFStatisticsUpdateStatisticDefinitionRequest(const PFStatisticsUpdateStatisticDefinitionRequest *p_value);
Variant to_variant_PFStatisticsUpdateStatisticsRequest(const PFStatisticsUpdateStatisticsRequest *p_value);
Variant to_variant_PFStatisticsUpdateStatisticsResponse(const PFStatisticsUpdateStatisticsResponse *p_value);
Variant to_variant_PFTitleDataManagementGetPublisherDataRequest(const PFTitleDataManagementGetPublisherDataRequest *p_value);
Variant to_variant_PFTitleDataManagementGetPublisherDataResult(const PFTitleDataManagementGetPublisherDataResult *p_value);
Variant to_variant_PFTitleDataManagementGetTimeResult(const PFTitleDataManagementGetTimeResult *p_value);
Variant to_variant_PFTitleDataManagementGetTitleDataRequest(const PFTitleDataManagementGetTitleDataRequest *p_value);
Variant to_variant_PFTitleDataManagementGetTitleDataResult(const PFTitleDataManagementGetTitleDataResult *p_value);
Variant to_variant_PFTitleDataManagementGetTitleNewsRequest(const PFTitleDataManagementGetTitleNewsRequest *p_value);
Variant to_variant_PFTitleDataManagementTitleNewsItem(const PFTitleDataManagementTitleNewsItem *p_value);
Variant to_variant_PFTitleDataManagementGetTitleNewsResult(const PFTitleDataManagementGetTitleNewsResult *p_value);

inline bool duplicate_xuser_handle_from_variant(const Variant &p_user, XUserHandle *r_user_handle, String *r_error) {
    if (r_user_handle == nullptr) {
        return false;
    }

    *r_user_handle = nullptr;
    if (p_user.get_type() != Variant::OBJECT) {
        if (r_error) *r_error = "Expected a GDKUser object for field user.";
        return false;
    }

    Object *object = Object::cast_to<Object>(p_user);
    if (object == nullptr) {
        if (r_error) *r_error = "The provided user object is null.";
        return false;
    }

    if (Object::cast_to<PlayFabUser>(object) != nullptr) {
        if (r_error) *r_error = "Field user requires a GDKUser object, not a PlayFabUser.";
        return false;
    }

    if (!object->has_method("is_signed_in") || !object->has_method("get_local_id")) {
        if (r_error) *r_error = "Field user requires a GDKUser object.";
        return false;
    }

    Variant signed_in = object->call("is_signed_in");
    if (signed_in.get_type() != Variant::BOOL || !static_cast<bool>(signed_in)) {
        if (r_error) *r_error = "Field user requires a signed-in GDKUser object.";
        return false;
    }

    Variant local_id_value = object->call("get_local_id");
    if (local_id_value.get_type() != Variant::INT) {
        if (r_error) *r_error = "The provided GDKUser does not expose an integer local_id.";
        return false;
    }

    XUserLocalId local_id = {};
    local_id.value = static_cast<uint64_t>(static_cast<int64_t>(local_id_value));
    if (local_id.value == 0) {
        if (r_error) *r_error = "The provided GDKUser does not have a valid local_id.";
        return false;
    }

    HRESULT hr = XUserFindUserByLocalId(local_id, r_user_handle);
    if (FAILED(hr)) {
        if (r_error) *r_error = "Failed to find an active XUserHandle for the provided GDKUser.";
        return false;
    }

    return true;
}

struct OwnedPFStringDictionaryEntry {
    PFStringDictionaryEntry value = {};
    std::string key_string;
    std::string value_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "key", "key", &field_value)) {
            key_string = String(field_value).utf8().get_data();
            value.key = key_string.empty() ? nullptr : key_string.c_str();
        }
        if (get_request_value(p_request, "value", "value", &field_value)) {
            value_string = String(field_value).utf8().get_data();
            value.value = value_string.empty() ? nullptr : value_string.c_str();
        }
        return true;
    }
};

struct OwnedPFAccountManagementAddOrUpdateContactEmailRequest {
    PFAccountManagementAddOrUpdateContactEmailRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    std::string emailAddress_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "emailAddress", "email_address", &field_value)) {
            emailAddress_string = String(field_value).utf8().get_data();
            value.emailAddress = emailAddress_string.empty() ? nullptr : emailAddress_string.c_str();
        }
        return true;
    }
};

struct OwnedPFAccountManagementBattleNetAccountPlayFabIdPair {
    PFAccountManagementBattleNetAccountPlayFabIdPair value = {};
    std::string battleNetAccountId_string;
    std::string playFabId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "battleNetAccountId", "battle_net_account_id", &field_value)) {
            battleNetAccountId_string = String(field_value).utf8().get_data();
            value.battleNetAccountId = battleNetAccountId_string.empty() ? nullptr : battleNetAccountId_string.c_str();
        }
        if (get_request_value(p_request, "playFabId", "play_fab_id", &field_value)) {
            playFabId_string = String(field_value).utf8().get_data();
            value.playFabId = playFabId_string.empty() ? nullptr : playFabId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFAccountManagementClientLinkBattleNetAccountRequest {
    PFAccountManagementClientLinkBattleNetAccountRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    bool forceLink_storage = {};
    std::string identityToken_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "forceLink", "force_link", &field_value)) {
            forceLink_storage = static_cast<bool>(field_value);
            value.forceLink = &forceLink_storage;
        }
        if (get_request_value(p_request, "identityToken", "identity_token", &field_value)) {
            identityToken_string = String(field_value).utf8().get_data();
            value.identityToken = identityToken_string.empty() ? nullptr : identityToken_string.c_str();
        }
        return true;
    }
};

struct OwnedPFAccountManagementClientLinkNintendoServiceAccountRequest {
    PFAccountManagementClientLinkNintendoServiceAccountRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    bool forceLink_storage = {};
    std::string identityToken_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "forceLink", "force_link", &field_value)) {
            forceLink_storage = static_cast<bool>(field_value);
            value.forceLink = &forceLink_storage;
        }
        if (get_request_value(p_request, "identityToken", "identity_token", &field_value)) {
            identityToken_string = String(field_value).utf8().get_data();
            value.identityToken = identityToken_string.empty() ? nullptr : identityToken_string.c_str();
        }
        return true;
    }
};

struct OwnedPFAccountManagementClientLinkPSNAccountRequest {
    PFAccountManagementClientLinkPSNAccountRequest value = {};
    std::string authCode_string;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    bool forceLink_storage = {};
    int32_t issuerId_storage = {};
    std::string redirectUri_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "authCode", "auth_code", &field_value)) {
            authCode_string = String(field_value).utf8().get_data();
            value.authCode = authCode_string.empty() ? nullptr : authCode_string.c_str();
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "forceLink", "force_link", &field_value)) {
            forceLink_storage = static_cast<bool>(field_value);
            value.forceLink = &forceLink_storage;
        }
        if (get_request_value(p_request, "issuerId", "issuer_id", &field_value)) {
            issuerId_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.issuerId = &issuerId_storage;
        }
        if (get_request_value(p_request, "redirectUri", "redirect_uri", &field_value)) {
            redirectUri_string = String(field_value).utf8().get_data();
            value.redirectUri = redirectUri_string.empty() ? nullptr : redirectUri_string.c_str();
        }
        return true;
    }
};

struct OwnedPFAccountManagementClientLinkXboxAccountRequest {
    PFAccountManagementClientLinkXboxAccountRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    bool forceLink_storage = {};
    XUserHandle user_handle = nullptr;

    ~OwnedPFAccountManagementClientLinkXboxAccountRequest() {
        if (user_handle != nullptr) {
            XUserCloseHandle(user_handle);
            user_handle = nullptr;
        }
    }

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "forceLink", "force_link", &field_value)) {
            forceLink_storage = static_cast<bool>(field_value);
            value.forceLink = &forceLink_storage;
        }
        if (!get_request_value(p_request, "user", "user", &field_value)) {
            if (r_error) *r_error = "Expected a GDKUser object for field user.";
            return false;
        }
        if (!duplicate_xuser_handle_from_variant(field_value, &user_handle, r_error)) return false;
        value.user = user_handle;
        return true;
    }
};

struct OwnedPFAccountManagementClientUnlinkBattleNetAccountRequest {
    PFAccountManagementClientUnlinkBattleNetAccountRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementClientUnlinkNintendoServiceAccountRequest {
    PFAccountManagementClientUnlinkNintendoServiceAccountRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementClientUnlinkPSNAccountRequest {
    PFAccountManagementClientUnlinkPSNAccountRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementClientUnlinkXboxAccountRequest {
    PFAccountManagementClientUnlinkXboxAccountRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementClientUpdateAvatarUrlRequest {
    PFAccountManagementClientUpdateAvatarUrlRequest value = {};
    std::string imageUrl_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "imageUrl", "image_url", &field_value)) {
            imageUrl_string = String(field_value).utf8().get_data();
            value.imageUrl = imageUrl_string.empty() ? nullptr : imageUrl_string.c_str();
        }
        return true;
    }
};

struct OwnedPFAccountManagementFacebookInstantGamesPlayFabIdPair {
    PFAccountManagementFacebookInstantGamesPlayFabIdPair value = {};
    std::string facebookInstantGamesId_string;
    std::string playFabId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "facebookInstantGamesId", "facebook_instant_games_id", &field_value)) {
            facebookInstantGamesId_string = String(field_value).utf8().get_data();
            value.facebookInstantGamesId = facebookInstantGamesId_string.empty() ? nullptr : facebookInstantGamesId_string.c_str();
        }
        if (get_request_value(p_request, "playFabId", "play_fab_id", &field_value)) {
            playFabId_string = String(field_value).utf8().get_data();
            value.playFabId = playFabId_string.empty() ? nullptr : playFabId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFAccountManagementFacebookPlayFabIdPair {
    PFAccountManagementFacebookPlayFabIdPair value = {};
    std::string facebookId_string;
    std::string playFabId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "facebookId", "facebook_id", &field_value)) {
            facebookId_string = String(field_value).utf8().get_data();
            value.facebookId = facebookId_string.empty() ? nullptr : facebookId_string.c_str();
        }
        if (get_request_value(p_request, "playFabId", "play_fab_id", &field_value)) {
            playFabId_string = String(field_value).utf8().get_data();
            value.playFabId = playFabId_string.empty() ? nullptr : playFabId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFAccountManagementGameCenterPlayFabIdPair {
    PFAccountManagementGameCenterPlayFabIdPair value = {};
    std::string gameCenterId_string;
    std::string playFabId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "gameCenterId", "game_center_id", &field_value)) {
            gameCenterId_string = String(field_value).utf8().get_data();
            value.gameCenterId = gameCenterId_string.empty() ? nullptr : gameCenterId_string.c_str();
        }
        if (get_request_value(p_request, "playFabId", "play_fab_id", &field_value)) {
            playFabId_string = String(field_value).utf8().get_data();
            value.playFabId = playFabId_string.empty() ? nullptr : playFabId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetAccountInfoRequest {
    PFAccountManagementGetAccountInfoRequest value = {};
    std::string email_string;
    std::string playFabId_string;
    std::string titleDisplayName_string;
    std::string username_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "email", "email", &field_value)) {
            email_string = String(field_value).utf8().get_data();
            value.email = email_string.empty() ? nullptr : email_string.c_str();
        }
        if (get_request_value(p_request, "playFabId", "play_fab_id", &field_value)) {
            playFabId_string = String(field_value).utf8().get_data();
            value.playFabId = playFabId_string.empty() ? nullptr : playFabId_string.c_str();
        }
        if (get_request_value(p_request, "titleDisplayName", "title_display_name", &field_value)) {
            titleDisplayName_string = String(field_value).utf8().get_data();
            value.titleDisplayName = titleDisplayName_string.empty() ? nullptr : titleDisplayName_string.c_str();
        }
        if (get_request_value(p_request, "username", "username", &field_value)) {
            username_string = String(field_value).utf8().get_data();
            value.username = username_string.empty() ? nullptr : username_string.c_str();
        }
        return true;
    }
};

struct OwnedPFUserAndroidDeviceInfo {
    PFUserAndroidDeviceInfo value = {};
    std::string androidDeviceId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "androidDeviceId", "android_device_id", &field_value)) {
            androidDeviceId_string = String(field_value).utf8().get_data();
            value.androidDeviceId = androidDeviceId_string.empty() ? nullptr : androidDeviceId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFUserAppleIdInfo {
    PFUserAppleIdInfo value = {};
    std::string appleSubjectId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "appleSubjectId", "apple_subject_id", &field_value)) {
            appleSubjectId_string = String(field_value).utf8().get_data();
            value.appleSubjectId = appleSubjectId_string.empty() ? nullptr : appleSubjectId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFUserBattleNetInfo {
    PFUserBattleNetInfo value = {};
    std::string battleNetAccountId_string;
    std::string battleNetBattleTag_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "battleNetAccountId", "battle_net_account_id", &field_value)) {
            battleNetAccountId_string = String(field_value).utf8().get_data();
            value.battleNetAccountId = battleNetAccountId_string.empty() ? nullptr : battleNetAccountId_string.c_str();
        }
        if (get_request_value(p_request, "battleNetBattleTag", "battle_net_battle_tag", &field_value)) {
            battleNetBattleTag_string = String(field_value).utf8().get_data();
            value.battleNetBattleTag = battleNetBattleTag_string.empty() ? nullptr : battleNetBattleTag_string.c_str();
        }
        return true;
    }
};

struct OwnedPFUserCustomIdInfo {
    PFUserCustomIdInfo value = {};
    std::string customId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customId", "custom_id", &field_value)) {
            customId_string = String(field_value).utf8().get_data();
            value.customId = customId_string.empty() ? nullptr : customId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFUserFacebookInfo {
    PFUserFacebookInfo value = {};
    std::string facebookId_string;
    std::string fullName_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "facebookId", "facebook_id", &field_value)) {
            facebookId_string = String(field_value).utf8().get_data();
            value.facebookId = facebookId_string.empty() ? nullptr : facebookId_string.c_str();
        }
        if (get_request_value(p_request, "fullName", "full_name", &field_value)) {
            fullName_string = String(field_value).utf8().get_data();
            value.fullName = fullName_string.empty() ? nullptr : fullName_string.c_str();
        }
        return true;
    }
};

struct OwnedPFUserFacebookInstantGamesIdInfo {
    PFUserFacebookInstantGamesIdInfo value = {};
    std::string facebookInstantGamesId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "facebookInstantGamesId", "facebook_instant_games_id", &field_value)) {
            facebookInstantGamesId_string = String(field_value).utf8().get_data();
            value.facebookInstantGamesId = facebookInstantGamesId_string.empty() ? nullptr : facebookInstantGamesId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFUserGameCenterInfo {
    PFUserGameCenterInfo value = {};
    std::string gameCenterId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "gameCenterId", "game_center_id", &field_value)) {
            gameCenterId_string = String(field_value).utf8().get_data();
            value.gameCenterId = gameCenterId_string.empty() ? nullptr : gameCenterId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFUserGoogleInfo {
    PFUserGoogleInfo value = {};
    std::string googleEmail_string;
    std::string googleGender_string;
    std::string googleId_string;
    std::string googleLocale_string;
    std::string googleName_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "googleEmail", "google_email", &field_value)) {
            googleEmail_string = String(field_value).utf8().get_data();
            value.googleEmail = googleEmail_string.empty() ? nullptr : googleEmail_string.c_str();
        }
        if (get_request_value(p_request, "googleGender", "google_gender", &field_value)) {
            googleGender_string = String(field_value).utf8().get_data();
            value.googleGender = googleGender_string.empty() ? nullptr : googleGender_string.c_str();
        }
        if (get_request_value(p_request, "googleId", "google_id", &field_value)) {
            googleId_string = String(field_value).utf8().get_data();
            value.googleId = googleId_string.empty() ? nullptr : googleId_string.c_str();
        }
        if (get_request_value(p_request, "googleLocale", "google_locale", &field_value)) {
            googleLocale_string = String(field_value).utf8().get_data();
            value.googleLocale = googleLocale_string.empty() ? nullptr : googleLocale_string.c_str();
        }
        if (get_request_value(p_request, "googleName", "google_name", &field_value)) {
            googleName_string = String(field_value).utf8().get_data();
            value.googleName = googleName_string.empty() ? nullptr : googleName_string.c_str();
        }
        return true;
    }
};

struct OwnedPFUserGooglePlayGamesInfo {
    PFUserGooglePlayGamesInfo value = {};
    std::string googlePlayGamesPlayerAvatarImageUrl_string;
    std::string googlePlayGamesPlayerDisplayName_string;
    std::string googlePlayGamesPlayerId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "googlePlayGamesPlayerAvatarImageUrl", "google_play_games_player_avatar_image_url", &field_value)) {
            googlePlayGamesPlayerAvatarImageUrl_string = String(field_value).utf8().get_data();
            value.googlePlayGamesPlayerAvatarImageUrl = googlePlayGamesPlayerAvatarImageUrl_string.empty() ? nullptr : googlePlayGamesPlayerAvatarImageUrl_string.c_str();
        }
        if (get_request_value(p_request, "googlePlayGamesPlayerDisplayName", "google_play_games_player_display_name", &field_value)) {
            googlePlayGamesPlayerDisplayName_string = String(field_value).utf8().get_data();
            value.googlePlayGamesPlayerDisplayName = googlePlayGamesPlayerDisplayName_string.empty() ? nullptr : googlePlayGamesPlayerDisplayName_string.c_str();
        }
        if (get_request_value(p_request, "googlePlayGamesPlayerId", "google_play_games_player_id", &field_value)) {
            googlePlayGamesPlayerId_string = String(field_value).utf8().get_data();
            value.googlePlayGamesPlayerId = googlePlayGamesPlayerId_string.empty() ? nullptr : googlePlayGamesPlayerId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFUserIosDeviceInfo {
    PFUserIosDeviceInfo value = {};
    std::string iosDeviceId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "iosDeviceId", "ios_device_id", &field_value)) {
            iosDeviceId_string = String(field_value).utf8().get_data();
            value.iosDeviceId = iosDeviceId_string.empty() ? nullptr : iosDeviceId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFUserKongregateInfo {
    PFUserKongregateInfo value = {};
    std::string kongregateId_string;
    std::string kongregateName_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "kongregateId", "kongregate_id", &field_value)) {
            kongregateId_string = String(field_value).utf8().get_data();
            value.kongregateId = kongregateId_string.empty() ? nullptr : kongregateId_string.c_str();
        }
        if (get_request_value(p_request, "kongregateName", "kongregate_name", &field_value)) {
            kongregateName_string = String(field_value).utf8().get_data();
            value.kongregateName = kongregateName_string.empty() ? nullptr : kongregateName_string.c_str();
        }
        return true;
    }
};

struct OwnedPFUserNintendoSwitchAccountIdInfo {
    PFUserNintendoSwitchAccountIdInfo value = {};
    std::string nintendoSwitchAccountSubjectId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "nintendoSwitchAccountSubjectId", "nintendo_switch_account_subject_id", &field_value)) {
            nintendoSwitchAccountSubjectId_string = String(field_value).utf8().get_data();
            value.nintendoSwitchAccountSubjectId = nintendoSwitchAccountSubjectId_string.empty() ? nullptr : nintendoSwitchAccountSubjectId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFUserNintendoSwitchDeviceIdInfo {
    PFUserNintendoSwitchDeviceIdInfo value = {};
    std::string nintendoSwitchDeviceId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "nintendoSwitchDeviceId", "nintendo_switch_device_id", &field_value)) {
            nintendoSwitchDeviceId_string = String(field_value).utf8().get_data();
            value.nintendoSwitchDeviceId = nintendoSwitchDeviceId_string.empty() ? nullptr : nintendoSwitchDeviceId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFUserOpenIdInfo {
    PFUserOpenIdInfo value = {};
    std::string connectionId_string;
    std::string issuer_string;
    std::string subject_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "connectionId", "connection_id", &field_value)) {
            connectionId_string = String(field_value).utf8().get_data();
            value.connectionId = connectionId_string.empty() ? nullptr : connectionId_string.c_str();
        }
        if (get_request_value(p_request, "issuer", "issuer", &field_value)) {
            issuer_string = String(field_value).utf8().get_data();
            value.issuer = issuer_string.empty() ? nullptr : issuer_string.c_str();
        }
        if (get_request_value(p_request, "subject", "subject", &field_value)) {
            subject_string = String(field_value).utf8().get_data();
            value.subject = subject_string.empty() ? nullptr : subject_string.c_str();
        }
        return true;
    }
};

struct OwnedPFUserPrivateAccountInfo {
    PFUserPrivateAccountInfo value = {};
    std::string email_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "email", "email", &field_value)) {
            email_string = String(field_value).utf8().get_data();
            value.email = email_string.empty() ? nullptr : email_string.c_str();
        }
        return true;
    }
};

struct OwnedPFUserPsnInfo {
    PFUserPsnInfo value = {};
    std::string psnAccountId_string;
    std::string psnOnlineId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "psnAccountId", "psn_account_id", &field_value)) {
            psnAccountId_string = String(field_value).utf8().get_data();
            value.psnAccountId = psnAccountId_string.empty() ? nullptr : psnAccountId_string.c_str();
        }
        if (get_request_value(p_request, "psnOnlineId", "psn_online_id", &field_value)) {
            psnOnlineId_string = String(field_value).utf8().get_data();
            value.psnOnlineId = psnOnlineId_string.empty() ? nullptr : psnOnlineId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFUserServerCustomIdInfo {
    PFUserServerCustomIdInfo value = {};
    std::string customId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customId", "custom_id", &field_value)) {
            customId_string = String(field_value).utf8().get_data();
            value.customId = customId_string.empty() ? nullptr : customId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFUserSteamInfo {
    PFUserSteamInfo value = {};
    PFTitleActivationStatus steamActivationStatus_storage = {};
    std::string steamCountry_string;
    PFCurrency steamCurrency_storage = {};
    std::string steamId_string;
    std::string steamName_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "steamActivationStatus", "steam_activation_status", &field_value)) {
            steamActivationStatus_storage = static_cast<PFTitleActivationStatus>(static_cast<int64_t>(field_value));
            value.steamActivationStatus = &steamActivationStatus_storage;
        }
        if (get_request_value(p_request, "steamCountry", "steam_country", &field_value)) {
            steamCountry_string = String(field_value).utf8().get_data();
            value.steamCountry = steamCountry_string.empty() ? nullptr : steamCountry_string.c_str();
        }
        if (get_request_value(p_request, "steamCurrency", "steam_currency", &field_value)) {
            steamCurrency_storage = static_cast<PFCurrency>(static_cast<int64_t>(field_value));
            value.steamCurrency = &steamCurrency_storage;
        }
        if (get_request_value(p_request, "steamId", "steam_id", &field_value)) {
            steamId_string = String(field_value).utf8().get_data();
            value.steamId = steamId_string.empty() ? nullptr : steamId_string.c_str();
        }
        if (get_request_value(p_request, "steamName", "steam_name", &field_value)) {
            steamName_string = String(field_value).utf8().get_data();
            value.steamName = steamName_string.empty() ? nullptr : steamName_string.c_str();
        }
        return true;
    }
};

struct OwnedPFEntityKey {
    PFEntityKey value = {};
    std::string id_string;
    std::string type_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "id", "id", &field_value)) {
            id_string = String(field_value).utf8().get_data();
            value.id = id_string.empty() ? nullptr : id_string.c_str();
        }
        if (get_request_value(p_request, "type", "type", &field_value)) {
            type_string = String(field_value).utf8().get_data();
            value.type = type_string.empty() ? nullptr : type_string.c_str();
        }
        return true;
    }
};

struct OwnedPFUserTitleInfo {
    PFUserTitleInfo value = {};
    std::string avatarUrl_string;
    std::string displayName_string;
    time_t firstLogin_storage = {};
    bool isBanned_storage = {};
    time_t lastLogin_storage = {};
    PFUserOrigination origination_storage = {};
    OwnedPFEntityKey titlePlayerAccount_owner;
    bool titlePlayerAccount_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "avatarUrl", "avatar_url", &field_value)) {
            avatarUrl_string = String(field_value).utf8().get_data();
            value.avatarUrl = avatarUrl_string.empty() ? nullptr : avatarUrl_string.c_str();
        }
        if (get_request_value(p_request, "created", "created", &field_value)) {
            value.created = static_cast<time_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "displayName", "display_name", &field_value)) {
            displayName_string = String(field_value).utf8().get_data();
            value.displayName = displayName_string.empty() ? nullptr : displayName_string.c_str();
        }
        if (get_request_value(p_request, "firstLogin", "first_login", &field_value)) {
            firstLogin_storage = static_cast<time_t>(static_cast<int64_t>(field_value));
            value.firstLogin = &firstLogin_storage;
        }
        if (get_request_value(p_request, "isBanned", "is_banned", &field_value)) {
            isBanned_storage = static_cast<bool>(field_value);
            value.isBanned = &isBanned_storage;
        }
        if (get_request_value(p_request, "lastLogin", "last_login", &field_value)) {
            lastLogin_storage = static_cast<time_t>(static_cast<int64_t>(field_value));
            value.lastLogin = &lastLogin_storage;
        }
        if (get_request_value(p_request, "origination", "origination", &field_value)) {
            origination_storage = static_cast<PFUserOrigination>(static_cast<int64_t>(field_value));
            value.origination = &origination_storage;
        }
        if (get_request_value(p_request, "titlePlayerAccount", "title_player_account", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field title_player_account."; return false; }
            if (!titlePlayerAccount_owner.from_dictionary(field_value, r_error)) return false;
            titlePlayerAccount_has_value = true;
            value.titlePlayerAccount = &titlePlayerAccount_owner.value;
        }
        return true;
    }
};

struct OwnedPFUserTwitchInfo {
    PFUserTwitchInfo value = {};
    std::string twitchId_string;
    std::string twitchUserName_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "twitchId", "twitch_id", &field_value)) {
            twitchId_string = String(field_value).utf8().get_data();
            value.twitchId = twitchId_string.empty() ? nullptr : twitchId_string.c_str();
        }
        if (get_request_value(p_request, "twitchUserName", "twitch_user_name", &field_value)) {
            twitchUserName_string = String(field_value).utf8().get_data();
            value.twitchUserName = twitchUserName_string.empty() ? nullptr : twitchUserName_string.c_str();
        }
        return true;
    }
};

struct OwnedPFUserXboxInfo {
    PFUserXboxInfo value = {};
    std::string xboxUserId_string;
    std::string xboxUserSandbox_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "xboxUserId", "xbox_user_id", &field_value)) {
            xboxUserId_string = String(field_value).utf8().get_data();
            value.xboxUserId = xboxUserId_string.empty() ? nullptr : xboxUserId_string.c_str();
        }
        if (get_request_value(p_request, "xboxUserSandbox", "xbox_user_sandbox", &field_value)) {
            xboxUserSandbox_string = String(field_value).utf8().get_data();
            value.xboxUserSandbox = xboxUserSandbox_string.empty() ? nullptr : xboxUserSandbox_string.c_str();
        }
        return true;
    }
};

struct OwnedPFUserAccountInfo {
    PFUserAccountInfo value = {};
    OwnedPFUserAndroidDeviceInfo androidDeviceInfo_owner;
    bool androidDeviceInfo_has_value = false;
    OwnedPFUserAppleIdInfo appleAccountInfo_owner;
    bool appleAccountInfo_has_value = false;
    OwnedPFUserBattleNetInfo battleNetAccountInfo_owner;
    bool battleNetAccountInfo_has_value = false;
    OwnedPFUserCustomIdInfo customIdInfo_owner;
    bool customIdInfo_has_value = false;
    OwnedPFUserFacebookInfo facebookInfo_owner;
    bool facebookInfo_has_value = false;
    OwnedPFUserFacebookInstantGamesIdInfo facebookInstantGamesIdInfo_owner;
    bool facebookInstantGamesIdInfo_has_value = false;
    OwnedPFUserGameCenterInfo gameCenterInfo_owner;
    bool gameCenterInfo_has_value = false;
    OwnedPFUserGoogleInfo googleInfo_owner;
    bool googleInfo_has_value = false;
    OwnedPFUserGooglePlayGamesInfo googlePlayGamesInfo_owner;
    bool googlePlayGamesInfo_has_value = false;
    OwnedPFUserIosDeviceInfo iosDeviceInfo_owner;
    bool iosDeviceInfo_has_value = false;
    OwnedPFUserKongregateInfo kongregateInfo_owner;
    bool kongregateInfo_has_value = false;
    OwnedPFUserNintendoSwitchAccountIdInfo nintendoSwitchAccountInfo_owner;
    bool nintendoSwitchAccountInfo_has_value = false;
    OwnedPFUserNintendoSwitchDeviceIdInfo nintendoSwitchDeviceIdInfo_owner;
    bool nintendoSwitchDeviceIdInfo_has_value = false;
    std::vector<OwnedPFUserOpenIdInfo> openIdInfo_items;
    std::vector<const PFUserOpenIdInfo *> openIdInfo_ptrs;
    std::string playFabId_string;
    OwnedPFUserPrivateAccountInfo privateInfo_owner;
    bool privateInfo_has_value = false;
    OwnedPFUserPsnInfo psnInfo_owner;
    bool psnInfo_has_value = false;
    OwnedPFUserServerCustomIdInfo serverCustomIdInfo_owner;
    bool serverCustomIdInfo_has_value = false;
    OwnedPFUserSteamInfo steamInfo_owner;
    bool steamInfo_has_value = false;
    OwnedPFUserTitleInfo titleInfo_owner;
    bool titleInfo_has_value = false;
    OwnedPFUserTwitchInfo twitchInfo_owner;
    bool twitchInfo_has_value = false;
    std::string username_string;
    OwnedPFUserXboxInfo xboxInfo_owner;
    bool xboxInfo_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "androidDeviceInfo", "android_device_info", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field android_device_info."; return false; }
            if (!androidDeviceInfo_owner.from_dictionary(field_value, r_error)) return false;
            androidDeviceInfo_has_value = true;
            value.androidDeviceInfo = &androidDeviceInfo_owner.value;
        }
        if (get_request_value(p_request, "appleAccountInfo", "apple_account_info", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field apple_account_info."; return false; }
            if (!appleAccountInfo_owner.from_dictionary(field_value, r_error)) return false;
            appleAccountInfo_has_value = true;
            value.appleAccountInfo = &appleAccountInfo_owner.value;
        }
        if (get_request_value(p_request, "battleNetAccountInfo", "battle_net_account_info", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field battle_net_account_info."; return false; }
            if (!battleNetAccountInfo_owner.from_dictionary(field_value, r_error)) return false;
            battleNetAccountInfo_has_value = true;
            value.battleNetAccountInfo = &battleNetAccountInfo_owner.value;
        }
        if (get_request_value(p_request, "created", "created", &field_value)) {
            value.created = static_cast<time_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "customIdInfo", "custom_id_info", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_id_info."; return false; }
            if (!customIdInfo_owner.from_dictionary(field_value, r_error)) return false;
            customIdInfo_has_value = true;
            value.customIdInfo = &customIdInfo_owner.value;
        }
        if (get_request_value(p_request, "facebookInfo", "facebook_info", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field facebook_info."; return false; }
            if (!facebookInfo_owner.from_dictionary(field_value, r_error)) return false;
            facebookInfo_has_value = true;
            value.facebookInfo = &facebookInfo_owner.value;
        }
        if (get_request_value(p_request, "facebookInstantGamesIdInfo", "facebook_instant_games_id_info", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field facebook_instant_games_id_info."; return false; }
            if (!facebookInstantGamesIdInfo_owner.from_dictionary(field_value, r_error)) return false;
            facebookInstantGamesIdInfo_has_value = true;
            value.facebookInstantGamesIdInfo = &facebookInstantGamesIdInfo_owner.value;
        }
        if (get_request_value(p_request, "gameCenterInfo", "game_center_info", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field game_center_info."; return false; }
            if (!gameCenterInfo_owner.from_dictionary(field_value, r_error)) return false;
            gameCenterInfo_has_value = true;
            value.gameCenterInfo = &gameCenterInfo_owner.value;
        }
        if (get_request_value(p_request, "googleInfo", "google_info", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field google_info."; return false; }
            if (!googleInfo_owner.from_dictionary(field_value, r_error)) return false;
            googleInfo_has_value = true;
            value.googleInfo = &googleInfo_owner.value;
        }
        if (get_request_value(p_request, "googlePlayGamesInfo", "google_play_games_info", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field google_play_games_info."; return false; }
            if (!googlePlayGamesInfo_owner.from_dictionary(field_value, r_error)) return false;
            googlePlayGamesInfo_has_value = true;
            value.googlePlayGamesInfo = &googlePlayGamesInfo_owner.value;
        }
        if (get_request_value(p_request, "iosDeviceInfo", "ios_device_info", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field ios_device_info."; return false; }
            if (!iosDeviceInfo_owner.from_dictionary(field_value, r_error)) return false;
            iosDeviceInfo_has_value = true;
            value.iosDeviceInfo = &iosDeviceInfo_owner.value;
        }
        if (get_request_value(p_request, "kongregateInfo", "kongregate_info", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field kongregate_info."; return false; }
            if (!kongregateInfo_owner.from_dictionary(field_value, r_error)) return false;
            kongregateInfo_has_value = true;
            value.kongregateInfo = &kongregateInfo_owner.value;
        }
        if (get_request_value(p_request, "nintendoSwitchAccountInfo", "nintendo_switch_account_info", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field nintendo_switch_account_info."; return false; }
            if (!nintendoSwitchAccountInfo_owner.from_dictionary(field_value, r_error)) return false;
            nintendoSwitchAccountInfo_has_value = true;
            value.nintendoSwitchAccountInfo = &nintendoSwitchAccountInfo_owner.value;
        }
        if (get_request_value(p_request, "nintendoSwitchDeviceIdInfo", "nintendo_switch_device_id_info", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field nintendo_switch_device_id_info."; return false; }
            if (!nintendoSwitchDeviceIdInfo_owner.from_dictionary(field_value, r_error)) return false;
            nintendoSwitchDeviceIdInfo_has_value = true;
            value.nintendoSwitchDeviceIdInfo = &nintendoSwitchDeviceIdInfo_owner.value;
        }
        if (get_request_value(p_request, "openIdInfo", "open_id_info", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field open_id_info."; return false; }
            Array source_array = field_value;
            openIdInfo_items.resize(source_array.size());
            openIdInfo_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field open_id_info."; return false; }
                if (!openIdInfo_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                openIdInfo_ptrs[static_cast<size_t>(i)] = &openIdInfo_items[static_cast<size_t>(i)].value;
            }
            value.openIdInfo = openIdInfo_ptrs.empty() ? nullptr : openIdInfo_ptrs.data();
            value.openIdInfoCount = static_cast<uint32_t>(openIdInfo_ptrs.size());
        }
        if (get_request_value(p_request, "playFabId", "play_fab_id", &field_value)) {
            playFabId_string = String(field_value).utf8().get_data();
            value.playFabId = playFabId_string.empty() ? nullptr : playFabId_string.c_str();
        }
        if (get_request_value(p_request, "privateInfo", "private_info", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field private_info."; return false; }
            if (!privateInfo_owner.from_dictionary(field_value, r_error)) return false;
            privateInfo_has_value = true;
            value.privateInfo = &privateInfo_owner.value;
        }
        if (get_request_value(p_request, "psnInfo", "psn_info", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field psn_info."; return false; }
            if (!psnInfo_owner.from_dictionary(field_value, r_error)) return false;
            psnInfo_has_value = true;
            value.psnInfo = &psnInfo_owner.value;
        }
        if (get_request_value(p_request, "serverCustomIdInfo", "server_custom_id_info", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field server_custom_id_info."; return false; }
            if (!serverCustomIdInfo_owner.from_dictionary(field_value, r_error)) return false;
            serverCustomIdInfo_has_value = true;
            value.serverCustomIdInfo = &serverCustomIdInfo_owner.value;
        }
        if (get_request_value(p_request, "steamInfo", "steam_info", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field steam_info."; return false; }
            if (!steamInfo_owner.from_dictionary(field_value, r_error)) return false;
            steamInfo_has_value = true;
            value.steamInfo = &steamInfo_owner.value;
        }
        if (get_request_value(p_request, "titleInfo", "title_info", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field title_info."; return false; }
            if (!titleInfo_owner.from_dictionary(field_value, r_error)) return false;
            titleInfo_has_value = true;
            value.titleInfo = &titleInfo_owner.value;
        }
        if (get_request_value(p_request, "twitchInfo", "twitch_info", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field twitch_info."; return false; }
            if (!twitchInfo_owner.from_dictionary(field_value, r_error)) return false;
            twitchInfo_has_value = true;
            value.twitchInfo = &twitchInfo_owner.value;
        }
        if (get_request_value(p_request, "username", "username", &field_value)) {
            username_string = String(field_value).utf8().get_data();
            value.username = username_string.empty() ? nullptr : username_string.c_str();
        }
        if (get_request_value(p_request, "xboxInfo", "xbox_info", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field xbox_info."; return false; }
            if (!xboxInfo_owner.from_dictionary(field_value, r_error)) return false;
            xboxInfo_has_value = true;
            value.xboxInfo = &xboxInfo_owner.value;
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetAccountInfoResult {
    PFAccountManagementGetAccountInfoResult value = {};
    OwnedPFUserAccountInfo accountInfo_owner;
    bool accountInfo_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "accountInfo", "account_info", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field account_info."; return false; }
            if (!accountInfo_owner.from_dictionary(field_value, r_error)) return false;
            accountInfo_has_value = true;
            value.accountInfo = &accountInfo_owner.value;
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetPlayFabIDsFromBattleNetAccountIdsRequest {
    PFAccountManagementGetPlayFabIDsFromBattleNetAccountIdsRequest value = {};
    std::vector<std::string> battleNetAccountIds_strings;
    std::vector<const char *> battleNetAccountIds_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "battleNetAccountIds", "battle_net_account_ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field battle_net_account_ids."; return false; }
            Array source_array = field_value;
            battleNetAccountIds_strings.clear(); battleNetAccountIds_ptrs.clear();
            battleNetAccountIds_strings.reserve(source_array.size()); battleNetAccountIds_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                battleNetAccountIds_strings.emplace_back(String(source_array[i]).utf8().get_data());
                battleNetAccountIds_ptrs.push_back(battleNetAccountIds_strings.back().c_str());
            }
            value.battleNetAccountIds = battleNetAccountIds_ptrs.empty() ? nullptr : battleNetAccountIds_ptrs.data();
            value.battleNetAccountIdsCount = static_cast<uint32_t>(battleNetAccountIds_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetPlayFabIDsFromBattleNetAccountIdsResult {
    PFAccountManagementGetPlayFabIDsFromBattleNetAccountIdsResult value = {};
    std::vector<OwnedPFAccountManagementBattleNetAccountPlayFabIdPair> data_items;
    std::vector<const PFAccountManagementBattleNetAccountPlayFabIdPair *> data_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "data", "data", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field data."; return false; }
            Array source_array = field_value;
            data_items.resize(source_array.size());
            data_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field data."; return false; }
                if (!data_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                data_ptrs[static_cast<size_t>(i)] = &data_items[static_cast<size_t>(i)].value;
            }
            value.data = data_ptrs.empty() ? nullptr : data_ptrs.data();
            value.dataCount = static_cast<uint32_t>(data_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetPlayFabIDsFromFacebookIDsRequest {
    PFAccountManagementGetPlayFabIDsFromFacebookIDsRequest value = {};
    std::vector<std::string> facebookIDs_strings;
    std::vector<const char *> facebookIDs_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "facebookIDs", "facebook_ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field facebook_ids."; return false; }
            Array source_array = field_value;
            facebookIDs_strings.clear(); facebookIDs_ptrs.clear();
            facebookIDs_strings.reserve(source_array.size()); facebookIDs_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                facebookIDs_strings.emplace_back(String(source_array[i]).utf8().get_data());
                facebookIDs_ptrs.push_back(facebookIDs_strings.back().c_str());
            }
            value.facebookIDs = facebookIDs_ptrs.empty() ? nullptr : facebookIDs_ptrs.data();
            value.facebookIDsCount = static_cast<uint32_t>(facebookIDs_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetPlayFabIDsFromFacebookIDsResult {
    PFAccountManagementGetPlayFabIDsFromFacebookIDsResult value = {};
    std::vector<OwnedPFAccountManagementFacebookPlayFabIdPair> data_items;
    std::vector<const PFAccountManagementFacebookPlayFabIdPair *> data_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "data", "data", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field data."; return false; }
            Array source_array = field_value;
            data_items.resize(source_array.size());
            data_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field data."; return false; }
                if (!data_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                data_ptrs[static_cast<size_t>(i)] = &data_items[static_cast<size_t>(i)].value;
            }
            value.data = data_ptrs.empty() ? nullptr : data_ptrs.data();
            value.dataCount = static_cast<uint32_t>(data_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetPlayFabIDsFromFacebookInstantGamesIdsRequest {
    PFAccountManagementGetPlayFabIDsFromFacebookInstantGamesIdsRequest value = {};
    std::vector<std::string> facebookInstantGamesIds_strings;
    std::vector<const char *> facebookInstantGamesIds_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "facebookInstantGamesIds", "facebook_instant_games_ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field facebook_instant_games_ids."; return false; }
            Array source_array = field_value;
            facebookInstantGamesIds_strings.clear(); facebookInstantGamesIds_ptrs.clear();
            facebookInstantGamesIds_strings.reserve(source_array.size()); facebookInstantGamesIds_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                facebookInstantGamesIds_strings.emplace_back(String(source_array[i]).utf8().get_data());
                facebookInstantGamesIds_ptrs.push_back(facebookInstantGamesIds_strings.back().c_str());
            }
            value.facebookInstantGamesIds = facebookInstantGamesIds_ptrs.empty() ? nullptr : facebookInstantGamesIds_ptrs.data();
            value.facebookInstantGamesIdsCount = static_cast<uint32_t>(facebookInstantGamesIds_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetPlayFabIDsFromFacebookInstantGamesIdsResult {
    PFAccountManagementGetPlayFabIDsFromFacebookInstantGamesIdsResult value = {};
    std::vector<OwnedPFAccountManagementFacebookInstantGamesPlayFabIdPair> data_items;
    std::vector<const PFAccountManagementFacebookInstantGamesPlayFabIdPair *> data_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "data", "data", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field data."; return false; }
            Array source_array = field_value;
            data_items.resize(source_array.size());
            data_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field data."; return false; }
                if (!data_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                data_ptrs[static_cast<size_t>(i)] = &data_items[static_cast<size_t>(i)].value;
            }
            value.data = data_ptrs.empty() ? nullptr : data_ptrs.data();
            value.dataCount = static_cast<uint32_t>(data_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetPlayFabIDsFromGameCenterIDsRequest {
    PFAccountManagementGetPlayFabIDsFromGameCenterIDsRequest value = {};
    std::vector<std::string> gameCenterIDs_strings;
    std::vector<const char *> gameCenterIDs_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "gameCenterIDs", "game_center_ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field game_center_ids."; return false; }
            Array source_array = field_value;
            gameCenterIDs_strings.clear(); gameCenterIDs_ptrs.clear();
            gameCenterIDs_strings.reserve(source_array.size()); gameCenterIDs_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                gameCenterIDs_strings.emplace_back(String(source_array[i]).utf8().get_data());
                gameCenterIDs_ptrs.push_back(gameCenterIDs_strings.back().c_str());
            }
            value.gameCenterIDs = gameCenterIDs_ptrs.empty() ? nullptr : gameCenterIDs_ptrs.data();
            value.gameCenterIDsCount = static_cast<uint32_t>(gameCenterIDs_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetPlayFabIDsFromGameCenterIDsResult {
    PFAccountManagementGetPlayFabIDsFromGameCenterIDsResult value = {};
    std::vector<OwnedPFAccountManagementGameCenterPlayFabIdPair> data_items;
    std::vector<const PFAccountManagementGameCenterPlayFabIdPair *> data_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "data", "data", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field data."; return false; }
            Array source_array = field_value;
            data_items.resize(source_array.size());
            data_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field data."; return false; }
                if (!data_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                data_ptrs[static_cast<size_t>(i)] = &data_items[static_cast<size_t>(i)].value;
            }
            value.data = data_ptrs.empty() ? nullptr : data_ptrs.data();
            value.dataCount = static_cast<uint32_t>(data_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetPlayFabIDsFromGoogleIDsRequest {
    PFAccountManagementGetPlayFabIDsFromGoogleIDsRequest value = {};
    std::vector<std::string> googleIDs_strings;
    std::vector<const char *> googleIDs_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "googleIDs", "google_ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field google_ids."; return false; }
            Array source_array = field_value;
            googleIDs_strings.clear(); googleIDs_ptrs.clear();
            googleIDs_strings.reserve(source_array.size()); googleIDs_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                googleIDs_strings.emplace_back(String(source_array[i]).utf8().get_data());
                googleIDs_ptrs.push_back(googleIDs_strings.back().c_str());
            }
            value.googleIDs = googleIDs_ptrs.empty() ? nullptr : googleIDs_ptrs.data();
            value.googleIDsCount = static_cast<uint32_t>(googleIDs_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementGooglePlayFabIdPair {
    PFAccountManagementGooglePlayFabIdPair value = {};
    std::string googleId_string;
    std::string playFabId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "googleId", "google_id", &field_value)) {
            googleId_string = String(field_value).utf8().get_data();
            value.googleId = googleId_string.empty() ? nullptr : googleId_string.c_str();
        }
        if (get_request_value(p_request, "playFabId", "play_fab_id", &field_value)) {
            playFabId_string = String(field_value).utf8().get_data();
            value.playFabId = playFabId_string.empty() ? nullptr : playFabId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetPlayFabIDsFromGoogleIDsResult {
    PFAccountManagementGetPlayFabIDsFromGoogleIDsResult value = {};
    std::vector<OwnedPFAccountManagementGooglePlayFabIdPair> data_items;
    std::vector<const PFAccountManagementGooglePlayFabIdPair *> data_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "data", "data", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field data."; return false; }
            Array source_array = field_value;
            data_items.resize(source_array.size());
            data_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field data."; return false; }
                if (!data_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                data_ptrs[static_cast<size_t>(i)] = &data_items[static_cast<size_t>(i)].value;
            }
            value.data = data_ptrs.empty() ? nullptr : data_ptrs.data();
            value.dataCount = static_cast<uint32_t>(data_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetPlayFabIDsFromGooglePlayGamesPlayerIDsRequest {
    PFAccountManagementGetPlayFabIDsFromGooglePlayGamesPlayerIDsRequest value = {};
    std::vector<std::string> googlePlayGamesPlayerIDs_strings;
    std::vector<const char *> googlePlayGamesPlayerIDs_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "googlePlayGamesPlayerIDs", "google_play_games_player_ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field google_play_games_player_ids."; return false; }
            Array source_array = field_value;
            googlePlayGamesPlayerIDs_strings.clear(); googlePlayGamesPlayerIDs_ptrs.clear();
            googlePlayGamesPlayerIDs_strings.reserve(source_array.size()); googlePlayGamesPlayerIDs_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                googlePlayGamesPlayerIDs_strings.emplace_back(String(source_array[i]).utf8().get_data());
                googlePlayGamesPlayerIDs_ptrs.push_back(googlePlayGamesPlayerIDs_strings.back().c_str());
            }
            value.googlePlayGamesPlayerIDs = googlePlayGamesPlayerIDs_ptrs.empty() ? nullptr : googlePlayGamesPlayerIDs_ptrs.data();
            value.googlePlayGamesPlayerIDsCount = static_cast<uint32_t>(googlePlayGamesPlayerIDs_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementGooglePlayGamesPlayFabIdPair {
    PFAccountManagementGooglePlayGamesPlayFabIdPair value = {};
    std::string googlePlayGamesPlayerId_string;
    std::string playFabId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "googlePlayGamesPlayerId", "google_play_games_player_id", &field_value)) {
            googlePlayGamesPlayerId_string = String(field_value).utf8().get_data();
            value.googlePlayGamesPlayerId = googlePlayGamesPlayerId_string.empty() ? nullptr : googlePlayGamesPlayerId_string.c_str();
        }
        if (get_request_value(p_request, "playFabId", "play_fab_id", &field_value)) {
            playFabId_string = String(field_value).utf8().get_data();
            value.playFabId = playFabId_string.empty() ? nullptr : playFabId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetPlayFabIDsFromGooglePlayGamesPlayerIDsResult {
    PFAccountManagementGetPlayFabIDsFromGooglePlayGamesPlayerIDsResult value = {};
    std::vector<OwnedPFAccountManagementGooglePlayGamesPlayFabIdPair> data_items;
    std::vector<const PFAccountManagementGooglePlayGamesPlayFabIdPair *> data_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "data", "data", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field data."; return false; }
            Array source_array = field_value;
            data_items.resize(source_array.size());
            data_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field data."; return false; }
                if (!data_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                data_ptrs[static_cast<size_t>(i)] = &data_items[static_cast<size_t>(i)].value;
            }
            value.data = data_ptrs.empty() ? nullptr : data_ptrs.data();
            value.dataCount = static_cast<uint32_t>(data_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetPlayFabIDsFromKongregateIDsRequest {
    PFAccountManagementGetPlayFabIDsFromKongregateIDsRequest value = {};
    std::vector<std::string> kongregateIDs_strings;
    std::vector<const char *> kongregateIDs_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "kongregateIDs", "kongregate_ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field kongregate_ids."; return false; }
            Array source_array = field_value;
            kongregateIDs_strings.clear(); kongregateIDs_ptrs.clear();
            kongregateIDs_strings.reserve(source_array.size()); kongregateIDs_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                kongregateIDs_strings.emplace_back(String(source_array[i]).utf8().get_data());
                kongregateIDs_ptrs.push_back(kongregateIDs_strings.back().c_str());
            }
            value.kongregateIDs = kongregateIDs_ptrs.empty() ? nullptr : kongregateIDs_ptrs.data();
            value.kongregateIDsCount = static_cast<uint32_t>(kongregateIDs_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementKongregatePlayFabIdPair {
    PFAccountManagementKongregatePlayFabIdPair value = {};
    std::string kongregateId_string;
    std::string playFabId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "kongregateId", "kongregate_id", &field_value)) {
            kongregateId_string = String(field_value).utf8().get_data();
            value.kongregateId = kongregateId_string.empty() ? nullptr : kongregateId_string.c_str();
        }
        if (get_request_value(p_request, "playFabId", "play_fab_id", &field_value)) {
            playFabId_string = String(field_value).utf8().get_data();
            value.playFabId = playFabId_string.empty() ? nullptr : playFabId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetPlayFabIDsFromKongregateIDsResult {
    PFAccountManagementGetPlayFabIDsFromKongregateIDsResult value = {};
    std::vector<OwnedPFAccountManagementKongregatePlayFabIdPair> data_items;
    std::vector<const PFAccountManagementKongregatePlayFabIdPair *> data_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "data", "data", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field data."; return false; }
            Array source_array = field_value;
            data_items.resize(source_array.size());
            data_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field data."; return false; }
                if (!data_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                data_ptrs[static_cast<size_t>(i)] = &data_items[static_cast<size_t>(i)].value;
            }
            value.data = data_ptrs.empty() ? nullptr : data_ptrs.data();
            value.dataCount = static_cast<uint32_t>(data_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetPlayFabIDsFromNintendoServiceAccountIdsRequest {
    PFAccountManagementGetPlayFabIDsFromNintendoServiceAccountIdsRequest value = {};
    std::vector<std::string> nintendoAccountIds_strings;
    std::vector<const char *> nintendoAccountIds_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "nintendoAccountIds", "nintendo_account_ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field nintendo_account_ids."; return false; }
            Array source_array = field_value;
            nintendoAccountIds_strings.clear(); nintendoAccountIds_ptrs.clear();
            nintendoAccountIds_strings.reserve(source_array.size()); nintendoAccountIds_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                nintendoAccountIds_strings.emplace_back(String(source_array[i]).utf8().get_data());
                nintendoAccountIds_ptrs.push_back(nintendoAccountIds_strings.back().c_str());
            }
            value.nintendoAccountIds = nintendoAccountIds_ptrs.empty() ? nullptr : nintendoAccountIds_ptrs.data();
            value.nintendoAccountIdsCount = static_cast<uint32_t>(nintendoAccountIds_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementNintendoServiceAccountPlayFabIdPair {
    PFAccountManagementNintendoServiceAccountPlayFabIdPair value = {};
    std::string nintendoServiceAccountId_string;
    std::string playFabId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "nintendoServiceAccountId", "nintendo_service_account_id", &field_value)) {
            nintendoServiceAccountId_string = String(field_value).utf8().get_data();
            value.nintendoServiceAccountId = nintendoServiceAccountId_string.empty() ? nullptr : nintendoServiceAccountId_string.c_str();
        }
        if (get_request_value(p_request, "playFabId", "play_fab_id", &field_value)) {
            playFabId_string = String(field_value).utf8().get_data();
            value.playFabId = playFabId_string.empty() ? nullptr : playFabId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetPlayFabIDsFromNintendoServiceAccountIdsResult {
    PFAccountManagementGetPlayFabIDsFromNintendoServiceAccountIdsResult value = {};
    std::vector<OwnedPFAccountManagementNintendoServiceAccountPlayFabIdPair> data_items;
    std::vector<const PFAccountManagementNintendoServiceAccountPlayFabIdPair *> data_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "data", "data", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field data."; return false; }
            Array source_array = field_value;
            data_items.resize(source_array.size());
            data_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field data."; return false; }
                if (!data_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                data_ptrs[static_cast<size_t>(i)] = &data_items[static_cast<size_t>(i)].value;
            }
            value.data = data_ptrs.empty() ? nullptr : data_ptrs.data();
            value.dataCount = static_cast<uint32_t>(data_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetPlayFabIDsFromNintendoSwitchDeviceIdsRequest {
    PFAccountManagementGetPlayFabIDsFromNintendoSwitchDeviceIdsRequest value = {};
    std::vector<std::string> nintendoSwitchDeviceIds_strings;
    std::vector<const char *> nintendoSwitchDeviceIds_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "nintendoSwitchDeviceIds", "nintendo_switch_device_ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field nintendo_switch_device_ids."; return false; }
            Array source_array = field_value;
            nintendoSwitchDeviceIds_strings.clear(); nintendoSwitchDeviceIds_ptrs.clear();
            nintendoSwitchDeviceIds_strings.reserve(source_array.size()); nintendoSwitchDeviceIds_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                nintendoSwitchDeviceIds_strings.emplace_back(String(source_array[i]).utf8().get_data());
                nintendoSwitchDeviceIds_ptrs.push_back(nintendoSwitchDeviceIds_strings.back().c_str());
            }
            value.nintendoSwitchDeviceIds = nintendoSwitchDeviceIds_ptrs.empty() ? nullptr : nintendoSwitchDeviceIds_ptrs.data();
            value.nintendoSwitchDeviceIdsCount = static_cast<uint32_t>(nintendoSwitchDeviceIds_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementNintendoSwitchPlayFabIdPair {
    PFAccountManagementNintendoSwitchPlayFabIdPair value = {};
    std::string nintendoSwitchDeviceId_string;
    std::string playFabId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "nintendoSwitchDeviceId", "nintendo_switch_device_id", &field_value)) {
            nintendoSwitchDeviceId_string = String(field_value).utf8().get_data();
            value.nintendoSwitchDeviceId = nintendoSwitchDeviceId_string.empty() ? nullptr : nintendoSwitchDeviceId_string.c_str();
        }
        if (get_request_value(p_request, "playFabId", "play_fab_id", &field_value)) {
            playFabId_string = String(field_value).utf8().get_data();
            value.playFabId = playFabId_string.empty() ? nullptr : playFabId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetPlayFabIDsFromNintendoSwitchDeviceIdsResult {
    PFAccountManagementGetPlayFabIDsFromNintendoSwitchDeviceIdsResult value = {};
    std::vector<OwnedPFAccountManagementNintendoSwitchPlayFabIdPair> data_items;
    std::vector<const PFAccountManagementNintendoSwitchPlayFabIdPair *> data_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "data", "data", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field data."; return false; }
            Array source_array = field_value;
            data_items.resize(source_array.size());
            data_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field data."; return false; }
                if (!data_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                data_ptrs[static_cast<size_t>(i)] = &data_items[static_cast<size_t>(i)].value;
            }
            value.data = data_ptrs.empty() ? nullptr : data_ptrs.data();
            value.dataCount = static_cast<uint32_t>(data_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetPlayFabIDsFromPSNAccountIDsRequest {
    PFAccountManagementGetPlayFabIDsFromPSNAccountIDsRequest value = {};
    int32_t issuerId_storage = {};
    std::vector<std::string> PSNAccountIDs_strings;
    std::vector<const char *> PSNAccountIDs_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "issuerId", "issuer_id", &field_value)) {
            issuerId_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.issuerId = &issuerId_storage;
        }
        if (get_request_value(p_request, "PSNAccountIDs", "psn_account_ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field psn_account_ids."; return false; }
            Array source_array = field_value;
            PSNAccountIDs_strings.clear(); PSNAccountIDs_ptrs.clear();
            PSNAccountIDs_strings.reserve(source_array.size()); PSNAccountIDs_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                PSNAccountIDs_strings.emplace_back(String(source_array[i]).utf8().get_data());
                PSNAccountIDs_ptrs.push_back(PSNAccountIDs_strings.back().c_str());
            }
            value.PSNAccountIDs = PSNAccountIDs_ptrs.empty() ? nullptr : PSNAccountIDs_ptrs.data();
            value.PSNAccountIDsCount = static_cast<uint32_t>(PSNAccountIDs_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementPSNAccountPlayFabIdPair {
    PFAccountManagementPSNAccountPlayFabIdPair value = {};
    std::string playFabId_string;
    std::string PSNAccountId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "playFabId", "play_fab_id", &field_value)) {
            playFabId_string = String(field_value).utf8().get_data();
            value.playFabId = playFabId_string.empty() ? nullptr : playFabId_string.c_str();
        }
        if (get_request_value(p_request, "PSNAccountId", "psn_account_id", &field_value)) {
            PSNAccountId_string = String(field_value).utf8().get_data();
            value.PSNAccountId = PSNAccountId_string.empty() ? nullptr : PSNAccountId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetPlayFabIDsFromPSNAccountIDsResult {
    PFAccountManagementGetPlayFabIDsFromPSNAccountIDsResult value = {};
    std::vector<OwnedPFAccountManagementPSNAccountPlayFabIdPair> data_items;
    std::vector<const PFAccountManagementPSNAccountPlayFabIdPair *> data_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "data", "data", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field data."; return false; }
            Array source_array = field_value;
            data_items.resize(source_array.size());
            data_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field data."; return false; }
                if (!data_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                data_ptrs[static_cast<size_t>(i)] = &data_items[static_cast<size_t>(i)].value;
            }
            value.data = data_ptrs.empty() ? nullptr : data_ptrs.data();
            value.dataCount = static_cast<uint32_t>(data_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetPlayFabIDsFromPSNOnlineIDsRequest {
    PFAccountManagementGetPlayFabIDsFromPSNOnlineIDsRequest value = {};
    int32_t issuerId_storage = {};
    std::vector<std::string> PSNOnlineIDs_strings;
    std::vector<const char *> PSNOnlineIDs_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "issuerId", "issuer_id", &field_value)) {
            issuerId_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.issuerId = &issuerId_storage;
        }
        if (get_request_value(p_request, "PSNOnlineIDs", "psn_online_ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field psn_online_ids."; return false; }
            Array source_array = field_value;
            PSNOnlineIDs_strings.clear(); PSNOnlineIDs_ptrs.clear();
            PSNOnlineIDs_strings.reserve(source_array.size()); PSNOnlineIDs_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                PSNOnlineIDs_strings.emplace_back(String(source_array[i]).utf8().get_data());
                PSNOnlineIDs_ptrs.push_back(PSNOnlineIDs_strings.back().c_str());
            }
            value.PSNOnlineIDs = PSNOnlineIDs_ptrs.empty() ? nullptr : PSNOnlineIDs_ptrs.data();
            value.PSNOnlineIDsCount = static_cast<uint32_t>(PSNOnlineIDs_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementPSNOnlinePlayFabIdPair {
    PFAccountManagementPSNOnlinePlayFabIdPair value = {};
    std::string playFabId_string;
    std::string PSNOnlineId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "playFabId", "play_fab_id", &field_value)) {
            playFabId_string = String(field_value).utf8().get_data();
            value.playFabId = playFabId_string.empty() ? nullptr : playFabId_string.c_str();
        }
        if (get_request_value(p_request, "PSNOnlineId", "psn_online_id", &field_value)) {
            PSNOnlineId_string = String(field_value).utf8().get_data();
            value.PSNOnlineId = PSNOnlineId_string.empty() ? nullptr : PSNOnlineId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetPlayFabIDsFromPSNOnlineIDsResult {
    PFAccountManagementGetPlayFabIDsFromPSNOnlineIDsResult value = {};
    std::vector<OwnedPFAccountManagementPSNOnlinePlayFabIdPair> data_items;
    std::vector<const PFAccountManagementPSNOnlinePlayFabIdPair *> data_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "data", "data", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field data."; return false; }
            Array source_array = field_value;
            data_items.resize(source_array.size());
            data_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field data."; return false; }
                if (!data_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                data_ptrs[static_cast<size_t>(i)] = &data_items[static_cast<size_t>(i)].value;
            }
            value.data = data_ptrs.empty() ? nullptr : data_ptrs.data();
            value.dataCount = static_cast<uint32_t>(data_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetPlayFabIDsFromSteamIDsRequest {
    PFAccountManagementGetPlayFabIDsFromSteamIDsRequest value = {};
    std::vector<std::string> steamStringIDs_strings;
    std::vector<const char *> steamStringIDs_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "steamStringIDs", "steam_string_ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field steam_string_ids."; return false; }
            Array source_array = field_value;
            steamStringIDs_strings.clear(); steamStringIDs_ptrs.clear();
            steamStringIDs_strings.reserve(source_array.size()); steamStringIDs_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                steamStringIDs_strings.emplace_back(String(source_array[i]).utf8().get_data());
                steamStringIDs_ptrs.push_back(steamStringIDs_strings.back().c_str());
            }
            value.steamStringIDs = steamStringIDs_ptrs.empty() ? nullptr : steamStringIDs_ptrs.data();
            value.steamStringIDsCount = static_cast<uint32_t>(steamStringIDs_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementSteamPlayFabIdPair {
    PFAccountManagementSteamPlayFabIdPair value = {};
    std::string playFabId_string;
    std::string steamStringId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "playFabId", "play_fab_id", &field_value)) {
            playFabId_string = String(field_value).utf8().get_data();
            value.playFabId = playFabId_string.empty() ? nullptr : playFabId_string.c_str();
        }
        if (get_request_value(p_request, "steamStringId", "steam_string_id", &field_value)) {
            steamStringId_string = String(field_value).utf8().get_data();
            value.steamStringId = steamStringId_string.empty() ? nullptr : steamStringId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetPlayFabIDsFromSteamIDsResult {
    PFAccountManagementGetPlayFabIDsFromSteamIDsResult value = {};
    std::vector<OwnedPFAccountManagementSteamPlayFabIdPair> data_items;
    std::vector<const PFAccountManagementSteamPlayFabIdPair *> data_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "data", "data", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field data."; return false; }
            Array source_array = field_value;
            data_items.resize(source_array.size());
            data_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field data."; return false; }
                if (!data_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                data_ptrs[static_cast<size_t>(i)] = &data_items[static_cast<size_t>(i)].value;
            }
            value.data = data_ptrs.empty() ? nullptr : data_ptrs.data();
            value.dataCount = static_cast<uint32_t>(data_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetPlayFabIDsFromSteamNamesRequest {
    PFAccountManagementGetPlayFabIDsFromSteamNamesRequest value = {};
    std::vector<std::string> steamNames_strings;
    std::vector<const char *> steamNames_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "steamNames", "steam_names", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field steam_names."; return false; }
            Array source_array = field_value;
            steamNames_strings.clear(); steamNames_ptrs.clear();
            steamNames_strings.reserve(source_array.size()); steamNames_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                steamNames_strings.emplace_back(String(source_array[i]).utf8().get_data());
                steamNames_ptrs.push_back(steamNames_strings.back().c_str());
            }
            value.steamNames = steamNames_ptrs.empty() ? nullptr : steamNames_ptrs.data();
            value.steamNamesCount = static_cast<uint32_t>(steamNames_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementSteamNamePlayFabIdPair {
    PFAccountManagementSteamNamePlayFabIdPair value = {};
    std::string playFabId_string;
    std::string steamName_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "playFabId", "play_fab_id", &field_value)) {
            playFabId_string = String(field_value).utf8().get_data();
            value.playFabId = playFabId_string.empty() ? nullptr : playFabId_string.c_str();
        }
        if (get_request_value(p_request, "steamName", "steam_name", &field_value)) {
            steamName_string = String(field_value).utf8().get_data();
            value.steamName = steamName_string.empty() ? nullptr : steamName_string.c_str();
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetPlayFabIDsFromSteamNamesResult {
    PFAccountManagementGetPlayFabIDsFromSteamNamesResult value = {};
    std::vector<OwnedPFAccountManagementSteamNamePlayFabIdPair> data_items;
    std::vector<const PFAccountManagementSteamNamePlayFabIdPair *> data_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "data", "data", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field data."; return false; }
            Array source_array = field_value;
            data_items.resize(source_array.size());
            data_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field data."; return false; }
                if (!data_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                data_ptrs[static_cast<size_t>(i)] = &data_items[static_cast<size_t>(i)].value;
            }
            value.data = data_ptrs.empty() ? nullptr : data_ptrs.data();
            value.dataCount = static_cast<uint32_t>(data_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetPlayFabIDsFromTwitchIDsRequest {
    PFAccountManagementGetPlayFabIDsFromTwitchIDsRequest value = {};
    std::vector<std::string> twitchIds_strings;
    std::vector<const char *> twitchIds_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "twitchIds", "twitch_ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field twitch_ids."; return false; }
            Array source_array = field_value;
            twitchIds_strings.clear(); twitchIds_ptrs.clear();
            twitchIds_strings.reserve(source_array.size()); twitchIds_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                twitchIds_strings.emplace_back(String(source_array[i]).utf8().get_data());
                twitchIds_ptrs.push_back(twitchIds_strings.back().c_str());
            }
            value.twitchIds = twitchIds_ptrs.empty() ? nullptr : twitchIds_ptrs.data();
            value.twitchIdsCount = static_cast<uint32_t>(twitchIds_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementTwitchPlayFabIdPair {
    PFAccountManagementTwitchPlayFabIdPair value = {};
    std::string playFabId_string;
    std::string twitchId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "playFabId", "play_fab_id", &field_value)) {
            playFabId_string = String(field_value).utf8().get_data();
            value.playFabId = playFabId_string.empty() ? nullptr : playFabId_string.c_str();
        }
        if (get_request_value(p_request, "twitchId", "twitch_id", &field_value)) {
            twitchId_string = String(field_value).utf8().get_data();
            value.twitchId = twitchId_string.empty() ? nullptr : twitchId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetPlayFabIDsFromTwitchIDsResult {
    PFAccountManagementGetPlayFabIDsFromTwitchIDsResult value = {};
    std::vector<OwnedPFAccountManagementTwitchPlayFabIdPair> data_items;
    std::vector<const PFAccountManagementTwitchPlayFabIdPair *> data_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "data", "data", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field data."; return false; }
            Array source_array = field_value;
            data_items.resize(source_array.size());
            data_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field data."; return false; }
                if (!data_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                data_ptrs[static_cast<size_t>(i)] = &data_items[static_cast<size_t>(i)].value;
            }
            value.data = data_ptrs.empty() ? nullptr : data_ptrs.data();
            value.dataCount = static_cast<uint32_t>(data_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetPlayFabIDsFromXboxLiveIDsRequest {
    PFAccountManagementGetPlayFabIDsFromXboxLiveIDsRequest value = {};
    std::string sandbox_string;
    std::vector<std::string> xboxLiveAccountIDs_strings;
    std::vector<const char *> xboxLiveAccountIDs_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "sandbox", "sandbox", &field_value)) {
            sandbox_string = String(field_value).utf8().get_data();
            value.sandbox = sandbox_string.empty() ? nullptr : sandbox_string.c_str();
        }
        if (get_request_value(p_request, "xboxLiveAccountIDs", "xbox_live_account_ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field xbox_live_account_ids."; return false; }
            Array source_array = field_value;
            xboxLiveAccountIDs_strings.clear(); xboxLiveAccountIDs_ptrs.clear();
            xboxLiveAccountIDs_strings.reserve(source_array.size()); xboxLiveAccountIDs_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                xboxLiveAccountIDs_strings.emplace_back(String(source_array[i]).utf8().get_data());
                xboxLiveAccountIDs_ptrs.push_back(xboxLiveAccountIDs_strings.back().c_str());
            }
            value.xboxLiveAccountIDs = xboxLiveAccountIDs_ptrs.empty() ? nullptr : xboxLiveAccountIDs_ptrs.data();
            value.xboxLiveAccountIDsCount = static_cast<uint32_t>(xboxLiveAccountIDs_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementXboxLiveAccountPlayFabIdPair {
    PFAccountManagementXboxLiveAccountPlayFabIdPair value = {};
    std::string playFabId_string;
    std::string xboxLiveAccountId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "playFabId", "play_fab_id", &field_value)) {
            playFabId_string = String(field_value).utf8().get_data();
            value.playFabId = playFabId_string.empty() ? nullptr : playFabId_string.c_str();
        }
        if (get_request_value(p_request, "xboxLiveAccountId", "xbox_live_account_id", &field_value)) {
            xboxLiveAccountId_string = String(field_value).utf8().get_data();
            value.xboxLiveAccountId = xboxLiveAccountId_string.empty() ? nullptr : xboxLiveAccountId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetPlayFabIDsFromXboxLiveIDsResult {
    PFAccountManagementGetPlayFabIDsFromXboxLiveIDsResult value = {};
    std::vector<OwnedPFAccountManagementXboxLiveAccountPlayFabIdPair> data_items;
    std::vector<const PFAccountManagementXboxLiveAccountPlayFabIdPair *> data_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "data", "data", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field data."; return false; }
            Array source_array = field_value;
            data_items.resize(source_array.size());
            data_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field data."; return false; }
                if (!data_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                data_ptrs[static_cast<size_t>(i)] = &data_items[static_cast<size_t>(i)].value;
            }
            value.data = data_ptrs.empty() ? nullptr : data_ptrs.data();
            value.dataCount = static_cast<uint32_t>(data_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFPlayerProfileViewConstraints {
    PFPlayerProfileViewConstraints value = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "showAvatarUrl", "show_avatar_url", &field_value)) {
            value.showAvatarUrl = static_cast<bool>(field_value);
        }
        if (get_request_value(p_request, "showBannedUntil", "show_banned_until", &field_value)) {
            value.showBannedUntil = static_cast<bool>(field_value);
        }
        if (get_request_value(p_request, "showCampaignAttributions", "show_campaign_attributions", &field_value)) {
            value.showCampaignAttributions = static_cast<bool>(field_value);
        }
        if (get_request_value(p_request, "showContactEmailAddresses", "show_contact_email_addresses", &field_value)) {
            value.showContactEmailAddresses = static_cast<bool>(field_value);
        }
        if (get_request_value(p_request, "showCreated", "show_created", &field_value)) {
            value.showCreated = static_cast<bool>(field_value);
        }
        if (get_request_value(p_request, "showDisplayName", "show_display_name", &field_value)) {
            value.showDisplayName = static_cast<bool>(field_value);
        }
        if (get_request_value(p_request, "showExperimentVariants", "show_experiment_variants", &field_value)) {
            value.showExperimentVariants = static_cast<bool>(field_value);
        }
        if (get_request_value(p_request, "showLastLogin", "show_last_login", &field_value)) {
            value.showLastLogin = static_cast<bool>(field_value);
        }
        if (get_request_value(p_request, "showLinkedAccounts", "show_linked_accounts", &field_value)) {
            value.showLinkedAccounts = static_cast<bool>(field_value);
        }
        if (get_request_value(p_request, "showLocations", "show_locations", &field_value)) {
            value.showLocations = static_cast<bool>(field_value);
        }
        if (get_request_value(p_request, "showMemberships", "show_memberships", &field_value)) {
            value.showMemberships = static_cast<bool>(field_value);
        }
        if (get_request_value(p_request, "showOrigination", "show_origination", &field_value)) {
            value.showOrigination = static_cast<bool>(field_value);
        }
        if (get_request_value(p_request, "showPushNotificationRegistrations", "show_push_notification_registrations", &field_value)) {
            value.showPushNotificationRegistrations = static_cast<bool>(field_value);
        }
        if (get_request_value(p_request, "showStatistics", "show_statistics", &field_value)) {
            value.showStatistics = static_cast<bool>(field_value);
        }
        if (get_request_value(p_request, "showTags", "show_tags", &field_value)) {
            value.showTags = static_cast<bool>(field_value);
        }
        if (get_request_value(p_request, "showTotalValueToDateInUsd", "show_total_value_to_date_in_usd", &field_value)) {
            value.showTotalValueToDateInUsd = static_cast<bool>(field_value);
        }
        if (get_request_value(p_request, "showValuesToDate", "show_values_to_date", &field_value)) {
            value.showValuesToDate = static_cast<bool>(field_value);
        }
        return true;
    }
};

struct OwnedPFGetPlayerCombinedInfoRequestParams {
    PFGetPlayerCombinedInfoRequestParams value = {};
    std::vector<std::string> playerStatisticNames_strings;
    std::vector<const char *> playerStatisticNames_ptrs;
    OwnedPFPlayerProfileViewConstraints profileConstraints_owner;
    bool profileConstraints_has_value = false;
    std::vector<std::string> titleDataKeys_strings;
    std::vector<const char *> titleDataKeys_ptrs;
    std::vector<std::string> userDataKeys_strings;
    std::vector<const char *> userDataKeys_ptrs;
    std::vector<std::string> userReadOnlyDataKeys_strings;
    std::vector<const char *> userReadOnlyDataKeys_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "getCharacterInventories", "get_character_inventories", &field_value)) {
            value.getCharacterInventories = static_cast<bool>(field_value);
        }
        if (get_request_value(p_request, "getCharacterList", "get_character_list", &field_value)) {
            value.getCharacterList = static_cast<bool>(field_value);
        }
        if (get_request_value(p_request, "getPlayerProfile", "get_player_profile", &field_value)) {
            value.getPlayerProfile = static_cast<bool>(field_value);
        }
        if (get_request_value(p_request, "getPlayerStatistics", "get_player_statistics", &field_value)) {
            value.getPlayerStatistics = static_cast<bool>(field_value);
        }
        if (get_request_value(p_request, "getTitleData", "get_title_data", &field_value)) {
            value.getTitleData = static_cast<bool>(field_value);
        }
        if (get_request_value(p_request, "getUserAccountInfo", "get_user_account_info", &field_value)) {
            value.getUserAccountInfo = static_cast<bool>(field_value);
        }
        if (get_request_value(p_request, "getUserData", "get_user_data", &field_value)) {
            value.getUserData = static_cast<bool>(field_value);
        }
        if (get_request_value(p_request, "getUserInventory", "get_user_inventory", &field_value)) {
            value.getUserInventory = static_cast<bool>(field_value);
        }
        if (get_request_value(p_request, "getUserReadOnlyData", "get_user_read_only_data", &field_value)) {
            value.getUserReadOnlyData = static_cast<bool>(field_value);
        }
        if (get_request_value(p_request, "getUserVirtualCurrency", "get_user_virtual_currency", &field_value)) {
            value.getUserVirtualCurrency = static_cast<bool>(field_value);
        }
        if (get_request_value(p_request, "playerStatisticNames", "player_statistic_names", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field player_statistic_names."; return false; }
            Array source_array = field_value;
            playerStatisticNames_strings.clear(); playerStatisticNames_ptrs.clear();
            playerStatisticNames_strings.reserve(source_array.size()); playerStatisticNames_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                playerStatisticNames_strings.emplace_back(String(source_array[i]).utf8().get_data());
                playerStatisticNames_ptrs.push_back(playerStatisticNames_strings.back().c_str());
            }
            value.playerStatisticNames = playerStatisticNames_ptrs.empty() ? nullptr : playerStatisticNames_ptrs.data();
            value.playerStatisticNamesCount = static_cast<uint32_t>(playerStatisticNames_ptrs.size());
        }
        if (get_request_value(p_request, "profileConstraints", "profile_constraints", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field profile_constraints."; return false; }
            if (!profileConstraints_owner.from_dictionary(field_value, r_error)) return false;
            profileConstraints_has_value = true;
            value.profileConstraints = &profileConstraints_owner.value;
        }
        if (get_request_value(p_request, "titleDataKeys", "title_data_keys", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field title_data_keys."; return false; }
            Array source_array = field_value;
            titleDataKeys_strings.clear(); titleDataKeys_ptrs.clear();
            titleDataKeys_strings.reserve(source_array.size()); titleDataKeys_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                titleDataKeys_strings.emplace_back(String(source_array[i]).utf8().get_data());
                titleDataKeys_ptrs.push_back(titleDataKeys_strings.back().c_str());
            }
            value.titleDataKeys = titleDataKeys_ptrs.empty() ? nullptr : titleDataKeys_ptrs.data();
            value.titleDataKeysCount = static_cast<uint32_t>(titleDataKeys_ptrs.size());
        }
        if (get_request_value(p_request, "userDataKeys", "user_data_keys", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field user_data_keys."; return false; }
            Array source_array = field_value;
            userDataKeys_strings.clear(); userDataKeys_ptrs.clear();
            userDataKeys_strings.reserve(source_array.size()); userDataKeys_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                userDataKeys_strings.emplace_back(String(source_array[i]).utf8().get_data());
                userDataKeys_ptrs.push_back(userDataKeys_strings.back().c_str());
            }
            value.userDataKeys = userDataKeys_ptrs.empty() ? nullptr : userDataKeys_ptrs.data();
            value.userDataKeysCount = static_cast<uint32_t>(userDataKeys_ptrs.size());
        }
        if (get_request_value(p_request, "userReadOnlyDataKeys", "user_read_only_data_keys", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field user_read_only_data_keys."; return false; }
            Array source_array = field_value;
            userReadOnlyDataKeys_strings.clear(); userReadOnlyDataKeys_ptrs.clear();
            userReadOnlyDataKeys_strings.reserve(source_array.size()); userReadOnlyDataKeys_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                userReadOnlyDataKeys_strings.emplace_back(String(source_array[i]).utf8().get_data());
                userReadOnlyDataKeys_ptrs.push_back(userReadOnlyDataKeys_strings.back().c_str());
            }
            value.userReadOnlyDataKeys = userReadOnlyDataKeys_ptrs.empty() ? nullptr : userReadOnlyDataKeys_ptrs.data();
            value.userReadOnlyDataKeysCount = static_cast<uint32_t>(userReadOnlyDataKeys_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetPlayerCombinedInfoRequest {
    PFAccountManagementGetPlayerCombinedInfoRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFGetPlayerCombinedInfoRequestParams infoRequestParameters_owner;
    bool infoRequestParameters_has_value = false;
    std::string playFabId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "infoRequestParameters", "info_request_parameters", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field info_request_parameters."; return false; }
            if (!infoRequestParameters_owner.from_dictionary(field_value, r_error)) return false;
            infoRequestParameters_has_value = true;
            value.infoRequestParameters = &infoRequestParameters_owner.value;
        }
        if (get_request_value(p_request, "playFabId", "play_fab_id", &field_value)) {
            playFabId_string = String(field_value).utf8().get_data();
            value.playFabId = playFabId_string.empty() ? nullptr : playFabId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFItemInstance {
    PFItemInstance value = {};
    std::string annotation_string;
    std::vector<std::string> bundleContents_strings;
    std::vector<const char *> bundleContents_ptrs;
    std::string bundleParent_string;
    std::string catalogVersion_string;
    std::vector<OwnedPFStringDictionaryEntry> customData_items;
    std::vector<PFStringDictionaryEntry> customData_values;
    std::string displayName_string;
    time_t expiration_storage = {};
    std::string itemClass_string;
    std::string itemId_string;
    std::string itemInstanceId_string;
    time_t purchaseDate_storage = {};
    int32_t remainingUses_storage = {};
    std::string unitCurrency_string;
    int32_t usesIncrementedBy_storage = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "annotation", "annotation", &field_value)) {
            annotation_string = String(field_value).utf8().get_data();
            value.annotation = annotation_string.empty() ? nullptr : annotation_string.c_str();
        }
        if (get_request_value(p_request, "bundleContents", "bundle_contents", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field bundle_contents."; return false; }
            Array source_array = field_value;
            bundleContents_strings.clear(); bundleContents_ptrs.clear();
            bundleContents_strings.reserve(source_array.size()); bundleContents_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                bundleContents_strings.emplace_back(String(source_array[i]).utf8().get_data());
                bundleContents_ptrs.push_back(bundleContents_strings.back().c_str());
            }
            value.bundleContents = bundleContents_ptrs.empty() ? nullptr : bundleContents_ptrs.data();
            value.bundleContentsCount = static_cast<uint32_t>(bundleContents_ptrs.size());
        }
        if (get_request_value(p_request, "bundleParent", "bundle_parent", &field_value)) {
            bundleParent_string = String(field_value).utf8().get_data();
            value.bundleParent = bundleParent_string.empty() ? nullptr : bundleParent_string.c_str();
        }
        if (get_request_value(p_request, "catalogVersion", "catalog_version", &field_value)) {
            catalogVersion_string = String(field_value).utf8().get_data();
            value.catalogVersion = catalogVersion_string.empty() ? nullptr : catalogVersion_string.c_str();
        }
        if (get_request_value(p_request, "customData", "custom_data", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_data."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customData_items.resize(keys.size());
            customData_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customData_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customData_values[static_cast<size_t>(i)] = customData_items[static_cast<size_t>(i)].value;
            }
            value.customData = customData_values.empty() ? nullptr : customData_values.data();
            value.customDataCount = static_cast<uint32_t>(customData_values.size());
        }
        if (get_request_value(p_request, "displayName", "display_name", &field_value)) {
            displayName_string = String(field_value).utf8().get_data();
            value.displayName = displayName_string.empty() ? nullptr : displayName_string.c_str();
        }
        if (get_request_value(p_request, "expiration", "expiration", &field_value)) {
            expiration_storage = static_cast<time_t>(static_cast<int64_t>(field_value));
            value.expiration = &expiration_storage;
        }
        if (get_request_value(p_request, "itemClass", "item_class", &field_value)) {
            itemClass_string = String(field_value).utf8().get_data();
            value.itemClass = itemClass_string.empty() ? nullptr : itemClass_string.c_str();
        }
        if (get_request_value(p_request, "itemId", "item_id", &field_value)) {
            itemId_string = String(field_value).utf8().get_data();
            value.itemId = itemId_string.empty() ? nullptr : itemId_string.c_str();
        }
        if (get_request_value(p_request, "itemInstanceId", "item_instance_id", &field_value)) {
            itemInstanceId_string = String(field_value).utf8().get_data();
            value.itemInstanceId = itemInstanceId_string.empty() ? nullptr : itemInstanceId_string.c_str();
        }
        if (get_request_value(p_request, "purchaseDate", "purchase_date", &field_value)) {
            purchaseDate_storage = static_cast<time_t>(static_cast<int64_t>(field_value));
            value.purchaseDate = &purchaseDate_storage;
        }
        if (get_request_value(p_request, "remainingUses", "remaining_uses", &field_value)) {
            remainingUses_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.remainingUses = &remainingUses_storage;
        }
        if (get_request_value(p_request, "unitCurrency", "unit_currency", &field_value)) {
            unitCurrency_string = String(field_value).utf8().get_data();
            value.unitCurrency = unitCurrency_string.empty() ? nullptr : unitCurrency_string.c_str();
        }
        if (get_request_value(p_request, "unitPrice", "unit_price", &field_value)) {
            value.unitPrice = static_cast<uint32_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "usesIncrementedBy", "uses_incremented_by", &field_value)) {
            usesIncrementedBy_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.usesIncrementedBy = &usesIncrementedBy_storage;
        }
        return true;
    }
};

struct OwnedPFCharacterInventory {
    PFCharacterInventory value = {};
    std::string characterId_string;
    std::vector<OwnedPFItemInstance> inventory_items;
    std::vector<const PFItemInstance *> inventory_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "characterId", "character_id", &field_value)) {
            characterId_string = String(field_value).utf8().get_data();
            value.characterId = characterId_string.empty() ? nullptr : characterId_string.c_str();
        }
        if (get_request_value(p_request, "inventory", "inventory", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field inventory."; return false; }
            Array source_array = field_value;
            inventory_items.resize(source_array.size());
            inventory_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field inventory."; return false; }
                if (!inventory_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                inventory_ptrs[static_cast<size_t>(i)] = &inventory_items[static_cast<size_t>(i)].value;
            }
            value.inventory = inventory_ptrs.empty() ? nullptr : inventory_ptrs.data();
            value.inventoryCount = static_cast<uint32_t>(inventory_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFCharacterResult {
    PFCharacterResult value = {};
    std::string characterId_string;
    std::string characterName_string;
    std::string characterType_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "characterId", "character_id", &field_value)) {
            characterId_string = String(field_value).utf8().get_data();
            value.characterId = characterId_string.empty() ? nullptr : characterId_string.c_str();
        }
        if (get_request_value(p_request, "characterName", "character_name", &field_value)) {
            characterName_string = String(field_value).utf8().get_data();
            value.characterName = characterName_string.empty() ? nullptr : characterName_string.c_str();
        }
        if (get_request_value(p_request, "characterType", "character_type", &field_value)) {
            characterType_string = String(field_value).utf8().get_data();
            value.characterType = characterType_string.empty() ? nullptr : characterType_string.c_str();
        }
        return true;
    }
};

struct OwnedPFAdCampaignAttributionModel {
    PFAdCampaignAttributionModel value = {};
    std::string campaignId_string;
    std::string platform_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "attributedAt", "attributed_at", &field_value)) {
            value.attributedAt = static_cast<time_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "campaignId", "campaign_id", &field_value)) {
            campaignId_string = String(field_value).utf8().get_data();
            value.campaignId = campaignId_string.empty() ? nullptr : campaignId_string.c_str();
        }
        if (get_request_value(p_request, "platform", "platform", &field_value)) {
            platform_string = String(field_value).utf8().get_data();
            value.platform = platform_string.empty() ? nullptr : platform_string.c_str();
        }
        return true;
    }
};

struct OwnedPFContactEmailInfoModel {
    PFContactEmailInfoModel value = {};
    std::string emailAddress_string;
    std::string name_string;
    PFEmailVerificationStatus verificationStatus_storage = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "emailAddress", "email_address", &field_value)) {
            emailAddress_string = String(field_value).utf8().get_data();
            value.emailAddress = emailAddress_string.empty() ? nullptr : emailAddress_string.c_str();
        }
        if (get_request_value(p_request, "name", "name", &field_value)) {
            name_string = String(field_value).utf8().get_data();
            value.name = name_string.empty() ? nullptr : name_string.c_str();
        }
        if (get_request_value(p_request, "verificationStatus", "verification_status", &field_value)) {
            verificationStatus_storage = static_cast<PFEmailVerificationStatus>(static_cast<int64_t>(field_value));
            value.verificationStatus = &verificationStatus_storage;
        }
        return true;
    }
};

struct OwnedPFLinkedPlatformAccountModel {
    PFLinkedPlatformAccountModel value = {};
    std::string email_string;
    PFLoginIdentityProvider platform_storage = {};
    std::string platformUserId_string;
    std::string username_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "email", "email", &field_value)) {
            email_string = String(field_value).utf8().get_data();
            value.email = email_string.empty() ? nullptr : email_string.c_str();
        }
        if (get_request_value(p_request, "platform", "platform", &field_value)) {
            platform_storage = static_cast<PFLoginIdentityProvider>(static_cast<int64_t>(field_value));
            value.platform = &platform_storage;
        }
        if (get_request_value(p_request, "platformUserId", "platform_user_id", &field_value)) {
            platformUserId_string = String(field_value).utf8().get_data();
            value.platformUserId = platformUserId_string.empty() ? nullptr : platformUserId_string.c_str();
        }
        if (get_request_value(p_request, "username", "username", &field_value)) {
            username_string = String(field_value).utf8().get_data();
            value.username = username_string.empty() ? nullptr : username_string.c_str();
        }
        return true;
    }
};

struct OwnedPFLocationModel {
    PFLocationModel value = {};
    std::string city_string;
    PFContinentCode continentCode_storage = {};
    PFCountryCode countryCode_storage = {};
    double latitude_storage = {};
    double longitude_storage = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "city", "city", &field_value)) {
            city_string = String(field_value).utf8().get_data();
            value.city = city_string.empty() ? nullptr : city_string.c_str();
        }
        if (get_request_value(p_request, "continentCode", "continent_code", &field_value)) {
            continentCode_storage = static_cast<PFContinentCode>(static_cast<int64_t>(field_value));
            value.continentCode = &continentCode_storage;
        }
        if (get_request_value(p_request, "countryCode", "country_code", &field_value)) {
            countryCode_storage = static_cast<PFCountryCode>(static_cast<int64_t>(field_value));
            value.countryCode = &countryCode_storage;
        }
        if (get_request_value(p_request, "latitude", "latitude", &field_value)) {
            latitude_storage = static_cast<double>(static_cast<double>(field_value));
            value.latitude = &latitude_storage;
        }
        if (get_request_value(p_request, "longitude", "longitude", &field_value)) {
            longitude_storage = static_cast<double>(static_cast<double>(field_value));
            value.longitude = &longitude_storage;
        }
        return true;
    }
};

struct OwnedPFSubscriptionModel {
    PFSubscriptionModel value = {};
    PFSubscriptionProviderStatus status_storage = {};
    std::string subscriptionId_string;
    std::string subscriptionItemId_string;
    std::string subscriptionProvider_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "expiration", "expiration", &field_value)) {
            value.expiration = static_cast<time_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "initialSubscriptionTime", "initial_subscription_time", &field_value)) {
            value.initialSubscriptionTime = static_cast<time_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "isActive", "is_active", &field_value)) {
            value.isActive = static_cast<bool>(field_value);
        }
        if (get_request_value(p_request, "status", "status", &field_value)) {
            status_storage = static_cast<PFSubscriptionProviderStatus>(static_cast<int64_t>(field_value));
            value.status = &status_storage;
        }
        if (get_request_value(p_request, "subscriptionId", "subscription_id", &field_value)) {
            subscriptionId_string = String(field_value).utf8().get_data();
            value.subscriptionId = subscriptionId_string.empty() ? nullptr : subscriptionId_string.c_str();
        }
        if (get_request_value(p_request, "subscriptionItemId", "subscription_item_id", &field_value)) {
            subscriptionItemId_string = String(field_value).utf8().get_data();
            value.subscriptionItemId = subscriptionItemId_string.empty() ? nullptr : subscriptionItemId_string.c_str();
        }
        if (get_request_value(p_request, "subscriptionProvider", "subscription_provider", &field_value)) {
            subscriptionProvider_string = String(field_value).utf8().get_data();
            value.subscriptionProvider = subscriptionProvider_string.empty() ? nullptr : subscriptionProvider_string.c_str();
        }
        return true;
    }
};

struct OwnedPFMembershipModel {
    PFMembershipModel value = {};
    std::string membershipId_string;
    time_t overrideExpiration_storage = {};
    std::vector<OwnedPFSubscriptionModel> subscriptions_items;
    std::vector<const PFSubscriptionModel *> subscriptions_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "isActive", "is_active", &field_value)) {
            value.isActive = static_cast<bool>(field_value);
        }
        if (get_request_value(p_request, "membershipExpiration", "membership_expiration", &field_value)) {
            value.membershipExpiration = static_cast<time_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "membershipId", "membership_id", &field_value)) {
            membershipId_string = String(field_value).utf8().get_data();
            value.membershipId = membershipId_string.empty() ? nullptr : membershipId_string.c_str();
        }
        if (get_request_value(p_request, "overrideExpiration", "override_expiration", &field_value)) {
            overrideExpiration_storage = static_cast<time_t>(static_cast<int64_t>(field_value));
            value.overrideExpiration = &overrideExpiration_storage;
        }
        if (get_request_value(p_request, "subscriptions", "subscriptions", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field subscriptions."; return false; }
            Array source_array = field_value;
            subscriptions_items.resize(source_array.size());
            subscriptions_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field subscriptions."; return false; }
                if (!subscriptions_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                subscriptions_ptrs[static_cast<size_t>(i)] = &subscriptions_items[static_cast<size_t>(i)].value;
            }
            value.subscriptions = subscriptions_ptrs.empty() ? nullptr : subscriptions_ptrs.data();
            value.subscriptionsCount = static_cast<uint32_t>(subscriptions_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFPushNotificationRegistrationModel {
    PFPushNotificationRegistrationModel value = {};
    std::string notificationEndpointARN_string;
    PFPushNotificationPlatform platform_storage = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "notificationEndpointARN", "notification_endpoint_arn", &field_value)) {
            notificationEndpointARN_string = String(field_value).utf8().get_data();
            value.notificationEndpointARN = notificationEndpointARN_string.empty() ? nullptr : notificationEndpointARN_string.c_str();
        }
        if (get_request_value(p_request, "platform", "platform", &field_value)) {
            platform_storage = static_cast<PFPushNotificationPlatform>(static_cast<int64_t>(field_value));
            value.platform = &platform_storage;
        }
        return true;
    }
};

struct OwnedPFStatisticModel {
    PFStatisticModel value = {};
    std::string name_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "name", "name", &field_value)) {
            name_string = String(field_value).utf8().get_data();
            value.name = name_string.empty() ? nullptr : name_string.c_str();
        }
        if (get_request_value(p_request, "value", "value", &field_value)) {
            value.value = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "version", "version", &field_value)) {
            value.version = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        return true;
    }
};

struct OwnedPFTagModel {
    PFTagModel value = {};
    std::string tagValue_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "tagValue", "tag_value", &field_value)) {
            tagValue_string = String(field_value).utf8().get_data();
            value.tagValue = tagValue_string.empty() ? nullptr : tagValue_string.c_str();
        }
        return true;
    }
};

struct OwnedPFValueToDateModel {
    PFValueToDateModel value = {};
    std::string currency_string;
    std::string totalValueAsDecimal_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "currency", "currency", &field_value)) {
            currency_string = String(field_value).utf8().get_data();
            value.currency = currency_string.empty() ? nullptr : currency_string.c_str();
        }
        if (get_request_value(p_request, "totalValue", "total_value", &field_value)) {
            value.totalValue = static_cast<uint32_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "totalValueAsDecimal", "total_value_as_decimal", &field_value)) {
            totalValueAsDecimal_string = String(field_value).utf8().get_data();
            value.totalValueAsDecimal = totalValueAsDecimal_string.empty() ? nullptr : totalValueAsDecimal_string.c_str();
        }
        return true;
    }
};

struct OwnedPFPlayerProfileModel {
    PFPlayerProfileModel value = {};
    std::vector<OwnedPFAdCampaignAttributionModel> adCampaignAttributions_items;
    std::vector<const PFAdCampaignAttributionModel *> adCampaignAttributions_ptrs;
    std::string avatarUrl_string;
    time_t bannedUntil_storage = {};
    std::vector<OwnedPFContactEmailInfoModel> contactEmailAddresses_items;
    std::vector<const PFContactEmailInfoModel *> contactEmailAddresses_ptrs;
    time_t created_storage = {};
    std::string displayName_string;
    std::vector<std::string> experimentVariants_strings;
    std::vector<const char *> experimentVariants_ptrs;
    time_t lastLogin_storage = {};
    std::vector<OwnedPFLinkedPlatformAccountModel> linkedAccounts_items;
    std::vector<const PFLinkedPlatformAccountModel *> linkedAccounts_ptrs;
    std::vector<OwnedPFLocationModel> locations_items;
    std::vector<const PFLocationModel *> locations_ptrs;
    std::vector<OwnedPFMembershipModel> memberships_items;
    std::vector<const PFMembershipModel *> memberships_ptrs;
    PFLoginIdentityProvider origination_storage = {};
    std::string playerId_string;
    std::string publisherId_string;
    std::vector<OwnedPFPushNotificationRegistrationModel> pushNotificationRegistrations_items;
    std::vector<const PFPushNotificationRegistrationModel *> pushNotificationRegistrations_ptrs;
    std::vector<OwnedPFStatisticModel> statistics_items;
    std::vector<const PFStatisticModel *> statistics_ptrs;
    std::vector<OwnedPFTagModel> tags_items;
    std::vector<const PFTagModel *> tags_ptrs;
    std::string titleId_string;
    uint32_t totalValueToDateInUSD_storage = {};
    std::vector<OwnedPFValueToDateModel> valuesToDate_items;
    std::vector<const PFValueToDateModel *> valuesToDate_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "adCampaignAttributions", "ad_campaign_attributions", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field ad_campaign_attributions."; return false; }
            Array source_array = field_value;
            adCampaignAttributions_items.resize(source_array.size());
            adCampaignAttributions_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field ad_campaign_attributions."; return false; }
                if (!adCampaignAttributions_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                adCampaignAttributions_ptrs[static_cast<size_t>(i)] = &adCampaignAttributions_items[static_cast<size_t>(i)].value;
            }
            value.adCampaignAttributions = adCampaignAttributions_ptrs.empty() ? nullptr : adCampaignAttributions_ptrs.data();
            value.adCampaignAttributionsCount = static_cast<uint32_t>(adCampaignAttributions_ptrs.size());
        }
        if (get_request_value(p_request, "avatarUrl", "avatar_url", &field_value)) {
            avatarUrl_string = String(field_value).utf8().get_data();
            value.avatarUrl = avatarUrl_string.empty() ? nullptr : avatarUrl_string.c_str();
        }
        if (get_request_value(p_request, "bannedUntil", "banned_until", &field_value)) {
            bannedUntil_storage = static_cast<time_t>(static_cast<int64_t>(field_value));
            value.bannedUntil = &bannedUntil_storage;
        }
        if (get_request_value(p_request, "contactEmailAddresses", "contact_email_addresses", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field contact_email_addresses."; return false; }
            Array source_array = field_value;
            contactEmailAddresses_items.resize(source_array.size());
            contactEmailAddresses_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field contact_email_addresses."; return false; }
                if (!contactEmailAddresses_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                contactEmailAddresses_ptrs[static_cast<size_t>(i)] = &contactEmailAddresses_items[static_cast<size_t>(i)].value;
            }
            value.contactEmailAddresses = contactEmailAddresses_ptrs.empty() ? nullptr : contactEmailAddresses_ptrs.data();
            value.contactEmailAddressesCount = static_cast<uint32_t>(contactEmailAddresses_ptrs.size());
        }
        if (get_request_value(p_request, "created", "created", &field_value)) {
            created_storage = static_cast<time_t>(static_cast<int64_t>(field_value));
            value.created = &created_storage;
        }
        if (get_request_value(p_request, "displayName", "display_name", &field_value)) {
            displayName_string = String(field_value).utf8().get_data();
            value.displayName = displayName_string.empty() ? nullptr : displayName_string.c_str();
        }
        if (get_request_value(p_request, "experimentVariants", "experiment_variants", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field experiment_variants."; return false; }
            Array source_array = field_value;
            experimentVariants_strings.clear(); experimentVariants_ptrs.clear();
            experimentVariants_strings.reserve(source_array.size()); experimentVariants_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                experimentVariants_strings.emplace_back(String(source_array[i]).utf8().get_data());
                experimentVariants_ptrs.push_back(experimentVariants_strings.back().c_str());
            }
            value.experimentVariants = experimentVariants_ptrs.empty() ? nullptr : experimentVariants_ptrs.data();
            value.experimentVariantsCount = static_cast<uint32_t>(experimentVariants_ptrs.size());
        }
        if (get_request_value(p_request, "lastLogin", "last_login", &field_value)) {
            lastLogin_storage = static_cast<time_t>(static_cast<int64_t>(field_value));
            value.lastLogin = &lastLogin_storage;
        }
        if (get_request_value(p_request, "linkedAccounts", "linked_accounts", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field linked_accounts."; return false; }
            Array source_array = field_value;
            linkedAccounts_items.resize(source_array.size());
            linkedAccounts_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field linked_accounts."; return false; }
                if (!linkedAccounts_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                linkedAccounts_ptrs[static_cast<size_t>(i)] = &linkedAccounts_items[static_cast<size_t>(i)].value;
            }
            value.linkedAccounts = linkedAccounts_ptrs.empty() ? nullptr : linkedAccounts_ptrs.data();
            value.linkedAccountsCount = static_cast<uint32_t>(linkedAccounts_ptrs.size());
        }
        if (get_request_value(p_request, "locations", "locations", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field locations."; return false; }
            Array source_array = field_value;
            locations_items.resize(source_array.size());
            locations_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field locations."; return false; }
                if (!locations_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                locations_ptrs[static_cast<size_t>(i)] = &locations_items[static_cast<size_t>(i)].value;
            }
            value.locations = locations_ptrs.empty() ? nullptr : locations_ptrs.data();
            value.locationsCount = static_cast<uint32_t>(locations_ptrs.size());
        }
        if (get_request_value(p_request, "memberships", "memberships", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field memberships."; return false; }
            Array source_array = field_value;
            memberships_items.resize(source_array.size());
            memberships_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field memberships."; return false; }
                if (!memberships_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                memberships_ptrs[static_cast<size_t>(i)] = &memberships_items[static_cast<size_t>(i)].value;
            }
            value.memberships = memberships_ptrs.empty() ? nullptr : memberships_ptrs.data();
            value.membershipsCount = static_cast<uint32_t>(memberships_ptrs.size());
        }
        if (get_request_value(p_request, "origination", "origination", &field_value)) {
            origination_storage = static_cast<PFLoginIdentityProvider>(static_cast<int64_t>(field_value));
            value.origination = &origination_storage;
        }
        if (get_request_value(p_request, "playerId", "player_id", &field_value)) {
            playerId_string = String(field_value).utf8().get_data();
            value.playerId = playerId_string.empty() ? nullptr : playerId_string.c_str();
        }
        if (get_request_value(p_request, "publisherId", "publisher_id", &field_value)) {
            publisherId_string = String(field_value).utf8().get_data();
            value.publisherId = publisherId_string.empty() ? nullptr : publisherId_string.c_str();
        }
        if (get_request_value(p_request, "pushNotificationRegistrations", "push_notification_registrations", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field push_notification_registrations."; return false; }
            Array source_array = field_value;
            pushNotificationRegistrations_items.resize(source_array.size());
            pushNotificationRegistrations_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field push_notification_registrations."; return false; }
                if (!pushNotificationRegistrations_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                pushNotificationRegistrations_ptrs[static_cast<size_t>(i)] = &pushNotificationRegistrations_items[static_cast<size_t>(i)].value;
            }
            value.pushNotificationRegistrations = pushNotificationRegistrations_ptrs.empty() ? nullptr : pushNotificationRegistrations_ptrs.data();
            value.pushNotificationRegistrationsCount = static_cast<uint32_t>(pushNotificationRegistrations_ptrs.size());
        }
        if (get_request_value(p_request, "statistics", "statistics", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field statistics."; return false; }
            Array source_array = field_value;
            statistics_items.resize(source_array.size());
            statistics_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field statistics."; return false; }
                if (!statistics_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                statistics_ptrs[static_cast<size_t>(i)] = &statistics_items[static_cast<size_t>(i)].value;
            }
            value.statistics = statistics_ptrs.empty() ? nullptr : statistics_ptrs.data();
            value.statisticsCount = static_cast<uint32_t>(statistics_ptrs.size());
        }
        if (get_request_value(p_request, "tags", "tags", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field tags."; return false; }
            Array source_array = field_value;
            tags_items.resize(source_array.size());
            tags_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field tags."; return false; }
                if (!tags_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                tags_ptrs[static_cast<size_t>(i)] = &tags_items[static_cast<size_t>(i)].value;
            }
            value.tags = tags_ptrs.empty() ? nullptr : tags_ptrs.data();
            value.tagsCount = static_cast<uint32_t>(tags_ptrs.size());
        }
        if (get_request_value(p_request, "titleId", "title_id", &field_value)) {
            titleId_string = String(field_value).utf8().get_data();
            value.titleId = titleId_string.empty() ? nullptr : titleId_string.c_str();
        }
        if (get_request_value(p_request, "totalValueToDateInUSD", "total_value_to_date_in_usd", &field_value)) {
            totalValueToDateInUSD_storage = static_cast<uint32_t>(static_cast<int64_t>(field_value));
            value.totalValueToDateInUSD = &totalValueToDateInUSD_storage;
        }
        if (get_request_value(p_request, "valuesToDate", "values_to_date", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field values_to_date."; return false; }
            Array source_array = field_value;
            valuesToDate_items.resize(source_array.size());
            valuesToDate_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field values_to_date."; return false; }
                if (!valuesToDate_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                valuesToDate_ptrs[static_cast<size_t>(i)] = &valuesToDate_items[static_cast<size_t>(i)].value;
            }
            value.valuesToDate = valuesToDate_ptrs.empty() ? nullptr : valuesToDate_ptrs.data();
            value.valuesToDateCount = static_cast<uint32_t>(valuesToDate_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFStatisticValue {
    PFStatisticValue value = {};
    std::string statisticName_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "statisticName", "statistic_name", &field_value)) {
            statisticName_string = String(field_value).utf8().get_data();
            value.statisticName = statisticName_string.empty() ? nullptr : statisticName_string.c_str();
        }
        if (get_request_value(p_request, "value", "value", &field_value)) {
            value.value = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "version", "version", &field_value)) {
            value.version = static_cast<uint32_t>(static_cast<int64_t>(field_value));
        }
        return true;
    }
};

struct OwnedPFUserDataRecord {
    PFUserDataRecord value = {};
    PFUserDataPermission permission_storage = {};
    std::string value_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "lastUpdated", "last_updated", &field_value)) {
            value.lastUpdated = static_cast<time_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "permission", "permission", &field_value)) {
            permission_storage = static_cast<PFUserDataPermission>(static_cast<int64_t>(field_value));
            value.permission = &permission_storage;
        }
        if (get_request_value(p_request, "value", "value", &field_value)) {
            value_string = String(field_value).utf8().get_data();
            value.value = value_string.empty() ? nullptr : value_string.c_str();
        }
        return true;
    }
};

struct OwnedPFUserDataRecordDictionaryEntry {
    PFUserDataRecordDictionaryEntry value = {};
    std::string key_string;
    OwnedPFUserDataRecord value_owner;
    bool value_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "key", "key", &field_value)) {
            key_string = String(field_value).utf8().get_data();
            value.key = key_string.empty() ? nullptr : key_string.c_str();
        }
        if (get_request_value(p_request, "value", "value", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field value."; return false; }
            if (!value_owner.from_dictionary(field_value, r_error)) return false;
            value_has_value = true;
            value.value = &value_owner.value;
        }
        return true;
    }
};

struct OwnedPFInt32DictionaryEntry {
    PFInt32DictionaryEntry value = {};
    std::string key_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "key", "key", &field_value)) {
            key_string = String(field_value).utf8().get_data();
            value.key = key_string.empty() ? nullptr : key_string.c_str();
        }
        if (get_request_value(p_request, "value", "value", &field_value)) {
            value.value = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        return true;
    }
};

struct OwnedPFVirtualCurrencyRechargeTime {
    PFVirtualCurrencyRechargeTime value = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "rechargeMax", "recharge_max", &field_value)) {
            value.rechargeMax = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "rechargeTime", "recharge_time", &field_value)) {
            value.rechargeTime = static_cast<time_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "secondsToRecharge", "seconds_to_recharge", &field_value)) {
            value.secondsToRecharge = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        return true;
    }
};

struct OwnedPFVirtualCurrencyRechargeTimeDictionaryEntry {
    PFVirtualCurrencyRechargeTimeDictionaryEntry value = {};
    std::string key_string;
    OwnedPFVirtualCurrencyRechargeTime value_owner;
    bool value_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "key", "key", &field_value)) {
            key_string = String(field_value).utf8().get_data();
            value.key = key_string.empty() ? nullptr : key_string.c_str();
        }
        if (get_request_value(p_request, "value", "value", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field value."; return false; }
            if (!value_owner.from_dictionary(field_value, r_error)) return false;
            value_has_value = true;
            value.value = &value_owner.value;
        }
        return true;
    }
};

struct OwnedPFGetPlayerCombinedInfoResultPayload {
    PFGetPlayerCombinedInfoResultPayload value = {};
    OwnedPFUserAccountInfo accountInfo_owner;
    bool accountInfo_has_value = false;
    std::vector<OwnedPFCharacterInventory> characterInventories_items;
    std::vector<const PFCharacterInventory *> characterInventories_ptrs;
    std::vector<OwnedPFCharacterResult> characterList_items;
    std::vector<const PFCharacterResult *> characterList_ptrs;
    OwnedPFPlayerProfileModel playerProfile_owner;
    bool playerProfile_has_value = false;
    std::vector<OwnedPFStatisticValue> playerStatistics_items;
    std::vector<const PFStatisticValue *> playerStatistics_ptrs;
    std::vector<OwnedPFStringDictionaryEntry> titleData_items;
    std::vector<PFStringDictionaryEntry> titleData_values;
    std::vector<OwnedPFUserDataRecordDictionaryEntry> userData_items;
    std::vector<PFUserDataRecordDictionaryEntry> userData_values;
    std::vector<OwnedPFItemInstance> userInventory_items;
    std::vector<const PFItemInstance *> userInventory_ptrs;
    std::vector<OwnedPFUserDataRecordDictionaryEntry> userReadOnlyData_items;
    std::vector<PFUserDataRecordDictionaryEntry> userReadOnlyData_values;
    std::vector<OwnedPFInt32DictionaryEntry> userVirtualCurrency_items;
    std::vector<PFInt32DictionaryEntry> userVirtualCurrency_values;
    std::vector<OwnedPFVirtualCurrencyRechargeTimeDictionaryEntry> userVirtualCurrencyRechargeTimes_items;
    std::vector<PFVirtualCurrencyRechargeTimeDictionaryEntry> userVirtualCurrencyRechargeTimes_values;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "accountInfo", "account_info", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field account_info."; return false; }
            if (!accountInfo_owner.from_dictionary(field_value, r_error)) return false;
            accountInfo_has_value = true;
            value.accountInfo = &accountInfo_owner.value;
        }
        if (get_request_value(p_request, "characterInventories", "character_inventories", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field character_inventories."; return false; }
            Array source_array = field_value;
            characterInventories_items.resize(source_array.size());
            characterInventories_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field character_inventories."; return false; }
                if (!characterInventories_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                characterInventories_ptrs[static_cast<size_t>(i)] = &characterInventories_items[static_cast<size_t>(i)].value;
            }
            value.characterInventories = characterInventories_ptrs.empty() ? nullptr : characterInventories_ptrs.data();
            value.characterInventoriesCount = static_cast<uint32_t>(characterInventories_ptrs.size());
        }
        if (get_request_value(p_request, "characterList", "character_list", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field character_list."; return false; }
            Array source_array = field_value;
            characterList_items.resize(source_array.size());
            characterList_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field character_list."; return false; }
                if (!characterList_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                characterList_ptrs[static_cast<size_t>(i)] = &characterList_items[static_cast<size_t>(i)].value;
            }
            value.characterList = characterList_ptrs.empty() ? nullptr : characterList_ptrs.data();
            value.characterListCount = static_cast<uint32_t>(characterList_ptrs.size());
        }
        if (get_request_value(p_request, "playerProfile", "player_profile", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field player_profile."; return false; }
            if (!playerProfile_owner.from_dictionary(field_value, r_error)) return false;
            playerProfile_has_value = true;
            value.playerProfile = &playerProfile_owner.value;
        }
        if (get_request_value(p_request, "playerStatistics", "player_statistics", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field player_statistics."; return false; }
            Array source_array = field_value;
            playerStatistics_items.resize(source_array.size());
            playerStatistics_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field player_statistics."; return false; }
                if (!playerStatistics_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                playerStatistics_ptrs[static_cast<size_t>(i)] = &playerStatistics_items[static_cast<size_t>(i)].value;
            }
            value.playerStatistics = playerStatistics_ptrs.empty() ? nullptr : playerStatistics_ptrs.data();
            value.playerStatisticsCount = static_cast<uint32_t>(playerStatistics_ptrs.size());
        }
        if (get_request_value(p_request, "titleData", "title_data", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field title_data."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            titleData_items.resize(keys.size());
            titleData_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!titleData_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                titleData_values[static_cast<size_t>(i)] = titleData_items[static_cast<size_t>(i)].value;
            }
            value.titleData = titleData_values.empty() ? nullptr : titleData_values.data();
            value.titleDataCount = static_cast<uint32_t>(titleData_values.size());
        }
        if (get_request_value(p_request, "userData", "user_data", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field user_data."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            userData_items.resize(keys.size());
            userData_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!userData_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                userData_values[static_cast<size_t>(i)] = userData_items[static_cast<size_t>(i)].value;
            }
            value.userData = userData_values.empty() ? nullptr : userData_values.data();
            value.userDataCount = static_cast<uint32_t>(userData_values.size());
        }
        if (get_request_value(p_request, "userDataVersion", "user_data_version", &field_value)) {
            value.userDataVersion = static_cast<uint32_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "userInventory", "user_inventory", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field user_inventory."; return false; }
            Array source_array = field_value;
            userInventory_items.resize(source_array.size());
            userInventory_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field user_inventory."; return false; }
                if (!userInventory_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                userInventory_ptrs[static_cast<size_t>(i)] = &userInventory_items[static_cast<size_t>(i)].value;
            }
            value.userInventory = userInventory_ptrs.empty() ? nullptr : userInventory_ptrs.data();
            value.userInventoryCount = static_cast<uint32_t>(userInventory_ptrs.size());
        }
        if (get_request_value(p_request, "userReadOnlyData", "user_read_only_data", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field user_read_only_data."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            userReadOnlyData_items.resize(keys.size());
            userReadOnlyData_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!userReadOnlyData_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                userReadOnlyData_values[static_cast<size_t>(i)] = userReadOnlyData_items[static_cast<size_t>(i)].value;
            }
            value.userReadOnlyData = userReadOnlyData_values.empty() ? nullptr : userReadOnlyData_values.data();
            value.userReadOnlyDataCount = static_cast<uint32_t>(userReadOnlyData_values.size());
        }
        if (get_request_value(p_request, "userReadOnlyDataVersion", "user_read_only_data_version", &field_value)) {
            value.userReadOnlyDataVersion = static_cast<uint32_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "userVirtualCurrency", "user_virtual_currency", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field user_virtual_currency."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            userVirtualCurrency_items.resize(keys.size());
            userVirtualCurrency_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!userVirtualCurrency_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                userVirtualCurrency_values[static_cast<size_t>(i)] = userVirtualCurrency_items[static_cast<size_t>(i)].value;
            }
            value.userVirtualCurrency = userVirtualCurrency_values.empty() ? nullptr : userVirtualCurrency_values.data();
            value.userVirtualCurrencyCount = static_cast<uint32_t>(userVirtualCurrency_values.size());
        }
        if (get_request_value(p_request, "userVirtualCurrencyRechargeTimes", "user_virtual_currency_recharge_times", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field user_virtual_currency_recharge_times."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            userVirtualCurrencyRechargeTimes_items.resize(keys.size());
            userVirtualCurrencyRechargeTimes_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!userVirtualCurrencyRechargeTimes_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                userVirtualCurrencyRechargeTimes_values[static_cast<size_t>(i)] = userVirtualCurrencyRechargeTimes_items[static_cast<size_t>(i)].value;
            }
            value.userVirtualCurrencyRechargeTimes = userVirtualCurrencyRechargeTimes_values.empty() ? nullptr : userVirtualCurrencyRechargeTimes_values.data();
            value.userVirtualCurrencyRechargeTimesCount = static_cast<uint32_t>(userVirtualCurrencyRechargeTimes_values.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetPlayerCombinedInfoResult {
    PFAccountManagementGetPlayerCombinedInfoResult value = {};
    OwnedPFGetPlayerCombinedInfoResultPayload infoResultPayload_owner;
    bool infoResultPayload_has_value = false;
    std::string playFabId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "infoResultPayload", "info_result_payload", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field info_result_payload."; return false; }
            if (!infoResultPayload_owner.from_dictionary(field_value, r_error)) return false;
            infoResultPayload_has_value = true;
            value.infoResultPayload = &infoResultPayload_owner.value;
        }
        if (get_request_value(p_request, "playFabId", "play_fab_id", &field_value)) {
            playFabId_string = String(field_value).utf8().get_data();
            value.playFabId = playFabId_string.empty() ? nullptr : playFabId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetPlayerProfileRequest {
    PFAccountManagementGetPlayerProfileRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    std::string playFabId_string;
    OwnedPFPlayerProfileViewConstraints profileConstraints_owner;
    bool profileConstraints_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "playFabId", "play_fab_id", &field_value)) {
            playFabId_string = String(field_value).utf8().get_data();
            value.playFabId = playFabId_string.empty() ? nullptr : playFabId_string.c_str();
        }
        if (get_request_value(p_request, "profileConstraints", "profile_constraints", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field profile_constraints."; return false; }
            if (!profileConstraints_owner.from_dictionary(field_value, r_error)) return false;
            profileConstraints_has_value = true;
            value.profileConstraints = &profileConstraints_owner.value;
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetPlayerProfileResult {
    PFAccountManagementGetPlayerProfileResult value = {};
    OwnedPFPlayerProfileModel playerProfile_owner;
    bool playerProfile_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "playerProfile", "player_profile", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field player_profile."; return false; }
            if (!playerProfile_owner.from_dictionary(field_value, r_error)) return false;
            playerProfile_has_value = true;
            value.playerProfile = &playerProfile_owner.value;
        }
        return true;
    }
};

struct OwnedPFEntityLineage {
    PFEntityLineage value = {};
    std::string characterId_string;
    std::string groupId_string;
    std::string masterPlayerAccountId_string;
    std::string namespaceId_string;
    std::string titleId_string;
    std::string titlePlayerAccountId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "characterId", "character_id", &field_value)) {
            characterId_string = String(field_value).utf8().get_data();
            value.characterId = characterId_string.empty() ? nullptr : characterId_string.c_str();
        }
        if (get_request_value(p_request, "groupId", "group_id", &field_value)) {
            groupId_string = String(field_value).utf8().get_data();
            value.groupId = groupId_string.empty() ? nullptr : groupId_string.c_str();
        }
        if (get_request_value(p_request, "masterPlayerAccountId", "master_player_account_id", &field_value)) {
            masterPlayerAccountId_string = String(field_value).utf8().get_data();
            value.masterPlayerAccountId = masterPlayerAccountId_string.empty() ? nullptr : masterPlayerAccountId_string.c_str();
        }
        if (get_request_value(p_request, "namespaceId", "namespace_id", &field_value)) {
            namespaceId_string = String(field_value).utf8().get_data();
            value.namespaceId = namespaceId_string.empty() ? nullptr : namespaceId_string.c_str();
        }
        if (get_request_value(p_request, "titleId", "title_id", &field_value)) {
            titleId_string = String(field_value).utf8().get_data();
            value.titleId = titleId_string.empty() ? nullptr : titleId_string.c_str();
        }
        if (get_request_value(p_request, "titlePlayerAccountId", "title_player_account_id", &field_value)) {
            titlePlayerAccountId_string = String(field_value).utf8().get_data();
            value.titlePlayerAccountId = titlePlayerAccountId_string.empty() ? nullptr : titlePlayerAccountId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFEntityLineageDictionaryEntry {
    PFEntityLineageDictionaryEntry value = {};
    std::string key_string;
    OwnedPFEntityLineage value_owner;
    bool value_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "key", "key", &field_value)) {
            key_string = String(field_value).utf8().get_data();
            value.key = key_string.empty() ? nullptr : key_string.c_str();
        }
        if (get_request_value(p_request, "value", "value", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field value."; return false; }
            if (!value_owner.from_dictionary(field_value, r_error)) return false;
            value_has_value = true;
            value.value = &value_owner.value;
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetTitlePlayersFromProviderIDsResponse {
    PFAccountManagementGetTitlePlayersFromProviderIDsResponse value = {};
    std::vector<OwnedPFEntityLineageDictionaryEntry> titlePlayerAccounts_items;
    std::vector<PFEntityLineageDictionaryEntry> titlePlayerAccounts_values;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "titlePlayerAccounts", "title_player_accounts", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field title_player_accounts."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            titlePlayerAccounts_items.resize(keys.size());
            titlePlayerAccounts_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!titlePlayerAccounts_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                titlePlayerAccounts_values[static_cast<size_t>(i)] = titlePlayerAccounts_items[static_cast<size_t>(i)].value;
            }
            value.titlePlayerAccounts = titlePlayerAccounts_values.empty() ? nullptr : titlePlayerAccounts_values.data();
            value.titlePlayerAccountsCount = static_cast<uint32_t>(titlePlayerAccounts_values.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementGetTitlePlayersFromXboxLiveIDsRequest {
    PFAccountManagementGetTitlePlayersFromXboxLiveIDsRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    std::string sandbox_string;
    std::string titleId_string;
    std::vector<std::string> xboxLiveIds_strings;
    std::vector<const char *> xboxLiveIds_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "sandbox", "sandbox", &field_value)) {
            sandbox_string = String(field_value).utf8().get_data();
            value.sandbox = sandbox_string.empty() ? nullptr : sandbox_string.c_str();
        }
        if (get_request_value(p_request, "titleId", "title_id", &field_value)) {
            titleId_string = String(field_value).utf8().get_data();
            value.titleId = titleId_string.empty() ? nullptr : titleId_string.c_str();
        }
        if (get_request_value(p_request, "xboxLiveIds", "xbox_live_ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field xbox_live_ids."; return false; }
            Array source_array = field_value;
            xboxLiveIds_strings.clear(); xboxLiveIds_ptrs.clear();
            xboxLiveIds_strings.reserve(source_array.size()); xboxLiveIds_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                xboxLiveIds_strings.emplace_back(String(source_array[i]).utf8().get_data());
                xboxLiveIds_ptrs.push_back(xboxLiveIds_strings.back().c_str());
            }
            value.xboxLiveIds = xboxLiveIds_ptrs.empty() ? nullptr : xboxLiveIds_ptrs.data();
            value.xboxLiveIdsCount = static_cast<uint32_t>(xboxLiveIds_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementLinkCustomIDRequest {
    PFAccountManagementLinkCustomIDRequest value = {};
    std::string customId_string;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    bool forceLink_storage = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customId", "custom_id", &field_value)) {
            customId_string = String(field_value).utf8().get_data();
            value.customId = customId_string.empty() ? nullptr : customId_string.c_str();
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "forceLink", "force_link", &field_value)) {
            forceLink_storage = static_cast<bool>(field_value);
            value.forceLink = &forceLink_storage;
        }
        return true;
    }
};

struct OwnedPFAccountManagementLinkOpenIdConnectRequest {
    PFAccountManagementLinkOpenIdConnectRequest value = {};
    std::string connectionId_string;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    bool forceLink_storage = {};
    std::string idToken_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "connectionId", "connection_id", &field_value)) {
            connectionId_string = String(field_value).utf8().get_data();
            value.connectionId = connectionId_string.empty() ? nullptr : connectionId_string.c_str();
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "forceLink", "force_link", &field_value)) {
            forceLink_storage = static_cast<bool>(field_value);
            value.forceLink = &forceLink_storage;
        }
        if (get_request_value(p_request, "idToken", "id_token", &field_value)) {
            idToken_string = String(field_value).utf8().get_data();
            value.idToken = idToken_string.empty() ? nullptr : idToken_string.c_str();
        }
        return true;
    }
};

struct OwnedPFAccountManagementLinkSteamAccountRequest {
    PFAccountManagementLinkSteamAccountRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    bool forceLink_storage = {};
    std::string steamTicket_string;
    bool ticketIsServiceSpecific_storage = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "forceLink", "force_link", &field_value)) {
            forceLink_storage = static_cast<bool>(field_value);
            value.forceLink = &forceLink_storage;
        }
        if (get_request_value(p_request, "steamTicket", "steam_ticket", &field_value)) {
            steamTicket_string = String(field_value).utf8().get_data();
            value.steamTicket = steamTicket_string.empty() ? nullptr : steamTicket_string.c_str();
        }
        if (get_request_value(p_request, "ticketIsServiceSpecific", "ticket_is_service_specific", &field_value)) {
            ticketIsServiceSpecific_storage = static_cast<bool>(field_value);
            value.ticketIsServiceSpecific = &ticketIsServiceSpecific_storage;
        }
        return true;
    }
};

struct OwnedPFAccountManagementRemoveContactEmailRequest {
    PFAccountManagementRemoveContactEmailRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementReportPlayerClientRequest {
    PFAccountManagementReportPlayerClientRequest value = {};
    std::string comment_string;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    std::string reporteeId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "comment", "comment", &field_value)) {
            comment_string = String(field_value).utf8().get_data();
            value.comment = comment_string.empty() ? nullptr : comment_string.c_str();
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "reporteeId", "reportee_id", &field_value)) {
            reporteeId_string = String(field_value).utf8().get_data();
            value.reporteeId = reporteeId_string.empty() ? nullptr : reporteeId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFAccountManagementReportPlayerClientResult {
    PFAccountManagementReportPlayerClientResult value = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "submissionsRemaining", "submissions_remaining", &field_value)) {
            value.submissionsRemaining = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        return true;
    }
};

struct OwnedPFAccountManagementSetDisplayNameRequest {
    PFAccountManagementSetDisplayNameRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    std::string displayName_string;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    int32_t expectedVersion_storage = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "displayName", "display_name", &field_value)) {
            displayName_string = String(field_value).utf8().get_data();
            value.displayName = displayName_string.empty() ? nullptr : displayName_string.c_str();
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "expectedVersion", "expected_version", &field_value)) {
            expectedVersion_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.expectedVersion = &expectedVersion_storage;
        }
        return true;
    }
};

struct OwnedPFAccountManagementSetDisplayNameResponse {
    PFAccountManagementSetDisplayNameResponse value = {};
    PFOperationTypes operationResult_storage = {};
    int32_t versionNumber_storage = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "operationResult", "operation_result", &field_value)) {
            operationResult_storage = static_cast<PFOperationTypes>(static_cast<int64_t>(field_value));
            value.operationResult = &operationResult_storage;
        }
        if (get_request_value(p_request, "versionNumber", "version_number", &field_value)) {
            versionNumber_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.versionNumber = &versionNumber_storage;
        }
        return true;
    }
};

struct OwnedPFAccountManagementUnlinkCustomIDRequest {
    PFAccountManagementUnlinkCustomIDRequest value = {};
    std::string customId_string;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customId", "custom_id", &field_value)) {
            customId_string = String(field_value).utf8().get_data();
            value.customId = customId_string.empty() ? nullptr : customId_string.c_str();
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementUnlinkOpenIdConnectRequest {
    PFAccountManagementUnlinkOpenIdConnectRequest value = {};
    std::string connectionId_string;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "connectionId", "connection_id", &field_value)) {
            connectionId_string = String(field_value).utf8().get_data();
            value.connectionId = connectionId_string.empty() ? nullptr : connectionId_string.c_str();
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementUnlinkSteamAccountRequest {
    PFAccountManagementUnlinkSteamAccountRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        return true;
    }
};

struct OwnedPFAccountManagementUpdateUserTitleDisplayNameRequest {
    PFAccountManagementUpdateUserTitleDisplayNameRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    std::string displayName_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "displayName", "display_name", &field_value)) {
            displayName_string = String(field_value).utf8().get_data();
            value.displayName = displayName_string.empty() ? nullptr : displayName_string.c_str();
        }
        return true;
    }
};

struct OwnedPFAccountManagementUpdateUserTitleDisplayNameResult {
    PFAccountManagementUpdateUserTitleDisplayNameResult value = {};
    std::string displayName_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "displayName", "display_name", &field_value)) {
            displayName_string = String(field_value).utf8().get_data();
            value.displayName = displayName_string.empty() ? nullptr : displayName_string.c_str();
        }
        return true;
    }
};

struct OwnedPFCatalogCatalogAlternateId {
    PFCatalogCatalogAlternateId value = {};
    std::string type_string;
    std::string value_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "type", "type", &field_value)) {
            type_string = String(field_value).utf8().get_data();
            value.type = type_string.empty() ? nullptr : type_string.c_str();
        }
        if (get_request_value(p_request, "value", "value", &field_value)) {
            value_string = String(field_value).utf8().get_data();
            value.value = value_string.empty() ? nullptr : value_string.c_str();
        }
        return true;
    }
};

struct OwnedPFCatalogCatalogSpecificConfig {
    PFCatalogCatalogSpecificConfig value = {};
    std::vector<std::string> contentTypes_strings;
    std::vector<const char *> contentTypes_ptrs;
    std::vector<std::string> tags_strings;
    std::vector<const char *> tags_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "contentTypes", "content_types", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field content_types."; return false; }
            Array source_array = field_value;
            contentTypes_strings.clear(); contentTypes_ptrs.clear();
            contentTypes_strings.reserve(source_array.size()); contentTypes_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                contentTypes_strings.emplace_back(String(source_array[i]).utf8().get_data());
                contentTypes_ptrs.push_back(contentTypes_strings.back().c_str());
            }
            value.contentTypes = contentTypes_ptrs.empty() ? nullptr : contentTypes_ptrs.data();
            value.contentTypesCount = static_cast<uint32_t>(contentTypes_ptrs.size());
        }
        if (get_request_value(p_request, "tags", "tags", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field tags."; return false; }
            Array source_array = field_value;
            tags_strings.clear(); tags_ptrs.clear();
            tags_strings.reserve(source_array.size()); tags_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                tags_strings.emplace_back(String(source_array[i]).utf8().get_data());
                tags_ptrs.push_back(tags_strings.back().c_str());
            }
            value.tags = tags_ptrs.empty() ? nullptr : tags_ptrs.data();
            value.tagsCount = static_cast<uint32_t>(tags_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFCatalogDeepLinkFormat {
    PFCatalogDeepLinkFormat value = {};
    std::string format_string;
    std::string platform_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "format", "format", &field_value)) {
            format_string = String(field_value).utf8().get_data();
            value.format = format_string.empty() ? nullptr : format_string.c_str();
        }
        if (get_request_value(p_request, "platform", "platform", &field_value)) {
            platform_string = String(field_value).utf8().get_data();
            value.platform = platform_string.empty() ? nullptr : platform_string.c_str();
        }
        return true;
    }
};

struct OwnedPFCatalogDisplayPropertyIndexInfo {
    PFCatalogDisplayPropertyIndexInfo value = {};
    std::string name_string;
    PFCatalogDisplayPropertyType type_storage = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "name", "name", &field_value)) {
            name_string = String(field_value).utf8().get_data();
            value.name = name_string.empty() ? nullptr : name_string.c_str();
        }
        if (get_request_value(p_request, "type", "type", &field_value)) {
            type_storage = static_cast<PFCatalogDisplayPropertyType>(static_cast<int64_t>(field_value));
            value.type = &type_storage;
        }
        return true;
    }
};

struct OwnedPFCatalogFileConfig {
    PFCatalogFileConfig value = {};
    std::vector<std::string> contentTypes_strings;
    std::vector<const char *> contentTypes_ptrs;
    std::vector<std::string> tags_strings;
    std::vector<const char *> tags_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "contentTypes", "content_types", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field content_types."; return false; }
            Array source_array = field_value;
            contentTypes_strings.clear(); contentTypes_ptrs.clear();
            contentTypes_strings.reserve(source_array.size()); contentTypes_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                contentTypes_strings.emplace_back(String(source_array[i]).utf8().get_data());
                contentTypes_ptrs.push_back(contentTypes_strings.back().c_str());
            }
            value.contentTypes = contentTypes_ptrs.empty() ? nullptr : contentTypes_ptrs.data();
            value.contentTypesCount = static_cast<uint32_t>(contentTypes_ptrs.size());
        }
        if (get_request_value(p_request, "tags", "tags", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field tags."; return false; }
            Array source_array = field_value;
            tags_strings.clear(); tags_ptrs.clear();
            tags_strings.reserve(source_array.size()); tags_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                tags_strings.emplace_back(String(source_array[i]).utf8().get_data());
                tags_ptrs.push_back(tags_strings.back().c_str());
            }
            value.tags = tags_ptrs.empty() ? nullptr : tags_ptrs.data();
            value.tagsCount = static_cast<uint32_t>(tags_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFCatalogImageConfig {
    PFCatalogImageConfig value = {};
    std::vector<std::string> tags_strings;
    std::vector<const char *> tags_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "tags", "tags", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field tags."; return false; }
            Array source_array = field_value;
            tags_strings.clear(); tags_ptrs.clear();
            tags_strings.reserve(source_array.size()); tags_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                tags_strings.emplace_back(String(source_array[i]).utf8().get_data());
                tags_ptrs.push_back(tags_strings.back().c_str());
            }
            value.tags = tags_ptrs.empty() ? nullptr : tags_ptrs.data();
            value.tagsCount = static_cast<uint32_t>(tags_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFCatalogCategoryRatingConfig {
    PFCatalogCategoryRatingConfig value = {};
    std::string name_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "name", "name", &field_value)) {
            name_string = String(field_value).utf8().get_data();
            value.name = name_string.empty() ? nullptr : name_string.c_str();
        }
        return true;
    }
};

struct OwnedPFCatalogReviewConfig {
    PFCatalogReviewConfig value = {};
    std::vector<OwnedPFCatalogCategoryRatingConfig> categoryRatings_items;
    std::vector<const PFCatalogCategoryRatingConfig *> categoryRatings_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "categoryRatings", "category_ratings", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field category_ratings."; return false; }
            Array source_array = field_value;
            categoryRatings_items.resize(source_array.size());
            categoryRatings_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field category_ratings."; return false; }
                if (!categoryRatings_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                categoryRatings_ptrs[static_cast<size_t>(i)] = &categoryRatings_items[static_cast<size_t>(i)].value;
            }
            value.categoryRatings = categoryRatings_ptrs.empty() ? nullptr : categoryRatings_ptrs.data();
            value.categoryRatingsCount = static_cast<uint32_t>(categoryRatings_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFCatalogCatalogConfig {
    PFCatalogCatalogConfig value = {};
    std::vector<OwnedPFEntityKey> adminEntities_items;
    std::vector<const PFEntityKey *> adminEntities_ptrs;
    OwnedPFCatalogCatalogSpecificConfig catalog_owner;
    bool catalog_has_value = false;
    std::vector<OwnedPFCatalogDeepLinkFormat> deepLinkFormats_items;
    std::vector<const PFCatalogDeepLinkFormat *> deepLinkFormats_ptrs;
    std::vector<OwnedPFCatalogDisplayPropertyIndexInfo> displayPropertyIndexInfos_items;
    std::vector<const PFCatalogDisplayPropertyIndexInfo *> displayPropertyIndexInfos_ptrs;
    OwnedPFCatalogFileConfig file_owner;
    bool file_has_value = false;
    OwnedPFCatalogImageConfig image_owner;
    bool image_has_value = false;
    std::vector<std::string> platforms_strings;
    std::vector<const char *> platforms_ptrs;
    OwnedPFCatalogReviewConfig review_owner;
    bool review_has_value = false;
    std::vector<OwnedPFEntityKey> reviewerEntities_items;
    std::vector<const PFEntityKey *> reviewerEntities_ptrs;
    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "adminEntities", "admin_entities", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field admin_entities."; return false; }
            Array source_array = field_value;
            adminEntities_items.resize(source_array.size());
            adminEntities_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field admin_entities."; return false; }
                if (!adminEntities_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                adminEntities_ptrs[static_cast<size_t>(i)] = &adminEntities_items[static_cast<size_t>(i)].value;
            }
            value.adminEntities = adminEntities_ptrs.empty() ? nullptr : adminEntities_ptrs.data();
            value.adminEntitiesCount = static_cast<uint32_t>(adminEntities_ptrs.size());
        }
        if (get_request_value(p_request, "catalog", "catalog", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field catalog."; return false; }
            if (!catalog_owner.from_dictionary(field_value, r_error)) return false;
            catalog_has_value = true;
            value.catalog = &catalog_owner.value;
        }
        if (get_request_value(p_request, "deepLinkFormats", "deep_link_formats", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field deep_link_formats."; return false; }
            Array source_array = field_value;
            deepLinkFormats_items.resize(source_array.size());
            deepLinkFormats_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field deep_link_formats."; return false; }
                if (!deepLinkFormats_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                deepLinkFormats_ptrs[static_cast<size_t>(i)] = &deepLinkFormats_items[static_cast<size_t>(i)].value;
            }
            value.deepLinkFormats = deepLinkFormats_ptrs.empty() ? nullptr : deepLinkFormats_ptrs.data();
            value.deepLinkFormatsCount = static_cast<uint32_t>(deepLinkFormats_ptrs.size());
        }
        if (get_request_value(p_request, "displayPropertyIndexInfos", "display_property_index_infos", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field display_property_index_infos."; return false; }
            Array source_array = field_value;
            displayPropertyIndexInfos_items.resize(source_array.size());
            displayPropertyIndexInfos_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field display_property_index_infos."; return false; }
                if (!displayPropertyIndexInfos_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                displayPropertyIndexInfos_ptrs[static_cast<size_t>(i)] = &displayPropertyIndexInfos_items[static_cast<size_t>(i)].value;
            }
            value.displayPropertyIndexInfos = displayPropertyIndexInfos_ptrs.empty() ? nullptr : displayPropertyIndexInfos_ptrs.data();
            value.displayPropertyIndexInfosCount = static_cast<uint32_t>(displayPropertyIndexInfos_ptrs.size());
        }
        if (get_request_value(p_request, "file", "file", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field file."; return false; }
            if (!file_owner.from_dictionary(field_value, r_error)) return false;
            file_has_value = true;
            value.file = &file_owner.value;
        }
        if (get_request_value(p_request, "image", "image", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field image."; return false; }
            if (!image_owner.from_dictionary(field_value, r_error)) return false;
            image_has_value = true;
            value.image = &image_owner.value;
        }
        if (get_request_value(p_request, "isCatalogEnabled", "is_catalog_enabled", &field_value)) {
            value.isCatalogEnabled = static_cast<bool>(field_value);
        }
        if (get_request_value(p_request, "platforms", "platforms", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field platforms."; return false; }
            Array source_array = field_value;
            platforms_strings.clear(); platforms_ptrs.clear();
            platforms_strings.reserve(source_array.size()); platforms_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                platforms_strings.emplace_back(String(source_array[i]).utf8().get_data());
                platforms_ptrs.push_back(platforms_strings.back().c_str());
            }
            value.platforms = platforms_ptrs.empty() ? nullptr : platforms_ptrs.data();
            value.platformsCount = static_cast<uint32_t>(platforms_ptrs.size());
        }
        if (get_request_value(p_request, "review", "review", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field review."; return false; }
            if (!review_owner.from_dictionary(field_value, r_error)) return false;
            review_has_value = true;
            value.review = &review_owner.value;
        }
        if (get_request_value(p_request, "reviewerEntities", "reviewer_entities", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field reviewer_entities."; return false; }
            Array source_array = field_value;
            reviewerEntities_items.resize(source_array.size());
            reviewerEntities_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field reviewer_entities."; return false; }
                if (!reviewerEntities_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                reviewerEntities_ptrs[static_cast<size_t>(i)] = &reviewerEntities_items[static_cast<size_t>(i)].value;
            }
            value.reviewerEntities = reviewerEntities_ptrs.empty() ? nullptr : reviewerEntities_ptrs.data();
            value.reviewerEntitiesCount = static_cast<uint32_t>(reviewerEntities_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFCatalogContent {
    PFCatalogContent value = {};
    std::string id_string;
    std::string maxClientVersion_string;
    std::string minClientVersion_string;
    std::vector<std::string> tags_strings;
    std::vector<const char *> tags_ptrs;
    std::string type_string;
    std::string url_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "id", "id", &field_value)) {
            id_string = String(field_value).utf8().get_data();
            value.id = id_string.empty() ? nullptr : id_string.c_str();
        }
        if (get_request_value(p_request, "maxClientVersion", "max_client_version", &field_value)) {
            maxClientVersion_string = String(field_value).utf8().get_data();
            value.maxClientVersion = maxClientVersion_string.empty() ? nullptr : maxClientVersion_string.c_str();
        }
        if (get_request_value(p_request, "minClientVersion", "min_client_version", &field_value)) {
            minClientVersion_string = String(field_value).utf8().get_data();
            value.minClientVersion = minClientVersion_string.empty() ? nullptr : minClientVersion_string.c_str();
        }
        if (get_request_value(p_request, "tags", "tags", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field tags."; return false; }
            Array source_array = field_value;
            tags_strings.clear(); tags_ptrs.clear();
            tags_strings.reserve(source_array.size()); tags_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                tags_strings.emplace_back(String(source_array[i]).utf8().get_data());
                tags_ptrs.push_back(tags_strings.back().c_str());
            }
            value.tags = tags_ptrs.empty() ? nullptr : tags_ptrs.data();
            value.tagsCount = static_cast<uint32_t>(tags_ptrs.size());
        }
        if (get_request_value(p_request, "type", "type", &field_value)) {
            type_string = String(field_value).utf8().get_data();
            value.type = type_string.empty() ? nullptr : type_string.c_str();
        }
        if (get_request_value(p_request, "url", "url", &field_value)) {
            url_string = String(field_value).utf8().get_data();
            value.url = url_string.empty() ? nullptr : url_string.c_str();
        }
        return true;
    }
};

struct OwnedPFCatalogDeepLink {
    PFCatalogDeepLink value = {};
    std::string platform_string;
    std::string url_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "platform", "platform", &field_value)) {
            platform_string = String(field_value).utf8().get_data();
            value.platform = platform_string.empty() ? nullptr : platform_string.c_str();
        }
        if (get_request_value(p_request, "url", "url", &field_value)) {
            url_string = String(field_value).utf8().get_data();
            value.url = url_string.empty() ? nullptr : url_string.c_str();
        }
        return true;
    }
};

struct OwnedPFJsonObject {
    PFJsonObject value = {};
    std::string stringValue_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "stringValue", "string_value", &field_value)) {
            stringValue_string = String(field_value).utf8().get_data();
            value.stringValue = stringValue_string.empty() ? nullptr : stringValue_string.c_str();
        }
        return true;
    }
};

struct OwnedPFCatalogImage {
    PFCatalogImage value = {};
    std::string id_string;
    std::string tag_string;
    std::string type_string;
    std::string url_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "id", "id", &field_value)) {
            id_string = String(field_value).utf8().get_data();
            value.id = id_string.empty() ? nullptr : id_string.c_str();
        }
        if (get_request_value(p_request, "tag", "tag", &field_value)) {
            tag_string = String(field_value).utf8().get_data();
            value.tag = tag_string.empty() ? nullptr : tag_string.c_str();
        }
        if (get_request_value(p_request, "type", "type", &field_value)) {
            type_string = String(field_value).utf8().get_data();
            value.type = type_string.empty() ? nullptr : type_string.c_str();
        }
        if (get_request_value(p_request, "url", "url", &field_value)) {
            url_string = String(field_value).utf8().get_data();
            value.url = url_string.empty() ? nullptr : url_string.c_str();
        }
        return true;
    }
};

struct OwnedPFCatalogCatalogPriceAmount {
    PFCatalogCatalogPriceAmount value = {};
    std::string itemId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "amount", "amount", &field_value)) {
            value.amount = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "itemId", "item_id", &field_value)) {
            itemId_string = String(field_value).utf8().get_data();
            value.itemId = itemId_string.empty() ? nullptr : itemId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFCatalogCatalogPrice {
    PFCatalogCatalogPrice value = {};
    std::vector<OwnedPFCatalogCatalogPriceAmount> amounts_items;
    std::vector<const PFCatalogCatalogPriceAmount *> amounts_ptrs;
    int32_t unitAmount_storage = {};
    double unitDurationInSeconds_storage = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "amounts", "amounts", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field amounts."; return false; }
            Array source_array = field_value;
            amounts_items.resize(source_array.size());
            amounts_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field amounts."; return false; }
                if (!amounts_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                amounts_ptrs[static_cast<size_t>(i)] = &amounts_items[static_cast<size_t>(i)].value;
            }
            value.amounts = amounts_ptrs.empty() ? nullptr : amounts_ptrs.data();
            value.amountsCount = static_cast<uint32_t>(amounts_ptrs.size());
        }
        if (get_request_value(p_request, "unitAmount", "unit_amount", &field_value)) {
            unitAmount_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.unitAmount = &unitAmount_storage;
        }
        if (get_request_value(p_request, "unitDurationInSeconds", "unit_duration_in_seconds", &field_value)) {
            unitDurationInSeconds_storage = static_cast<double>(static_cast<double>(field_value));
            value.unitDurationInSeconds = &unitDurationInSeconds_storage;
        }
        return true;
    }
};

struct OwnedPFCatalogCatalogPriceOptions {
    PFCatalogCatalogPriceOptions value = {};
    std::vector<OwnedPFCatalogCatalogPrice> prices_items;
    std::vector<const PFCatalogCatalogPrice *> prices_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "prices", "prices", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field prices."; return false; }
            Array source_array = field_value;
            prices_items.resize(source_array.size());
            prices_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field prices."; return false; }
                if (!prices_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                prices_ptrs[static_cast<size_t>(i)] = &prices_items[static_cast<size_t>(i)].value;
            }
            value.prices = prices_ptrs.empty() ? nullptr : prices_ptrs.data();
            value.pricesCount = static_cast<uint32_t>(prices_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFCatalogCatalogItemReference {
    PFCatalogCatalogItemReference value = {};
    int32_t amount_storage = {};
    std::string id_string;
    OwnedPFCatalogCatalogPriceOptions priceOptions_owner;
    bool priceOptions_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "amount", "amount", &field_value)) {
            amount_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.amount = &amount_storage;
        }
        if (get_request_value(p_request, "id", "id", &field_value)) {
            id_string = String(field_value).utf8().get_data();
            value.id = id_string.empty() ? nullptr : id_string.c_str();
        }
        if (get_request_value(p_request, "priceOptions", "price_options", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field price_options."; return false; }
            if (!priceOptions_owner.from_dictionary(field_value, r_error)) return false;
            priceOptions_has_value = true;
            value.priceOptions = &priceOptions_owner.value;
        }
        return true;
    }
};

struct OwnedPFCatalogKeywordSet {
    PFCatalogKeywordSet value = {};
    std::vector<std::string> values_strings;
    std::vector<const char *> values_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "values", "values", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field values."; return false; }
            Array source_array = field_value;
            values_strings.clear(); values_ptrs.clear();
            values_strings.reserve(source_array.size()); values_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                values_strings.emplace_back(String(source_array[i]).utf8().get_data());
                values_ptrs.push_back(values_strings.back().c_str());
            }
            value.values = values_ptrs.empty() ? nullptr : values_ptrs.data();
            value.valuesCount = static_cast<uint32_t>(values_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFCatalogKeywordSetDictionaryEntry {
    PFCatalogKeywordSetDictionaryEntry value = {};
    std::string key_string;
    OwnedPFCatalogKeywordSet value_owner;
    bool value_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "key", "key", &field_value)) {
            key_string = String(field_value).utf8().get_data();
            value.key = key_string.empty() ? nullptr : key_string.c_str();
        }
        if (get_request_value(p_request, "value", "value", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field value."; return false; }
            if (!value_owner.from_dictionary(field_value, r_error)) return false;
            value_has_value = true;
            value.value = &value_owner.value;
        }
        return true;
    }
};

struct OwnedPFCatalogModerationState {
    PFCatalogModerationState value = {};
    time_t lastModifiedDate_storage = {};
    std::string reason_string;
    PFCatalogModerationStatus status_storage = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "lastModifiedDate", "last_modified_date", &field_value)) {
            lastModifiedDate_storage = static_cast<time_t>(static_cast<int64_t>(field_value));
            value.lastModifiedDate = &lastModifiedDate_storage;
        }
        if (get_request_value(p_request, "reason", "reason", &field_value)) {
            reason_string = String(field_value).utf8().get_data();
            value.reason = reason_string.empty() ? nullptr : reason_string.c_str();
        }
        if (get_request_value(p_request, "status", "status", &field_value)) {
            status_storage = static_cast<PFCatalogModerationStatus>(static_cast<int64_t>(field_value));
            value.status = &status_storage;
        }
        return true;
    }
};

struct OwnedPFCatalogRating {
    PFCatalogRating value = {};
    float average_storage = {};
    int32_t count1Star_storage = {};
    int32_t count2Star_storage = {};
    int32_t count3Star_storage = {};
    int32_t count4Star_storage = {};
    int32_t count5Star_storage = {};
    int32_t totalCount_storage = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "average", "average", &field_value)) {
            average_storage = static_cast<float>(static_cast<double>(field_value));
            value.average = &average_storage;
        }
        if (get_request_value(p_request, "count1Star", "count1_star", &field_value)) {
            count1Star_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.count1Star = &count1Star_storage;
        }
        if (get_request_value(p_request, "count2Star", "count2_star", &field_value)) {
            count2Star_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.count2Star = &count2Star_storage;
        }
        if (get_request_value(p_request, "count3Star", "count3_star", &field_value)) {
            count3Star_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.count3Star = &count3Star_storage;
        }
        if (get_request_value(p_request, "count4Star", "count4_star", &field_value)) {
            count4Star_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.count4Star = &count4Star_storage;
        }
        if (get_request_value(p_request, "count5Star", "count5_star", &field_value)) {
            count5Star_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.count5Star = &count5Star_storage;
        }
        return true;
    }
};

struct OwnedPFCatalogRealMoneyPriceDetails {
    PFCatalogRealMoneyPriceDetails value = {};
    std::vector<OwnedPFInt32DictionaryEntry> appleAppStorePrices_items;
    std::vector<PFInt32DictionaryEntry> appleAppStorePrices_values;
    std::vector<OwnedPFInt32DictionaryEntry> googlePlayPrices_items;
    std::vector<PFInt32DictionaryEntry> googlePlayPrices_values;
    std::vector<OwnedPFInt32DictionaryEntry> microsoftStorePrices_items;
    std::vector<PFInt32DictionaryEntry> microsoftStorePrices_values;
    std::vector<OwnedPFInt32DictionaryEntry> nintendoEShopPrices_items;
    std::vector<PFInt32DictionaryEntry> nintendoEShopPrices_values;
    std::vector<OwnedPFInt32DictionaryEntry> playStationStorePrices_items;
    std::vector<PFInt32DictionaryEntry> playStationStorePrices_values;
    std::vector<OwnedPFInt32DictionaryEntry> steamPrices_items;
    std::vector<PFInt32DictionaryEntry> steamPrices_values;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "appleAppStorePrices", "apple_app_store_prices", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field apple_app_store_prices."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            appleAppStorePrices_items.resize(keys.size());
            appleAppStorePrices_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!appleAppStorePrices_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                appleAppStorePrices_values[static_cast<size_t>(i)] = appleAppStorePrices_items[static_cast<size_t>(i)].value;
            }
            value.appleAppStorePrices = appleAppStorePrices_values.empty() ? nullptr : appleAppStorePrices_values.data();
            value.appleAppStorePricesCount = static_cast<uint32_t>(appleAppStorePrices_values.size());
        }
        if (get_request_value(p_request, "googlePlayPrices", "google_play_prices", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field google_play_prices."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            googlePlayPrices_items.resize(keys.size());
            googlePlayPrices_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!googlePlayPrices_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                googlePlayPrices_values[static_cast<size_t>(i)] = googlePlayPrices_items[static_cast<size_t>(i)].value;
            }
            value.googlePlayPrices = googlePlayPrices_values.empty() ? nullptr : googlePlayPrices_values.data();
            value.googlePlayPricesCount = static_cast<uint32_t>(googlePlayPrices_values.size());
        }
        if (get_request_value(p_request, "microsoftStorePrices", "microsoft_store_prices", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field microsoft_store_prices."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            microsoftStorePrices_items.resize(keys.size());
            microsoftStorePrices_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!microsoftStorePrices_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                microsoftStorePrices_values[static_cast<size_t>(i)] = microsoftStorePrices_items[static_cast<size_t>(i)].value;
            }
            value.microsoftStorePrices = microsoftStorePrices_values.empty() ? nullptr : microsoftStorePrices_values.data();
            value.microsoftStorePricesCount = static_cast<uint32_t>(microsoftStorePrices_values.size());
        }
        if (get_request_value(p_request, "nintendoEShopPrices", "nintendo_e_shop_prices", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field nintendo_e_shop_prices."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            nintendoEShopPrices_items.resize(keys.size());
            nintendoEShopPrices_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!nintendoEShopPrices_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                nintendoEShopPrices_values[static_cast<size_t>(i)] = nintendoEShopPrices_items[static_cast<size_t>(i)].value;
            }
            value.nintendoEShopPrices = nintendoEShopPrices_values.empty() ? nullptr : nintendoEShopPrices_values.data();
            value.nintendoEShopPricesCount = static_cast<uint32_t>(nintendoEShopPrices_values.size());
        }
        if (get_request_value(p_request, "playStationStorePrices", "play_station_store_prices", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field play_station_store_prices."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            playStationStorePrices_items.resize(keys.size());
            playStationStorePrices_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!playStationStorePrices_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                playStationStorePrices_values[static_cast<size_t>(i)] = playStationStorePrices_items[static_cast<size_t>(i)].value;
            }
            value.playStationStorePrices = playStationStorePrices_values.empty() ? nullptr : playStationStorePrices_values.data();
            value.playStationStorePricesCount = static_cast<uint32_t>(playStationStorePrices_values.size());
        }
        if (get_request_value(p_request, "steamPrices", "steam_prices", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field steam_prices."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            steamPrices_items.resize(keys.size());
            steamPrices_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!steamPrices_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                steamPrices_values[static_cast<size_t>(i)] = steamPrices_items[static_cast<size_t>(i)].value;
            }
            value.steamPrices = steamPrices_values.empty() ? nullptr : steamPrices_values.data();
            value.steamPricesCount = static_cast<uint32_t>(steamPrices_values.size());
        }
        return true;
    }
};

struct OwnedPFCatalogFilterOptions {
    PFCatalogFilterOptions value = {};
    std::string filter_string;
    bool includeAllItems_storage = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "filter", "filter", &field_value)) {
            filter_string = String(field_value).utf8().get_data();
            value.filter = filter_string.empty() ? nullptr : filter_string.c_str();
        }
        if (get_request_value(p_request, "includeAllItems", "include_all_items", &field_value)) {
            includeAllItems_storage = static_cast<bool>(field_value);
            value.includeAllItems = &includeAllItems_storage;
        }
        return true;
    }
};

struct OwnedPFCatalogPermissions {
    PFCatalogPermissions value = {};
    std::vector<std::string> segmentIds_strings;
    std::vector<const char *> segmentIds_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "segmentIds", "segment_ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field segment_ids."; return false; }
            Array source_array = field_value;
            segmentIds_strings.clear(); segmentIds_ptrs.clear();
            segmentIds_strings.reserve(source_array.size()); segmentIds_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                segmentIds_strings.emplace_back(String(source_array[i]).utf8().get_data());
                segmentIds_ptrs.push_back(segmentIds_strings.back().c_str());
            }
            value.segmentIds = segmentIds_ptrs.empty() ? nullptr : segmentIds_ptrs.data();
            value.segmentIdsCount = static_cast<uint32_t>(segmentIds_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFCatalogCatalogPriceAmountOverride {
    PFCatalogCatalogPriceAmountOverride value = {};
    int32_t fixedValue_storage = {};
    std::string itemId_string;
    double multiplier_storage = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "fixedValue", "fixed_value", &field_value)) {
            fixedValue_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.fixedValue = &fixedValue_storage;
        }
        if (get_request_value(p_request, "itemId", "item_id", &field_value)) {
            itemId_string = String(field_value).utf8().get_data();
            value.itemId = itemId_string.empty() ? nullptr : itemId_string.c_str();
        }
        if (get_request_value(p_request, "multiplier", "multiplier", &field_value)) {
            multiplier_storage = static_cast<double>(static_cast<double>(field_value));
            value.multiplier = &multiplier_storage;
        }
        return true;
    }
};

struct OwnedPFCatalogCatalogPriceOverride {
    PFCatalogCatalogPriceOverride value = {};
    std::vector<OwnedPFCatalogCatalogPriceAmountOverride> amounts_items;
    std::vector<const PFCatalogCatalogPriceAmountOverride *> amounts_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "amounts", "amounts", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field amounts."; return false; }
            Array source_array = field_value;
            amounts_items.resize(source_array.size());
            amounts_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field amounts."; return false; }
                if (!amounts_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                amounts_ptrs[static_cast<size_t>(i)] = &amounts_items[static_cast<size_t>(i)].value;
            }
            value.amounts = amounts_ptrs.empty() ? nullptr : amounts_ptrs.data();
            value.amountsCount = static_cast<uint32_t>(amounts_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFCatalogCatalogPriceOptionsOverride {
    PFCatalogCatalogPriceOptionsOverride value = {};
    std::vector<OwnedPFCatalogCatalogPriceOverride> prices_items;
    std::vector<const PFCatalogCatalogPriceOverride *> prices_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "prices", "prices", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field prices."; return false; }
            Array source_array = field_value;
            prices_items.resize(source_array.size());
            prices_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field prices."; return false; }
                if (!prices_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                prices_ptrs[static_cast<size_t>(i)] = &prices_items[static_cast<size_t>(i)].value;
            }
            value.prices = prices_ptrs.empty() ? nullptr : prices_ptrs.data();
            value.pricesCount = static_cast<uint32_t>(prices_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFCatalogStoreDetails {
    PFCatalogStoreDetails value = {};
    OwnedPFCatalogFilterOptions filterOptions_owner;
    bool filterOptions_has_value = false;
    OwnedPFCatalogPermissions permissions_owner;
    bool permissions_has_value = false;
    OwnedPFCatalogCatalogPriceOptionsOverride priceOptionsOverride_owner;
    bool priceOptionsOverride_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "filterOptions", "filter_options", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field filter_options."; return false; }
            if (!filterOptions_owner.from_dictionary(field_value, r_error)) return false;
            filterOptions_has_value = true;
            value.filterOptions = &filterOptions_owner.value;
        }
        if (get_request_value(p_request, "permissions", "permissions", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field permissions."; return false; }
            if (!permissions_owner.from_dictionary(field_value, r_error)) return false;
            permissions_has_value = true;
            value.permissions = &permissions_owner.value;
        }
        if (get_request_value(p_request, "priceOptionsOverride", "price_options_override", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field price_options_override."; return false; }
            if (!priceOptionsOverride_owner.from_dictionary(field_value, r_error)) return false;
            priceOptionsOverride_has_value = true;
            value.priceOptionsOverride = &priceOptionsOverride_owner.value;
        }
        return true;
    }
};

struct OwnedPFCatalogCatalogItem {
    PFCatalogCatalogItem value = {};
    std::vector<OwnedPFCatalogCatalogAlternateId> alternateIds_items;
    std::vector<const PFCatalogCatalogAlternateId *> alternateIds_ptrs;
    std::vector<OwnedPFCatalogContent> contents_items;
    std::vector<const PFCatalogContent *> contents_ptrs;
    std::string contentType_string;
    time_t creationDate_storage = {};
    OwnedPFEntityKey creatorEntity_owner;
    bool creatorEntity_has_value = false;
    std::vector<OwnedPFCatalogDeepLink> deepLinks_items;
    std::vector<const PFCatalogDeepLink *> deepLinks_ptrs;
    std::string defaultStackId_string;
    std::vector<OwnedPFStringDictionaryEntry> description_items;
    std::vector<PFStringDictionaryEntry> description_values;
    std::string displayProperties_json;
    std::string displayVersion_string;
    time_t endDate_storage = {};
    std::string eTag_string;
    std::string id_string;
    std::vector<OwnedPFCatalogImage> images_items;
    std::vector<const PFCatalogImage *> images_ptrs;
    bool isHidden_storage = {};
    std::vector<OwnedPFCatalogCatalogItemReference> itemReferences_items;
    std::vector<const PFCatalogCatalogItemReference *> itemReferences_ptrs;
    std::vector<OwnedPFCatalogKeywordSetDictionaryEntry> keywords_items;
    std::vector<PFCatalogKeywordSetDictionaryEntry> keywords_values;
    time_t lastModifiedDate_storage = {};
    OwnedPFCatalogModerationState moderation_owner;
    bool moderation_has_value = false;
    std::vector<std::string> platforms_strings;
    std::vector<const char *> platforms_ptrs;
    OwnedPFCatalogCatalogPriceOptions priceOptions_owner;
    bool priceOptions_has_value = false;
    OwnedPFCatalogRating rating_owner;
    bool rating_has_value = false;
    OwnedPFCatalogRealMoneyPriceDetails realMoneyPriceDetails_owner;
    bool realMoneyPriceDetails_has_value = false;
    time_t startDate_storage = {};
    OwnedPFCatalogStoreDetails storeDetails_owner;
    bool storeDetails_has_value = false;
    std::vector<std::string> tags_strings;
    std::vector<const char *> tags_ptrs;
    std::vector<OwnedPFStringDictionaryEntry> title_items;
    std::vector<PFStringDictionaryEntry> title_values;
    std::string type_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "alternateIds", "alternate_ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field alternate_ids."; return false; }
            Array source_array = field_value;
            alternateIds_items.resize(source_array.size());
            alternateIds_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field alternate_ids."; return false; }
                if (!alternateIds_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                alternateIds_ptrs[static_cast<size_t>(i)] = &alternateIds_items[static_cast<size_t>(i)].value;
            }
            value.alternateIds = alternateIds_ptrs.empty() ? nullptr : alternateIds_ptrs.data();
            value.alternateIdsCount = static_cast<uint32_t>(alternateIds_ptrs.size());
        }
        if (get_request_value(p_request, "contents", "contents", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field contents."; return false; }
            Array source_array = field_value;
            contents_items.resize(source_array.size());
            contents_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field contents."; return false; }
                if (!contents_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                contents_ptrs[static_cast<size_t>(i)] = &contents_items[static_cast<size_t>(i)].value;
            }
            value.contents = contents_ptrs.empty() ? nullptr : contents_ptrs.data();
            value.contentsCount = static_cast<uint32_t>(contents_ptrs.size());
        }
        if (get_request_value(p_request, "contentType", "content_type", &field_value)) {
            contentType_string = String(field_value).utf8().get_data();
            value.contentType = contentType_string.empty() ? nullptr : contentType_string.c_str();
        }
        if (get_request_value(p_request, "creationDate", "creation_date", &field_value)) {
            creationDate_storage = static_cast<time_t>(static_cast<int64_t>(field_value));
            value.creationDate = &creationDate_storage;
        }
        if (get_request_value(p_request, "creatorEntity", "creator_entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field creator_entity."; return false; }
            if (!creatorEntity_owner.from_dictionary(field_value, r_error)) return false;
            creatorEntity_has_value = true;
            value.creatorEntity = &creatorEntity_owner.value;
        }
        if (get_request_value(p_request, "deepLinks", "deep_links", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field deep_links."; return false; }
            Array source_array = field_value;
            deepLinks_items.resize(source_array.size());
            deepLinks_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field deep_links."; return false; }
                if (!deepLinks_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                deepLinks_ptrs[static_cast<size_t>(i)] = &deepLinks_items[static_cast<size_t>(i)].value;
            }
            value.deepLinks = deepLinks_ptrs.empty() ? nullptr : deepLinks_ptrs.data();
            value.deepLinksCount = static_cast<uint32_t>(deepLinks_ptrs.size());
        }
        if (get_request_value(p_request, "defaultStackId", "default_stack_id", &field_value)) {
            defaultStackId_string = String(field_value).utf8().get_data();
            value.defaultStackId = defaultStackId_string.empty() ? nullptr : defaultStackId_string.c_str();
        }
        if (get_request_value(p_request, "description", "description", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field description."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            description_items.resize(keys.size());
            description_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!description_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                description_values[static_cast<size_t>(i)] = description_items[static_cast<size_t>(i)].value;
            }
            value.description = description_values.empty() ? nullptr : description_values.data();
            value.descriptionCount = static_cast<uint32_t>(description_values.size());
        }
        if (get_request_value(p_request, "displayProperties", "display_properties", &field_value)) {
            if (field_value.get_type() == Variant::STRING) {
                displayProperties_json = String(field_value).utf8().get_data();
            } else {
                displayProperties_json = variant_to_json_string(field_value).utf8().get_data();
            }
            value.displayProperties.stringValue = displayProperties_json.empty() ? nullptr : displayProperties_json.c_str();
        }
        if (get_request_value(p_request, "displayVersion", "display_version", &field_value)) {
            displayVersion_string = String(field_value).utf8().get_data();
            value.displayVersion = displayVersion_string.empty() ? nullptr : displayVersion_string.c_str();
        }
        if (get_request_value(p_request, "endDate", "end_date", &field_value)) {
            endDate_storage = static_cast<time_t>(static_cast<int64_t>(field_value));
            value.endDate = &endDate_storage;
        }
        if (get_request_value(p_request, "eTag", "e_tag", &field_value)) {
            eTag_string = String(field_value).utf8().get_data();
            value.eTag = eTag_string.empty() ? nullptr : eTag_string.c_str();
        }
        if (get_request_value(p_request, "id", "id", &field_value)) {
            id_string = String(field_value).utf8().get_data();
            value.id = id_string.empty() ? nullptr : id_string.c_str();
        }
        if (get_request_value(p_request, "images", "images", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field images."; return false; }
            Array source_array = field_value;
            images_items.resize(source_array.size());
            images_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field images."; return false; }
                if (!images_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                images_ptrs[static_cast<size_t>(i)] = &images_items[static_cast<size_t>(i)].value;
            }
            value.images = images_ptrs.empty() ? nullptr : images_ptrs.data();
            value.imagesCount = static_cast<uint32_t>(images_ptrs.size());
        }
        if (get_request_value(p_request, "isHidden", "is_hidden", &field_value)) {
            isHidden_storage = static_cast<bool>(field_value);
            value.isHidden = &isHidden_storage;
        }
        if (get_request_value(p_request, "itemReferences", "item_references", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field item_references."; return false; }
            Array source_array = field_value;
            itemReferences_items.resize(source_array.size());
            itemReferences_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field item_references."; return false; }
                if (!itemReferences_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                itemReferences_ptrs[static_cast<size_t>(i)] = &itemReferences_items[static_cast<size_t>(i)].value;
            }
            value.itemReferences = itemReferences_ptrs.empty() ? nullptr : itemReferences_ptrs.data();
            value.itemReferencesCount = static_cast<uint32_t>(itemReferences_ptrs.size());
        }
        if (get_request_value(p_request, "keywords", "keywords", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field keywords."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            keywords_items.resize(keys.size());
            keywords_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!keywords_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                keywords_values[static_cast<size_t>(i)] = keywords_items[static_cast<size_t>(i)].value;
            }
            value.keywords = keywords_values.empty() ? nullptr : keywords_values.data();
            value.keywordsCount = static_cast<uint32_t>(keywords_values.size());
        }
        if (get_request_value(p_request, "lastModifiedDate", "last_modified_date", &field_value)) {
            lastModifiedDate_storage = static_cast<time_t>(static_cast<int64_t>(field_value));
            value.lastModifiedDate = &lastModifiedDate_storage;
        }
        if (get_request_value(p_request, "moderation", "moderation", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field moderation."; return false; }
            if (!moderation_owner.from_dictionary(field_value, r_error)) return false;
            moderation_has_value = true;
            value.moderation = &moderation_owner.value;
        }
        if (get_request_value(p_request, "platforms", "platforms", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field platforms."; return false; }
            Array source_array = field_value;
            platforms_strings.clear(); platforms_ptrs.clear();
            platforms_strings.reserve(source_array.size()); platforms_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                platforms_strings.emplace_back(String(source_array[i]).utf8().get_data());
                platforms_ptrs.push_back(platforms_strings.back().c_str());
            }
            value.platforms = platforms_ptrs.empty() ? nullptr : platforms_ptrs.data();
            value.platformsCount = static_cast<uint32_t>(platforms_ptrs.size());
        }
        if (get_request_value(p_request, "priceOptions", "price_options", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field price_options."; return false; }
            if (!priceOptions_owner.from_dictionary(field_value, r_error)) return false;
            priceOptions_has_value = true;
            value.priceOptions = &priceOptions_owner.value;
        }
        if (get_request_value(p_request, "rating", "rating", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field rating."; return false; }
            if (!rating_owner.from_dictionary(field_value, r_error)) return false;
            rating_has_value = true;
            value.rating = &rating_owner.value;
        }
        if (get_request_value(p_request, "realMoneyPriceDetails", "real_money_price_details", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field real_money_price_details."; return false; }
            if (!realMoneyPriceDetails_owner.from_dictionary(field_value, r_error)) return false;
            realMoneyPriceDetails_has_value = true;
            value.realMoneyPriceDetails = &realMoneyPriceDetails_owner.value;
        }
        if (get_request_value(p_request, "startDate", "start_date", &field_value)) {
            startDate_storage = static_cast<time_t>(static_cast<int64_t>(field_value));
            value.startDate = &startDate_storage;
        }
        if (get_request_value(p_request, "storeDetails", "store_details", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field store_details."; return false; }
            if (!storeDetails_owner.from_dictionary(field_value, r_error)) return false;
            storeDetails_has_value = true;
            value.storeDetails = &storeDetails_owner.value;
        }
        if (get_request_value(p_request, "tags", "tags", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field tags."; return false; }
            Array source_array = field_value;
            tags_strings.clear(); tags_ptrs.clear();
            tags_strings.reserve(source_array.size()); tags_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                tags_strings.emplace_back(String(source_array[i]).utf8().get_data());
                tags_ptrs.push_back(tags_strings.back().c_str());
            }
            value.tags = tags_ptrs.empty() ? nullptr : tags_ptrs.data();
            value.tagsCount = static_cast<uint32_t>(tags_ptrs.size());
        }
        if (get_request_value(p_request, "title", "title", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field title."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            title_items.resize(keys.size());
            title_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!title_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                title_values[static_cast<size_t>(i)] = title_items[static_cast<size_t>(i)].value;
            }
            value.title = title_values.empty() ? nullptr : title_values.data();
            value.titleCount = static_cast<uint32_t>(title_values.size());
        }
        if (get_request_value(p_request, "type", "type", &field_value)) {
            type_string = String(field_value).utf8().get_data();
            value.type = type_string.empty() ? nullptr : type_string.c_str();
        }
        return true;
    }
};

struct OwnedPFCatalogCreateDraftItemRequest {
    PFCatalogCreateDraftItemRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFCatalogCatalogItem item_owner;
    bool item_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "item", "item", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field item."; return false; }
            if (!item_owner.from_dictionary(field_value, r_error)) return false;
            item_has_value = true;
            value.item = &item_owner.value;
        }
        if (get_request_value(p_request, "publish", "publish", &field_value)) {
            value.publish = static_cast<bool>(field_value);
        }
        return true;
    }
};

struct OwnedPFCatalogCreateDraftItemResponse {
    PFCatalogCreateDraftItemResponse value = {};
    OwnedPFCatalogCatalogItem item_owner;
    bool item_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "item", "item", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field item."; return false; }
            if (!item_owner.from_dictionary(field_value, r_error)) return false;
            item_has_value = true;
            value.item = &item_owner.value;
        }
        return true;
    }
};

struct OwnedPFCatalogUploadInfo {
    PFCatalogUploadInfo value = {};
    std::string fileName_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "fileName", "file_name", &field_value)) {
            fileName_string = String(field_value).utf8().get_data();
            value.fileName = fileName_string.empty() ? nullptr : fileName_string.c_str();
        }
        return true;
    }
};

struct OwnedPFCatalogCreateUploadUrlsRequest {
    PFCatalogCreateUploadUrlsRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    std::vector<OwnedPFCatalogUploadInfo> files_items;
    std::vector<const PFCatalogUploadInfo *> files_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "files", "files", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field files."; return false; }
            Array source_array = field_value;
            files_items.resize(source_array.size());
            files_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field files."; return false; }
                if (!files_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                files_ptrs[static_cast<size_t>(i)] = &files_items[static_cast<size_t>(i)].value;
            }
            value.files = files_ptrs.empty() ? nullptr : files_ptrs.data();
            value.filesCount = static_cast<uint32_t>(files_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFCatalogUploadUrlMetadata {
    PFCatalogUploadUrlMetadata value = {};
    std::string fileName_string;
    std::string id_string;
    std::string url_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "fileName", "file_name", &field_value)) {
            fileName_string = String(field_value).utf8().get_data();
            value.fileName = fileName_string.empty() ? nullptr : fileName_string.c_str();
        }
        if (get_request_value(p_request, "id", "id", &field_value)) {
            id_string = String(field_value).utf8().get_data();
            value.id = id_string.empty() ? nullptr : id_string.c_str();
        }
        if (get_request_value(p_request, "url", "url", &field_value)) {
            url_string = String(field_value).utf8().get_data();
            value.url = url_string.empty() ? nullptr : url_string.c_str();
        }
        return true;
    }
};

struct OwnedPFCatalogCreateUploadUrlsResponse {
    PFCatalogCreateUploadUrlsResponse value = {};
    std::vector<OwnedPFCatalogUploadUrlMetadata> uploadUrls_items;
    std::vector<const PFCatalogUploadUrlMetadata *> uploadUrls_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "uploadUrls", "upload_urls", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field upload_urls."; return false; }
            Array source_array = field_value;
            uploadUrls_items.resize(source_array.size());
            uploadUrls_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field upload_urls."; return false; }
                if (!uploadUrls_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                uploadUrls_ptrs[static_cast<size_t>(i)] = &uploadUrls_items[static_cast<size_t>(i)].value;
            }
            value.uploadUrls = uploadUrls_ptrs.empty() ? nullptr : uploadUrls_ptrs.data();
            value.uploadUrlsCount = static_cast<uint32_t>(uploadUrls_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFCatalogDeleteEntityItemReviewsRequest {
    PFCatalogDeleteEntityItemReviewsRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        return true;
    }
};

struct OwnedPFCatalogDeleteItemRequest {
    PFCatalogDeleteItemRequest value = {};
    OwnedPFCatalogCatalogAlternateId alternateId_owner;
    bool alternateId_has_value = false;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::string id_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "alternateId", "alternate_id", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field alternate_id."; return false; }
            if (!alternateId_owner.from_dictionary(field_value, r_error)) return false;
            alternateId_has_value = true;
            value.alternateId = &alternateId_owner.value;
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "id", "id", &field_value)) {
            id_string = String(field_value).utf8().get_data();
            value.id = id_string.empty() ? nullptr : id_string.c_str();
        }
        return true;
    }
};

struct OwnedPFCatalogGetCatalogConfigRequest {
    PFCatalogGetCatalogConfigRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        return true;
    }
};

struct OwnedPFCatalogGetCatalogConfigResponse {
    PFCatalogGetCatalogConfigResponse value = {};
    OwnedPFCatalogCatalogConfig config_owner;
    bool config_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "config", "config", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field config."; return false; }
            if (!config_owner.from_dictionary(field_value, r_error)) return false;
            config_has_value = true;
            value.config = &config_owner.value;
        }
        return true;
    }
};

struct OwnedPFCatalogGetDraftItemRequest {
    PFCatalogGetDraftItemRequest value = {};
    OwnedPFCatalogCatalogAlternateId alternateId_owner;
    bool alternateId_has_value = false;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::string id_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "alternateId", "alternate_id", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field alternate_id."; return false; }
            if (!alternateId_owner.from_dictionary(field_value, r_error)) return false;
            alternateId_has_value = true;
            value.alternateId = &alternateId_owner.value;
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "id", "id", &field_value)) {
            id_string = String(field_value).utf8().get_data();
            value.id = id_string.empty() ? nullptr : id_string.c_str();
        }
        return true;
    }
};

struct OwnedPFCatalogGetDraftItemResponse {
    PFCatalogGetDraftItemResponse value = {};
    OwnedPFCatalogCatalogItem item_owner;
    bool item_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "item", "item", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field item."; return false; }
            if (!item_owner.from_dictionary(field_value, r_error)) return false;
            item_has_value = true;
            value.item = &item_owner.value;
        }
        return true;
    }
};

struct OwnedPFCatalogGetDraftItemsRequest {
    PFCatalogGetDraftItemsRequest value = {};
    std::vector<OwnedPFCatalogCatalogAlternateId> alternateIds_items;
    std::vector<const PFCatalogCatalogAlternateId *> alternateIds_ptrs;
    std::string continuationToken_string;
    int32_t count_storage = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::vector<std::string> ids_strings;
    std::vector<const char *> ids_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "alternateIds", "alternate_ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field alternate_ids."; return false; }
            Array source_array = field_value;
            alternateIds_items.resize(source_array.size());
            alternateIds_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field alternate_ids."; return false; }
                if (!alternateIds_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                alternateIds_ptrs[static_cast<size_t>(i)] = &alternateIds_items[static_cast<size_t>(i)].value;
            }
            value.alternateIds = alternateIds_ptrs.empty() ? nullptr : alternateIds_ptrs.data();
            value.alternateIdsCount = static_cast<uint32_t>(alternateIds_ptrs.size());
        }
        if (get_request_value(p_request, "continuationToken", "continuation_token", &field_value)) {
            continuationToken_string = String(field_value).utf8().get_data();
            value.continuationToken = continuationToken_string.empty() ? nullptr : continuationToken_string.c_str();
        }
        if (get_request_value(p_request, "count", "count", &field_value)) {
            count_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.count = &count_storage;
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "ids", "ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field ids."; return false; }
            Array source_array = field_value;
            ids_strings.clear(); ids_ptrs.clear();
            ids_strings.reserve(source_array.size()); ids_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                ids_strings.emplace_back(String(source_array[i]).utf8().get_data());
                ids_ptrs.push_back(ids_strings.back().c_str());
            }
            value.ids = ids_ptrs.empty() ? nullptr : ids_ptrs.data();
            value.idsCount = static_cast<uint32_t>(ids_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFCatalogGetDraftItemsResponse {
    PFCatalogGetDraftItemsResponse value = {};
    std::string continuationToken_string;
    std::vector<OwnedPFCatalogCatalogItem> items_items;
    std::vector<const PFCatalogCatalogItem *> items_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "continuationToken", "continuation_token", &field_value)) {
            continuationToken_string = String(field_value).utf8().get_data();
            value.continuationToken = continuationToken_string.empty() ? nullptr : continuationToken_string.c_str();
        }
        if (get_request_value(p_request, "items", "items", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field items."; return false; }
            Array source_array = field_value;
            items_items.resize(source_array.size());
            items_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field items."; return false; }
                if (!items_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                items_ptrs[static_cast<size_t>(i)] = &items_items[static_cast<size_t>(i)].value;
            }
            value.items = items_ptrs.empty() ? nullptr : items_ptrs.data();
            value.itemsCount = static_cast<uint32_t>(items_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFCatalogGetEntityDraftItemsRequest {
    PFCatalogGetEntityDraftItemsRequest value = {};
    std::string continuationToken_string;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::string filter_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "continuationToken", "continuation_token", &field_value)) {
            continuationToken_string = String(field_value).utf8().get_data();
            value.continuationToken = continuationToken_string.empty() ? nullptr : continuationToken_string.c_str();
        }
        if (get_request_value(p_request, "count", "count", &field_value)) {
            value.count = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "filter", "filter", &field_value)) {
            filter_string = String(field_value).utf8().get_data();
            value.filter = filter_string.empty() ? nullptr : filter_string.c_str();
        }
        return true;
    }
};

struct OwnedPFCatalogGetEntityDraftItemsResponse {
    PFCatalogGetEntityDraftItemsResponse value = {};
    std::string continuationToken_string;
    std::vector<OwnedPFCatalogCatalogItem> items_items;
    std::vector<const PFCatalogCatalogItem *> items_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "continuationToken", "continuation_token", &field_value)) {
            continuationToken_string = String(field_value).utf8().get_data();
            value.continuationToken = continuationToken_string.empty() ? nullptr : continuationToken_string.c_str();
        }
        if (get_request_value(p_request, "items", "items", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field items."; return false; }
            Array source_array = field_value;
            items_items.resize(source_array.size());
            items_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field items."; return false; }
                if (!items_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                items_ptrs[static_cast<size_t>(i)] = &items_items[static_cast<size_t>(i)].value;
            }
            value.items = items_ptrs.empty() ? nullptr : items_ptrs.data();
            value.itemsCount = static_cast<uint32_t>(items_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFCatalogGetEntityItemReviewRequest {
    PFCatalogGetEntityItemReviewRequest value = {};
    OwnedPFCatalogCatalogAlternateId alternateId_owner;
    bool alternateId_has_value = false;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::string id_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "alternateId", "alternate_id", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field alternate_id."; return false; }
            if (!alternateId_owner.from_dictionary(field_value, r_error)) return false;
            alternateId_has_value = true;
            value.alternateId = &alternateId_owner.value;
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "id", "id", &field_value)) {
            id_string = String(field_value).utf8().get_data();
            value.id = id_string.empty() ? nullptr : id_string.c_str();
        }
        return true;
    }
};

struct OwnedPFCatalogReview {
    PFCatalogReview value = {};
    std::vector<OwnedPFInt32DictionaryEntry> categoryRatings_items;
    std::vector<PFInt32DictionaryEntry> categoryRatings_values;
    std::string itemId_string;
    std::string itemVersion_string;
    std::string locale_string;
    OwnedPFEntityKey reviewerEntity_owner;
    bool reviewerEntity_has_value = false;
    std::string reviewId_string;
    std::string reviewText_string;
    std::string title_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "categoryRatings", "category_ratings", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field category_ratings."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            categoryRatings_items.resize(keys.size());
            categoryRatings_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!categoryRatings_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                categoryRatings_values[static_cast<size_t>(i)] = categoryRatings_items[static_cast<size_t>(i)].value;
            }
            value.categoryRatings = categoryRatings_values.empty() ? nullptr : categoryRatings_values.data();
            value.categoryRatingsCount = static_cast<uint32_t>(categoryRatings_values.size());
        }
        if (get_request_value(p_request, "helpfulNegative", "helpful_negative", &field_value)) {
            value.helpfulNegative = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "helpfulPositive", "helpful_positive", &field_value)) {
            value.helpfulPositive = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "isInstalled", "is_installed", &field_value)) {
            value.isInstalled = static_cast<bool>(field_value);
        }
        if (get_request_value(p_request, "itemId", "item_id", &field_value)) {
            itemId_string = String(field_value).utf8().get_data();
            value.itemId = itemId_string.empty() ? nullptr : itemId_string.c_str();
        }
        if (get_request_value(p_request, "itemVersion", "item_version", &field_value)) {
            itemVersion_string = String(field_value).utf8().get_data();
            value.itemVersion = itemVersion_string.empty() ? nullptr : itemVersion_string.c_str();
        }
        if (get_request_value(p_request, "locale", "locale", &field_value)) {
            locale_string = String(field_value).utf8().get_data();
            value.locale = locale_string.empty() ? nullptr : locale_string.c_str();
        }
        if (get_request_value(p_request, "rating", "rating", &field_value)) {
            value.rating = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "reviewerEntity", "reviewer_entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field reviewer_entity."; return false; }
            if (!reviewerEntity_owner.from_dictionary(field_value, r_error)) return false;
            reviewerEntity_has_value = true;
            value.reviewerEntity = &reviewerEntity_owner.value;
        }
        if (get_request_value(p_request, "reviewId", "review_id", &field_value)) {
            reviewId_string = String(field_value).utf8().get_data();
            value.reviewId = reviewId_string.empty() ? nullptr : reviewId_string.c_str();
        }
        if (get_request_value(p_request, "reviewText", "review_text", &field_value)) {
            reviewText_string = String(field_value).utf8().get_data();
            value.reviewText = reviewText_string.empty() ? nullptr : reviewText_string.c_str();
        }
        if (get_request_value(p_request, "submitted", "submitted", &field_value)) {
            value.submitted = static_cast<time_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "title", "title", &field_value)) {
            title_string = String(field_value).utf8().get_data();
            value.title = title_string.empty() ? nullptr : title_string.c_str();
        }
        return true;
    }
};

struct OwnedPFCatalogGetEntityItemReviewResponse {
    PFCatalogGetEntityItemReviewResponse value = {};
    OwnedPFCatalogReview review_owner;
    bool review_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "review", "review", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field review."; return false; }
            if (!review_owner.from_dictionary(field_value, r_error)) return false;
            review_has_value = true;
            value.review = &review_owner.value;
        }
        return true;
    }
};

struct OwnedPFCatalogGetItemContainersRequest {
    PFCatalogGetItemContainersRequest value = {};
    OwnedPFCatalogCatalogAlternateId alternateId_owner;
    bool alternateId_has_value = false;
    std::string continuationToken_string;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::string id_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "alternateId", "alternate_id", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field alternate_id."; return false; }
            if (!alternateId_owner.from_dictionary(field_value, r_error)) return false;
            alternateId_has_value = true;
            value.alternateId = &alternateId_owner.value;
        }
        if (get_request_value(p_request, "continuationToken", "continuation_token", &field_value)) {
            continuationToken_string = String(field_value).utf8().get_data();
            value.continuationToken = continuationToken_string.empty() ? nullptr : continuationToken_string.c_str();
        }
        if (get_request_value(p_request, "count", "count", &field_value)) {
            value.count = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "id", "id", &field_value)) {
            id_string = String(field_value).utf8().get_data();
            value.id = id_string.empty() ? nullptr : id_string.c_str();
        }
        return true;
    }
};

struct OwnedPFCatalogGetItemContainersResponse {
    PFCatalogGetItemContainersResponse value = {};
    std::vector<OwnedPFCatalogCatalogItem> containers_items;
    std::vector<const PFCatalogCatalogItem *> containers_ptrs;
    std::string continuationToken_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "containers", "containers", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field containers."; return false; }
            Array source_array = field_value;
            containers_items.resize(source_array.size());
            containers_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field containers."; return false; }
                if (!containers_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                containers_ptrs[static_cast<size_t>(i)] = &containers_items[static_cast<size_t>(i)].value;
            }
            value.containers = containers_ptrs.empty() ? nullptr : containers_ptrs.data();
            value.containersCount = static_cast<uint32_t>(containers_ptrs.size());
        }
        if (get_request_value(p_request, "continuationToken", "continuation_token", &field_value)) {
            continuationToken_string = String(field_value).utf8().get_data();
            value.continuationToken = continuationToken_string.empty() ? nullptr : continuationToken_string.c_str();
        }
        return true;
    }
};

struct OwnedPFCatalogGetItemModerationStateRequest {
    PFCatalogGetItemModerationStateRequest value = {};
    OwnedPFCatalogCatalogAlternateId alternateId_owner;
    bool alternateId_has_value = false;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    std::string id_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "alternateId", "alternate_id", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field alternate_id."; return false; }
            if (!alternateId_owner.from_dictionary(field_value, r_error)) return false;
            alternateId_has_value = true;
            value.alternateId = &alternateId_owner.value;
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "id", "id", &field_value)) {
            id_string = String(field_value).utf8().get_data();
            value.id = id_string.empty() ? nullptr : id_string.c_str();
        }
        return true;
    }
};

struct OwnedPFCatalogGetItemModerationStateResponse {
    PFCatalogGetItemModerationStateResponse value = {};
    OwnedPFCatalogModerationState state_owner;
    bool state_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "state", "state", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field state."; return false; }
            if (!state_owner.from_dictionary(field_value, r_error)) return false;
            state_has_value = true;
            value.state = &state_owner.value;
        }
        return true;
    }
};

struct OwnedPFCatalogGetItemPublishStatusRequest {
    PFCatalogGetItemPublishStatusRequest value = {};
    OwnedPFCatalogCatalogAlternateId alternateId_owner;
    bool alternateId_has_value = false;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::string id_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "alternateId", "alternate_id", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field alternate_id."; return false; }
            if (!alternateId_owner.from_dictionary(field_value, r_error)) return false;
            alternateId_has_value = true;
            value.alternateId = &alternateId_owner.value;
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "id", "id", &field_value)) {
            id_string = String(field_value).utf8().get_data();
            value.id = id_string.empty() ? nullptr : id_string.c_str();
        }
        return true;
    }
};

struct OwnedPFCatalogGetItemPublishStatusResponse {
    PFCatalogGetItemPublishStatusResponse value = {};
    PFCatalogPublishResult result_storage = {};
    std::string statusMessage_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "result", "result", &field_value)) {
            result_storage = static_cast<PFCatalogPublishResult>(static_cast<int64_t>(field_value));
            value.result = &result_storage;
        }
        if (get_request_value(p_request, "statusMessage", "status_message", &field_value)) {
            statusMessage_string = String(field_value).utf8().get_data();
            value.statusMessage = statusMessage_string.empty() ? nullptr : statusMessage_string.c_str();
        }
        return true;
    }
};

struct OwnedPFCatalogGetItemRequest {
    PFCatalogGetItemRequest value = {};
    OwnedPFCatalogCatalogAlternateId alternateId_owner;
    bool alternateId_has_value = false;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::string id_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "alternateId", "alternate_id", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field alternate_id."; return false; }
            if (!alternateId_owner.from_dictionary(field_value, r_error)) return false;
            alternateId_has_value = true;
            value.alternateId = &alternateId_owner.value;
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "id", "id", &field_value)) {
            id_string = String(field_value).utf8().get_data();
            value.id = id_string.empty() ? nullptr : id_string.c_str();
        }
        return true;
    }
};

struct OwnedPFCatalogGetItemResponse {
    PFCatalogGetItemResponse value = {};
    OwnedPFCatalogCatalogItem item_owner;
    bool item_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "item", "item", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field item."; return false; }
            if (!item_owner.from_dictionary(field_value, r_error)) return false;
            item_has_value = true;
            value.item = &item_owner.value;
        }
        return true;
    }
};

struct OwnedPFCatalogGetItemReviewSummaryRequest {
    PFCatalogGetItemReviewSummaryRequest value = {};
    OwnedPFCatalogCatalogAlternateId alternateId_owner;
    bool alternateId_has_value = false;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    std::string id_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "alternateId", "alternate_id", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field alternate_id."; return false; }
            if (!alternateId_owner.from_dictionary(field_value, r_error)) return false;
            alternateId_has_value = true;
            value.alternateId = &alternateId_owner.value;
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "id", "id", &field_value)) {
            id_string = String(field_value).utf8().get_data();
            value.id = id_string.empty() ? nullptr : id_string.c_str();
        }
        return true;
    }
};

struct OwnedPFCatalogGetItemReviewSummaryResponse {
    PFCatalogGetItemReviewSummaryResponse value = {};
    OwnedPFCatalogReview leastFavorableReview_owner;
    bool leastFavorableReview_has_value = false;
    OwnedPFCatalogReview mostFavorableReview_owner;
    bool mostFavorableReview_has_value = false;
    OwnedPFCatalogRating rating_owner;
    bool rating_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "leastFavorableReview", "least_favorable_review", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field least_favorable_review."; return false; }
            if (!leastFavorableReview_owner.from_dictionary(field_value, r_error)) return false;
            leastFavorableReview_has_value = true;
            value.leastFavorableReview = &leastFavorableReview_owner.value;
        }
        if (get_request_value(p_request, "mostFavorableReview", "most_favorable_review", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field most_favorable_review."; return false; }
            if (!mostFavorableReview_owner.from_dictionary(field_value, r_error)) return false;
            mostFavorableReview_has_value = true;
            value.mostFavorableReview = &mostFavorableReview_owner.value;
        }
        if (get_request_value(p_request, "rating", "rating", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field rating."; return false; }
            if (!rating_owner.from_dictionary(field_value, r_error)) return false;
            rating_has_value = true;
            value.rating = &rating_owner.value;
        }
        return true;
    }
};

struct OwnedPFCatalogGetItemReviewsRequest {
    PFCatalogGetItemReviewsRequest value = {};
    OwnedPFCatalogCatalogAlternateId alternateId_owner;
    bool alternateId_has_value = false;
    std::string continuationToken_string;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    std::string id_string;
    std::string orderBy_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "alternateId", "alternate_id", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field alternate_id."; return false; }
            if (!alternateId_owner.from_dictionary(field_value, r_error)) return false;
            alternateId_has_value = true;
            value.alternateId = &alternateId_owner.value;
        }
        if (get_request_value(p_request, "continuationToken", "continuation_token", &field_value)) {
            continuationToken_string = String(field_value).utf8().get_data();
            value.continuationToken = continuationToken_string.empty() ? nullptr : continuationToken_string.c_str();
        }
        if (get_request_value(p_request, "count", "count", &field_value)) {
            value.count = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "id", "id", &field_value)) {
            id_string = String(field_value).utf8().get_data();
            value.id = id_string.empty() ? nullptr : id_string.c_str();
        }
        if (get_request_value(p_request, "orderBy", "order_by", &field_value)) {
            orderBy_string = String(field_value).utf8().get_data();
            value.orderBy = orderBy_string.empty() ? nullptr : orderBy_string.c_str();
        }
        return true;
    }
};

struct OwnedPFCatalogGetItemReviewsResponse {
    PFCatalogGetItemReviewsResponse value = {};
    std::string continuationToken_string;
    std::vector<OwnedPFCatalogReview> reviews_items;
    std::vector<const PFCatalogReview *> reviews_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "continuationToken", "continuation_token", &field_value)) {
            continuationToken_string = String(field_value).utf8().get_data();
            value.continuationToken = continuationToken_string.empty() ? nullptr : continuationToken_string.c_str();
        }
        if (get_request_value(p_request, "reviews", "reviews", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field reviews."; return false; }
            Array source_array = field_value;
            reviews_items.resize(source_array.size());
            reviews_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field reviews."; return false; }
                if (!reviews_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                reviews_ptrs[static_cast<size_t>(i)] = &reviews_items[static_cast<size_t>(i)].value;
            }
            value.reviews = reviews_ptrs.empty() ? nullptr : reviews_ptrs.data();
            value.reviewsCount = static_cast<uint32_t>(reviews_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFCatalogGetItemsRequest {
    PFCatalogGetItemsRequest value = {};
    std::vector<OwnedPFCatalogCatalogAlternateId> alternateIds_items;
    std::vector<const PFCatalogCatalogAlternateId *> alternateIds_ptrs;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::vector<std::string> ids_strings;
    std::vector<const char *> ids_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "alternateIds", "alternate_ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field alternate_ids."; return false; }
            Array source_array = field_value;
            alternateIds_items.resize(source_array.size());
            alternateIds_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field alternate_ids."; return false; }
                if (!alternateIds_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                alternateIds_ptrs[static_cast<size_t>(i)] = &alternateIds_items[static_cast<size_t>(i)].value;
            }
            value.alternateIds = alternateIds_ptrs.empty() ? nullptr : alternateIds_ptrs.data();
            value.alternateIdsCount = static_cast<uint32_t>(alternateIds_ptrs.size());
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "ids", "ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field ids."; return false; }
            Array source_array = field_value;
            ids_strings.clear(); ids_ptrs.clear();
            ids_strings.reserve(source_array.size()); ids_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                ids_strings.emplace_back(String(source_array[i]).utf8().get_data());
                ids_ptrs.push_back(ids_strings.back().c_str());
            }
            value.ids = ids_ptrs.empty() ? nullptr : ids_ptrs.data();
            value.idsCount = static_cast<uint32_t>(ids_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFCatalogGetItemsResponse {
    PFCatalogGetItemsResponse value = {};
    std::vector<OwnedPFCatalogCatalogItem> items_items;
    std::vector<const PFCatalogCatalogItem *> items_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "items", "items", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field items."; return false; }
            Array source_array = field_value;
            items_items.resize(source_array.size());
            items_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field items."; return false; }
                if (!items_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                items_ptrs[static_cast<size_t>(i)] = &items_items[static_cast<size_t>(i)].value;
            }
            value.items = items_ptrs.empty() ? nullptr : items_ptrs.data();
            value.itemsCount = static_cast<uint32_t>(items_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFCatalogPublishDraftItemRequest {
    PFCatalogPublishDraftItemRequest value = {};
    OwnedPFCatalogCatalogAlternateId alternateId_owner;
    bool alternateId_has_value = false;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::string eTag_string;
    std::string id_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "alternateId", "alternate_id", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field alternate_id."; return false; }
            if (!alternateId_owner.from_dictionary(field_value, r_error)) return false;
            alternateId_has_value = true;
            value.alternateId = &alternateId_owner.value;
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "eTag", "e_tag", &field_value)) {
            eTag_string = String(field_value).utf8().get_data();
            value.eTag = eTag_string.empty() ? nullptr : eTag_string.c_str();
        }
        if (get_request_value(p_request, "id", "id", &field_value)) {
            id_string = String(field_value).utf8().get_data();
            value.id = id_string.empty() ? nullptr : id_string.c_str();
        }
        return true;
    }
};

struct OwnedPFCatalogReportItemRequest {
    PFCatalogReportItemRequest value = {};
    OwnedPFCatalogCatalogAlternateId alternateId_owner;
    bool alternateId_has_value = false;
    PFCatalogConcernCategory concernCategory_storage = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::string id_string;
    std::string reason_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "alternateId", "alternate_id", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field alternate_id."; return false; }
            if (!alternateId_owner.from_dictionary(field_value, r_error)) return false;
            alternateId_has_value = true;
            value.alternateId = &alternateId_owner.value;
        }
        if (get_request_value(p_request, "concernCategory", "concern_category", &field_value)) {
            concernCategory_storage = static_cast<PFCatalogConcernCategory>(static_cast<int64_t>(field_value));
            value.concernCategory = &concernCategory_storage;
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "id", "id", &field_value)) {
            id_string = String(field_value).utf8().get_data();
            value.id = id_string.empty() ? nullptr : id_string.c_str();
        }
        if (get_request_value(p_request, "reason", "reason", &field_value)) {
            reason_string = String(field_value).utf8().get_data();
            value.reason = reason_string.empty() ? nullptr : reason_string.c_str();
        }
        return true;
    }
};

struct OwnedPFCatalogReportItemReviewRequest {
    PFCatalogReportItemReviewRequest value = {};
    OwnedPFCatalogCatalogAlternateId alternateId_owner;
    bool alternateId_has_value = false;
    PFCatalogConcernCategory concernCategory_storage = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::string itemId_string;
    std::string reason_string;
    std::string reviewId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "alternateId", "alternate_id", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field alternate_id."; return false; }
            if (!alternateId_owner.from_dictionary(field_value, r_error)) return false;
            alternateId_has_value = true;
            value.alternateId = &alternateId_owner.value;
        }
        if (get_request_value(p_request, "concernCategory", "concern_category", &field_value)) {
            concernCategory_storage = static_cast<PFCatalogConcernCategory>(static_cast<int64_t>(field_value));
            value.concernCategory = &concernCategory_storage;
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "itemId", "item_id", &field_value)) {
            itemId_string = String(field_value).utf8().get_data();
            value.itemId = itemId_string.empty() ? nullptr : itemId_string.c_str();
        }
        if (get_request_value(p_request, "reason", "reason", &field_value)) {
            reason_string = String(field_value).utf8().get_data();
            value.reason = reason_string.empty() ? nullptr : reason_string.c_str();
        }
        if (get_request_value(p_request, "reviewId", "review_id", &field_value)) {
            reviewId_string = String(field_value).utf8().get_data();
            value.reviewId = reviewId_string.empty() ? nullptr : reviewId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFCatalogReviewItemRequest {
    PFCatalogReviewItemRequest value = {};
    OwnedPFCatalogCatalogAlternateId alternateId_owner;
    bool alternateId_has_value = false;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::string id_string;
    OwnedPFCatalogReview review_owner;
    bool review_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "alternateId", "alternate_id", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field alternate_id."; return false; }
            if (!alternateId_owner.from_dictionary(field_value, r_error)) return false;
            alternateId_has_value = true;
            value.alternateId = &alternateId_owner.value;
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "id", "id", &field_value)) {
            id_string = String(field_value).utf8().get_data();
            value.id = id_string.empty() ? nullptr : id_string.c_str();
        }
        if (get_request_value(p_request, "review", "review", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field review."; return false; }
            if (!review_owner.from_dictionary(field_value, r_error)) return false;
            review_has_value = true;
            value.review = &review_owner.value;
        }
        return true;
    }
};

struct OwnedPFCatalogReviewTakedown {
    PFCatalogReviewTakedown value = {};
    OwnedPFCatalogCatalogAlternateId alternateId_owner;
    bool alternateId_has_value = false;
    std::string itemId_string;
    std::string reviewId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "alternateId", "alternate_id", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field alternate_id."; return false; }
            if (!alternateId_owner.from_dictionary(field_value, r_error)) return false;
            alternateId_has_value = true;
            value.alternateId = &alternateId_owner.value;
        }
        if (get_request_value(p_request, "itemId", "item_id", &field_value)) {
            itemId_string = String(field_value).utf8().get_data();
            value.itemId = itemId_string.empty() ? nullptr : itemId_string.c_str();
        }
        if (get_request_value(p_request, "reviewId", "review_id", &field_value)) {
            reviewId_string = String(field_value).utf8().get_data();
            value.reviewId = reviewId_string.empty() ? nullptr : reviewId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFCatalogStoreReference {
    PFCatalogStoreReference value = {};
    OwnedPFCatalogCatalogAlternateId alternateId_owner;
    bool alternateId_has_value = false;
    std::string id_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "alternateId", "alternate_id", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field alternate_id."; return false; }
            if (!alternateId_owner.from_dictionary(field_value, r_error)) return false;
            alternateId_has_value = true;
            value.alternateId = &alternateId_owner.value;
        }
        if (get_request_value(p_request, "id", "id", &field_value)) {
            id_string = String(field_value).utf8().get_data();
            value.id = id_string.empty() ? nullptr : id_string.c_str();
        }
        return true;
    }
};

struct OwnedPFCatalogSearchItemsRequest {
    PFCatalogSearchItemsRequest value = {};
    std::string continuationToken_string;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::string filter_string;
    std::string language_string;
    std::string orderBy_string;
    std::string search_string;
    std::string select_string;
    OwnedPFCatalogStoreReference store_owner;
    bool store_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "continuationToken", "continuation_token", &field_value)) {
            continuationToken_string = String(field_value).utf8().get_data();
            value.continuationToken = continuationToken_string.empty() ? nullptr : continuationToken_string.c_str();
        }
        if (get_request_value(p_request, "count", "count", &field_value)) {
            value.count = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "filter", "filter", &field_value)) {
            filter_string = String(field_value).utf8().get_data();
            value.filter = filter_string.empty() ? nullptr : filter_string.c_str();
        }
        if (get_request_value(p_request, "language", "language", &field_value)) {
            language_string = String(field_value).utf8().get_data();
            value.language = language_string.empty() ? nullptr : language_string.c_str();
        }
        if (get_request_value(p_request, "orderBy", "order_by", &field_value)) {
            orderBy_string = String(field_value).utf8().get_data();
            value.orderBy = orderBy_string.empty() ? nullptr : orderBy_string.c_str();
        }
        if (get_request_value(p_request, "search", "search", &field_value)) {
            search_string = String(field_value).utf8().get_data();
            value.search = search_string.empty() ? nullptr : search_string.c_str();
        }
        if (get_request_value(p_request, "select", "select", &field_value)) {
            select_string = String(field_value).utf8().get_data();
            value.select = select_string.empty() ? nullptr : select_string.c_str();
        }
        if (get_request_value(p_request, "store", "store", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field store."; return false; }
            if (!store_owner.from_dictionary(field_value, r_error)) return false;
            store_has_value = true;
            value.store = &store_owner.value;
        }
        return true;
    }
};

struct OwnedPFCatalogSearchItemsResponse {
    PFCatalogSearchItemsResponse value = {};
    std::string continuationToken_string;
    std::vector<OwnedPFCatalogCatalogItem> items_items;
    std::vector<const PFCatalogCatalogItem *> items_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "continuationToken", "continuation_token", &field_value)) {
            continuationToken_string = String(field_value).utf8().get_data();
            value.continuationToken = continuationToken_string.empty() ? nullptr : continuationToken_string.c_str();
        }
        if (get_request_value(p_request, "items", "items", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field items."; return false; }
            Array source_array = field_value;
            items_items.resize(source_array.size());
            items_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field items."; return false; }
                if (!items_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                items_ptrs[static_cast<size_t>(i)] = &items_items[static_cast<size_t>(i)].value;
            }
            value.items = items_ptrs.empty() ? nullptr : items_ptrs.data();
            value.itemsCount = static_cast<uint32_t>(items_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFCatalogSetItemModerationStateRequest {
    PFCatalogSetItemModerationStateRequest value = {};
    OwnedPFCatalogCatalogAlternateId alternateId_owner;
    bool alternateId_has_value = false;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    std::string id_string;
    std::string reason_string;
    PFCatalogModerationStatus status_storage = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "alternateId", "alternate_id", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field alternate_id."; return false; }
            if (!alternateId_owner.from_dictionary(field_value, r_error)) return false;
            alternateId_has_value = true;
            value.alternateId = &alternateId_owner.value;
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "id", "id", &field_value)) {
            id_string = String(field_value).utf8().get_data();
            value.id = id_string.empty() ? nullptr : id_string.c_str();
        }
        if (get_request_value(p_request, "reason", "reason", &field_value)) {
            reason_string = String(field_value).utf8().get_data();
            value.reason = reason_string.empty() ? nullptr : reason_string.c_str();
        }
        if (get_request_value(p_request, "status", "status", &field_value)) {
            status_storage = static_cast<PFCatalogModerationStatus>(static_cast<int64_t>(field_value));
            value.status = &status_storage;
        }
        return true;
    }
};

struct OwnedPFCatalogSubmitItemReviewVoteRequest {
    PFCatalogSubmitItemReviewVoteRequest value = {};
    OwnedPFCatalogCatalogAlternateId alternateId_owner;
    bool alternateId_has_value = false;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::string itemId_string;
    std::string reviewId_string;
    PFCatalogHelpfulnessVote vote_storage = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "alternateId", "alternate_id", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field alternate_id."; return false; }
            if (!alternateId_owner.from_dictionary(field_value, r_error)) return false;
            alternateId_has_value = true;
            value.alternateId = &alternateId_owner.value;
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "itemId", "item_id", &field_value)) {
            itemId_string = String(field_value).utf8().get_data();
            value.itemId = itemId_string.empty() ? nullptr : itemId_string.c_str();
        }
        if (get_request_value(p_request, "reviewId", "review_id", &field_value)) {
            reviewId_string = String(field_value).utf8().get_data();
            value.reviewId = reviewId_string.empty() ? nullptr : reviewId_string.c_str();
        }
        if (get_request_value(p_request, "vote", "vote", &field_value)) {
            vote_storage = static_cast<PFCatalogHelpfulnessVote>(static_cast<int64_t>(field_value));
            value.vote = &vote_storage;
        }
        return true;
    }
};

struct OwnedPFCatalogTakedownItemReviewsRequest {
    PFCatalogTakedownItemReviewsRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    std::vector<OwnedPFCatalogReviewTakedown> reviews_items;
    std::vector<const PFCatalogReviewTakedown *> reviews_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "reviews", "reviews", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field reviews."; return false; }
            Array source_array = field_value;
            reviews_items.resize(source_array.size());
            reviews_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field reviews."; return false; }
                if (!reviews_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                reviews_ptrs[static_cast<size_t>(i)] = &reviews_items[static_cast<size_t>(i)].value;
            }
            value.reviews = reviews_ptrs.empty() ? nullptr : reviews_ptrs.data();
            value.reviewsCount = static_cast<uint32_t>(reviews_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFCatalogUpdateCatalogConfigRequest {
    PFCatalogUpdateCatalogConfigRequest value = {};
    OwnedPFCatalogCatalogConfig config_owner;
    bool config_has_value = false;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "config", "config", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field config."; return false; }
            if (!config_owner.from_dictionary(field_value, r_error)) return false;
            config_has_value = true;
            value.config = &config_owner.value;
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        return true;
    }
};

struct OwnedPFCatalogUpdateDraftItemRequest {
    PFCatalogUpdateDraftItemRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFCatalogCatalogItem item_owner;
    bool item_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "item", "item", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field item."; return false; }
            if (!item_owner.from_dictionary(field_value, r_error)) return false;
            item_has_value = true;
            value.item = &item_owner.value;
        }
        if (get_request_value(p_request, "publish", "publish", &field_value)) {
            value.publish = static_cast<bool>(field_value);
        }
        return true;
    }
};

struct OwnedPFCatalogUpdateDraftItemResponse {
    PFCatalogUpdateDraftItemResponse value = {};
    OwnedPFCatalogCatalogItem item_owner;
    bool item_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "item", "item", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field item."; return false; }
            if (!item_owner.from_dictionary(field_value, r_error)) return false;
            item_has_value = true;
            value.item = &item_owner.value;
        }
        return true;
    }
};

struct OwnedPFCloudScriptExecuteCloudScriptRequest {
    PFCloudScriptExecuteCloudScriptRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    std::string functionName_string;
    std::string functionParameter_json;
    bool generatePlayStreamEvent_storage = {};
    PFCloudScriptCloudScriptRevisionOption revisionSelection_storage = {};
    int32_t specificRevision_storage = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "functionName", "function_name", &field_value)) {
            functionName_string = String(field_value).utf8().get_data();
            value.functionName = functionName_string.empty() ? nullptr : functionName_string.c_str();
        }
        if (get_request_value(p_request, "functionParameter", "function_parameter", &field_value)) {
            if (field_value.get_type() == Variant::STRING) {
                functionParameter_json = String(field_value).utf8().get_data();
            } else {
                functionParameter_json = variant_to_json_string(field_value).utf8().get_data();
            }
            value.functionParameter.stringValue = functionParameter_json.empty() ? nullptr : functionParameter_json.c_str();
        }
        if (get_request_value(p_request, "generatePlayStreamEvent", "generate_play_stream_event", &field_value)) {
            generatePlayStreamEvent_storage = static_cast<bool>(field_value);
            value.generatePlayStreamEvent = &generatePlayStreamEvent_storage;
        }
        if (get_request_value(p_request, "revisionSelection", "revision_selection", &field_value)) {
            revisionSelection_storage = static_cast<PFCloudScriptCloudScriptRevisionOption>(static_cast<int64_t>(field_value));
            value.revisionSelection = &revisionSelection_storage;
        }
        if (get_request_value(p_request, "specificRevision", "specific_revision", &field_value)) {
            specificRevision_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.specificRevision = &specificRevision_storage;
        }
        return true;
    }
};

struct OwnedPFCloudScriptScriptExecutionError {
    PFCloudScriptScriptExecutionError value = {};
    std::string error_string;
    std::string message_string;
    std::string stackTrace_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "error", "error", &field_value)) {
            error_string = String(field_value).utf8().get_data();
            value.error = error_string.empty() ? nullptr : error_string.c_str();
        }
        if (get_request_value(p_request, "message", "message", &field_value)) {
            message_string = String(field_value).utf8().get_data();
            value.message = message_string.empty() ? nullptr : message_string.c_str();
        }
        if (get_request_value(p_request, "stackTrace", "stack_trace", &field_value)) {
            stackTrace_string = String(field_value).utf8().get_data();
            value.stackTrace = stackTrace_string.empty() ? nullptr : stackTrace_string.c_str();
        }
        return true;
    }
};

struct OwnedPFCloudScriptLogStatement {
    PFCloudScriptLogStatement value = {};
    std::string data_json;
    std::string level_string;
    std::string message_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "data", "data", &field_value)) {
            if (field_value.get_type() == Variant::STRING) {
                data_json = String(field_value).utf8().get_data();
            } else {
                data_json = variant_to_json_string(field_value).utf8().get_data();
            }
            value.data.stringValue = data_json.empty() ? nullptr : data_json.c_str();
        }
        if (get_request_value(p_request, "level", "level", &field_value)) {
            level_string = String(field_value).utf8().get_data();
            value.level = level_string.empty() ? nullptr : level_string.c_str();
        }
        if (get_request_value(p_request, "message", "message", &field_value)) {
            message_string = String(field_value).utf8().get_data();
            value.message = message_string.empty() ? nullptr : message_string.c_str();
        }
        return true;
    }
};

struct OwnedPFCloudScriptExecuteCloudScriptResult {
    PFCloudScriptExecuteCloudScriptResult value = {};
    OwnedPFCloudScriptScriptExecutionError error_owner;
    bool error_has_value = false;
    std::string functionName_string;
    std::string functionResult_json;
    bool functionResultTooLarge_storage = {};
    std::vector<OwnedPFCloudScriptLogStatement> logs_items;
    std::vector<const PFCloudScriptLogStatement *> logs_ptrs;
    bool logsTooLarge_storage = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "aPIRequestsIssued", "a_pi_requests_issued", &field_value)) {
            value.aPIRequestsIssued = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "error", "error", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field error."; return false; }
            if (!error_owner.from_dictionary(field_value, r_error)) return false;
            error_has_value = true;
            value.error = &error_owner.value;
        }
        if (get_request_value(p_request, "executionTimeSeconds", "execution_time_seconds", &field_value)) {
            value.executionTimeSeconds = static_cast<double>(static_cast<double>(field_value));
        }
        if (get_request_value(p_request, "functionName", "function_name", &field_value)) {
            functionName_string = String(field_value).utf8().get_data();
            value.functionName = functionName_string.empty() ? nullptr : functionName_string.c_str();
        }
        if (get_request_value(p_request, "functionResult", "function_result", &field_value)) {
            if (field_value.get_type() == Variant::STRING) {
                functionResult_json = String(field_value).utf8().get_data();
            } else {
                functionResult_json = variant_to_json_string(field_value).utf8().get_data();
            }
            value.functionResult.stringValue = functionResult_json.empty() ? nullptr : functionResult_json.c_str();
        }
        if (get_request_value(p_request, "functionResultTooLarge", "function_result_too_large", &field_value)) {
            functionResultTooLarge_storage = static_cast<bool>(field_value);
            value.functionResultTooLarge = &functionResultTooLarge_storage;
        }
        if (get_request_value(p_request, "httpRequestsIssued", "http_requests_issued", &field_value)) {
            value.httpRequestsIssued = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "logs", "logs", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field logs."; return false; }
            Array source_array = field_value;
            logs_items.resize(source_array.size());
            logs_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field logs."; return false; }
                if (!logs_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                logs_ptrs[static_cast<size_t>(i)] = &logs_items[static_cast<size_t>(i)].value;
            }
            value.logs = logs_ptrs.empty() ? nullptr : logs_ptrs.data();
            value.logsCount = static_cast<uint32_t>(logs_ptrs.size());
        }
        if (get_request_value(p_request, "logsTooLarge", "logs_too_large", &field_value)) {
            logsTooLarge_storage = static_cast<bool>(field_value);
            value.logsTooLarge = &logsTooLarge_storage;
        }
        if (get_request_value(p_request, "memoryConsumedBytes", "memory_consumed_bytes", &field_value)) {
            value.memoryConsumedBytes = static_cast<uint32_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "processorTimeSeconds", "processor_time_seconds", &field_value)) {
            value.processorTimeSeconds = static_cast<double>(static_cast<double>(field_value));
        }
        if (get_request_value(p_request, "revision", "revision", &field_value)) {
            value.revision = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        return true;
    }
};

struct OwnedPFCloudScriptExecuteEntityCloudScriptRequest {
    PFCloudScriptExecuteEntityCloudScriptRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::string functionName_string;
    std::string functionParameter_json;
    bool generatePlayStreamEvent_storage = {};
    PFCloudScriptCloudScriptRevisionOption revisionSelection_storage = {};
    int32_t specificRevision_storage = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "functionName", "function_name", &field_value)) {
            functionName_string = String(field_value).utf8().get_data();
            value.functionName = functionName_string.empty() ? nullptr : functionName_string.c_str();
        }
        if (get_request_value(p_request, "functionParameter", "function_parameter", &field_value)) {
            if (field_value.get_type() == Variant::STRING) {
                functionParameter_json = String(field_value).utf8().get_data();
            } else {
                functionParameter_json = variant_to_json_string(field_value).utf8().get_data();
            }
            value.functionParameter.stringValue = functionParameter_json.empty() ? nullptr : functionParameter_json.c_str();
        }
        if (get_request_value(p_request, "generatePlayStreamEvent", "generate_play_stream_event", &field_value)) {
            generatePlayStreamEvent_storage = static_cast<bool>(field_value);
            value.generatePlayStreamEvent = &generatePlayStreamEvent_storage;
        }
        if (get_request_value(p_request, "revisionSelection", "revision_selection", &field_value)) {
            revisionSelection_storage = static_cast<PFCloudScriptCloudScriptRevisionOption>(static_cast<int64_t>(field_value));
            value.revisionSelection = &revisionSelection_storage;
        }
        if (get_request_value(p_request, "specificRevision", "specific_revision", &field_value)) {
            specificRevision_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.specificRevision = &specificRevision_storage;
        }
        return true;
    }
};

struct OwnedPFCloudScriptExecuteFunctionRequest {
    PFCloudScriptExecuteFunctionRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::string functionName_string;
    std::string functionParameter_json;
    bool generatePlayStreamEvent_storage = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "functionName", "function_name", &field_value)) {
            functionName_string = String(field_value).utf8().get_data();
            value.functionName = functionName_string.empty() ? nullptr : functionName_string.c_str();
        }
        if (get_request_value(p_request, "functionParameter", "function_parameter", &field_value)) {
            if (field_value.get_type() == Variant::STRING) {
                functionParameter_json = String(field_value).utf8().get_data();
            } else {
                functionParameter_json = variant_to_json_string(field_value).utf8().get_data();
            }
            value.functionParameter.stringValue = functionParameter_json.empty() ? nullptr : functionParameter_json.c_str();
        }
        if (get_request_value(p_request, "generatePlayStreamEvent", "generate_play_stream_event", &field_value)) {
            generatePlayStreamEvent_storage = static_cast<bool>(field_value);
            value.generatePlayStreamEvent = &generatePlayStreamEvent_storage;
        }
        return true;
    }
};

struct OwnedPFCloudScriptFunctionExecutionError {
    PFCloudScriptFunctionExecutionError value = {};
    std::string error_string;
    std::string message_string;
    std::string stackTrace_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "error", "error", &field_value)) {
            error_string = String(field_value).utf8().get_data();
            value.error = error_string.empty() ? nullptr : error_string.c_str();
        }
        if (get_request_value(p_request, "message", "message", &field_value)) {
            message_string = String(field_value).utf8().get_data();
            value.message = message_string.empty() ? nullptr : message_string.c_str();
        }
        if (get_request_value(p_request, "stackTrace", "stack_trace", &field_value)) {
            stackTrace_string = String(field_value).utf8().get_data();
            value.stackTrace = stackTrace_string.empty() ? nullptr : stackTrace_string.c_str();
        }
        return true;
    }
};

struct OwnedPFCloudScriptExecuteFunctionResult {
    PFCloudScriptExecuteFunctionResult value = {};
    OwnedPFCloudScriptFunctionExecutionError error_owner;
    bool error_has_value = false;
    std::string functionName_string;
    std::string functionResult_json;
    int32_t functionResultSize_storage = {};
    bool functionResultTooLarge_storage = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "error", "error", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field error."; return false; }
            if (!error_owner.from_dictionary(field_value, r_error)) return false;
            error_has_value = true;
            value.error = &error_owner.value;
        }
        if (get_request_value(p_request, "executionTimeMilliseconds", "execution_time_milliseconds", &field_value)) {
            value.executionTimeMilliseconds = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "functionName", "function_name", &field_value)) {
            functionName_string = String(field_value).utf8().get_data();
            value.functionName = functionName_string.empty() ? nullptr : functionName_string.c_str();
        }
        if (get_request_value(p_request, "functionResult", "function_result", &field_value)) {
            if (field_value.get_type() == Variant::STRING) {
                functionResult_json = String(field_value).utf8().get_data();
            } else {
                functionResult_json = variant_to_json_string(field_value).utf8().get_data();
            }
            value.functionResult.stringValue = functionResult_json.empty() ? nullptr : functionResult_json.c_str();
        }
        if (get_request_value(p_request, "functionResultSize", "function_result_size", &field_value)) {
            functionResultSize_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.functionResultSize = &functionResultSize_storage;
        }
        if (get_request_value(p_request, "functionResultTooLarge", "function_result_too_large", &field_value)) {
            functionResultTooLarge_storage = static_cast<bool>(field_value);
            value.functionResultTooLarge = &functionResultTooLarge_storage;
        }
        return true;
    }
};

struct OwnedPFDataAbortFileUploadsRequest {
    PFDataAbortFileUploadsRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::vector<std::string> fileNames_strings;
    std::vector<const char *> fileNames_ptrs;
    int32_t profileVersion_storage = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "fileNames", "file_names", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field file_names."; return false; }
            Array source_array = field_value;
            fileNames_strings.clear(); fileNames_ptrs.clear();
            fileNames_strings.reserve(source_array.size()); fileNames_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                fileNames_strings.emplace_back(String(source_array[i]).utf8().get_data());
                fileNames_ptrs.push_back(fileNames_strings.back().c_str());
            }
            value.fileNames = fileNames_ptrs.empty() ? nullptr : fileNames_ptrs.data();
            value.fileNamesCount = static_cast<uint32_t>(fileNames_ptrs.size());
        }
        if (get_request_value(p_request, "profileVersion", "profile_version", &field_value)) {
            profileVersion_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.profileVersion = &profileVersion_storage;
        }
        return true;
    }
};

struct OwnedPFDataAbortFileUploadsResponse {
    PFDataAbortFileUploadsResponse value = {};
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "profileVersion", "profile_version", &field_value)) {
            value.profileVersion = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        return true;
    }
};

struct OwnedPFDataDeleteFilesRequest {
    PFDataDeleteFilesRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::vector<std::string> fileNames_strings;
    std::vector<const char *> fileNames_ptrs;
    int32_t profileVersion_storage = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "fileNames", "file_names", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field file_names."; return false; }
            Array source_array = field_value;
            fileNames_strings.clear(); fileNames_ptrs.clear();
            fileNames_strings.reserve(source_array.size()); fileNames_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                fileNames_strings.emplace_back(String(source_array[i]).utf8().get_data());
                fileNames_ptrs.push_back(fileNames_strings.back().c_str());
            }
            value.fileNames = fileNames_ptrs.empty() ? nullptr : fileNames_ptrs.data();
            value.fileNamesCount = static_cast<uint32_t>(fileNames_ptrs.size());
        }
        if (get_request_value(p_request, "profileVersion", "profile_version", &field_value)) {
            profileVersion_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.profileVersion = &profileVersion_storage;
        }
        return true;
    }
};

struct OwnedPFDataDeleteFilesResponse {
    PFDataDeleteFilesResponse value = {};
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "profileVersion", "profile_version", &field_value)) {
            value.profileVersion = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        return true;
    }
};

struct OwnedPFDataFinalizeFileUploadsRequest {
    PFDataFinalizeFileUploadsRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::vector<std::string> fileNames_strings;
    std::vector<const char *> fileNames_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "fileNames", "file_names", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field file_names."; return false; }
            Array source_array = field_value;
            fileNames_strings.clear(); fileNames_ptrs.clear();
            fileNames_strings.reserve(source_array.size()); fileNames_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                fileNames_strings.emplace_back(String(source_array[i]).utf8().get_data());
                fileNames_ptrs.push_back(fileNames_strings.back().c_str());
            }
            value.fileNames = fileNames_ptrs.empty() ? nullptr : fileNames_ptrs.data();
            value.fileNamesCount = static_cast<uint32_t>(fileNames_ptrs.size());
        }
        if (get_request_value(p_request, "profileVersion", "profile_version", &field_value)) {
            value.profileVersion = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        return true;
    }
};

struct OwnedPFDataGetFileMetadata {
    PFDataGetFileMetadata value = {};
    std::string checksum_string;
    std::string downloadUrl_string;
    std::string fileName_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "checksum", "checksum", &field_value)) {
            checksum_string = String(field_value).utf8().get_data();
            value.checksum = checksum_string.empty() ? nullptr : checksum_string.c_str();
        }
        if (get_request_value(p_request, "downloadUrl", "download_url", &field_value)) {
            downloadUrl_string = String(field_value).utf8().get_data();
            value.downloadUrl = downloadUrl_string.empty() ? nullptr : downloadUrl_string.c_str();
        }
        if (get_request_value(p_request, "fileName", "file_name", &field_value)) {
            fileName_string = String(field_value).utf8().get_data();
            value.fileName = fileName_string.empty() ? nullptr : fileName_string.c_str();
        }
        if (get_request_value(p_request, "lastModified", "last_modified", &field_value)) {
            value.lastModified = static_cast<time_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "size", "size", &field_value)) {
            value.size = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        return true;
    }
};

struct OwnedPFDataGetFileMetadataDictionaryEntry {
    PFDataGetFileMetadataDictionaryEntry value = {};
    std::string key_string;
    OwnedPFDataGetFileMetadata value_owner;
    bool value_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "key", "key", &field_value)) {
            key_string = String(field_value).utf8().get_data();
            value.key = key_string.empty() ? nullptr : key_string.c_str();
        }
        if (get_request_value(p_request, "value", "value", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field value."; return false; }
            if (!value_owner.from_dictionary(field_value, r_error)) return false;
            value_has_value = true;
            value.value = &value_owner.value;
        }
        return true;
    }
};

struct OwnedPFDataFinalizeFileUploadsResponse {
    PFDataFinalizeFileUploadsResponse value = {};
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::vector<OwnedPFDataGetFileMetadataDictionaryEntry> metadata_items;
    std::vector<PFDataGetFileMetadataDictionaryEntry> metadata_values;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "metadata", "metadata", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field metadata."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            metadata_items.resize(keys.size());
            metadata_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!metadata_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                metadata_values[static_cast<size_t>(i)] = metadata_items[static_cast<size_t>(i)].value;
            }
            value.metadata = metadata_values.empty() ? nullptr : metadata_values.data();
            value.metadataCount = static_cast<uint32_t>(metadata_values.size());
        }
        if (get_request_value(p_request, "profileVersion", "profile_version", &field_value)) {
            value.profileVersion = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        return true;
    }
};

struct OwnedPFDataGetFilesRequest {
    PFDataGetFilesRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        return true;
    }
};

struct OwnedPFDataGetFilesResponse {
    PFDataGetFilesResponse value = {};
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::vector<OwnedPFDataGetFileMetadataDictionaryEntry> metadata_items;
    std::vector<PFDataGetFileMetadataDictionaryEntry> metadata_values;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "metadata", "metadata", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field metadata."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            metadata_items.resize(keys.size());
            metadata_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!metadata_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                metadata_values[static_cast<size_t>(i)] = metadata_items[static_cast<size_t>(i)].value;
            }
            value.metadata = metadata_values.empty() ? nullptr : metadata_values.data();
            value.metadataCount = static_cast<uint32_t>(metadata_values.size());
        }
        if (get_request_value(p_request, "profileVersion", "profile_version", &field_value)) {
            value.profileVersion = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        return true;
    }
};

struct OwnedPFDataGetObjectsRequest {
    PFDataGetObjectsRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    bool escapeObject_storage = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "escapeObject", "escape_object", &field_value)) {
            escapeObject_storage = static_cast<bool>(field_value);
            value.escapeObject = &escapeObject_storage;
        }
        return true;
    }
};

struct OwnedPFDataObjectResult {
    PFDataObjectResult value = {};
    std::string dataObject_json;
    std::string escapedDataObject_string;
    std::string objectName_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "dataObject", "data_object", &field_value)) {
            if (field_value.get_type() == Variant::STRING) {
                dataObject_json = String(field_value).utf8().get_data();
            } else {
                dataObject_json = variant_to_json_string(field_value).utf8().get_data();
            }
            value.dataObject.stringValue = dataObject_json.empty() ? nullptr : dataObject_json.c_str();
        }
        if (get_request_value(p_request, "escapedDataObject", "escaped_data_object", &field_value)) {
            escapedDataObject_string = String(field_value).utf8().get_data();
            value.escapedDataObject = escapedDataObject_string.empty() ? nullptr : escapedDataObject_string.c_str();
        }
        if (get_request_value(p_request, "objectName", "object_name", &field_value)) {
            objectName_string = String(field_value).utf8().get_data();
            value.objectName = objectName_string.empty() ? nullptr : objectName_string.c_str();
        }
        return true;
    }
};

struct OwnedPFDataObjectResultDictionaryEntry {
    PFDataObjectResultDictionaryEntry value = {};
    std::string key_string;
    OwnedPFDataObjectResult value_owner;
    bool value_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "key", "key", &field_value)) {
            key_string = String(field_value).utf8().get_data();
            value.key = key_string.empty() ? nullptr : key_string.c_str();
        }
        if (get_request_value(p_request, "value", "value", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field value."; return false; }
            if (!value_owner.from_dictionary(field_value, r_error)) return false;
            value_has_value = true;
            value.value = &value_owner.value;
        }
        return true;
    }
};

struct OwnedPFDataGetObjectsResponse {
    PFDataGetObjectsResponse value = {};
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::vector<OwnedPFDataObjectResultDictionaryEntry> objects_items;
    std::vector<PFDataObjectResultDictionaryEntry> objects_values;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "objects", "objects", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field objects."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            objects_items.resize(keys.size());
            objects_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!objects_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                objects_values[static_cast<size_t>(i)] = objects_items[static_cast<size_t>(i)].value;
            }
            value.objects = objects_values.empty() ? nullptr : objects_values.data();
            value.objectsCount = static_cast<uint32_t>(objects_values.size());
        }
        if (get_request_value(p_request, "profileVersion", "profile_version", &field_value)) {
            value.profileVersion = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        return true;
    }
};

struct OwnedPFDataInitiateFileUploadMetadata {
    PFDataInitiateFileUploadMetadata value = {};
    std::string fileName_string;
    std::string uploadUrl_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "fileName", "file_name", &field_value)) {
            fileName_string = String(field_value).utf8().get_data();
            value.fileName = fileName_string.empty() ? nullptr : fileName_string.c_str();
        }
        if (get_request_value(p_request, "uploadUrl", "upload_url", &field_value)) {
            uploadUrl_string = String(field_value).utf8().get_data();
            value.uploadUrl = uploadUrl_string.empty() ? nullptr : uploadUrl_string.c_str();
        }
        return true;
    }
};

struct OwnedPFDataInitiateFileUploadsRequest {
    PFDataInitiateFileUploadsRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::vector<std::string> fileNames_strings;
    std::vector<const char *> fileNames_ptrs;
    int32_t profileVersion_storage = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "fileNames", "file_names", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field file_names."; return false; }
            Array source_array = field_value;
            fileNames_strings.clear(); fileNames_ptrs.clear();
            fileNames_strings.reserve(source_array.size()); fileNames_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                fileNames_strings.emplace_back(String(source_array[i]).utf8().get_data());
                fileNames_ptrs.push_back(fileNames_strings.back().c_str());
            }
            value.fileNames = fileNames_ptrs.empty() ? nullptr : fileNames_ptrs.data();
            value.fileNamesCount = static_cast<uint32_t>(fileNames_ptrs.size());
        }
        if (get_request_value(p_request, "profileVersion", "profile_version", &field_value)) {
            profileVersion_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.profileVersion = &profileVersion_storage;
        }
        return true;
    }
};

struct OwnedPFDataInitiateFileUploadsResponse {
    PFDataInitiateFileUploadsResponse value = {};
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::vector<OwnedPFDataInitiateFileUploadMetadata> uploadDetails_items;
    std::vector<const PFDataInitiateFileUploadMetadata *> uploadDetails_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "profileVersion", "profile_version", &field_value)) {
            value.profileVersion = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "uploadDetails", "upload_details", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field upload_details."; return false; }
            Array source_array = field_value;
            uploadDetails_items.resize(source_array.size());
            uploadDetails_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field upload_details."; return false; }
                if (!uploadDetails_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                uploadDetails_ptrs[static_cast<size_t>(i)] = &uploadDetails_items[static_cast<size_t>(i)].value;
            }
            value.uploadDetails = uploadDetails_ptrs.empty() ? nullptr : uploadDetails_ptrs.data();
            value.uploadDetailsCount = static_cast<uint32_t>(uploadDetails_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFDataSetObject {
    PFDataSetObject value = {};
    std::string dataObject_json;
    bool deleteObject_storage = {};
    std::string escapedDataObject_string;
    std::string objectName_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "dataObject", "data_object", &field_value)) {
            if (field_value.get_type() == Variant::STRING) {
                dataObject_json = String(field_value).utf8().get_data();
            } else {
                dataObject_json = variant_to_json_string(field_value).utf8().get_data();
            }
            value.dataObject.stringValue = dataObject_json.empty() ? nullptr : dataObject_json.c_str();
        }
        if (get_request_value(p_request, "deleteObject", "delete_object", &field_value)) {
            deleteObject_storage = static_cast<bool>(field_value);
            value.deleteObject = &deleteObject_storage;
        }
        if (get_request_value(p_request, "escapedDataObject", "escaped_data_object", &field_value)) {
            escapedDataObject_string = String(field_value).utf8().get_data();
            value.escapedDataObject = escapedDataObject_string.empty() ? nullptr : escapedDataObject_string.c_str();
        }
        if (get_request_value(p_request, "objectName", "object_name", &field_value)) {
            objectName_string = String(field_value).utf8().get_data();
            value.objectName = objectName_string.empty() ? nullptr : objectName_string.c_str();
        }
        return true;
    }
};

struct OwnedPFDataSetObjectInfo {
    PFDataSetObjectInfo value = {};
    std::string objectName_string;
    std::string operationReason_string;
    PFOperationTypes setResult_storage = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "objectName", "object_name", &field_value)) {
            objectName_string = String(field_value).utf8().get_data();
            value.objectName = objectName_string.empty() ? nullptr : objectName_string.c_str();
        }
        if (get_request_value(p_request, "operationReason", "operation_reason", &field_value)) {
            operationReason_string = String(field_value).utf8().get_data();
            value.operationReason = operationReason_string.empty() ? nullptr : operationReason_string.c_str();
        }
        if (get_request_value(p_request, "setResult", "set_result", &field_value)) {
            setResult_storage = static_cast<PFOperationTypes>(static_cast<int64_t>(field_value));
            value.setResult = &setResult_storage;
        }
        return true;
    }
};

struct OwnedPFDataSetObjectsRequest {
    PFDataSetObjectsRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    int32_t expectedProfileVersion_storage = {};
    std::vector<OwnedPFDataSetObject> objects_items;
    std::vector<const PFDataSetObject *> objects_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "expectedProfileVersion", "expected_profile_version", &field_value)) {
            expectedProfileVersion_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.expectedProfileVersion = &expectedProfileVersion_storage;
        }
        if (get_request_value(p_request, "objects", "objects", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field objects."; return false; }
            Array source_array = field_value;
            objects_items.resize(source_array.size());
            objects_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field objects."; return false; }
                if (!objects_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                objects_ptrs[static_cast<size_t>(i)] = &objects_items[static_cast<size_t>(i)].value;
            }
            value.objects = objects_ptrs.empty() ? nullptr : objects_ptrs.data();
            value.objectsCount = static_cast<uint32_t>(objects_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFDataSetObjectsResponse {
    PFDataSetObjectsResponse value = {};
    std::vector<OwnedPFDataSetObjectInfo> setResults_items;
    std::vector<const PFDataSetObjectInfo *> setResults_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "profileVersion", "profile_version", &field_value)) {
            value.profileVersion = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "setResults", "set_results", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field set_results."; return false; }
            Array source_array = field_value;
            setResults_items.resize(source_array.size());
            setResults_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field set_results."; return false; }
                if (!setResults_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                setResults_ptrs[static_cast<size_t>(i)] = &setResults_items[static_cast<size_t>(i)].value;
            }
            value.setResults = setResults_ptrs.empty() ? nullptr : setResults_ptrs.data();
            value.setResultsCount = static_cast<uint32_t>(setResults_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFEntityKeyDictionaryEntry {
    PFEntityKeyDictionaryEntry value = {};
    std::string key_string;
    OwnedPFEntityKey value_owner;
    bool value_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "key", "key", &field_value)) {
            key_string = String(field_value).utf8().get_data();
            value.key = key_string.empty() ? nullptr : key_string.c_str();
        }
        if (get_request_value(p_request, "value", "value", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field value."; return false; }
            if (!value_owner.from_dictionary(field_value, r_error)) return false;
            value_has_value = true;
            value.value = &value_owner.value;
        }
        return true;
    }
};

struct OwnedPFEntityStatisticValue {
    PFEntityStatisticValue value = {};
    std::string metadata_string;
    std::string name_string;
    std::vector<std::string> scores_strings;
    std::vector<const char *> scores_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "metadata", "metadata", &field_value)) {
            metadata_string = String(field_value).utf8().get_data();
            value.metadata = metadata_string.empty() ? nullptr : metadata_string.c_str();
        }
        if (get_request_value(p_request, "name", "name", &field_value)) {
            name_string = String(field_value).utf8().get_data();
            value.name = name_string.empty() ? nullptr : name_string.c_str();
        }
        if (get_request_value(p_request, "scores", "scores", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field scores."; return false; }
            Array source_array = field_value;
            scores_strings.clear(); scores_ptrs.clear();
            scores_strings.reserve(source_array.size()); scores_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                scores_strings.emplace_back(String(source_array[i]).utf8().get_data());
                scores_ptrs.push_back(scores_strings.back().c_str());
            }
            value.scores = scores_ptrs.empty() ? nullptr : scores_ptrs.data();
            value.scoresCount = static_cast<uint32_t>(scores_ptrs.size());
        }
        if (get_request_value(p_request, "version", "version", &field_value)) {
            value.version = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        return true;
    }
};

struct OwnedPFEntityStatisticValueDictionaryEntry {
    PFEntityStatisticValueDictionaryEntry value = {};
    std::string key_string;
    OwnedPFEntityStatisticValue value_owner;
    bool value_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "key", "key", &field_value)) {
            key_string = String(field_value).utf8().get_data();
            value.key = key_string.empty() ? nullptr : key_string.c_str();
        }
        if (get_request_value(p_request, "value", "value", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field value."; return false; }
            if (!value_owner.from_dictionary(field_value, r_error)) return false;
            value_has_value = true;
            value.value = &value_owner.value;
        }
        return true;
    }
};

struct OwnedPFExperimentationGetTreatmentAssignmentRequest {
    PFExperimentationGetTreatmentAssignmentRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        return true;
    }
};

struct OwnedPFVariable {
    PFVariable value = {};
    std::string name_string;
    std::string value_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "name", "name", &field_value)) {
            name_string = String(field_value).utf8().get_data();
            value.name = name_string.empty() ? nullptr : name_string.c_str();
        }
        if (get_request_value(p_request, "value", "value", &field_value)) {
            value_string = String(field_value).utf8().get_data();
            value.value = value_string.empty() ? nullptr : value_string.c_str();
        }
        return true;
    }
};

struct OwnedPFTreatmentAssignment {
    PFTreatmentAssignment value = {};
    std::vector<OwnedPFVariable> variables_items;
    std::vector<const PFVariable *> variables_ptrs;
    std::vector<std::string> variants_strings;
    std::vector<const char *> variants_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "variables", "variables", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field variables."; return false; }
            Array source_array = field_value;
            variables_items.resize(source_array.size());
            variables_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field variables."; return false; }
                if (!variables_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                variables_ptrs[static_cast<size_t>(i)] = &variables_items[static_cast<size_t>(i)].value;
            }
            value.variables = variables_ptrs.empty() ? nullptr : variables_ptrs.data();
            value.variablesCount = static_cast<uint32_t>(variables_ptrs.size());
        }
        if (get_request_value(p_request, "variants", "variants", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field variants."; return false; }
            Array source_array = field_value;
            variants_strings.clear(); variants_ptrs.clear();
            variants_strings.reserve(source_array.size()); variants_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                variants_strings.emplace_back(String(source_array[i]).utf8().get_data());
                variants_ptrs.push_back(variants_strings.back().c_str());
            }
            value.variants = variants_ptrs.empty() ? nullptr : variants_ptrs.data();
            value.variantsCount = static_cast<uint32_t>(variants_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFExperimentationGetTreatmentAssignmentResult {
    PFExperimentationGetTreatmentAssignmentResult value = {};
    OwnedPFTreatmentAssignment treatmentAssignment_owner;
    bool treatmentAssignment_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "treatmentAssignment", "treatment_assignment", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field treatment_assignment."; return false; }
            if (!treatmentAssignment_owner.from_dictionary(field_value, r_error)) return false;
            treatmentAssignment_has_value = true;
            value.treatmentAssignment = &treatmentAssignment_owner.value;
        }
        return true;
    }
};

struct OwnedPFFriendsAddFriendResult {
    PFFriendsAddFriendResult value = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "created", "created", &field_value)) {
            value.created = static_cast<bool>(field_value);
        }
        return true;
    }
};

struct OwnedPFFriendsClientAddFriendRequest {
    PFFriendsClientAddFriendRequest value = {};
    std::string friendEmail_string;
    std::string friendPlayFabId_string;
    std::string friendTitleDisplayName_string;
    std::string friendUsername_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "friendEmail", "friend_email", &field_value)) {
            friendEmail_string = String(field_value).utf8().get_data();
            value.friendEmail = friendEmail_string.empty() ? nullptr : friendEmail_string.c_str();
        }
        if (get_request_value(p_request, "friendPlayFabId", "friend_play_fab_id", &field_value)) {
            friendPlayFabId_string = String(field_value).utf8().get_data();
            value.friendPlayFabId = friendPlayFabId_string.empty() ? nullptr : friendPlayFabId_string.c_str();
        }
        if (get_request_value(p_request, "friendTitleDisplayName", "friend_title_display_name", &field_value)) {
            friendTitleDisplayName_string = String(field_value).utf8().get_data();
            value.friendTitleDisplayName = friendTitleDisplayName_string.empty() ? nullptr : friendTitleDisplayName_string.c_str();
        }
        if (get_request_value(p_request, "friendUsername", "friend_username", &field_value)) {
            friendUsername_string = String(field_value).utf8().get_data();
            value.friendUsername = friendUsername_string.empty() ? nullptr : friendUsername_string.c_str();
        }
        return true;
    }
};

struct OwnedPFFriendsClientGetFriendsListRequest {
    PFFriendsClientGetFriendsListRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    PFFriendsExternalFriendSources externalPlatformFriends_storage = {};
    OwnedPFPlayerProfileViewConstraints profileConstraints_owner;
    bool profileConstraints_has_value = false;
    XUserHandle user_handle = nullptr;

    ~OwnedPFFriendsClientGetFriendsListRequest() {
        if (user_handle != nullptr) {
            XUserCloseHandle(user_handle);
            user_handle = nullptr;
        }
    }

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "externalPlatformFriends", "external_platform_friends", &field_value)) {
            externalPlatformFriends_storage = static_cast<PFFriendsExternalFriendSources>(static_cast<int64_t>(field_value));
            value.externalPlatformFriends = &externalPlatformFriends_storage;
        }
        if (get_request_value(p_request, "profileConstraints", "profile_constraints", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field profile_constraints."; return false; }
            if (!profileConstraints_owner.from_dictionary(field_value, r_error)) return false;
            profileConstraints_has_value = true;
            value.profileConstraints = &profileConstraints_owner.value;
        }
        if (!get_request_value(p_request, "user", "user", &field_value)) {
            if (r_error) *r_error = "Expected a GDKUser object for field user.";
            return false;
        }
        if (!duplicate_xuser_handle_from_variant(field_value, &user_handle, r_error)) return false;
        value.user = user_handle;
        return true;
    }
};

struct OwnedPFFriendsClientRemoveFriendRequest {
    PFFriendsClientRemoveFriendRequest value = {};
    std::string friendPlayFabId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "friendPlayFabId", "friend_play_fab_id", &field_value)) {
            friendPlayFabId_string = String(field_value).utf8().get_data();
            value.friendPlayFabId = friendPlayFabId_string.empty() ? nullptr : friendPlayFabId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFFriendsClientSetFriendTagsRequest {
    PFFriendsClientSetFriendTagsRequest value = {};
    std::string friendPlayFabId_string;
    std::vector<std::string> tags_strings;
    std::vector<const char *> tags_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "friendPlayFabId", "friend_play_fab_id", &field_value)) {
            friendPlayFabId_string = String(field_value).utf8().get_data();
            value.friendPlayFabId = friendPlayFabId_string.empty() ? nullptr : friendPlayFabId_string.c_str();
        }
        if (get_request_value(p_request, "tags", "tags", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field tags."; return false; }
            Array source_array = field_value;
            tags_strings.clear(); tags_ptrs.clear();
            tags_strings.reserve(source_array.size()); tags_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                tags_strings.emplace_back(String(source_array[i]).utf8().get_data());
                tags_ptrs.push_back(tags_strings.back().c_str());
            }
            value.tags = tags_ptrs.empty() ? nullptr : tags_ptrs.data();
            value.tagsCount = static_cast<uint32_t>(tags_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFFriendsFriendInfo {
    PFFriendsFriendInfo value = {};
    OwnedPFUserFacebookInfo facebookInfo_owner;
    bool facebookInfo_has_value = false;
    std::string friendPlayFabId_string;
    OwnedPFUserGameCenterInfo gameCenterInfo_owner;
    bool gameCenterInfo_has_value = false;
    OwnedPFPlayerProfileModel profile_owner;
    bool profile_has_value = false;
    OwnedPFUserPsnInfo PSNInfo_owner;
    bool PSNInfo_has_value = false;
    OwnedPFUserSteamInfo steamInfo_owner;
    bool steamInfo_has_value = false;
    std::vector<std::string> tags_strings;
    std::vector<const char *> tags_ptrs;
    std::string titleDisplayName_string;
    std::string username_string;
    OwnedPFUserXboxInfo xboxInfo_owner;
    bool xboxInfo_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "facebookInfo", "facebook_info", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field facebook_info."; return false; }
            if (!facebookInfo_owner.from_dictionary(field_value, r_error)) return false;
            facebookInfo_has_value = true;
            value.facebookInfo = &facebookInfo_owner.value;
        }
        if (get_request_value(p_request, "friendPlayFabId", "friend_play_fab_id", &field_value)) {
            friendPlayFabId_string = String(field_value).utf8().get_data();
            value.friendPlayFabId = friendPlayFabId_string.empty() ? nullptr : friendPlayFabId_string.c_str();
        }
        if (get_request_value(p_request, "gameCenterInfo", "game_center_info", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field game_center_info."; return false; }
            if (!gameCenterInfo_owner.from_dictionary(field_value, r_error)) return false;
            gameCenterInfo_has_value = true;
            value.gameCenterInfo = &gameCenterInfo_owner.value;
        }
        if (get_request_value(p_request, "profile", "profile", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field profile."; return false; }
            if (!profile_owner.from_dictionary(field_value, r_error)) return false;
            profile_has_value = true;
            value.profile = &profile_owner.value;
        }
        if (get_request_value(p_request, "PSNInfo", "psn_info", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field psn_info."; return false; }
            if (!PSNInfo_owner.from_dictionary(field_value, r_error)) return false;
            PSNInfo_has_value = true;
            value.PSNInfo = &PSNInfo_owner.value;
        }
        if (get_request_value(p_request, "steamInfo", "steam_info", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field steam_info."; return false; }
            if (!steamInfo_owner.from_dictionary(field_value, r_error)) return false;
            steamInfo_has_value = true;
            value.steamInfo = &steamInfo_owner.value;
        }
        if (get_request_value(p_request, "tags", "tags", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field tags."; return false; }
            Array source_array = field_value;
            tags_strings.clear(); tags_ptrs.clear();
            tags_strings.reserve(source_array.size()); tags_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                tags_strings.emplace_back(String(source_array[i]).utf8().get_data());
                tags_ptrs.push_back(tags_strings.back().c_str());
            }
            value.tags = tags_ptrs.empty() ? nullptr : tags_ptrs.data();
            value.tagsCount = static_cast<uint32_t>(tags_ptrs.size());
        }
        if (get_request_value(p_request, "titleDisplayName", "title_display_name", &field_value)) {
            titleDisplayName_string = String(field_value).utf8().get_data();
            value.titleDisplayName = titleDisplayName_string.empty() ? nullptr : titleDisplayName_string.c_str();
        }
        if (get_request_value(p_request, "username", "username", &field_value)) {
            username_string = String(field_value).utf8().get_data();
            value.username = username_string.empty() ? nullptr : username_string.c_str();
        }
        if (get_request_value(p_request, "xboxInfo", "xbox_info", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field xbox_info."; return false; }
            if (!xboxInfo_owner.from_dictionary(field_value, r_error)) return false;
            xboxInfo_has_value = true;
            value.xboxInfo = &xboxInfo_owner.value;
        }
        return true;
    }
};

struct OwnedPFFriendsGetFriendsListResult {
    PFFriendsGetFriendsListResult value = {};
    std::vector<OwnedPFFriendsFriendInfo> friends_items;
    std::vector<const PFFriendsFriendInfo *> friends_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "friends", "friends", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field friends."; return false; }
            Array source_array = field_value;
            friends_items.resize(source_array.size());
            friends_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field friends."; return false; }
                if (!friends_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                friends_ptrs[static_cast<size_t>(i)] = &friends_items[static_cast<size_t>(i)].value;
            }
            value.friends = friends_ptrs.empty() ? nullptr : friends_ptrs.data();
            value.friendsCount = static_cast<uint32_t>(friends_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFGroupsAcceptGroupApplicationRequest {
    PFGroupsAcceptGroupApplicationRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    OwnedPFEntityKey group_owner;
    bool group_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "group", "group", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field group."; return false; }
            if (!group_owner.from_dictionary(field_value, r_error)) return false;
            group_has_value = true;
            value.group = &group_owner.value;
        }
        return true;
    }
};

struct OwnedPFGroupsAcceptGroupInvitationRequest {
    PFGroupsAcceptGroupInvitationRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    OwnedPFEntityKey group_owner;
    bool group_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "group", "group", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field group."; return false; }
            if (!group_owner.from_dictionary(field_value, r_error)) return false;
            group_has_value = true;
            value.group = &group_owner.value;
        }
        return true;
    }
};

struct OwnedPFGroupsAddMembersRequest {
    PFGroupsAddMembersRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey group_owner;
    bool group_has_value = false;
    std::vector<OwnedPFEntityKey> members_items;
    std::vector<const PFEntityKey *> members_ptrs;
    std::string roleId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "group", "group", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field group."; return false; }
            if (!group_owner.from_dictionary(field_value, r_error)) return false;
            group_has_value = true;
            value.group = &group_owner.value;
        }
        if (get_request_value(p_request, "members", "members", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field members."; return false; }
            Array source_array = field_value;
            members_items.resize(source_array.size());
            members_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field members."; return false; }
                if (!members_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                members_ptrs[static_cast<size_t>(i)] = &members_items[static_cast<size_t>(i)].value;
            }
            value.members = members_ptrs.empty() ? nullptr : members_ptrs.data();
            value.membersCount = static_cast<uint32_t>(members_ptrs.size());
        }
        if (get_request_value(p_request, "roleId", "role_id", &field_value)) {
            roleId_string = String(field_value).utf8().get_data();
            value.roleId = roleId_string.empty() ? nullptr : roleId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFGroupsApplyToGroupRequest {
    PFGroupsApplyToGroupRequest value = {};
    bool autoAcceptOutstandingInvite_storage = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    OwnedPFEntityKey group_owner;
    bool group_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "autoAcceptOutstandingInvite", "auto_accept_outstanding_invite", &field_value)) {
            autoAcceptOutstandingInvite_storage = static_cast<bool>(field_value);
            value.autoAcceptOutstandingInvite = &autoAcceptOutstandingInvite_storage;
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "group", "group", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field group."; return false; }
            if (!group_owner.from_dictionary(field_value, r_error)) return false;
            group_has_value = true;
            value.group = &group_owner.value;
        }
        return true;
    }
};

struct OwnedPFGroupsEntityWithLineage {
    PFGroupsEntityWithLineage value = {};
    OwnedPFEntityKey key_owner;
    bool key_has_value = false;
    std::vector<OwnedPFEntityKeyDictionaryEntry> lineage_items;
    std::vector<PFEntityKeyDictionaryEntry> lineage_values;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "key", "key", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field key."; return false; }
            if (!key_owner.from_dictionary(field_value, r_error)) return false;
            key_has_value = true;
            value.key = &key_owner.value;
        }
        if (get_request_value(p_request, "lineage", "lineage", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field lineage."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            lineage_items.resize(keys.size());
            lineage_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!lineage_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                lineage_values[static_cast<size_t>(i)] = lineage_items[static_cast<size_t>(i)].value;
            }
            value.lineage = lineage_values.empty() ? nullptr : lineage_values.data();
            value.lineageCount = static_cast<uint32_t>(lineage_values.size());
        }
        return true;
    }
};

struct OwnedPFGroupsApplyToGroupResponse {
    PFGroupsApplyToGroupResponse value = {};
    OwnedPFGroupsEntityWithLineage entity_owner;
    bool entity_has_value = false;
    OwnedPFEntityKey group_owner;
    bool group_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "expires", "expires", &field_value)) {
            value.expires = static_cast<time_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "group", "group", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field group."; return false; }
            if (!group_owner.from_dictionary(field_value, r_error)) return false;
            group_has_value = true;
            value.group = &group_owner.value;
        }
        return true;
    }
};

struct OwnedPFGroupsBlockEntityRequest {
    PFGroupsBlockEntityRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    OwnedPFEntityKey group_owner;
    bool group_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "group", "group", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field group."; return false; }
            if (!group_owner.from_dictionary(field_value, r_error)) return false;
            group_has_value = true;
            value.group = &group_owner.value;
        }
        return true;
    }
};

struct OwnedPFGroupsChangeMemberRoleRequest {
    PFGroupsChangeMemberRoleRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    std::string destinationRoleId_string;
    OwnedPFEntityKey group_owner;
    bool group_has_value = false;
    std::vector<OwnedPFEntityKey> members_items;
    std::vector<const PFEntityKey *> members_ptrs;
    std::string originRoleId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "destinationRoleId", "destination_role_id", &field_value)) {
            destinationRoleId_string = String(field_value).utf8().get_data();
            value.destinationRoleId = destinationRoleId_string.empty() ? nullptr : destinationRoleId_string.c_str();
        }
        if (get_request_value(p_request, "group", "group", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field group."; return false; }
            if (!group_owner.from_dictionary(field_value, r_error)) return false;
            group_has_value = true;
            value.group = &group_owner.value;
        }
        if (get_request_value(p_request, "members", "members", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field members."; return false; }
            Array source_array = field_value;
            members_items.resize(source_array.size());
            members_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field members."; return false; }
                if (!members_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                members_ptrs[static_cast<size_t>(i)] = &members_items[static_cast<size_t>(i)].value;
            }
            value.members = members_ptrs.empty() ? nullptr : members_ptrs.data();
            value.membersCount = static_cast<uint32_t>(members_ptrs.size());
        }
        if (get_request_value(p_request, "originRoleId", "origin_role_id", &field_value)) {
            originRoleId_string = String(field_value).utf8().get_data();
            value.originRoleId = originRoleId_string.empty() ? nullptr : originRoleId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFGroupsCreateGroupRequest {
    PFGroupsCreateGroupRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::string groupName_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "groupName", "group_name", &field_value)) {
            groupName_string = String(field_value).utf8().get_data();
            value.groupName = groupName_string.empty() ? nullptr : groupName_string.c_str();
        }
        return true;
    }
};

struct OwnedPFGroupsCreateGroupResponse {
    PFGroupsCreateGroupResponse value = {};
    std::string adminRoleId_string;
    OwnedPFEntityKey group_owner;
    bool group_has_value = false;
    std::string groupName_string;
    std::string memberRoleId_string;
    std::vector<OwnedPFStringDictionaryEntry> roles_items;
    std::vector<PFStringDictionaryEntry> roles_values;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "adminRoleId", "admin_role_id", &field_value)) {
            adminRoleId_string = String(field_value).utf8().get_data();
            value.adminRoleId = adminRoleId_string.empty() ? nullptr : adminRoleId_string.c_str();
        }
        if (get_request_value(p_request, "created", "created", &field_value)) {
            value.created = static_cast<time_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "group", "group", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field group."; return false; }
            if (!group_owner.from_dictionary(field_value, r_error)) return false;
            group_has_value = true;
            value.group = &group_owner.value;
        }
        if (get_request_value(p_request, "groupName", "group_name", &field_value)) {
            groupName_string = String(field_value).utf8().get_data();
            value.groupName = groupName_string.empty() ? nullptr : groupName_string.c_str();
        }
        if (get_request_value(p_request, "memberRoleId", "member_role_id", &field_value)) {
            memberRoleId_string = String(field_value).utf8().get_data();
            value.memberRoleId = memberRoleId_string.empty() ? nullptr : memberRoleId_string.c_str();
        }
        if (get_request_value(p_request, "profileVersion", "profile_version", &field_value)) {
            value.profileVersion = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "roles", "roles", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field roles."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            roles_items.resize(keys.size());
            roles_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!roles_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                roles_values[static_cast<size_t>(i)] = roles_items[static_cast<size_t>(i)].value;
            }
            value.roles = roles_values.empty() ? nullptr : roles_values.data();
            value.rolesCount = static_cast<uint32_t>(roles_values.size());
        }
        return true;
    }
};

struct OwnedPFGroupsCreateGroupRoleRequest {
    PFGroupsCreateGroupRoleRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey group_owner;
    bool group_has_value = false;
    std::string roleId_string;
    std::string roleName_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "group", "group", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field group."; return false; }
            if (!group_owner.from_dictionary(field_value, r_error)) return false;
            group_has_value = true;
            value.group = &group_owner.value;
        }
        if (get_request_value(p_request, "roleId", "role_id", &field_value)) {
            roleId_string = String(field_value).utf8().get_data();
            value.roleId = roleId_string.empty() ? nullptr : roleId_string.c_str();
        }
        if (get_request_value(p_request, "roleName", "role_name", &field_value)) {
            roleName_string = String(field_value).utf8().get_data();
            value.roleName = roleName_string.empty() ? nullptr : roleName_string.c_str();
        }
        return true;
    }
};

struct OwnedPFGroupsCreateGroupRoleResponse {
    PFGroupsCreateGroupRoleResponse value = {};
    std::string roleId_string;
    std::string roleName_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "profileVersion", "profile_version", &field_value)) {
            value.profileVersion = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "roleId", "role_id", &field_value)) {
            roleId_string = String(field_value).utf8().get_data();
            value.roleId = roleId_string.empty() ? nullptr : roleId_string.c_str();
        }
        if (get_request_value(p_request, "roleName", "role_name", &field_value)) {
            roleName_string = String(field_value).utf8().get_data();
            value.roleName = roleName_string.empty() ? nullptr : roleName_string.c_str();
        }
        return true;
    }
};

struct OwnedPFGroupsDeleteGroupRequest {
    PFGroupsDeleteGroupRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey group_owner;
    bool group_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "group", "group", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field group."; return false; }
            if (!group_owner.from_dictionary(field_value, r_error)) return false;
            group_has_value = true;
            value.group = &group_owner.value;
        }
        return true;
    }
};

struct OwnedPFGroupsDeleteRoleRequest {
    PFGroupsDeleteRoleRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey group_owner;
    bool group_has_value = false;
    std::string roleId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "group", "group", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field group."; return false; }
            if (!group_owner.from_dictionary(field_value, r_error)) return false;
            group_has_value = true;
            value.group = &group_owner.value;
        }
        if (get_request_value(p_request, "roleId", "role_id", &field_value)) {
            roleId_string = String(field_value).utf8().get_data();
            value.roleId = roleId_string.empty() ? nullptr : roleId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFGroupsEntityMemberRole {
    PFGroupsEntityMemberRole value = {};
    std::vector<OwnedPFGroupsEntityWithLineage> members_items;
    std::vector<const PFGroupsEntityWithLineage *> members_ptrs;
    std::string roleId_string;
    std::string roleName_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "members", "members", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field members."; return false; }
            Array source_array = field_value;
            members_items.resize(source_array.size());
            members_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field members."; return false; }
                if (!members_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                members_ptrs[static_cast<size_t>(i)] = &members_items[static_cast<size_t>(i)].value;
            }
            value.members = members_ptrs.empty() ? nullptr : members_ptrs.data();
            value.membersCount = static_cast<uint32_t>(members_ptrs.size());
        }
        if (get_request_value(p_request, "roleId", "role_id", &field_value)) {
            roleId_string = String(field_value).utf8().get_data();
            value.roleId = roleId_string.empty() ? nullptr : roleId_string.c_str();
        }
        if (get_request_value(p_request, "roleName", "role_name", &field_value)) {
            roleName_string = String(field_value).utf8().get_data();
            value.roleName = roleName_string.empty() ? nullptr : roleName_string.c_str();
        }
        return true;
    }
};

struct OwnedPFGroupsGetGroupRequest {
    PFGroupsGetGroupRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey group_owner;
    bool group_has_value = false;
    std::string groupName_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "group", "group", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field group."; return false; }
            if (!group_owner.from_dictionary(field_value, r_error)) return false;
            group_has_value = true;
            value.group = &group_owner.value;
        }
        if (get_request_value(p_request, "groupName", "group_name", &field_value)) {
            groupName_string = String(field_value).utf8().get_data();
            value.groupName = groupName_string.empty() ? nullptr : groupName_string.c_str();
        }
        return true;
    }
};

struct OwnedPFGroupsGetGroupResponse {
    PFGroupsGetGroupResponse value = {};
    std::string adminRoleId_string;
    OwnedPFEntityKey group_owner;
    bool group_has_value = false;
    std::string groupName_string;
    std::string memberRoleId_string;
    std::vector<OwnedPFStringDictionaryEntry> roles_items;
    std::vector<PFStringDictionaryEntry> roles_values;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "adminRoleId", "admin_role_id", &field_value)) {
            adminRoleId_string = String(field_value).utf8().get_data();
            value.adminRoleId = adminRoleId_string.empty() ? nullptr : adminRoleId_string.c_str();
        }
        if (get_request_value(p_request, "created", "created", &field_value)) {
            value.created = static_cast<time_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "group", "group", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field group."; return false; }
            if (!group_owner.from_dictionary(field_value, r_error)) return false;
            group_has_value = true;
            value.group = &group_owner.value;
        }
        if (get_request_value(p_request, "groupName", "group_name", &field_value)) {
            groupName_string = String(field_value).utf8().get_data();
            value.groupName = groupName_string.empty() ? nullptr : groupName_string.c_str();
        }
        if (get_request_value(p_request, "memberRoleId", "member_role_id", &field_value)) {
            memberRoleId_string = String(field_value).utf8().get_data();
            value.memberRoleId = memberRoleId_string.empty() ? nullptr : memberRoleId_string.c_str();
        }
        if (get_request_value(p_request, "profileVersion", "profile_version", &field_value)) {
            value.profileVersion = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "roles", "roles", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field roles."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            roles_items.resize(keys.size());
            roles_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!roles_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                roles_values[static_cast<size_t>(i)] = roles_items[static_cast<size_t>(i)].value;
            }
            value.roles = roles_values.empty() ? nullptr : roles_values.data();
            value.rolesCount = static_cast<uint32_t>(roles_values.size());
        }
        return true;
    }
};

struct OwnedPFGroupsGroupApplication {
    PFGroupsGroupApplication value = {};
    OwnedPFGroupsEntityWithLineage entity_owner;
    bool entity_has_value = false;
    OwnedPFEntityKey group_owner;
    bool group_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "expires", "expires", &field_value)) {
            value.expires = static_cast<time_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "group", "group", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field group."; return false; }
            if (!group_owner.from_dictionary(field_value, r_error)) return false;
            group_has_value = true;
            value.group = &group_owner.value;
        }
        return true;
    }
};

struct OwnedPFGroupsGroupBlock {
    PFGroupsGroupBlock value = {};
    OwnedPFGroupsEntityWithLineage entity_owner;
    bool entity_has_value = false;
    OwnedPFEntityKey group_owner;
    bool group_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "group", "group", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field group."; return false; }
            if (!group_owner.from_dictionary(field_value, r_error)) return false;
            group_has_value = true;
            value.group = &group_owner.value;
        }
        return true;
    }
};

struct OwnedPFGroupsGroupInvitation {
    PFGroupsGroupInvitation value = {};
    OwnedPFEntityKey group_owner;
    bool group_has_value = false;
    OwnedPFGroupsEntityWithLineage invitedByEntity_owner;
    bool invitedByEntity_has_value = false;
    OwnedPFGroupsEntityWithLineage invitedEntity_owner;
    bool invitedEntity_has_value = false;
    std::string roleId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "expires", "expires", &field_value)) {
            value.expires = static_cast<time_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "group", "group", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field group."; return false; }
            if (!group_owner.from_dictionary(field_value, r_error)) return false;
            group_has_value = true;
            value.group = &group_owner.value;
        }
        if (get_request_value(p_request, "invitedByEntity", "invited_by_entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field invited_by_entity."; return false; }
            if (!invitedByEntity_owner.from_dictionary(field_value, r_error)) return false;
            invitedByEntity_has_value = true;
            value.invitedByEntity = &invitedByEntity_owner.value;
        }
        if (get_request_value(p_request, "invitedEntity", "invited_entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field invited_entity."; return false; }
            if (!invitedEntity_owner.from_dictionary(field_value, r_error)) return false;
            invitedEntity_has_value = true;
            value.invitedEntity = &invitedEntity_owner.value;
        }
        if (get_request_value(p_request, "roleId", "role_id", &field_value)) {
            roleId_string = String(field_value).utf8().get_data();
            value.roleId = roleId_string.empty() ? nullptr : roleId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFGroupsGroupRole {
    PFGroupsGroupRole value = {};
    std::string roleId_string;
    std::string roleName_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "roleId", "role_id", &field_value)) {
            roleId_string = String(field_value).utf8().get_data();
            value.roleId = roleId_string.empty() ? nullptr : roleId_string.c_str();
        }
        if (get_request_value(p_request, "roleName", "role_name", &field_value)) {
            roleName_string = String(field_value).utf8().get_data();
            value.roleName = roleName_string.empty() ? nullptr : roleName_string.c_str();
        }
        return true;
    }
};

struct OwnedPFGroupsGroupWithRoles {
    PFGroupsGroupWithRoles value = {};
    OwnedPFEntityKey group_owner;
    bool group_has_value = false;
    std::string groupName_string;
    std::vector<OwnedPFGroupsGroupRole> roles_items;
    std::vector<const PFGroupsGroupRole *> roles_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "group", "group", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field group."; return false; }
            if (!group_owner.from_dictionary(field_value, r_error)) return false;
            group_has_value = true;
            value.group = &group_owner.value;
        }
        if (get_request_value(p_request, "groupName", "group_name", &field_value)) {
            groupName_string = String(field_value).utf8().get_data();
            value.groupName = groupName_string.empty() ? nullptr : groupName_string.c_str();
        }
        if (get_request_value(p_request, "profileVersion", "profile_version", &field_value)) {
            value.profileVersion = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "roles", "roles", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field roles."; return false; }
            Array source_array = field_value;
            roles_items.resize(source_array.size());
            roles_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field roles."; return false; }
                if (!roles_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                roles_ptrs[static_cast<size_t>(i)] = &roles_items[static_cast<size_t>(i)].value;
            }
            value.roles = roles_ptrs.empty() ? nullptr : roles_ptrs.data();
            value.rolesCount = static_cast<uint32_t>(roles_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFGroupsInviteToGroupRequest {
    PFGroupsInviteToGroupRequest value = {};
    bool autoAcceptOutstandingApplication_storage = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    OwnedPFEntityKey group_owner;
    bool group_has_value = false;
    std::string roleId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "autoAcceptOutstandingApplication", "auto_accept_outstanding_application", &field_value)) {
            autoAcceptOutstandingApplication_storage = static_cast<bool>(field_value);
            value.autoAcceptOutstandingApplication = &autoAcceptOutstandingApplication_storage;
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "group", "group", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field group."; return false; }
            if (!group_owner.from_dictionary(field_value, r_error)) return false;
            group_has_value = true;
            value.group = &group_owner.value;
        }
        if (get_request_value(p_request, "roleId", "role_id", &field_value)) {
            roleId_string = String(field_value).utf8().get_data();
            value.roleId = roleId_string.empty() ? nullptr : roleId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFGroupsInviteToGroupResponse {
    PFGroupsInviteToGroupResponse value = {};
    OwnedPFEntityKey group_owner;
    bool group_has_value = false;
    OwnedPFGroupsEntityWithLineage invitedByEntity_owner;
    bool invitedByEntity_has_value = false;
    OwnedPFGroupsEntityWithLineage invitedEntity_owner;
    bool invitedEntity_has_value = false;
    std::string roleId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "expires", "expires", &field_value)) {
            value.expires = static_cast<time_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "group", "group", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field group."; return false; }
            if (!group_owner.from_dictionary(field_value, r_error)) return false;
            group_has_value = true;
            value.group = &group_owner.value;
        }
        if (get_request_value(p_request, "invitedByEntity", "invited_by_entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field invited_by_entity."; return false; }
            if (!invitedByEntity_owner.from_dictionary(field_value, r_error)) return false;
            invitedByEntity_has_value = true;
            value.invitedByEntity = &invitedByEntity_owner.value;
        }
        if (get_request_value(p_request, "invitedEntity", "invited_entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field invited_entity."; return false; }
            if (!invitedEntity_owner.from_dictionary(field_value, r_error)) return false;
            invitedEntity_has_value = true;
            value.invitedEntity = &invitedEntity_owner.value;
        }
        if (get_request_value(p_request, "roleId", "role_id", &field_value)) {
            roleId_string = String(field_value).utf8().get_data();
            value.roleId = roleId_string.empty() ? nullptr : roleId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFGroupsIsMemberRequest {
    PFGroupsIsMemberRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    OwnedPFEntityKey group_owner;
    bool group_has_value = false;
    std::string roleId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "group", "group", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field group."; return false; }
            if (!group_owner.from_dictionary(field_value, r_error)) return false;
            group_has_value = true;
            value.group = &group_owner.value;
        }
        if (get_request_value(p_request, "roleId", "role_id", &field_value)) {
            roleId_string = String(field_value).utf8().get_data();
            value.roleId = roleId_string.empty() ? nullptr : roleId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFGroupsIsMemberResponse {
    PFGroupsIsMemberResponse value = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "isMember", "is_member", &field_value)) {
            value.isMember = static_cast<bool>(field_value);
        }
        return true;
    }
};

struct OwnedPFGroupsListGroupApplicationsRequest {
    PFGroupsListGroupApplicationsRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey group_owner;
    bool group_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "group", "group", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field group."; return false; }
            if (!group_owner.from_dictionary(field_value, r_error)) return false;
            group_has_value = true;
            value.group = &group_owner.value;
        }
        return true;
    }
};

struct OwnedPFGroupsListGroupApplicationsResponse {
    PFGroupsListGroupApplicationsResponse value = {};
    std::vector<OwnedPFGroupsGroupApplication> applications_items;
    std::vector<const PFGroupsGroupApplication *> applications_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "applications", "applications", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field applications."; return false; }
            Array source_array = field_value;
            applications_items.resize(source_array.size());
            applications_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field applications."; return false; }
                if (!applications_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                applications_ptrs[static_cast<size_t>(i)] = &applications_items[static_cast<size_t>(i)].value;
            }
            value.applications = applications_ptrs.empty() ? nullptr : applications_ptrs.data();
            value.applicationsCount = static_cast<uint32_t>(applications_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFGroupsListGroupBlocksRequest {
    PFGroupsListGroupBlocksRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey group_owner;
    bool group_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "group", "group", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field group."; return false; }
            if (!group_owner.from_dictionary(field_value, r_error)) return false;
            group_has_value = true;
            value.group = &group_owner.value;
        }
        return true;
    }
};

struct OwnedPFGroupsListGroupBlocksResponse {
    PFGroupsListGroupBlocksResponse value = {};
    std::vector<OwnedPFGroupsGroupBlock> blockedEntities_items;
    std::vector<const PFGroupsGroupBlock *> blockedEntities_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "blockedEntities", "blocked_entities", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field blocked_entities."; return false; }
            Array source_array = field_value;
            blockedEntities_items.resize(source_array.size());
            blockedEntities_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field blocked_entities."; return false; }
                if (!blockedEntities_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                blockedEntities_ptrs[static_cast<size_t>(i)] = &blockedEntities_items[static_cast<size_t>(i)].value;
            }
            value.blockedEntities = blockedEntities_ptrs.empty() ? nullptr : blockedEntities_ptrs.data();
            value.blockedEntitiesCount = static_cast<uint32_t>(blockedEntities_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFGroupsListGroupInvitationsRequest {
    PFGroupsListGroupInvitationsRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey group_owner;
    bool group_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "group", "group", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field group."; return false; }
            if (!group_owner.from_dictionary(field_value, r_error)) return false;
            group_has_value = true;
            value.group = &group_owner.value;
        }
        return true;
    }
};

struct OwnedPFGroupsListGroupInvitationsResponse {
    PFGroupsListGroupInvitationsResponse value = {};
    std::vector<OwnedPFGroupsGroupInvitation> invitations_items;
    std::vector<const PFGroupsGroupInvitation *> invitations_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "invitations", "invitations", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field invitations."; return false; }
            Array source_array = field_value;
            invitations_items.resize(source_array.size());
            invitations_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field invitations."; return false; }
                if (!invitations_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                invitations_ptrs[static_cast<size_t>(i)] = &invitations_items[static_cast<size_t>(i)].value;
            }
            value.invitations = invitations_ptrs.empty() ? nullptr : invitations_ptrs.data();
            value.invitationsCount = static_cast<uint32_t>(invitations_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFGroupsListGroupMembersRequest {
    PFGroupsListGroupMembersRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey group_owner;
    bool group_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "group", "group", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field group."; return false; }
            if (!group_owner.from_dictionary(field_value, r_error)) return false;
            group_has_value = true;
            value.group = &group_owner.value;
        }
        return true;
    }
};

struct OwnedPFGroupsListGroupMembersResponse {
    PFGroupsListGroupMembersResponse value = {};
    std::vector<OwnedPFGroupsEntityMemberRole> members_items;
    std::vector<const PFGroupsEntityMemberRole *> members_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "members", "members", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field members."; return false; }
            Array source_array = field_value;
            members_items.resize(source_array.size());
            members_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field members."; return false; }
                if (!members_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                members_ptrs[static_cast<size_t>(i)] = &members_items[static_cast<size_t>(i)].value;
            }
            value.members = members_ptrs.empty() ? nullptr : members_ptrs.data();
            value.membersCount = static_cast<uint32_t>(members_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFGroupsListMembershipOpportunitiesRequest {
    PFGroupsListMembershipOpportunitiesRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        return true;
    }
};

struct OwnedPFGroupsListMembershipOpportunitiesResponse {
    PFGroupsListMembershipOpportunitiesResponse value = {};
    std::vector<OwnedPFGroupsGroupApplication> applications_items;
    std::vector<const PFGroupsGroupApplication *> applications_ptrs;
    std::vector<OwnedPFGroupsGroupInvitation> invitations_items;
    std::vector<const PFGroupsGroupInvitation *> invitations_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "applications", "applications", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field applications."; return false; }
            Array source_array = field_value;
            applications_items.resize(source_array.size());
            applications_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field applications."; return false; }
                if (!applications_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                applications_ptrs[static_cast<size_t>(i)] = &applications_items[static_cast<size_t>(i)].value;
            }
            value.applications = applications_ptrs.empty() ? nullptr : applications_ptrs.data();
            value.applicationsCount = static_cast<uint32_t>(applications_ptrs.size());
        }
        if (get_request_value(p_request, "invitations", "invitations", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field invitations."; return false; }
            Array source_array = field_value;
            invitations_items.resize(source_array.size());
            invitations_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field invitations."; return false; }
                if (!invitations_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                invitations_ptrs[static_cast<size_t>(i)] = &invitations_items[static_cast<size_t>(i)].value;
            }
            value.invitations = invitations_ptrs.empty() ? nullptr : invitations_ptrs.data();
            value.invitationsCount = static_cast<uint32_t>(invitations_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFGroupsListMembershipRequest {
    PFGroupsListMembershipRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        return true;
    }
};

struct OwnedPFGroupsListMembershipResponse {
    PFGroupsListMembershipResponse value = {};
    std::vector<OwnedPFGroupsGroupWithRoles> groups_items;
    std::vector<const PFGroupsGroupWithRoles *> groups_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "groups", "groups", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field groups."; return false; }
            Array source_array = field_value;
            groups_items.resize(source_array.size());
            groups_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field groups."; return false; }
                if (!groups_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                groups_ptrs[static_cast<size_t>(i)] = &groups_items[static_cast<size_t>(i)].value;
            }
            value.groups = groups_ptrs.empty() ? nullptr : groups_ptrs.data();
            value.groupsCount = static_cast<uint32_t>(groups_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFGroupsRemoveGroupApplicationRequest {
    PFGroupsRemoveGroupApplicationRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    OwnedPFEntityKey group_owner;
    bool group_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "group", "group", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field group."; return false; }
            if (!group_owner.from_dictionary(field_value, r_error)) return false;
            group_has_value = true;
            value.group = &group_owner.value;
        }
        return true;
    }
};

struct OwnedPFGroupsRemoveGroupInvitationRequest {
    PFGroupsRemoveGroupInvitationRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    OwnedPFEntityKey group_owner;
    bool group_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "group", "group", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field group."; return false; }
            if (!group_owner.from_dictionary(field_value, r_error)) return false;
            group_has_value = true;
            value.group = &group_owner.value;
        }
        return true;
    }
};

struct OwnedPFGroupsRemoveMembersRequest {
    PFGroupsRemoveMembersRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey group_owner;
    bool group_has_value = false;
    std::vector<OwnedPFEntityKey> members_items;
    std::vector<const PFEntityKey *> members_ptrs;
    std::string roleId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "group", "group", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field group."; return false; }
            if (!group_owner.from_dictionary(field_value, r_error)) return false;
            group_has_value = true;
            value.group = &group_owner.value;
        }
        if (get_request_value(p_request, "members", "members", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field members."; return false; }
            Array source_array = field_value;
            members_items.resize(source_array.size());
            members_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field members."; return false; }
                if (!members_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                members_ptrs[static_cast<size_t>(i)] = &members_items[static_cast<size_t>(i)].value;
            }
            value.members = members_ptrs.empty() ? nullptr : members_ptrs.data();
            value.membersCount = static_cast<uint32_t>(members_ptrs.size());
        }
        if (get_request_value(p_request, "roleId", "role_id", &field_value)) {
            roleId_string = String(field_value).utf8().get_data();
            value.roleId = roleId_string.empty() ? nullptr : roleId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFGroupsUnblockEntityRequest {
    PFGroupsUnblockEntityRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    OwnedPFEntityKey group_owner;
    bool group_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "group", "group", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field group."; return false; }
            if (!group_owner.from_dictionary(field_value, r_error)) return false;
            group_has_value = true;
            value.group = &group_owner.value;
        }
        return true;
    }
};

struct OwnedPFGroupsUpdateGroupRequest {
    PFGroupsUpdateGroupRequest value = {};
    std::string adminRoleId_string;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    int32_t expectedProfileVersion_storage = {};
    OwnedPFEntityKey group_owner;
    bool group_has_value = false;
    std::string groupName_string;
    std::string memberRoleId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "adminRoleId", "admin_role_id", &field_value)) {
            adminRoleId_string = String(field_value).utf8().get_data();
            value.adminRoleId = adminRoleId_string.empty() ? nullptr : adminRoleId_string.c_str();
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "expectedProfileVersion", "expected_profile_version", &field_value)) {
            expectedProfileVersion_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.expectedProfileVersion = &expectedProfileVersion_storage;
        }
        if (get_request_value(p_request, "group", "group", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field group."; return false; }
            if (!group_owner.from_dictionary(field_value, r_error)) return false;
            group_has_value = true;
            value.group = &group_owner.value;
        }
        if (get_request_value(p_request, "groupName", "group_name", &field_value)) {
            groupName_string = String(field_value).utf8().get_data();
            value.groupName = groupName_string.empty() ? nullptr : groupName_string.c_str();
        }
        if (get_request_value(p_request, "memberRoleId", "member_role_id", &field_value)) {
            memberRoleId_string = String(field_value).utf8().get_data();
            value.memberRoleId = memberRoleId_string.empty() ? nullptr : memberRoleId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFGroupsUpdateGroupResponse {
    PFGroupsUpdateGroupResponse value = {};
    std::string operationReason_string;
    PFOperationTypes setResult_storage = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "operationReason", "operation_reason", &field_value)) {
            operationReason_string = String(field_value).utf8().get_data();
            value.operationReason = operationReason_string.empty() ? nullptr : operationReason_string.c_str();
        }
        if (get_request_value(p_request, "profileVersion", "profile_version", &field_value)) {
            value.profileVersion = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "setResult", "set_result", &field_value)) {
            setResult_storage = static_cast<PFOperationTypes>(static_cast<int64_t>(field_value));
            value.setResult = &setResult_storage;
        }
        return true;
    }
};

struct OwnedPFGroupsUpdateGroupRoleRequest {
    PFGroupsUpdateGroupRoleRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    int32_t expectedProfileVersion_storage = {};
    OwnedPFEntityKey group_owner;
    bool group_has_value = false;
    std::string roleId_string;
    std::string roleName_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "expectedProfileVersion", "expected_profile_version", &field_value)) {
            expectedProfileVersion_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.expectedProfileVersion = &expectedProfileVersion_storage;
        }
        if (get_request_value(p_request, "group", "group", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field group."; return false; }
            if (!group_owner.from_dictionary(field_value, r_error)) return false;
            group_has_value = true;
            value.group = &group_owner.value;
        }
        if (get_request_value(p_request, "roleId", "role_id", &field_value)) {
            roleId_string = String(field_value).utf8().get_data();
            value.roleId = roleId_string.empty() ? nullptr : roleId_string.c_str();
        }
        if (get_request_value(p_request, "roleName", "role_name", &field_value)) {
            roleName_string = String(field_value).utf8().get_data();
            value.roleName = roleName_string.empty() ? nullptr : roleName_string.c_str();
        }
        return true;
    }
};

struct OwnedPFGroupsUpdateGroupRoleResponse {
    PFGroupsUpdateGroupRoleResponse value = {};
    std::string operationReason_string;
    PFOperationTypes setResult_storage = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "operationReason", "operation_reason", &field_value)) {
            operationReason_string = String(field_value).utf8().get_data();
            value.operationReason = operationReason_string.empty() ? nullptr : operationReason_string.c_str();
        }
        if (get_request_value(p_request, "profileVersion", "profile_version", &field_value)) {
            value.profileVersion = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "setResult", "set_result", &field_value)) {
            setResult_storage = static_cast<PFOperationTypes>(static_cast<int64_t>(field_value));
            value.setResult = &setResult_storage;
        }
        return true;
    }
};

struct OwnedPFInventoryAlternateId {
    PFInventoryAlternateId value = {};
    std::string type_string;
    std::string value_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "type", "type", &field_value)) {
            type_string = String(field_value).utf8().get_data();
            value.type = type_string.empty() ? nullptr : type_string.c_str();
        }
        if (get_request_value(p_request, "value", "value", &field_value)) {
            value_string = String(field_value).utf8().get_data();
            value.value = value_string.empty() ? nullptr : value_string.c_str();
        }
        return true;
    }
};

struct OwnedPFInventoryInventoryItemReference {
    PFInventoryInventoryItemReference value = {};
    OwnedPFInventoryAlternateId alternateId_owner;
    bool alternateId_has_value = false;
    std::string id_string;
    std::string stackId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "alternateId", "alternate_id", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field alternate_id."; return false; }
            if (!alternateId_owner.from_dictionary(field_value, r_error)) return false;
            alternateId_has_value = true;
            value.alternateId = &alternateId_owner.value;
        }
        if (get_request_value(p_request, "id", "id", &field_value)) {
            id_string = String(field_value).utf8().get_data();
            value.id = id_string.empty() ? nullptr : id_string.c_str();
        }
        if (get_request_value(p_request, "stackId", "stack_id", &field_value)) {
            stackId_string = String(field_value).utf8().get_data();
            value.stackId = stackId_string.empty() ? nullptr : stackId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFInventoryInitialValues {
    PFInventoryInitialValues value = {};
    std::string displayProperties_json;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "displayProperties", "display_properties", &field_value)) {
            if (field_value.get_type() == Variant::STRING) {
                displayProperties_json = String(field_value).utf8().get_data();
            } else {
                displayProperties_json = variant_to_json_string(field_value).utf8().get_data();
            }
            value.displayProperties.stringValue = displayProperties_json.empty() ? nullptr : displayProperties_json.c_str();
        }
        return true;
    }
};

struct OwnedPFInventoryAddInventoryItemsOperation {
    PFInventoryAddInventoryItemsOperation value = {};
    int32_t amount_storage = {};
    double durationInSeconds_storage = {};
    OwnedPFInventoryInventoryItemReference item_owner;
    bool item_has_value = false;
    OwnedPFInventoryInitialValues newStackValues_owner;
    bool newStackValues_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "amount", "amount", &field_value)) {
            amount_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.amount = &amount_storage;
        }
        if (get_request_value(p_request, "durationInSeconds", "duration_in_seconds", &field_value)) {
            durationInSeconds_storage = static_cast<double>(static_cast<double>(field_value));
            value.durationInSeconds = &durationInSeconds_storage;
        }
        if (get_request_value(p_request, "item", "item", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field item."; return false; }
            if (!item_owner.from_dictionary(field_value, r_error)) return false;
            item_has_value = true;
            value.item = &item_owner.value;
        }
        if (get_request_value(p_request, "newStackValues", "new_stack_values", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field new_stack_values."; return false; }
            if (!newStackValues_owner.from_dictionary(field_value, r_error)) return false;
            newStackValues_has_value = true;
            value.newStackValues = &newStackValues_owner.value;
        }
        return true;
    }
};

struct OwnedPFInventoryAddInventoryItemsRequest {
    PFInventoryAddInventoryItemsRequest value = {};
    int32_t amount_storage = {};
    std::string collectionId_string;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    double durationInSeconds_storage = {};
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::string eTag_string;
    std::string idempotencyId_string;
    OwnedPFInventoryInventoryItemReference item_owner;
    bool item_has_value = false;
    OwnedPFInventoryInitialValues newStackValues_owner;
    bool newStackValues_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "amount", "amount", &field_value)) {
            amount_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.amount = &amount_storage;
        }
        if (get_request_value(p_request, "collectionId", "collection_id", &field_value)) {
            collectionId_string = String(field_value).utf8().get_data();
            value.collectionId = collectionId_string.empty() ? nullptr : collectionId_string.c_str();
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "durationInSeconds", "duration_in_seconds", &field_value)) {
            durationInSeconds_storage = static_cast<double>(static_cast<double>(field_value));
            value.durationInSeconds = &durationInSeconds_storage;
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "eTag", "e_tag", &field_value)) {
            eTag_string = String(field_value).utf8().get_data();
            value.eTag = eTag_string.empty() ? nullptr : eTag_string.c_str();
        }
        if (get_request_value(p_request, "idempotencyId", "idempotency_id", &field_value)) {
            idempotencyId_string = String(field_value).utf8().get_data();
            value.idempotencyId = idempotencyId_string.empty() ? nullptr : idempotencyId_string.c_str();
        }
        if (get_request_value(p_request, "item", "item", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field item."; return false; }
            if (!item_owner.from_dictionary(field_value, r_error)) return false;
            item_has_value = true;
            value.item = &item_owner.value;
        }
        if (get_request_value(p_request, "newStackValues", "new_stack_values", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field new_stack_values."; return false; }
            if (!newStackValues_owner.from_dictionary(field_value, r_error)) return false;
            newStackValues_has_value = true;
            value.newStackValues = &newStackValues_owner.value;
        }
        return true;
    }
};

struct OwnedPFInventoryAddInventoryItemsResponse {
    PFInventoryAddInventoryItemsResponse value = {};
    std::string eTag_string;
    std::string idempotencyId_string;
    std::vector<std::string> transactionIds_strings;
    std::vector<const char *> transactionIds_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "eTag", "e_tag", &field_value)) {
            eTag_string = String(field_value).utf8().get_data();
            value.eTag = eTag_string.empty() ? nullptr : eTag_string.c_str();
        }
        if (get_request_value(p_request, "idempotencyId", "idempotency_id", &field_value)) {
            idempotencyId_string = String(field_value).utf8().get_data();
            value.idempotencyId = idempotencyId_string.empty() ? nullptr : idempotencyId_string.c_str();
        }
        if (get_request_value(p_request, "transactionIds", "transaction_ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field transaction_ids."; return false; }
            Array source_array = field_value;
            transactionIds_strings.clear(); transactionIds_ptrs.clear();
            transactionIds_strings.reserve(source_array.size()); transactionIds_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                transactionIds_strings.emplace_back(String(source_array[i]).utf8().get_data());
                transactionIds_ptrs.push_back(transactionIds_strings.back().c_str());
            }
            value.transactionIds = transactionIds_ptrs.empty() ? nullptr : transactionIds_ptrs.data();
            value.transactionIdsCount = static_cast<uint32_t>(transactionIds_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFInventoryDeleteInventoryCollectionRequest {
    PFInventoryDeleteInventoryCollectionRequest value = {};
    std::string collectionId_string;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::string eTag_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "collectionId", "collection_id", &field_value)) {
            collectionId_string = String(field_value).utf8().get_data();
            value.collectionId = collectionId_string.empty() ? nullptr : collectionId_string.c_str();
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "eTag", "e_tag", &field_value)) {
            eTag_string = String(field_value).utf8().get_data();
            value.eTag = eTag_string.empty() ? nullptr : eTag_string.c_str();
        }
        return true;
    }
};

struct OwnedPFInventoryDeleteInventoryItemsOperation {
    PFInventoryDeleteInventoryItemsOperation value = {};
    OwnedPFInventoryInventoryItemReference item_owner;
    bool item_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "item", "item", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field item."; return false; }
            if (!item_owner.from_dictionary(field_value, r_error)) return false;
            item_has_value = true;
            value.item = &item_owner.value;
        }
        return true;
    }
};

struct OwnedPFInventoryDeleteInventoryItemsRequest {
    PFInventoryDeleteInventoryItemsRequest value = {};
    std::string collectionId_string;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::string eTag_string;
    std::string idempotencyId_string;
    OwnedPFInventoryInventoryItemReference item_owner;
    bool item_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "collectionId", "collection_id", &field_value)) {
            collectionId_string = String(field_value).utf8().get_data();
            value.collectionId = collectionId_string.empty() ? nullptr : collectionId_string.c_str();
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "eTag", "e_tag", &field_value)) {
            eTag_string = String(field_value).utf8().get_data();
            value.eTag = eTag_string.empty() ? nullptr : eTag_string.c_str();
        }
        if (get_request_value(p_request, "idempotencyId", "idempotency_id", &field_value)) {
            idempotencyId_string = String(field_value).utf8().get_data();
            value.idempotencyId = idempotencyId_string.empty() ? nullptr : idempotencyId_string.c_str();
        }
        if (get_request_value(p_request, "item", "item", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field item."; return false; }
            if (!item_owner.from_dictionary(field_value, r_error)) return false;
            item_has_value = true;
            value.item = &item_owner.value;
        }
        return true;
    }
};

struct OwnedPFInventoryDeleteInventoryItemsResponse {
    PFInventoryDeleteInventoryItemsResponse value = {};
    std::string eTag_string;
    std::string idempotencyId_string;
    std::vector<std::string> transactionIds_strings;
    std::vector<const char *> transactionIds_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "eTag", "e_tag", &field_value)) {
            eTag_string = String(field_value).utf8().get_data();
            value.eTag = eTag_string.empty() ? nullptr : eTag_string.c_str();
        }
        if (get_request_value(p_request, "idempotencyId", "idempotency_id", &field_value)) {
            idempotencyId_string = String(field_value).utf8().get_data();
            value.idempotencyId = idempotencyId_string.empty() ? nullptr : idempotencyId_string.c_str();
        }
        if (get_request_value(p_request, "transactionIds", "transaction_ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field transaction_ids."; return false; }
            Array source_array = field_value;
            transactionIds_strings.clear(); transactionIds_ptrs.clear();
            transactionIds_strings.reserve(source_array.size()); transactionIds_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                transactionIds_strings.emplace_back(String(source_array[i]).utf8().get_data());
                transactionIds_ptrs.push_back(transactionIds_strings.back().c_str());
            }
            value.transactionIds = transactionIds_ptrs.empty() ? nullptr : transactionIds_ptrs.data();
            value.transactionIdsCount = static_cast<uint32_t>(transactionIds_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFInventoryPurchasePriceAmount {
    PFInventoryPurchasePriceAmount value = {};
    std::string itemId_string;
    std::string stackId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "amount", "amount", &field_value)) {
            value.amount = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "itemId", "item_id", &field_value)) {
            itemId_string = String(field_value).utf8().get_data();
            value.itemId = itemId_string.empty() ? nullptr : itemId_string.c_str();
        }
        if (get_request_value(p_request, "stackId", "stack_id", &field_value)) {
            stackId_string = String(field_value).utf8().get_data();
            value.stackId = stackId_string.empty() ? nullptr : stackId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFInventoryPurchaseInventoryItemsOperation {
    PFInventoryPurchaseInventoryItemsOperation value = {};
    int32_t amount_storage = {};
    double durationInSeconds_storage = {};
    OwnedPFInventoryInventoryItemReference item_owner;
    bool item_has_value = false;
    OwnedPFInventoryInitialValues newStackValues_owner;
    bool newStackValues_has_value = false;
    std::vector<OwnedPFInventoryPurchasePriceAmount> priceAmounts_items;
    std::vector<const PFInventoryPurchasePriceAmount *> priceAmounts_ptrs;
    std::string storeId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "amount", "amount", &field_value)) {
            amount_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.amount = &amount_storage;
        }
        if (get_request_value(p_request, "deleteEmptyStacks", "delete_empty_stacks", &field_value)) {
            value.deleteEmptyStacks = static_cast<bool>(field_value);
        }
        if (get_request_value(p_request, "durationInSeconds", "duration_in_seconds", &field_value)) {
            durationInSeconds_storage = static_cast<double>(static_cast<double>(field_value));
            value.durationInSeconds = &durationInSeconds_storage;
        }
        if (get_request_value(p_request, "item", "item", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field item."; return false; }
            if (!item_owner.from_dictionary(field_value, r_error)) return false;
            item_has_value = true;
            value.item = &item_owner.value;
        }
        if (get_request_value(p_request, "newStackValues", "new_stack_values", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field new_stack_values."; return false; }
            if (!newStackValues_owner.from_dictionary(field_value, r_error)) return false;
            newStackValues_has_value = true;
            value.newStackValues = &newStackValues_owner.value;
        }
        if (get_request_value(p_request, "priceAmounts", "price_amounts", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field price_amounts."; return false; }
            Array source_array = field_value;
            priceAmounts_items.resize(source_array.size());
            priceAmounts_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field price_amounts."; return false; }
                if (!priceAmounts_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                priceAmounts_ptrs[static_cast<size_t>(i)] = &priceAmounts_items[static_cast<size_t>(i)].value;
            }
            value.priceAmounts = priceAmounts_ptrs.empty() ? nullptr : priceAmounts_ptrs.data();
            value.priceAmountsCount = static_cast<uint32_t>(priceAmounts_ptrs.size());
        }
        if (get_request_value(p_request, "storeId", "store_id", &field_value)) {
            storeId_string = String(field_value).utf8().get_data();
            value.storeId = storeId_string.empty() ? nullptr : storeId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFInventorySubtractInventoryItemsOperation {
    PFInventorySubtractInventoryItemsOperation value = {};
    int32_t amount_storage = {};
    double durationInSeconds_storage = {};
    OwnedPFInventoryInventoryItemReference item_owner;
    bool item_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "amount", "amount", &field_value)) {
            amount_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.amount = &amount_storage;
        }
        if (get_request_value(p_request, "deleteEmptyStacks", "delete_empty_stacks", &field_value)) {
            value.deleteEmptyStacks = static_cast<bool>(field_value);
        }
        if (get_request_value(p_request, "durationInSeconds", "duration_in_seconds", &field_value)) {
            durationInSeconds_storage = static_cast<double>(static_cast<double>(field_value));
            value.durationInSeconds = &durationInSeconds_storage;
        }
        if (get_request_value(p_request, "item", "item", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field item."; return false; }
            if (!item_owner.from_dictionary(field_value, r_error)) return false;
            item_has_value = true;
            value.item = &item_owner.value;
        }
        return true;
    }
};

struct OwnedPFInventoryTransferInventoryItemsOperation {
    PFInventoryTransferInventoryItemsOperation value = {};
    int32_t amount_storage = {};
    OwnedPFInventoryInventoryItemReference givingItem_owner;
    bool givingItem_has_value = false;
    OwnedPFInventoryInitialValues newStackValues_owner;
    bool newStackValues_has_value = false;
    OwnedPFInventoryInventoryItemReference receivingItem_owner;
    bool receivingItem_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "amount", "amount", &field_value)) {
            amount_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.amount = &amount_storage;
        }
        if (get_request_value(p_request, "deleteEmptyStacks", "delete_empty_stacks", &field_value)) {
            value.deleteEmptyStacks = static_cast<bool>(field_value);
        }
        if (get_request_value(p_request, "givingItem", "giving_item", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field giving_item."; return false; }
            if (!givingItem_owner.from_dictionary(field_value, r_error)) return false;
            givingItem_has_value = true;
            value.givingItem = &givingItem_owner.value;
        }
        if (get_request_value(p_request, "newStackValues", "new_stack_values", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field new_stack_values."; return false; }
            if (!newStackValues_owner.from_dictionary(field_value, r_error)) return false;
            newStackValues_has_value = true;
            value.newStackValues = &newStackValues_owner.value;
        }
        if (get_request_value(p_request, "receivingItem", "receiving_item", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field receiving_item."; return false; }
            if (!receivingItem_owner.from_dictionary(field_value, r_error)) return false;
            receivingItem_has_value = true;
            value.receivingItem = &receivingItem_owner.value;
        }
        return true;
    }
};

struct OwnedPFInventoryInventoryItem {
    PFInventoryInventoryItem value = {};
    int32_t amount_storage = {};
    std::string displayProperties_json;
    time_t expirationDate_storage = {};
    std::string id_string;
    std::string stackId_string;
    time_t startDate_storage = {};
    std::string type_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "amount", "amount", &field_value)) {
            amount_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.amount = &amount_storage;
        }
        if (get_request_value(p_request, "displayProperties", "display_properties", &field_value)) {
            if (field_value.get_type() == Variant::STRING) {
                displayProperties_json = String(field_value).utf8().get_data();
            } else {
                displayProperties_json = variant_to_json_string(field_value).utf8().get_data();
            }
            value.displayProperties.stringValue = displayProperties_json.empty() ? nullptr : displayProperties_json.c_str();
        }
        if (get_request_value(p_request, "expirationDate", "expiration_date", &field_value)) {
            expirationDate_storage = static_cast<time_t>(static_cast<int64_t>(field_value));
            value.expirationDate = &expirationDate_storage;
        }
        if (get_request_value(p_request, "id", "id", &field_value)) {
            id_string = String(field_value).utf8().get_data();
            value.id = id_string.empty() ? nullptr : id_string.c_str();
        }
        if (get_request_value(p_request, "stackId", "stack_id", &field_value)) {
            stackId_string = String(field_value).utf8().get_data();
            value.stackId = stackId_string.empty() ? nullptr : stackId_string.c_str();
        }
        if (get_request_value(p_request, "startDate", "start_date", &field_value)) {
            startDate_storage = static_cast<time_t>(static_cast<int64_t>(field_value));
            value.startDate = &startDate_storage;
        }
        if (get_request_value(p_request, "type", "type", &field_value)) {
            type_string = String(field_value).utf8().get_data();
            value.type = type_string.empty() ? nullptr : type_string.c_str();
        }
        return true;
    }
};

struct OwnedPFInventoryUpdateInventoryItemsOperation {
    PFInventoryUpdateInventoryItemsOperation value = {};
    OwnedPFInventoryInventoryItem item_owner;
    bool item_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "item", "item", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field item."; return false; }
            if (!item_owner.from_dictionary(field_value, r_error)) return false;
            item_has_value = true;
            value.item = &item_owner.value;
        }
        return true;
    }
};

struct OwnedPFInventoryInventoryOperation {
    PFInventoryInventoryOperation value = {};
    OwnedPFInventoryAddInventoryItemsOperation add_owner;
    bool add_has_value = false;
    OwnedPFInventoryDeleteInventoryItemsOperation deleteOp_owner;
    bool deleteOp_has_value = false;
    OwnedPFInventoryPurchaseInventoryItemsOperation purchase_owner;
    bool purchase_has_value = false;
    OwnedPFInventorySubtractInventoryItemsOperation subtract_owner;
    bool subtract_has_value = false;
    OwnedPFInventoryTransferInventoryItemsOperation transfer_owner;
    bool transfer_has_value = false;
    OwnedPFInventoryUpdateInventoryItemsOperation update_owner;
    bool update_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "add", "add", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field add."; return false; }
            if (!add_owner.from_dictionary(field_value, r_error)) return false;
            add_has_value = true;
            value.add = &add_owner.value;
        }
        if (get_request_value(p_request, "deleteOp", "delete_op", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field delete_op."; return false; }
            if (!deleteOp_owner.from_dictionary(field_value, r_error)) return false;
            deleteOp_has_value = true;
            value.deleteOp = &deleteOp_owner.value;
        }
        if (get_request_value(p_request, "purchase", "purchase", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field purchase."; return false; }
            if (!purchase_owner.from_dictionary(field_value, r_error)) return false;
            purchase_has_value = true;
            value.purchase = &purchase_owner.value;
        }
        if (get_request_value(p_request, "subtract", "subtract", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field subtract."; return false; }
            if (!subtract_owner.from_dictionary(field_value, r_error)) return false;
            subtract_has_value = true;
            value.subtract = &subtract_owner.value;
        }
        if (get_request_value(p_request, "transfer", "transfer", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field transfer."; return false; }
            if (!transfer_owner.from_dictionary(field_value, r_error)) return false;
            transfer_has_value = true;
            value.transfer = &transfer_owner.value;
        }
        if (get_request_value(p_request, "update", "update", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field update."; return false; }
            if (!update_owner.from_dictionary(field_value, r_error)) return false;
            update_has_value = true;
            value.update = &update_owner.value;
        }
        return true;
    }
};

struct OwnedPFInventoryExecuteInventoryOperationsRequest {
    PFInventoryExecuteInventoryOperationsRequest value = {};
    std::string collectionId_string;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::string eTag_string;
    std::string idempotencyId_string;
    std::vector<OwnedPFInventoryInventoryOperation> operations_items;
    std::vector<const PFInventoryInventoryOperation *> operations_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "collectionId", "collection_id", &field_value)) {
            collectionId_string = String(field_value).utf8().get_data();
            value.collectionId = collectionId_string.empty() ? nullptr : collectionId_string.c_str();
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "eTag", "e_tag", &field_value)) {
            eTag_string = String(field_value).utf8().get_data();
            value.eTag = eTag_string.empty() ? nullptr : eTag_string.c_str();
        }
        if (get_request_value(p_request, "idempotencyId", "idempotency_id", &field_value)) {
            idempotencyId_string = String(field_value).utf8().get_data();
            value.idempotencyId = idempotencyId_string.empty() ? nullptr : idempotencyId_string.c_str();
        }
        if (get_request_value(p_request, "operations", "operations", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field operations."; return false; }
            Array source_array = field_value;
            operations_items.resize(source_array.size());
            operations_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field operations."; return false; }
                if (!operations_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                operations_ptrs[static_cast<size_t>(i)] = &operations_items[static_cast<size_t>(i)].value;
            }
            value.operations = operations_ptrs.empty() ? nullptr : operations_ptrs.data();
            value.operationsCount = static_cast<uint32_t>(operations_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFInventoryExecuteInventoryOperationsResponse {
    PFInventoryExecuteInventoryOperationsResponse value = {};
    std::string eTag_string;
    std::string idempotencyId_string;
    std::vector<std::string> transactionIds_strings;
    std::vector<const char *> transactionIds_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "eTag", "e_tag", &field_value)) {
            eTag_string = String(field_value).utf8().get_data();
            value.eTag = eTag_string.empty() ? nullptr : eTag_string.c_str();
        }
        if (get_request_value(p_request, "idempotencyId", "idempotency_id", &field_value)) {
            idempotencyId_string = String(field_value).utf8().get_data();
            value.idempotencyId = idempotencyId_string.empty() ? nullptr : idempotencyId_string.c_str();
        }
        if (get_request_value(p_request, "transactionIds", "transaction_ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field transaction_ids."; return false; }
            Array source_array = field_value;
            transactionIds_strings.clear(); transactionIds_ptrs.clear();
            transactionIds_strings.reserve(source_array.size()); transactionIds_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                transactionIds_strings.emplace_back(String(source_array[i]).utf8().get_data());
                transactionIds_ptrs.push_back(transactionIds_strings.back().c_str());
            }
            value.transactionIds = transactionIds_ptrs.empty() ? nullptr : transactionIds_ptrs.data();
            value.transactionIdsCount = static_cast<uint32_t>(transactionIds_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFInventoryExecuteTransferOperationsRequest {
    PFInventoryExecuteTransferOperationsRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    std::string givingCollectionId_string;
    OwnedPFEntityKey givingEntity_owner;
    bool givingEntity_has_value = false;
    std::string givingETag_string;
    std::string idempotencyId_string;
    std::vector<OwnedPFInventoryTransferInventoryItemsOperation> operations_items;
    std::vector<const PFInventoryTransferInventoryItemsOperation *> operations_ptrs;
    std::string receivingCollectionId_string;
    OwnedPFEntityKey receivingEntity_owner;
    bool receivingEntity_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "givingCollectionId", "giving_collection_id", &field_value)) {
            givingCollectionId_string = String(field_value).utf8().get_data();
            value.givingCollectionId = givingCollectionId_string.empty() ? nullptr : givingCollectionId_string.c_str();
        }
        if (get_request_value(p_request, "givingEntity", "giving_entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field giving_entity."; return false; }
            if (!givingEntity_owner.from_dictionary(field_value, r_error)) return false;
            givingEntity_has_value = true;
            value.givingEntity = &givingEntity_owner.value;
        }
        if (get_request_value(p_request, "givingETag", "giving_e_tag", &field_value)) {
            givingETag_string = String(field_value).utf8().get_data();
            value.givingETag = givingETag_string.empty() ? nullptr : givingETag_string.c_str();
        }
        if (get_request_value(p_request, "idempotencyId", "idempotency_id", &field_value)) {
            idempotencyId_string = String(field_value).utf8().get_data();
            value.idempotencyId = idempotencyId_string.empty() ? nullptr : idempotencyId_string.c_str();
        }
        if (get_request_value(p_request, "operations", "operations", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field operations."; return false; }
            Array source_array = field_value;
            operations_items.resize(source_array.size());
            operations_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field operations."; return false; }
                if (!operations_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                operations_ptrs[static_cast<size_t>(i)] = &operations_items[static_cast<size_t>(i)].value;
            }
            value.operations = operations_ptrs.empty() ? nullptr : operations_ptrs.data();
            value.operationsCount = static_cast<uint32_t>(operations_ptrs.size());
        }
        if (get_request_value(p_request, "receivingCollectionId", "receiving_collection_id", &field_value)) {
            receivingCollectionId_string = String(field_value).utf8().get_data();
            value.receivingCollectionId = receivingCollectionId_string.empty() ? nullptr : receivingCollectionId_string.c_str();
        }
        if (get_request_value(p_request, "receivingEntity", "receiving_entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field receiving_entity."; return false; }
            if (!receivingEntity_owner.from_dictionary(field_value, r_error)) return false;
            receivingEntity_has_value = true;
            value.receivingEntity = &receivingEntity_owner.value;
        }
        return true;
    }
};

struct OwnedPFInventoryExecuteTransferOperationsResponse {
    PFInventoryExecuteTransferOperationsResponse value = {};
    std::string givingETag_string;
    std::vector<std::string> givingTransactionIds_strings;
    std::vector<const char *> givingTransactionIds_ptrs;
    std::string idempotencyId_string;
    std::string operationStatus_string;
    std::string operationToken_string;
    std::string receivingETag_string;
    std::vector<std::string> receivingTransactionIds_strings;
    std::vector<const char *> receivingTransactionIds_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "givingETag", "giving_e_tag", &field_value)) {
            givingETag_string = String(field_value).utf8().get_data();
            value.givingETag = givingETag_string.empty() ? nullptr : givingETag_string.c_str();
        }
        if (get_request_value(p_request, "givingTransactionIds", "giving_transaction_ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field giving_transaction_ids."; return false; }
            Array source_array = field_value;
            givingTransactionIds_strings.clear(); givingTransactionIds_ptrs.clear();
            givingTransactionIds_strings.reserve(source_array.size()); givingTransactionIds_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                givingTransactionIds_strings.emplace_back(String(source_array[i]).utf8().get_data());
                givingTransactionIds_ptrs.push_back(givingTransactionIds_strings.back().c_str());
            }
            value.givingTransactionIds = givingTransactionIds_ptrs.empty() ? nullptr : givingTransactionIds_ptrs.data();
            value.givingTransactionIdsCount = static_cast<uint32_t>(givingTransactionIds_ptrs.size());
        }
        if (get_request_value(p_request, "idempotencyId", "idempotency_id", &field_value)) {
            idempotencyId_string = String(field_value).utf8().get_data();
            value.idempotencyId = idempotencyId_string.empty() ? nullptr : idempotencyId_string.c_str();
        }
        if (get_request_value(p_request, "operationStatus", "operation_status", &field_value)) {
            operationStatus_string = String(field_value).utf8().get_data();
            value.operationStatus = operationStatus_string.empty() ? nullptr : operationStatus_string.c_str();
        }
        if (get_request_value(p_request, "operationToken", "operation_token", &field_value)) {
            operationToken_string = String(field_value).utf8().get_data();
            value.operationToken = operationToken_string.empty() ? nullptr : operationToken_string.c_str();
        }
        if (get_request_value(p_request, "receivingETag", "receiving_e_tag", &field_value)) {
            receivingETag_string = String(field_value).utf8().get_data();
            value.receivingETag = receivingETag_string.empty() ? nullptr : receivingETag_string.c_str();
        }
        if (get_request_value(p_request, "receivingTransactionIds", "receiving_transaction_ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field receiving_transaction_ids."; return false; }
            Array source_array = field_value;
            receivingTransactionIds_strings.clear(); receivingTransactionIds_ptrs.clear();
            receivingTransactionIds_strings.reserve(source_array.size()); receivingTransactionIds_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                receivingTransactionIds_strings.emplace_back(String(source_array[i]).utf8().get_data());
                receivingTransactionIds_ptrs.push_back(receivingTransactionIds_strings.back().c_str());
            }
            value.receivingTransactionIds = receivingTransactionIds_ptrs.empty() ? nullptr : receivingTransactionIds_ptrs.data();
            value.receivingTransactionIdsCount = static_cast<uint32_t>(receivingTransactionIds_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFInventoryGetInventoryCollectionIdsRequest {
    PFInventoryGetInventoryCollectionIdsRequest value = {};
    std::string continuationToken_string;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "continuationToken", "continuation_token", &field_value)) {
            continuationToken_string = String(field_value).utf8().get_data();
            value.continuationToken = continuationToken_string.empty() ? nullptr : continuationToken_string.c_str();
        }
        if (get_request_value(p_request, "count", "count", &field_value)) {
            value.count = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        return true;
    }
};

struct OwnedPFInventoryGetInventoryCollectionIdsResponse {
    PFInventoryGetInventoryCollectionIdsResponse value = {};
    std::vector<std::string> collectionIds_strings;
    std::vector<const char *> collectionIds_ptrs;
    std::string continuationToken_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "collectionIds", "collection_ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field collection_ids."; return false; }
            Array source_array = field_value;
            collectionIds_strings.clear(); collectionIds_ptrs.clear();
            collectionIds_strings.reserve(source_array.size()); collectionIds_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                collectionIds_strings.emplace_back(String(source_array[i]).utf8().get_data());
                collectionIds_ptrs.push_back(collectionIds_strings.back().c_str());
            }
            value.collectionIds = collectionIds_ptrs.empty() ? nullptr : collectionIds_ptrs.data();
            value.collectionIdsCount = static_cast<uint32_t>(collectionIds_ptrs.size());
        }
        if (get_request_value(p_request, "continuationToken", "continuation_token", &field_value)) {
            continuationToken_string = String(field_value).utf8().get_data();
            value.continuationToken = continuationToken_string.empty() ? nullptr : continuationToken_string.c_str();
        }
        return true;
    }
};

struct OwnedPFInventoryGetInventoryItemsRequest {
    PFInventoryGetInventoryItemsRequest value = {};
    std::string collectionId_string;
    std::string continuationToken_string;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::string filter_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "collectionId", "collection_id", &field_value)) {
            collectionId_string = String(field_value).utf8().get_data();
            value.collectionId = collectionId_string.empty() ? nullptr : collectionId_string.c_str();
        }
        if (get_request_value(p_request, "continuationToken", "continuation_token", &field_value)) {
            continuationToken_string = String(field_value).utf8().get_data();
            value.continuationToken = continuationToken_string.empty() ? nullptr : continuationToken_string.c_str();
        }
        if (get_request_value(p_request, "count", "count", &field_value)) {
            value.count = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "filter", "filter", &field_value)) {
            filter_string = String(field_value).utf8().get_data();
            value.filter = filter_string.empty() ? nullptr : filter_string.c_str();
        }
        return true;
    }
};

struct OwnedPFInventoryGetInventoryItemsResponse {
    PFInventoryGetInventoryItemsResponse value = {};
    std::string continuationToken_string;
    std::string eTag_string;
    std::vector<OwnedPFInventoryInventoryItem> items_items;
    std::vector<const PFInventoryInventoryItem *> items_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "continuationToken", "continuation_token", &field_value)) {
            continuationToken_string = String(field_value).utf8().get_data();
            value.continuationToken = continuationToken_string.empty() ? nullptr : continuationToken_string.c_str();
        }
        if (get_request_value(p_request, "eTag", "e_tag", &field_value)) {
            eTag_string = String(field_value).utf8().get_data();
            value.eTag = eTag_string.empty() ? nullptr : eTag_string.c_str();
        }
        if (get_request_value(p_request, "items", "items", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field items."; return false; }
            Array source_array = field_value;
            items_items.resize(source_array.size());
            items_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field items."; return false; }
                if (!items_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                items_ptrs[static_cast<size_t>(i)] = &items_items[static_cast<size_t>(i)].value;
            }
            value.items = items_ptrs.empty() ? nullptr : items_ptrs.data();
            value.itemsCount = static_cast<uint32_t>(items_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFInventoryGetInventoryOperationStatusRequest {
    PFInventoryGetInventoryOperationStatusRequest value = {};
    std::string collectionId_string;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::string operationToken_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "collectionId", "collection_id", &field_value)) {
            collectionId_string = String(field_value).utf8().get_data();
            value.collectionId = collectionId_string.empty() ? nullptr : collectionId_string.c_str();
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "operationToken", "operation_token", &field_value)) {
            operationToken_string = String(field_value).utf8().get_data();
            value.operationToken = operationToken_string.empty() ? nullptr : operationToken_string.c_str();
        }
        return true;
    }
};

struct OwnedPFInventoryGetInventoryOperationStatusResponse {
    PFInventoryGetInventoryOperationStatusResponse value = {};
    std::string operationStatus_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "operationStatus", "operation_status", &field_value)) {
            operationStatus_string = String(field_value).utf8().get_data();
            value.operationStatus = operationStatus_string.empty() ? nullptr : operationStatus_string.c_str();
        }
        return true;
    }
};

struct OwnedPFInventoryGetTransactionHistoryRequest {
    PFInventoryGetTransactionHistoryRequest value = {};
    std::string collectionId_string;
    std::string continuationToken_string;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::string filter_string;
    std::string orderBy_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "collectionId", "collection_id", &field_value)) {
            collectionId_string = String(field_value).utf8().get_data();
            value.collectionId = collectionId_string.empty() ? nullptr : collectionId_string.c_str();
        }
        if (get_request_value(p_request, "continuationToken", "continuation_token", &field_value)) {
            continuationToken_string = String(field_value).utf8().get_data();
            value.continuationToken = continuationToken_string.empty() ? nullptr : continuationToken_string.c_str();
        }
        if (get_request_value(p_request, "count", "count", &field_value)) {
            value.count = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "filter", "filter", &field_value)) {
            filter_string = String(field_value).utf8().get_data();
            value.filter = filter_string.empty() ? nullptr : filter_string.c_str();
        }
        if (get_request_value(p_request, "orderBy", "order_by", &field_value)) {
            orderBy_string = String(field_value).utf8().get_data();
            value.orderBy = orderBy_string.empty() ? nullptr : orderBy_string.c_str();
        }
        return true;
    }
};

struct OwnedPFInventoryTransactionClawbackDetails {
    PFInventoryTransactionClawbackDetails value = {};
    std::string transactionIdClawedback_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "transactionIdClawedback", "transaction_id_clawedback", &field_value)) {
            transactionIdClawedback_string = String(field_value).utf8().get_data();
            value.transactionIdClawedback = transactionIdClawedback_string.empty() ? nullptr : transactionIdClawedback_string.c_str();
        }
        return true;
    }
};

struct OwnedPFInventoryTransactionOperation {
    PFInventoryTransactionOperation value = {};
    int32_t amount_storage = {};
    double durationInSeconds_storage = {};
    std::string itemFriendlyId_string;
    std::string itemId_string;
    std::string itemType_string;
    std::string stackId_string;
    std::string type_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "amount", "amount", &field_value)) {
            amount_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.amount = &amount_storage;
        }
        if (get_request_value(p_request, "durationInSeconds", "duration_in_seconds", &field_value)) {
            durationInSeconds_storage = static_cast<double>(static_cast<double>(field_value));
            value.durationInSeconds = &durationInSeconds_storage;
        }
        if (get_request_value(p_request, "itemFriendlyId", "item_friendly_id", &field_value)) {
            itemFriendlyId_string = String(field_value).utf8().get_data();
            value.itemFriendlyId = itemFriendlyId_string.empty() ? nullptr : itemFriendlyId_string.c_str();
        }
        if (get_request_value(p_request, "itemId", "item_id", &field_value)) {
            itemId_string = String(field_value).utf8().get_data();
            value.itemId = itemId_string.empty() ? nullptr : itemId_string.c_str();
        }
        if (get_request_value(p_request, "itemType", "item_type", &field_value)) {
            itemType_string = String(field_value).utf8().get_data();
            value.itemType = itemType_string.empty() ? nullptr : itemType_string.c_str();
        }
        if (get_request_value(p_request, "stackId", "stack_id", &field_value)) {
            stackId_string = String(field_value).utf8().get_data();
            value.stackId = stackId_string.empty() ? nullptr : stackId_string.c_str();
        }
        if (get_request_value(p_request, "type", "type", &field_value)) {
            type_string = String(field_value).utf8().get_data();
            value.type = type_string.empty() ? nullptr : type_string.c_str();
        }
        return true;
    }
};

struct OwnedPFInventoryTransactionPurchaseDetails {
    PFInventoryTransactionPurchaseDetails value = {};
    std::string itemFriendlyId_string;
    std::string itemId_string;
    std::string storeFriendlyId_string;
    std::string storeId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "itemFriendlyId", "item_friendly_id", &field_value)) {
            itemFriendlyId_string = String(field_value).utf8().get_data();
            value.itemFriendlyId = itemFriendlyId_string.empty() ? nullptr : itemFriendlyId_string.c_str();
        }
        if (get_request_value(p_request, "itemId", "item_id", &field_value)) {
            itemId_string = String(field_value).utf8().get_data();
            value.itemId = itemId_string.empty() ? nullptr : itemId_string.c_str();
        }
        if (get_request_value(p_request, "storeFriendlyId", "store_friendly_id", &field_value)) {
            storeFriendlyId_string = String(field_value).utf8().get_data();
            value.storeFriendlyId = storeFriendlyId_string.empty() ? nullptr : storeFriendlyId_string.c_str();
        }
        if (get_request_value(p_request, "storeId", "store_id", &field_value)) {
            storeId_string = String(field_value).utf8().get_data();
            value.storeId = storeId_string.empty() ? nullptr : storeId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFInventoryTransactionRedeemDetails {
    PFInventoryTransactionRedeemDetails value = {};
    std::string marketplace_string;
    std::string marketplaceTransactionId_string;
    std::string offerId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "marketplace", "marketplace", &field_value)) {
            marketplace_string = String(field_value).utf8().get_data();
            value.marketplace = marketplace_string.empty() ? nullptr : marketplace_string.c_str();
        }
        if (get_request_value(p_request, "marketplaceTransactionId", "marketplace_transaction_id", &field_value)) {
            marketplaceTransactionId_string = String(field_value).utf8().get_data();
            value.marketplaceTransactionId = marketplaceTransactionId_string.empty() ? nullptr : marketplaceTransactionId_string.c_str();
        }
        if (get_request_value(p_request, "offerId", "offer_id", &field_value)) {
            offerId_string = String(field_value).utf8().get_data();
            value.offerId = offerId_string.empty() ? nullptr : offerId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFInventoryTransactionTransferDetails {
    PFInventoryTransactionTransferDetails value = {};
    std::string givingCollectionId_string;
    OwnedPFEntityKey givingEntity_owner;
    bool givingEntity_has_value = false;
    std::string receivingCollectionId_string;
    OwnedPFEntityKey receivingEntity_owner;
    bool receivingEntity_has_value = false;
    std::string transferId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "givingCollectionId", "giving_collection_id", &field_value)) {
            givingCollectionId_string = String(field_value).utf8().get_data();
            value.givingCollectionId = givingCollectionId_string.empty() ? nullptr : givingCollectionId_string.c_str();
        }
        if (get_request_value(p_request, "givingEntity", "giving_entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field giving_entity."; return false; }
            if (!givingEntity_owner.from_dictionary(field_value, r_error)) return false;
            givingEntity_has_value = true;
            value.givingEntity = &givingEntity_owner.value;
        }
        if (get_request_value(p_request, "receivingCollectionId", "receiving_collection_id", &field_value)) {
            receivingCollectionId_string = String(field_value).utf8().get_data();
            value.receivingCollectionId = receivingCollectionId_string.empty() ? nullptr : receivingCollectionId_string.c_str();
        }
        if (get_request_value(p_request, "receivingEntity", "receiving_entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field receiving_entity."; return false; }
            if (!receivingEntity_owner.from_dictionary(field_value, r_error)) return false;
            receivingEntity_has_value = true;
            value.receivingEntity = &receivingEntity_owner.value;
        }
        if (get_request_value(p_request, "transferId", "transfer_id", &field_value)) {
            transferId_string = String(field_value).utf8().get_data();
            value.transferId = transferId_string.empty() ? nullptr : transferId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFInventoryTransaction {
    PFInventoryTransaction value = {};
    std::string apiName_string;
    OwnedPFInventoryTransactionClawbackDetails clawbackDetails_owner;
    bool clawbackDetails_has_value = false;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    std::string itemType_string;
    std::vector<OwnedPFInventoryTransactionOperation> operations_items;
    std::vector<const PFInventoryTransactionOperation *> operations_ptrs;
    std::string operationType_string;
    OwnedPFInventoryTransactionPurchaseDetails purchaseDetails_owner;
    bool purchaseDetails_has_value = false;
    OwnedPFInventoryTransactionRedeemDetails redeemDetails_owner;
    bool redeemDetails_has_value = false;
    std::string transactionId_string;
    OwnedPFInventoryTransactionTransferDetails transferDetails_owner;
    bool transferDetails_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "apiName", "api_name", &field_value)) {
            apiName_string = String(field_value).utf8().get_data();
            value.apiName = apiName_string.empty() ? nullptr : apiName_string.c_str();
        }
        if (get_request_value(p_request, "clawbackDetails", "clawback_details", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field clawback_details."; return false; }
            if (!clawbackDetails_owner.from_dictionary(field_value, r_error)) return false;
            clawbackDetails_has_value = true;
            value.clawbackDetails = &clawbackDetails_owner.value;
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "itemType", "item_type", &field_value)) {
            itemType_string = String(field_value).utf8().get_data();
            value.itemType = itemType_string.empty() ? nullptr : itemType_string.c_str();
        }
        if (get_request_value(p_request, "operations", "operations", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field operations."; return false; }
            Array source_array = field_value;
            operations_items.resize(source_array.size());
            operations_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field operations."; return false; }
                if (!operations_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                operations_ptrs[static_cast<size_t>(i)] = &operations_items[static_cast<size_t>(i)].value;
            }
            value.operations = operations_ptrs.empty() ? nullptr : operations_ptrs.data();
            value.operationsCount = static_cast<uint32_t>(operations_ptrs.size());
        }
        if (get_request_value(p_request, "operationType", "operation_type", &field_value)) {
            operationType_string = String(field_value).utf8().get_data();
            value.operationType = operationType_string.empty() ? nullptr : operationType_string.c_str();
        }
        if (get_request_value(p_request, "purchaseDetails", "purchase_details", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field purchase_details."; return false; }
            if (!purchaseDetails_owner.from_dictionary(field_value, r_error)) return false;
            purchaseDetails_has_value = true;
            value.purchaseDetails = &purchaseDetails_owner.value;
        }
        if (get_request_value(p_request, "redeemDetails", "redeem_details", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field redeem_details."; return false; }
            if (!redeemDetails_owner.from_dictionary(field_value, r_error)) return false;
            redeemDetails_has_value = true;
            value.redeemDetails = &redeemDetails_owner.value;
        }
        if (get_request_value(p_request, "timestamp", "timestamp", &field_value)) {
            value.timestamp = static_cast<time_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "transactionId", "transaction_id", &field_value)) {
            transactionId_string = String(field_value).utf8().get_data();
            value.transactionId = transactionId_string.empty() ? nullptr : transactionId_string.c_str();
        }
        if (get_request_value(p_request, "transferDetails", "transfer_details", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field transfer_details."; return false; }
            if (!transferDetails_owner.from_dictionary(field_value, r_error)) return false;
            transferDetails_has_value = true;
            value.transferDetails = &transferDetails_owner.value;
        }
        return true;
    }
};

struct OwnedPFInventoryGetTransactionHistoryResponse {
    PFInventoryGetTransactionHistoryResponse value = {};
    std::string continuationToken_string;
    std::vector<OwnedPFInventoryTransaction> transactions_items;
    std::vector<const PFInventoryTransaction *> transactions_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "continuationToken", "continuation_token", &field_value)) {
            continuationToken_string = String(field_value).utf8().get_data();
            value.continuationToken = continuationToken_string.empty() ? nullptr : continuationToken_string.c_str();
        }
        if (get_request_value(p_request, "transactions", "transactions", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field transactions."; return false; }
            Array source_array = field_value;
            transactions_items.resize(source_array.size());
            transactions_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field transactions."; return false; }
                if (!transactions_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                transactions_ptrs[static_cast<size_t>(i)] = &transactions_items[static_cast<size_t>(i)].value;
            }
            value.transactions = transactions_ptrs.empty() ? nullptr : transactions_ptrs.data();
            value.transactionsCount = static_cast<uint32_t>(transactions_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFInventoryGooglePlayProductPurchase {
    PFInventoryGooglePlayProductPurchase value = {};
    std::string productId_string;
    std::string token_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "productId", "product_id", &field_value)) {
            productId_string = String(field_value).utf8().get_data();
            value.productId = productId_string.empty() ? nullptr : productId_string.c_str();
        }
        if (get_request_value(p_request, "token", "token", &field_value)) {
            token_string = String(field_value).utf8().get_data();
            value.token = token_string.empty() ? nullptr : token_string.c_str();
        }
        return true;
    }
};

struct OwnedPFInventoryPurchaseInventoryItemsRequest {
    PFInventoryPurchaseInventoryItemsRequest value = {};
    int32_t amount_storage = {};
    std::string collectionId_string;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    double durationInSeconds_storage = {};
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::string eTag_string;
    std::string idempotencyId_string;
    OwnedPFInventoryInventoryItemReference item_owner;
    bool item_has_value = false;
    OwnedPFInventoryInitialValues newStackValues_owner;
    bool newStackValues_has_value = false;
    std::vector<OwnedPFInventoryPurchasePriceAmount> priceAmounts_items;
    std::vector<const PFInventoryPurchasePriceAmount *> priceAmounts_ptrs;
    std::string storeId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "amount", "amount", &field_value)) {
            amount_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.amount = &amount_storage;
        }
        if (get_request_value(p_request, "collectionId", "collection_id", &field_value)) {
            collectionId_string = String(field_value).utf8().get_data();
            value.collectionId = collectionId_string.empty() ? nullptr : collectionId_string.c_str();
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "deleteEmptyStacks", "delete_empty_stacks", &field_value)) {
            value.deleteEmptyStacks = static_cast<bool>(field_value);
        }
        if (get_request_value(p_request, "durationInSeconds", "duration_in_seconds", &field_value)) {
            durationInSeconds_storage = static_cast<double>(static_cast<double>(field_value));
            value.durationInSeconds = &durationInSeconds_storage;
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "eTag", "e_tag", &field_value)) {
            eTag_string = String(field_value).utf8().get_data();
            value.eTag = eTag_string.empty() ? nullptr : eTag_string.c_str();
        }
        if (get_request_value(p_request, "idempotencyId", "idempotency_id", &field_value)) {
            idempotencyId_string = String(field_value).utf8().get_data();
            value.idempotencyId = idempotencyId_string.empty() ? nullptr : idempotencyId_string.c_str();
        }
        if (get_request_value(p_request, "item", "item", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field item."; return false; }
            if (!item_owner.from_dictionary(field_value, r_error)) return false;
            item_has_value = true;
            value.item = &item_owner.value;
        }
        if (get_request_value(p_request, "newStackValues", "new_stack_values", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field new_stack_values."; return false; }
            if (!newStackValues_owner.from_dictionary(field_value, r_error)) return false;
            newStackValues_has_value = true;
            value.newStackValues = &newStackValues_owner.value;
        }
        if (get_request_value(p_request, "priceAmounts", "price_amounts", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field price_amounts."; return false; }
            Array source_array = field_value;
            priceAmounts_items.resize(source_array.size());
            priceAmounts_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field price_amounts."; return false; }
                if (!priceAmounts_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                priceAmounts_ptrs[static_cast<size_t>(i)] = &priceAmounts_items[static_cast<size_t>(i)].value;
            }
            value.priceAmounts = priceAmounts_ptrs.empty() ? nullptr : priceAmounts_ptrs.data();
            value.priceAmountsCount = static_cast<uint32_t>(priceAmounts_ptrs.size());
        }
        if (get_request_value(p_request, "storeId", "store_id", &field_value)) {
            storeId_string = String(field_value).utf8().get_data();
            value.storeId = storeId_string.empty() ? nullptr : storeId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFInventoryPurchaseInventoryItemsResponse {
    PFInventoryPurchaseInventoryItemsResponse value = {};
    std::string eTag_string;
    std::string idempotencyId_string;
    std::vector<std::string> transactionIds_strings;
    std::vector<const char *> transactionIds_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "eTag", "e_tag", &field_value)) {
            eTag_string = String(field_value).utf8().get_data();
            value.eTag = eTag_string.empty() ? nullptr : eTag_string.c_str();
        }
        if (get_request_value(p_request, "idempotencyId", "idempotency_id", &field_value)) {
            idempotencyId_string = String(field_value).utf8().get_data();
            value.idempotencyId = idempotencyId_string.empty() ? nullptr : idempotencyId_string.c_str();
        }
        if (get_request_value(p_request, "transactionIds", "transaction_ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field transaction_ids."; return false; }
            Array source_array = field_value;
            transactionIds_strings.clear(); transactionIds_ptrs.clear();
            transactionIds_strings.reserve(source_array.size()); transactionIds_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                transactionIds_strings.emplace_back(String(source_array[i]).utf8().get_data());
                transactionIds_ptrs.push_back(transactionIds_strings.back().c_str());
            }
            value.transactionIds = transactionIds_ptrs.empty() ? nullptr : transactionIds_ptrs.data();
            value.transactionIdsCount = static_cast<uint32_t>(transactionIds_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFInventoryRedeemAppleAppStoreInventoryItemsRequest {
    PFInventoryRedeemAppleAppStoreInventoryItemsRequest value = {};
    std::string collectionId_string;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::string receipt_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "collectionId", "collection_id", &field_value)) {
            collectionId_string = String(field_value).utf8().get_data();
            value.collectionId = collectionId_string.empty() ? nullptr : collectionId_string.c_str();
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "receipt", "receipt", &field_value)) {
            receipt_string = String(field_value).utf8().get_data();
            value.receipt = receipt_string.empty() ? nullptr : receipt_string.c_str();
        }
        return true;
    }
};

struct OwnedPFInventoryRedemptionFailure {
    PFInventoryRedemptionFailure value = {};
    std::string failureCode_string;
    std::string failureDetails_string;
    std::string marketplaceAlternateId_string;
    std::string marketplaceTransactionId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "failureCode", "failure_code", &field_value)) {
            failureCode_string = String(field_value).utf8().get_data();
            value.failureCode = failureCode_string.empty() ? nullptr : failureCode_string.c_str();
        }
        if (get_request_value(p_request, "failureDetails", "failure_details", &field_value)) {
            failureDetails_string = String(field_value).utf8().get_data();
            value.failureDetails = failureDetails_string.empty() ? nullptr : failureDetails_string.c_str();
        }
        if (get_request_value(p_request, "marketplaceAlternateId", "marketplace_alternate_id", &field_value)) {
            marketplaceAlternateId_string = String(field_value).utf8().get_data();
            value.marketplaceAlternateId = marketplaceAlternateId_string.empty() ? nullptr : marketplaceAlternateId_string.c_str();
        }
        if (get_request_value(p_request, "marketplaceTransactionId", "marketplace_transaction_id", &field_value)) {
            marketplaceTransactionId_string = String(field_value).utf8().get_data();
            value.marketplaceTransactionId = marketplaceTransactionId_string.empty() ? nullptr : marketplaceTransactionId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFInventoryRedemptionSuccess {
    PFInventoryRedemptionSuccess value = {};
    time_t expirationTimestamp_storage = {};
    std::string marketplaceAlternateId_string;
    std::string marketplaceTransactionId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "expirationTimestamp", "expiration_timestamp", &field_value)) {
            expirationTimestamp_storage = static_cast<time_t>(static_cast<int64_t>(field_value));
            value.expirationTimestamp = &expirationTimestamp_storage;
        }
        if (get_request_value(p_request, "marketplaceAlternateId", "marketplace_alternate_id", &field_value)) {
            marketplaceAlternateId_string = String(field_value).utf8().get_data();
            value.marketplaceAlternateId = marketplaceAlternateId_string.empty() ? nullptr : marketplaceAlternateId_string.c_str();
        }
        if (get_request_value(p_request, "marketplaceTransactionId", "marketplace_transaction_id", &field_value)) {
            marketplaceTransactionId_string = String(field_value).utf8().get_data();
            value.marketplaceTransactionId = marketplaceTransactionId_string.empty() ? nullptr : marketplaceTransactionId_string.c_str();
        }
        if (get_request_value(p_request, "successTimestamp", "success_timestamp", &field_value)) {
            value.successTimestamp = static_cast<time_t>(static_cast<int64_t>(field_value));
        }
        return true;
    }
};

struct OwnedPFInventoryRedeemAppleAppStoreInventoryItemsResponse {
    PFInventoryRedeemAppleAppStoreInventoryItemsResponse value = {};
    std::vector<OwnedPFInventoryRedemptionFailure> failed_items;
    std::vector<const PFInventoryRedemptionFailure *> failed_ptrs;
    std::vector<OwnedPFInventoryRedemptionSuccess> succeeded_items;
    std::vector<const PFInventoryRedemptionSuccess *> succeeded_ptrs;
    std::vector<std::string> transactionIds_strings;
    std::vector<const char *> transactionIds_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "failed", "failed", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field failed."; return false; }
            Array source_array = field_value;
            failed_items.resize(source_array.size());
            failed_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field failed."; return false; }
                if (!failed_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                failed_ptrs[static_cast<size_t>(i)] = &failed_items[static_cast<size_t>(i)].value;
            }
            value.failed = failed_ptrs.empty() ? nullptr : failed_ptrs.data();
            value.failedCount = static_cast<uint32_t>(failed_ptrs.size());
        }
        if (get_request_value(p_request, "succeeded", "succeeded", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field succeeded."; return false; }
            Array source_array = field_value;
            succeeded_items.resize(source_array.size());
            succeeded_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field succeeded."; return false; }
                if (!succeeded_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                succeeded_ptrs[static_cast<size_t>(i)] = &succeeded_items[static_cast<size_t>(i)].value;
            }
            value.succeeded = succeeded_ptrs.empty() ? nullptr : succeeded_ptrs.data();
            value.succeededCount = static_cast<uint32_t>(succeeded_ptrs.size());
        }
        if (get_request_value(p_request, "transactionIds", "transaction_ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field transaction_ids."; return false; }
            Array source_array = field_value;
            transactionIds_strings.clear(); transactionIds_ptrs.clear();
            transactionIds_strings.reserve(source_array.size()); transactionIds_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                transactionIds_strings.emplace_back(String(source_array[i]).utf8().get_data());
                transactionIds_ptrs.push_back(transactionIds_strings.back().c_str());
            }
            value.transactionIds = transactionIds_ptrs.empty() ? nullptr : transactionIds_ptrs.data();
            value.transactionIdsCount = static_cast<uint32_t>(transactionIds_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFInventoryRedeemGooglePlayInventoryItemsRequest {
    PFInventoryRedeemGooglePlayInventoryItemsRequest value = {};
    std::string collectionId_string;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::vector<OwnedPFInventoryGooglePlayProductPurchase> purchases_items;
    std::vector<const PFInventoryGooglePlayProductPurchase *> purchases_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "collectionId", "collection_id", &field_value)) {
            collectionId_string = String(field_value).utf8().get_data();
            value.collectionId = collectionId_string.empty() ? nullptr : collectionId_string.c_str();
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "purchases", "purchases", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field purchases."; return false; }
            Array source_array = field_value;
            purchases_items.resize(source_array.size());
            purchases_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field purchases."; return false; }
                if (!purchases_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                purchases_ptrs[static_cast<size_t>(i)] = &purchases_items[static_cast<size_t>(i)].value;
            }
            value.purchases = purchases_ptrs.empty() ? nullptr : purchases_ptrs.data();
            value.purchasesCount = static_cast<uint32_t>(purchases_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFInventoryRedeemGooglePlayInventoryItemsResponse {
    PFInventoryRedeemGooglePlayInventoryItemsResponse value = {};
    std::vector<OwnedPFInventoryRedemptionFailure> failed_items;
    std::vector<const PFInventoryRedemptionFailure *> failed_ptrs;
    std::vector<OwnedPFInventoryRedemptionSuccess> succeeded_items;
    std::vector<const PFInventoryRedemptionSuccess *> succeeded_ptrs;
    std::vector<std::string> transactionIds_strings;
    std::vector<const char *> transactionIds_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "failed", "failed", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field failed."; return false; }
            Array source_array = field_value;
            failed_items.resize(source_array.size());
            failed_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field failed."; return false; }
                if (!failed_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                failed_ptrs[static_cast<size_t>(i)] = &failed_items[static_cast<size_t>(i)].value;
            }
            value.failed = failed_ptrs.empty() ? nullptr : failed_ptrs.data();
            value.failedCount = static_cast<uint32_t>(failed_ptrs.size());
        }
        if (get_request_value(p_request, "succeeded", "succeeded", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field succeeded."; return false; }
            Array source_array = field_value;
            succeeded_items.resize(source_array.size());
            succeeded_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field succeeded."; return false; }
                if (!succeeded_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                succeeded_ptrs[static_cast<size_t>(i)] = &succeeded_items[static_cast<size_t>(i)].value;
            }
            value.succeeded = succeeded_ptrs.empty() ? nullptr : succeeded_ptrs.data();
            value.succeededCount = static_cast<uint32_t>(succeeded_ptrs.size());
        }
        if (get_request_value(p_request, "transactionIds", "transaction_ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field transaction_ids."; return false; }
            Array source_array = field_value;
            transactionIds_strings.clear(); transactionIds_ptrs.clear();
            transactionIds_strings.reserve(source_array.size()); transactionIds_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                transactionIds_strings.emplace_back(String(source_array[i]).utf8().get_data());
                transactionIds_ptrs.push_back(transactionIds_strings.back().c_str());
            }
            value.transactionIds = transactionIds_ptrs.empty() ? nullptr : transactionIds_ptrs.data();
            value.transactionIdsCount = static_cast<uint32_t>(transactionIds_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFInventoryRedeemMicrosoftStoreInventoryItemsRequest {
    PFInventoryRedeemMicrosoftStoreInventoryItemsRequest value = {};
    std::string collectionId_string;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    XUserHandle user_handle = nullptr;

    ~OwnedPFInventoryRedeemMicrosoftStoreInventoryItemsRequest() {
        if (user_handle != nullptr) {
            XUserCloseHandle(user_handle);
            user_handle = nullptr;
        }
    }

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "collectionId", "collection_id", &field_value)) {
            collectionId_string = String(field_value).utf8().get_data();
            value.collectionId = collectionId_string.empty() ? nullptr : collectionId_string.c_str();
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (!get_request_value(p_request, "user", "user", &field_value)) {
            if (r_error) *r_error = "Expected a GDKUser object for field user.";
            return false;
        }
        if (!duplicate_xuser_handle_from_variant(field_value, &user_handle, r_error)) return false;
        value.user = user_handle;
        return true;
    }
};

struct OwnedPFInventoryRedeemMicrosoftStoreInventoryItemsResponse {
    PFInventoryRedeemMicrosoftStoreInventoryItemsResponse value = {};
    std::vector<OwnedPFInventoryRedemptionFailure> failed_items;
    std::vector<const PFInventoryRedemptionFailure *> failed_ptrs;
    std::vector<OwnedPFInventoryRedemptionSuccess> succeeded_items;
    std::vector<const PFInventoryRedemptionSuccess *> succeeded_ptrs;
    std::vector<std::string> transactionIds_strings;
    std::vector<const char *> transactionIds_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "failed", "failed", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field failed."; return false; }
            Array source_array = field_value;
            failed_items.resize(source_array.size());
            failed_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field failed."; return false; }
                if (!failed_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                failed_ptrs[static_cast<size_t>(i)] = &failed_items[static_cast<size_t>(i)].value;
            }
            value.failed = failed_ptrs.empty() ? nullptr : failed_ptrs.data();
            value.failedCount = static_cast<uint32_t>(failed_ptrs.size());
        }
        if (get_request_value(p_request, "succeeded", "succeeded", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field succeeded."; return false; }
            Array source_array = field_value;
            succeeded_items.resize(source_array.size());
            succeeded_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field succeeded."; return false; }
                if (!succeeded_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                succeeded_ptrs[static_cast<size_t>(i)] = &succeeded_items[static_cast<size_t>(i)].value;
            }
            value.succeeded = succeeded_ptrs.empty() ? nullptr : succeeded_ptrs.data();
            value.succeededCount = static_cast<uint32_t>(succeeded_ptrs.size());
        }
        if (get_request_value(p_request, "transactionIds", "transaction_ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field transaction_ids."; return false; }
            Array source_array = field_value;
            transactionIds_strings.clear(); transactionIds_ptrs.clear();
            transactionIds_strings.reserve(source_array.size()); transactionIds_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                transactionIds_strings.emplace_back(String(source_array[i]).utf8().get_data());
                transactionIds_ptrs.push_back(transactionIds_strings.back().c_str());
            }
            value.transactionIds = transactionIds_ptrs.empty() ? nullptr : transactionIds_ptrs.data();
            value.transactionIdsCount = static_cast<uint32_t>(transactionIds_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFInventoryRedeemNintendoEShopInventoryItemsRequest {
    PFInventoryRedeemNintendoEShopInventoryItemsRequest value = {};
    std::string collectionId_string;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::string nintendoServiceAccountIdToken_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "collectionId", "collection_id", &field_value)) {
            collectionId_string = String(field_value).utf8().get_data();
            value.collectionId = collectionId_string.empty() ? nullptr : collectionId_string.c_str();
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "nintendoServiceAccountIdToken", "nintendo_service_account_id_token", &field_value)) {
            nintendoServiceAccountIdToken_string = String(field_value).utf8().get_data();
            value.nintendoServiceAccountIdToken = nintendoServiceAccountIdToken_string.empty() ? nullptr : nintendoServiceAccountIdToken_string.c_str();
        }
        return true;
    }
};

struct OwnedPFInventoryRedeemNintendoEShopInventoryItemsResponse {
    PFInventoryRedeemNintendoEShopInventoryItemsResponse value = {};
    std::vector<OwnedPFInventoryRedemptionFailure> failed_items;
    std::vector<const PFInventoryRedemptionFailure *> failed_ptrs;
    std::vector<OwnedPFInventoryRedemptionSuccess> succeeded_items;
    std::vector<const PFInventoryRedemptionSuccess *> succeeded_ptrs;
    std::vector<std::string> transactionIds_strings;
    std::vector<const char *> transactionIds_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "failed", "failed", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field failed."; return false; }
            Array source_array = field_value;
            failed_items.resize(source_array.size());
            failed_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field failed."; return false; }
                if (!failed_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                failed_ptrs[static_cast<size_t>(i)] = &failed_items[static_cast<size_t>(i)].value;
            }
            value.failed = failed_ptrs.empty() ? nullptr : failed_ptrs.data();
            value.failedCount = static_cast<uint32_t>(failed_ptrs.size());
        }
        if (get_request_value(p_request, "succeeded", "succeeded", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field succeeded."; return false; }
            Array source_array = field_value;
            succeeded_items.resize(source_array.size());
            succeeded_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field succeeded."; return false; }
                if (!succeeded_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                succeeded_ptrs[static_cast<size_t>(i)] = &succeeded_items[static_cast<size_t>(i)].value;
            }
            value.succeeded = succeeded_ptrs.empty() ? nullptr : succeeded_ptrs.data();
            value.succeededCount = static_cast<uint32_t>(succeeded_ptrs.size());
        }
        if (get_request_value(p_request, "transactionIds", "transaction_ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field transaction_ids."; return false; }
            Array source_array = field_value;
            transactionIds_strings.clear(); transactionIds_ptrs.clear();
            transactionIds_strings.reserve(source_array.size()); transactionIds_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                transactionIds_strings.emplace_back(String(source_array[i]).utf8().get_data());
                transactionIds_ptrs.push_back(transactionIds_strings.back().c_str());
            }
            value.transactionIds = transactionIds_ptrs.empty() ? nullptr : transactionIds_ptrs.data();
            value.transactionIdsCount = static_cast<uint32_t>(transactionIds_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFInventoryRedeemPlayStationStoreInventoryItemsRequest {
    PFInventoryRedeemPlayStationStoreInventoryItemsRequest value = {};
    std::string authorizationCode_string;
    std::string collectionId_string;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::string redirectUri_string;
    std::string serviceLabel_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "authorizationCode", "authorization_code", &field_value)) {
            authorizationCode_string = String(field_value).utf8().get_data();
            value.authorizationCode = authorizationCode_string.empty() ? nullptr : authorizationCode_string.c_str();
        }
        if (get_request_value(p_request, "collectionId", "collection_id", &field_value)) {
            collectionId_string = String(field_value).utf8().get_data();
            value.collectionId = collectionId_string.empty() ? nullptr : collectionId_string.c_str();
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "redirectUri", "redirect_uri", &field_value)) {
            redirectUri_string = String(field_value).utf8().get_data();
            value.redirectUri = redirectUri_string.empty() ? nullptr : redirectUri_string.c_str();
        }
        if (get_request_value(p_request, "serviceLabel", "service_label", &field_value)) {
            serviceLabel_string = String(field_value).utf8().get_data();
            value.serviceLabel = serviceLabel_string.empty() ? nullptr : serviceLabel_string.c_str();
        }
        return true;
    }
};

struct OwnedPFInventoryRedeemPlayStationStoreInventoryItemsResponse {
    PFInventoryRedeemPlayStationStoreInventoryItemsResponse value = {};
    std::vector<OwnedPFInventoryRedemptionFailure> failed_items;
    std::vector<const PFInventoryRedemptionFailure *> failed_ptrs;
    std::vector<OwnedPFInventoryRedemptionSuccess> succeeded_items;
    std::vector<const PFInventoryRedemptionSuccess *> succeeded_ptrs;
    std::vector<std::string> transactionIds_strings;
    std::vector<const char *> transactionIds_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "failed", "failed", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field failed."; return false; }
            Array source_array = field_value;
            failed_items.resize(source_array.size());
            failed_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field failed."; return false; }
                if (!failed_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                failed_ptrs[static_cast<size_t>(i)] = &failed_items[static_cast<size_t>(i)].value;
            }
            value.failed = failed_ptrs.empty() ? nullptr : failed_ptrs.data();
            value.failedCount = static_cast<uint32_t>(failed_ptrs.size());
        }
        if (get_request_value(p_request, "succeeded", "succeeded", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field succeeded."; return false; }
            Array source_array = field_value;
            succeeded_items.resize(source_array.size());
            succeeded_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field succeeded."; return false; }
                if (!succeeded_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                succeeded_ptrs[static_cast<size_t>(i)] = &succeeded_items[static_cast<size_t>(i)].value;
            }
            value.succeeded = succeeded_ptrs.empty() ? nullptr : succeeded_ptrs.data();
            value.succeededCount = static_cast<uint32_t>(succeeded_ptrs.size());
        }
        if (get_request_value(p_request, "transactionIds", "transaction_ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field transaction_ids."; return false; }
            Array source_array = field_value;
            transactionIds_strings.clear(); transactionIds_ptrs.clear();
            transactionIds_strings.reserve(source_array.size()); transactionIds_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                transactionIds_strings.emplace_back(String(source_array[i]).utf8().get_data());
                transactionIds_ptrs.push_back(transactionIds_strings.back().c_str());
            }
            value.transactionIds = transactionIds_ptrs.empty() ? nullptr : transactionIds_ptrs.data();
            value.transactionIdsCount = static_cast<uint32_t>(transactionIds_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFInventoryRedeemSteamInventoryItemsRequest {
    PFInventoryRedeemSteamInventoryItemsRequest value = {};
    std::string collectionId_string;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "collectionId", "collection_id", &field_value)) {
            collectionId_string = String(field_value).utf8().get_data();
            value.collectionId = collectionId_string.empty() ? nullptr : collectionId_string.c_str();
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        return true;
    }
};

struct OwnedPFInventoryRedeemSteamInventoryItemsResponse {
    PFInventoryRedeemSteamInventoryItemsResponse value = {};
    std::vector<OwnedPFInventoryRedemptionFailure> failed_items;
    std::vector<const PFInventoryRedemptionFailure *> failed_ptrs;
    std::vector<OwnedPFInventoryRedemptionSuccess> succeeded_items;
    std::vector<const PFInventoryRedemptionSuccess *> succeeded_ptrs;
    std::vector<std::string> transactionIds_strings;
    std::vector<const char *> transactionIds_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "failed", "failed", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field failed."; return false; }
            Array source_array = field_value;
            failed_items.resize(source_array.size());
            failed_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field failed."; return false; }
                if (!failed_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                failed_ptrs[static_cast<size_t>(i)] = &failed_items[static_cast<size_t>(i)].value;
            }
            value.failed = failed_ptrs.empty() ? nullptr : failed_ptrs.data();
            value.failedCount = static_cast<uint32_t>(failed_ptrs.size());
        }
        if (get_request_value(p_request, "succeeded", "succeeded", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field succeeded."; return false; }
            Array source_array = field_value;
            succeeded_items.resize(source_array.size());
            succeeded_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field succeeded."; return false; }
                if (!succeeded_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                succeeded_ptrs[static_cast<size_t>(i)] = &succeeded_items[static_cast<size_t>(i)].value;
            }
            value.succeeded = succeeded_ptrs.empty() ? nullptr : succeeded_ptrs.data();
            value.succeededCount = static_cast<uint32_t>(succeeded_ptrs.size());
        }
        if (get_request_value(p_request, "transactionIds", "transaction_ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field transaction_ids."; return false; }
            Array source_array = field_value;
            transactionIds_strings.clear(); transactionIds_ptrs.clear();
            transactionIds_strings.reserve(source_array.size()); transactionIds_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                transactionIds_strings.emplace_back(String(source_array[i]).utf8().get_data());
                transactionIds_ptrs.push_back(transactionIds_strings.back().c_str());
            }
            value.transactionIds = transactionIds_ptrs.empty() ? nullptr : transactionIds_ptrs.data();
            value.transactionIdsCount = static_cast<uint32_t>(transactionIds_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFInventorySubtractInventoryItemsRequest {
    PFInventorySubtractInventoryItemsRequest value = {};
    int32_t amount_storage = {};
    std::string collectionId_string;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    double durationInSeconds_storage = {};
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::string eTag_string;
    std::string idempotencyId_string;
    OwnedPFInventoryInventoryItemReference item_owner;
    bool item_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "amount", "amount", &field_value)) {
            amount_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.amount = &amount_storage;
        }
        if (get_request_value(p_request, "collectionId", "collection_id", &field_value)) {
            collectionId_string = String(field_value).utf8().get_data();
            value.collectionId = collectionId_string.empty() ? nullptr : collectionId_string.c_str();
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "deleteEmptyStacks", "delete_empty_stacks", &field_value)) {
            value.deleteEmptyStacks = static_cast<bool>(field_value);
        }
        if (get_request_value(p_request, "durationInSeconds", "duration_in_seconds", &field_value)) {
            durationInSeconds_storage = static_cast<double>(static_cast<double>(field_value));
            value.durationInSeconds = &durationInSeconds_storage;
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "eTag", "e_tag", &field_value)) {
            eTag_string = String(field_value).utf8().get_data();
            value.eTag = eTag_string.empty() ? nullptr : eTag_string.c_str();
        }
        if (get_request_value(p_request, "idempotencyId", "idempotency_id", &field_value)) {
            idempotencyId_string = String(field_value).utf8().get_data();
            value.idempotencyId = idempotencyId_string.empty() ? nullptr : idempotencyId_string.c_str();
        }
        if (get_request_value(p_request, "item", "item", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field item."; return false; }
            if (!item_owner.from_dictionary(field_value, r_error)) return false;
            item_has_value = true;
            value.item = &item_owner.value;
        }
        return true;
    }
};

struct OwnedPFInventorySubtractInventoryItemsResponse {
    PFInventorySubtractInventoryItemsResponse value = {};
    std::string eTag_string;
    std::string idempotencyId_string;
    std::vector<std::string> transactionIds_strings;
    std::vector<const char *> transactionIds_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "eTag", "e_tag", &field_value)) {
            eTag_string = String(field_value).utf8().get_data();
            value.eTag = eTag_string.empty() ? nullptr : eTag_string.c_str();
        }
        if (get_request_value(p_request, "idempotencyId", "idempotency_id", &field_value)) {
            idempotencyId_string = String(field_value).utf8().get_data();
            value.idempotencyId = idempotencyId_string.empty() ? nullptr : idempotencyId_string.c_str();
        }
        if (get_request_value(p_request, "transactionIds", "transaction_ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field transaction_ids."; return false; }
            Array source_array = field_value;
            transactionIds_strings.clear(); transactionIds_ptrs.clear();
            transactionIds_strings.reserve(source_array.size()); transactionIds_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                transactionIds_strings.emplace_back(String(source_array[i]).utf8().get_data());
                transactionIds_ptrs.push_back(transactionIds_strings.back().c_str());
            }
            value.transactionIds = transactionIds_ptrs.empty() ? nullptr : transactionIds_ptrs.data();
            value.transactionIdsCount = static_cast<uint32_t>(transactionIds_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFInventoryTransferInventoryItemsRequest {
    PFInventoryTransferInventoryItemsRequest value = {};
    int32_t amount_storage = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    std::string givingCollectionId_string;
    OwnedPFEntityKey givingEntity_owner;
    bool givingEntity_has_value = false;
    std::string givingETag_string;
    OwnedPFInventoryInventoryItemReference givingItem_owner;
    bool givingItem_has_value = false;
    std::string idempotencyId_string;
    OwnedPFInventoryInitialValues newStackValues_owner;
    bool newStackValues_has_value = false;
    std::string receivingCollectionId_string;
    OwnedPFEntityKey receivingEntity_owner;
    bool receivingEntity_has_value = false;
    OwnedPFInventoryInventoryItemReference receivingItem_owner;
    bool receivingItem_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "amount", "amount", &field_value)) {
            amount_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.amount = &amount_storage;
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "deleteEmptyStacks", "delete_empty_stacks", &field_value)) {
            value.deleteEmptyStacks = static_cast<bool>(field_value);
        }
        if (get_request_value(p_request, "givingCollectionId", "giving_collection_id", &field_value)) {
            givingCollectionId_string = String(field_value).utf8().get_data();
            value.givingCollectionId = givingCollectionId_string.empty() ? nullptr : givingCollectionId_string.c_str();
        }
        if (get_request_value(p_request, "givingEntity", "giving_entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field giving_entity."; return false; }
            if (!givingEntity_owner.from_dictionary(field_value, r_error)) return false;
            givingEntity_has_value = true;
            value.givingEntity = &givingEntity_owner.value;
        }
        if (get_request_value(p_request, "givingETag", "giving_e_tag", &field_value)) {
            givingETag_string = String(field_value).utf8().get_data();
            value.givingETag = givingETag_string.empty() ? nullptr : givingETag_string.c_str();
        }
        if (get_request_value(p_request, "givingItem", "giving_item", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field giving_item."; return false; }
            if (!givingItem_owner.from_dictionary(field_value, r_error)) return false;
            givingItem_has_value = true;
            value.givingItem = &givingItem_owner.value;
        }
        if (get_request_value(p_request, "idempotencyId", "idempotency_id", &field_value)) {
            idempotencyId_string = String(field_value).utf8().get_data();
            value.idempotencyId = idempotencyId_string.empty() ? nullptr : idempotencyId_string.c_str();
        }
        if (get_request_value(p_request, "newStackValues", "new_stack_values", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field new_stack_values."; return false; }
            if (!newStackValues_owner.from_dictionary(field_value, r_error)) return false;
            newStackValues_has_value = true;
            value.newStackValues = &newStackValues_owner.value;
        }
        if (get_request_value(p_request, "receivingCollectionId", "receiving_collection_id", &field_value)) {
            receivingCollectionId_string = String(field_value).utf8().get_data();
            value.receivingCollectionId = receivingCollectionId_string.empty() ? nullptr : receivingCollectionId_string.c_str();
        }
        if (get_request_value(p_request, "receivingEntity", "receiving_entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field receiving_entity."; return false; }
            if (!receivingEntity_owner.from_dictionary(field_value, r_error)) return false;
            receivingEntity_has_value = true;
            value.receivingEntity = &receivingEntity_owner.value;
        }
        if (get_request_value(p_request, "receivingItem", "receiving_item", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field receiving_item."; return false; }
            if (!receivingItem_owner.from_dictionary(field_value, r_error)) return false;
            receivingItem_has_value = true;
            value.receivingItem = &receivingItem_owner.value;
        }
        return true;
    }
};

struct OwnedPFInventoryTransferInventoryItemsResponse {
    PFInventoryTransferInventoryItemsResponse value = {};
    std::string givingETag_string;
    std::vector<std::string> givingTransactionIds_strings;
    std::vector<const char *> givingTransactionIds_ptrs;
    std::string idempotencyId_string;
    std::string operationStatus_string;
    std::string operationToken_string;
    std::vector<std::string> receivingTransactionIds_strings;
    std::vector<const char *> receivingTransactionIds_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "givingETag", "giving_e_tag", &field_value)) {
            givingETag_string = String(field_value).utf8().get_data();
            value.givingETag = givingETag_string.empty() ? nullptr : givingETag_string.c_str();
        }
        if (get_request_value(p_request, "givingTransactionIds", "giving_transaction_ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field giving_transaction_ids."; return false; }
            Array source_array = field_value;
            givingTransactionIds_strings.clear(); givingTransactionIds_ptrs.clear();
            givingTransactionIds_strings.reserve(source_array.size()); givingTransactionIds_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                givingTransactionIds_strings.emplace_back(String(source_array[i]).utf8().get_data());
                givingTransactionIds_ptrs.push_back(givingTransactionIds_strings.back().c_str());
            }
            value.givingTransactionIds = givingTransactionIds_ptrs.empty() ? nullptr : givingTransactionIds_ptrs.data();
            value.givingTransactionIdsCount = static_cast<uint32_t>(givingTransactionIds_ptrs.size());
        }
        if (get_request_value(p_request, "idempotencyId", "idempotency_id", &field_value)) {
            idempotencyId_string = String(field_value).utf8().get_data();
            value.idempotencyId = idempotencyId_string.empty() ? nullptr : idempotencyId_string.c_str();
        }
        if (get_request_value(p_request, "operationStatus", "operation_status", &field_value)) {
            operationStatus_string = String(field_value).utf8().get_data();
            value.operationStatus = operationStatus_string.empty() ? nullptr : operationStatus_string.c_str();
        }
        if (get_request_value(p_request, "operationToken", "operation_token", &field_value)) {
            operationToken_string = String(field_value).utf8().get_data();
            value.operationToken = operationToken_string.empty() ? nullptr : operationToken_string.c_str();
        }
        if (get_request_value(p_request, "receivingTransactionIds", "receiving_transaction_ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field receiving_transaction_ids."; return false; }
            Array source_array = field_value;
            receivingTransactionIds_strings.clear(); receivingTransactionIds_ptrs.clear();
            receivingTransactionIds_strings.reserve(source_array.size()); receivingTransactionIds_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                receivingTransactionIds_strings.emplace_back(String(source_array[i]).utf8().get_data());
                receivingTransactionIds_ptrs.push_back(receivingTransactionIds_strings.back().c_str());
            }
            value.receivingTransactionIds = receivingTransactionIds_ptrs.empty() ? nullptr : receivingTransactionIds_ptrs.data();
            value.receivingTransactionIdsCount = static_cast<uint32_t>(receivingTransactionIds_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFInventoryUpdateInventoryItemsRequest {
    PFInventoryUpdateInventoryItemsRequest value = {};
    std::string collectionId_string;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::string eTag_string;
    std::string idempotencyId_string;
    OwnedPFInventoryInventoryItem item_owner;
    bool item_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "collectionId", "collection_id", &field_value)) {
            collectionId_string = String(field_value).utf8().get_data();
            value.collectionId = collectionId_string.empty() ? nullptr : collectionId_string.c_str();
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "eTag", "e_tag", &field_value)) {
            eTag_string = String(field_value).utf8().get_data();
            value.eTag = eTag_string.empty() ? nullptr : eTag_string.c_str();
        }
        if (get_request_value(p_request, "idempotencyId", "idempotency_id", &field_value)) {
            idempotencyId_string = String(field_value).utf8().get_data();
            value.idempotencyId = idempotencyId_string.empty() ? nullptr : idempotencyId_string.c_str();
        }
        if (get_request_value(p_request, "item", "item", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field item."; return false; }
            if (!item_owner.from_dictionary(field_value, r_error)) return false;
            item_has_value = true;
            value.item = &item_owner.value;
        }
        return true;
    }
};

struct OwnedPFInventoryUpdateInventoryItemsResponse {
    PFInventoryUpdateInventoryItemsResponse value = {};
    std::string eTag_string;
    std::string idempotencyId_string;
    std::vector<std::string> transactionIds_strings;
    std::vector<const char *> transactionIds_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "eTag", "e_tag", &field_value)) {
            eTag_string = String(field_value).utf8().get_data();
            value.eTag = eTag_string.empty() ? nullptr : eTag_string.c_str();
        }
        if (get_request_value(p_request, "idempotencyId", "idempotency_id", &field_value)) {
            idempotencyId_string = String(field_value).utf8().get_data();
            value.idempotencyId = idempotencyId_string.empty() ? nullptr : idempotencyId_string.c_str();
        }
        if (get_request_value(p_request, "transactionIds", "transaction_ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field transaction_ids."; return false; }
            Array source_array = field_value;
            transactionIds_strings.clear(); transactionIds_ptrs.clear();
            transactionIds_strings.reserve(source_array.size()); transactionIds_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                transactionIds_strings.emplace_back(String(source_array[i]).utf8().get_data());
                transactionIds_ptrs.push_back(transactionIds_strings.back().c_str());
            }
            value.transactionIds = transactionIds_ptrs.empty() ? nullptr : transactionIds_ptrs.data();
            value.transactionIdsCount = static_cast<uint32_t>(transactionIds_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFLocalizationGetLanguageListRequest {
    PFLocalizationGetLanguageListRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        return true;
    }
};

struct OwnedPFLocalizationGetLanguageListResponse {
    PFLocalizationGetLanguageListResponse value = {};
    std::vector<std::string> languageList_strings;
    std::vector<const char *> languageList_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "languageList", "language_list", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field language_list."; return false; }
            Array source_array = field_value;
            languageList_strings.clear(); languageList_ptrs.clear();
            languageList_strings.reserve(source_array.size()); languageList_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                languageList_strings.emplace_back(String(source_array[i]).utf8().get_data());
                languageList_ptrs.push_back(languageList_strings.back().c_str());
            }
            value.languageList = languageList_ptrs.empty() ? nullptr : languageList_ptrs.data();
            value.languageListCount = static_cast<uint32_t>(languageList_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFPlayerDataManagementClientDeletePlayerCustomPropertiesRequest {
    PFPlayerDataManagementClientDeletePlayerCustomPropertiesRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    int32_t expectedPropertiesVersion_storage = {};
    std::vector<std::string> propertyNames_strings;
    std::vector<const char *> propertyNames_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "expectedPropertiesVersion", "expected_properties_version", &field_value)) {
            expectedPropertiesVersion_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.expectedPropertiesVersion = &expectedPropertiesVersion_storage;
        }
        if (get_request_value(p_request, "propertyNames", "property_names", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field property_names."; return false; }
            Array source_array = field_value;
            propertyNames_strings.clear(); propertyNames_ptrs.clear();
            propertyNames_strings.reserve(source_array.size()); propertyNames_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                propertyNames_strings.emplace_back(String(source_array[i]).utf8().get_data());
                propertyNames_ptrs.push_back(propertyNames_strings.back().c_str());
            }
            value.propertyNames = propertyNames_ptrs.empty() ? nullptr : propertyNames_ptrs.data();
            value.propertyNamesCount = static_cast<uint32_t>(propertyNames_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFPlayerDataManagementDeletedPropertyDetails {
    PFPlayerDataManagementDeletedPropertyDetails value = {};
    std::string name_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "name", "name", &field_value)) {
            name_string = String(field_value).utf8().get_data();
            value.name = name_string.empty() ? nullptr : name_string.c_str();
        }
        if (get_request_value(p_request, "wasDeleted", "was_deleted", &field_value)) {
            value.wasDeleted = static_cast<bool>(field_value);
        }
        return true;
    }
};

struct OwnedPFPlayerDataManagementClientDeletePlayerCustomPropertiesResult {
    PFPlayerDataManagementClientDeletePlayerCustomPropertiesResult value = {};
    std::vector<OwnedPFPlayerDataManagementDeletedPropertyDetails> deletedProperties_items;
    std::vector<const PFPlayerDataManagementDeletedPropertyDetails *> deletedProperties_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "deletedProperties", "deleted_properties", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field deleted_properties."; return false; }
            Array source_array = field_value;
            deletedProperties_items.resize(source_array.size());
            deletedProperties_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field deleted_properties."; return false; }
                if (!deletedProperties_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                deletedProperties_ptrs[static_cast<size_t>(i)] = &deletedProperties_items[static_cast<size_t>(i)].value;
            }
            value.deletedProperties = deletedProperties_ptrs.empty() ? nullptr : deletedProperties_ptrs.data();
            value.deletedPropertiesCount = static_cast<uint32_t>(deletedProperties_ptrs.size());
        }
        if (get_request_value(p_request, "propertiesVersion", "properties_version", &field_value)) {
            value.propertiesVersion = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        return true;
    }
};

struct OwnedPFPlayerDataManagementClientGetPlayerCustomPropertyRequest {
    PFPlayerDataManagementClientGetPlayerCustomPropertyRequest value = {};
    std::string propertyName_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "propertyName", "property_name", &field_value)) {
            propertyName_string = String(field_value).utf8().get_data();
            value.propertyName = propertyName_string.empty() ? nullptr : propertyName_string.c_str();
        }
        return true;
    }
};

struct OwnedPFPlayerDataManagementCustomPropertyDetails {
    PFPlayerDataManagementCustomPropertyDetails value = {};
    std::string name_string;
    std::string value_json;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "name", "name", &field_value)) {
            name_string = String(field_value).utf8().get_data();
            value.name = name_string.empty() ? nullptr : name_string.c_str();
        }
        if (get_request_value(p_request, "value", "value", &field_value)) {
            if (field_value.get_type() == Variant::STRING) {
                value_json = String(field_value).utf8().get_data();
            } else {
                value_json = variant_to_json_string(field_value).utf8().get_data();
            }
            value.value.stringValue = value_json.empty() ? nullptr : value_json.c_str();
        }
        return true;
    }
};

struct OwnedPFPlayerDataManagementClientGetPlayerCustomPropertyResult {
    PFPlayerDataManagementClientGetPlayerCustomPropertyResult value = {};
    OwnedPFPlayerDataManagementCustomPropertyDetails property_owner;
    bool property_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "propertiesVersion", "properties_version", &field_value)) {
            value.propertiesVersion = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "property", "property", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field property."; return false; }
            if (!property_owner.from_dictionary(field_value, r_error)) return false;
            property_has_value = true;
            value.property = &property_owner.value;
        }
        return true;
    }
};

struct OwnedPFPlayerDataManagementClientGetUserDataResult {
    PFPlayerDataManagementClientGetUserDataResult value = {};
    std::vector<OwnedPFUserDataRecordDictionaryEntry> data_items;
    std::vector<PFUserDataRecordDictionaryEntry> data_values;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "data", "data", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field data."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            data_items.resize(keys.size());
            data_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!data_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                data_values[static_cast<size_t>(i)] = data_items[static_cast<size_t>(i)].value;
            }
            value.data = data_values.empty() ? nullptr : data_values.data();
            value.dataCount = static_cast<uint32_t>(data_values.size());
        }
        if (get_request_value(p_request, "dataVersion", "data_version", &field_value)) {
            value.dataVersion = static_cast<uint32_t>(static_cast<int64_t>(field_value));
        }
        return true;
    }
};

struct OwnedPFPlayerDataManagementClientListPlayerCustomPropertiesResult {
    PFPlayerDataManagementClientListPlayerCustomPropertiesResult value = {};
    std::vector<OwnedPFPlayerDataManagementCustomPropertyDetails> properties_items;
    std::vector<const PFPlayerDataManagementCustomPropertyDetails *> properties_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "properties", "properties", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field properties."; return false; }
            Array source_array = field_value;
            properties_items.resize(source_array.size());
            properties_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field properties."; return false; }
                if (!properties_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                properties_ptrs[static_cast<size_t>(i)] = &properties_items[static_cast<size_t>(i)].value;
            }
            value.properties = properties_ptrs.empty() ? nullptr : properties_ptrs.data();
            value.propertiesCount = static_cast<uint32_t>(properties_ptrs.size());
        }
        if (get_request_value(p_request, "propertiesVersion", "properties_version", &field_value)) {
            value.propertiesVersion = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        return true;
    }
};

struct OwnedPFPlayerDataManagementUpdateProperty {
    PFPlayerDataManagementUpdateProperty value = {};
    std::string name_string;
    std::string value_json;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "name", "name", &field_value)) {
            name_string = String(field_value).utf8().get_data();
            value.name = name_string.empty() ? nullptr : name_string.c_str();
        }
        if (get_request_value(p_request, "value", "value", &field_value)) {
            if (field_value.get_type() == Variant::STRING) {
                value_json = String(field_value).utf8().get_data();
            } else {
                value_json = variant_to_json_string(field_value).utf8().get_data();
            }
            value.value.stringValue = value_json.empty() ? nullptr : value_json.c_str();
        }
        return true;
    }
};

struct OwnedPFPlayerDataManagementClientUpdatePlayerCustomPropertiesRequest {
    PFPlayerDataManagementClientUpdatePlayerCustomPropertiesRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    int32_t expectedPropertiesVersion_storage = {};
    std::vector<OwnedPFPlayerDataManagementUpdateProperty> properties_items;
    std::vector<const PFPlayerDataManagementUpdateProperty *> properties_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "expectedPropertiesVersion", "expected_properties_version", &field_value)) {
            expectedPropertiesVersion_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.expectedPropertiesVersion = &expectedPropertiesVersion_storage;
        }
        if (get_request_value(p_request, "properties", "properties", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field properties."; return false; }
            Array source_array = field_value;
            properties_items.resize(source_array.size());
            properties_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field properties."; return false; }
                if (!properties_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                properties_ptrs[static_cast<size_t>(i)] = &properties_items[static_cast<size_t>(i)].value;
            }
            value.properties = properties_ptrs.empty() ? nullptr : properties_ptrs.data();
            value.propertiesCount = static_cast<uint32_t>(properties_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFPlayerDataManagementClientUpdatePlayerCustomPropertiesResult {
    PFPlayerDataManagementClientUpdatePlayerCustomPropertiesResult value = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "propertiesVersion", "properties_version", &field_value)) {
            value.propertiesVersion = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        return true;
    }
};

struct OwnedPFPlayerDataManagementClientUpdateUserDataRequest {
    PFPlayerDataManagementClientUpdateUserDataRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    std::vector<OwnedPFStringDictionaryEntry> data_items;
    std::vector<PFStringDictionaryEntry> data_values;
    std::vector<std::string> keysToRemove_strings;
    std::vector<const char *> keysToRemove_ptrs;
    PFUserDataPermission permission_storage = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "data", "data", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field data."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            data_items.resize(keys.size());
            data_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!data_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                data_values[static_cast<size_t>(i)] = data_items[static_cast<size_t>(i)].value;
            }
            value.data = data_values.empty() ? nullptr : data_values.data();
            value.dataCount = static_cast<uint32_t>(data_values.size());
        }
        if (get_request_value(p_request, "keysToRemove", "keys_to_remove", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field keys_to_remove."; return false; }
            Array source_array = field_value;
            keysToRemove_strings.clear(); keysToRemove_ptrs.clear();
            keysToRemove_strings.reserve(source_array.size()); keysToRemove_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                keysToRemove_strings.emplace_back(String(source_array[i]).utf8().get_data());
                keysToRemove_ptrs.push_back(keysToRemove_strings.back().c_str());
            }
            value.keysToRemove = keysToRemove_ptrs.empty() ? nullptr : keysToRemove_ptrs.data();
            value.keysToRemoveCount = static_cast<uint32_t>(keysToRemove_ptrs.size());
        }
        if (get_request_value(p_request, "permission", "permission", &field_value)) {
            permission_storage = static_cast<PFUserDataPermission>(static_cast<int64_t>(field_value));
            value.permission = &permission_storage;
        }
        return true;
    }
};

struct OwnedPFPlayerDataManagementGetUserDataRequest {
    PFPlayerDataManagementGetUserDataRequest value = {};
    uint32_t ifChangedFromDataVersion_storage = {};
    std::vector<std::string> keys_strings;
    std::vector<const char *> keys_ptrs;
    std::string playFabId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "ifChangedFromDataVersion", "if_changed_from_data_version", &field_value)) {
            ifChangedFromDataVersion_storage = static_cast<uint32_t>(static_cast<int64_t>(field_value));
            value.ifChangedFromDataVersion = &ifChangedFromDataVersion_storage;
        }
        if (get_request_value(p_request, "keys", "keys", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field keys."; return false; }
            Array source_array = field_value;
            keys_strings.clear(); keys_ptrs.clear();
            keys_strings.reserve(source_array.size()); keys_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                keys_strings.emplace_back(String(source_array[i]).utf8().get_data());
                keys_ptrs.push_back(keys_strings.back().c_str());
            }
            value.keys = keys_ptrs.empty() ? nullptr : keys_ptrs.data();
            value.keysCount = static_cast<uint32_t>(keys_ptrs.size());
        }
        if (get_request_value(p_request, "playFabId", "play_fab_id", &field_value)) {
            playFabId_string = String(field_value).utf8().get_data();
            value.playFabId = playFabId_string.empty() ? nullptr : playFabId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFPlayerDataManagementUpdateUserDataResult {
    PFPlayerDataManagementUpdateUserDataResult value = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "dataVersion", "data_version", &field_value)) {
            value.dataVersion = static_cast<uint32_t>(static_cast<int64_t>(field_value));
        }
        return true;
    }
};

struct OwnedPFProfilesEntityDataObject {
    PFProfilesEntityDataObject value = {};
    std::string dataObject_json;
    std::string escapedDataObject_string;
    std::string objectName_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "dataObject", "data_object", &field_value)) {
            if (field_value.get_type() == Variant::STRING) {
                dataObject_json = String(field_value).utf8().get_data();
            } else {
                dataObject_json = variant_to_json_string(field_value).utf8().get_data();
            }
            value.dataObject.stringValue = dataObject_json.empty() ? nullptr : dataObject_json.c_str();
        }
        if (get_request_value(p_request, "escapedDataObject", "escaped_data_object", &field_value)) {
            escapedDataObject_string = String(field_value).utf8().get_data();
            value.escapedDataObject = escapedDataObject_string.empty() ? nullptr : escapedDataObject_string.c_str();
        }
        if (get_request_value(p_request, "objectName", "object_name", &field_value)) {
            objectName_string = String(field_value).utf8().get_data();
            value.objectName = objectName_string.empty() ? nullptr : objectName_string.c_str();
        }
        return true;
    }
};

struct OwnedPFProfilesEntityDataObjectDictionaryEntry {
    PFProfilesEntityDataObjectDictionaryEntry value = {};
    std::string key_string;
    OwnedPFProfilesEntityDataObject value_owner;
    bool value_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "key", "key", &field_value)) {
            key_string = String(field_value).utf8().get_data();
            value.key = key_string.empty() ? nullptr : key_string.c_str();
        }
        if (get_request_value(p_request, "value", "value", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field value."; return false; }
            if (!value_owner.from_dictionary(field_value, r_error)) return false;
            value_has_value = true;
            value.value = &value_owner.value;
        }
        return true;
    }
};

struct OwnedPFProfilesEntityPermissionStatement {
    PFProfilesEntityPermissionStatement value = {};
    std::string action_string;
    std::string comment_string;
    std::string condition_json;
    std::string principal_json;
    std::string resource_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "action", "action", &field_value)) {
            action_string = String(field_value).utf8().get_data();
            value.action = action_string.empty() ? nullptr : action_string.c_str();
        }
        if (get_request_value(p_request, "comment", "comment", &field_value)) {
            comment_string = String(field_value).utf8().get_data();
            value.comment = comment_string.empty() ? nullptr : comment_string.c_str();
        }
        if (get_request_value(p_request, "condition", "condition", &field_value)) {
            if (field_value.get_type() == Variant::STRING) {
                condition_json = String(field_value).utf8().get_data();
            } else {
                condition_json = variant_to_json_string(field_value).utf8().get_data();
            }
            value.condition.stringValue = condition_json.empty() ? nullptr : condition_json.c_str();
        }
        if (get_request_value(p_request, "effect", "effect", &field_value)) {
            value.effect = static_cast<PFProfilesEffectType>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "principal", "principal", &field_value)) {
            if (field_value.get_type() == Variant::STRING) {
                principal_json = String(field_value).utf8().get_data();
            } else {
                principal_json = variant_to_json_string(field_value).utf8().get_data();
            }
            value.principal.stringValue = principal_json.empty() ? nullptr : principal_json.c_str();
        }
        if (get_request_value(p_request, "resource", "resource", &field_value)) {
            resource_string = String(field_value).utf8().get_data();
            value.resource = resource_string.empty() ? nullptr : resource_string.c_str();
        }
        return true;
    }
};

struct OwnedPFProfilesEntityProfileFileMetadata {
    PFProfilesEntityProfileFileMetadata value = {};
    std::string checksum_string;
    std::string fileName_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "checksum", "checksum", &field_value)) {
            checksum_string = String(field_value).utf8().get_data();
            value.checksum = checksum_string.empty() ? nullptr : checksum_string.c_str();
        }
        if (get_request_value(p_request, "fileName", "file_name", &field_value)) {
            fileName_string = String(field_value).utf8().get_data();
            value.fileName = fileName_string.empty() ? nullptr : fileName_string.c_str();
        }
        if (get_request_value(p_request, "lastModified", "last_modified", &field_value)) {
            value.lastModified = static_cast<time_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "size", "size", &field_value)) {
            value.size = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        return true;
    }
};

struct OwnedPFProfilesEntityProfileFileMetadataDictionaryEntry {
    PFProfilesEntityProfileFileMetadataDictionaryEntry value = {};
    std::string key_string;
    OwnedPFProfilesEntityProfileFileMetadata value_owner;
    bool value_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "key", "key", &field_value)) {
            key_string = String(field_value).utf8().get_data();
            value.key = key_string.empty() ? nullptr : key_string.c_str();
        }
        if (get_request_value(p_request, "value", "value", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field value."; return false; }
            if (!value_owner.from_dictionary(field_value, r_error)) return false;
            value_has_value = true;
            value.value = &value_owner.value;
        }
        return true;
    }
};

struct OwnedPFProfilesEntityProfileBody {
    PFProfilesEntityProfileBody value = {};
    std::string avatarUrl_string;
    std::string displayName_string;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::string entityChain_string;
    std::vector<std::string> experimentVariants_strings;
    std::vector<const char *> experimentVariants_ptrs;
    std::vector<OwnedPFProfilesEntityProfileFileMetadataDictionaryEntry> files_items;
    std::vector<PFProfilesEntityProfileFileMetadataDictionaryEntry> files_values;
    std::string language_string;
    OwnedPFEntityLineage lineage_owner;
    bool lineage_has_value = false;
    std::vector<OwnedPFProfilesEntityDataObjectDictionaryEntry> objects_items;
    std::vector<PFProfilesEntityDataObjectDictionaryEntry> objects_values;
    std::vector<OwnedPFProfilesEntityPermissionStatement> permissions_items;
    std::vector<const PFProfilesEntityPermissionStatement *> permissions_ptrs;
    std::vector<OwnedPFEntityStatisticValueDictionaryEntry> statistics_items;
    std::vector<PFEntityStatisticValueDictionaryEntry> statistics_values;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "avatarUrl", "avatar_url", &field_value)) {
            avatarUrl_string = String(field_value).utf8().get_data();
            value.avatarUrl = avatarUrl_string.empty() ? nullptr : avatarUrl_string.c_str();
        }
        if (get_request_value(p_request, "created", "created", &field_value)) {
            value.created = static_cast<time_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "displayName", "display_name", &field_value)) {
            displayName_string = String(field_value).utf8().get_data();
            value.displayName = displayName_string.empty() ? nullptr : displayName_string.c_str();
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "entityChain", "entity_chain", &field_value)) {
            entityChain_string = String(field_value).utf8().get_data();
            value.entityChain = entityChain_string.empty() ? nullptr : entityChain_string.c_str();
        }
        if (get_request_value(p_request, "experimentVariants", "experiment_variants", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field experiment_variants."; return false; }
            Array source_array = field_value;
            experimentVariants_strings.clear(); experimentVariants_ptrs.clear();
            experimentVariants_strings.reserve(source_array.size()); experimentVariants_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                experimentVariants_strings.emplace_back(String(source_array[i]).utf8().get_data());
                experimentVariants_ptrs.push_back(experimentVariants_strings.back().c_str());
            }
            value.experimentVariants = experimentVariants_ptrs.empty() ? nullptr : experimentVariants_ptrs.data();
            value.experimentVariantsCount = static_cast<uint32_t>(experimentVariants_ptrs.size());
        }
        if (get_request_value(p_request, "files", "files", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field files."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            files_items.resize(keys.size());
            files_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!files_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                files_values[static_cast<size_t>(i)] = files_items[static_cast<size_t>(i)].value;
            }
            value.files = files_values.empty() ? nullptr : files_values.data();
            value.filesCount = static_cast<uint32_t>(files_values.size());
        }
        if (get_request_value(p_request, "language", "language", &field_value)) {
            language_string = String(field_value).utf8().get_data();
            value.language = language_string.empty() ? nullptr : language_string.c_str();
        }
        if (get_request_value(p_request, "lineage", "lineage", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field lineage."; return false; }
            if (!lineage_owner.from_dictionary(field_value, r_error)) return false;
            lineage_has_value = true;
            value.lineage = &lineage_owner.value;
        }
        if (get_request_value(p_request, "objects", "objects", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field objects."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            objects_items.resize(keys.size());
            objects_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!objects_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                objects_values[static_cast<size_t>(i)] = objects_items[static_cast<size_t>(i)].value;
            }
            value.objects = objects_values.empty() ? nullptr : objects_values.data();
            value.objectsCount = static_cast<uint32_t>(objects_values.size());
        }
        if (get_request_value(p_request, "permissions", "permissions", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field permissions."; return false; }
            Array source_array = field_value;
            permissions_items.resize(source_array.size());
            permissions_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field permissions."; return false; }
                if (!permissions_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                permissions_ptrs[static_cast<size_t>(i)] = &permissions_items[static_cast<size_t>(i)].value;
            }
            value.permissions = permissions_ptrs.empty() ? nullptr : permissions_ptrs.data();
            value.permissionsCount = static_cast<uint32_t>(permissions_ptrs.size());
        }
        if (get_request_value(p_request, "statistics", "statistics", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field statistics."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            statistics_items.resize(keys.size());
            statistics_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!statistics_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                statistics_values[static_cast<size_t>(i)] = statistics_items[static_cast<size_t>(i)].value;
            }
            value.statistics = statistics_values.empty() ? nullptr : statistics_values.data();
            value.statisticsCount = static_cast<uint32_t>(statistics_values.size());
        }
        if (get_request_value(p_request, "versionNumber", "version_number", &field_value)) {
            value.versionNumber = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        return true;
    }
};

struct OwnedPFProfilesGetEntityProfileRequest {
    PFProfilesGetEntityProfileRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    bool dataAsObject_storage = {};
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "dataAsObject", "data_as_object", &field_value)) {
            dataAsObject_storage = static_cast<bool>(field_value);
            value.dataAsObject = &dataAsObject_storage;
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        return true;
    }
};

struct OwnedPFProfilesGetEntityProfileResponse {
    PFProfilesGetEntityProfileResponse value = {};
    OwnedPFProfilesEntityProfileBody profile_owner;
    bool profile_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "profile", "profile", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field profile."; return false; }
            if (!profile_owner.from_dictionary(field_value, r_error)) return false;
            profile_has_value = true;
            value.profile = &profile_owner.value;
        }
        return true;
    }
};

struct OwnedPFProfilesGetEntityProfilesRequest {
    PFProfilesGetEntityProfilesRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    bool dataAsObject_storage = {};
    std::vector<OwnedPFEntityKey> entities_items;
    std::vector<const PFEntityKey *> entities_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "dataAsObject", "data_as_object", &field_value)) {
            dataAsObject_storage = static_cast<bool>(field_value);
            value.dataAsObject = &dataAsObject_storage;
        }
        if (get_request_value(p_request, "entities", "entities", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field entities."; return false; }
            Array source_array = field_value;
            entities_items.resize(source_array.size());
            entities_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field entities."; return false; }
                if (!entities_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                entities_ptrs[static_cast<size_t>(i)] = &entities_items[static_cast<size_t>(i)].value;
            }
            value.entities = entities_ptrs.empty() ? nullptr : entities_ptrs.data();
            value.entitiesCount = static_cast<uint32_t>(entities_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFProfilesGetEntityProfilesResponse {
    PFProfilesGetEntityProfilesResponse value = {};
    std::vector<OwnedPFProfilesEntityProfileBody> profiles_items;
    std::vector<const PFProfilesEntityProfileBody *> profiles_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "profiles", "profiles", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field profiles."; return false; }
            Array source_array = field_value;
            profiles_items.resize(source_array.size());
            profiles_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field profiles."; return false; }
                if (!profiles_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                profiles_ptrs[static_cast<size_t>(i)] = &profiles_items[static_cast<size_t>(i)].value;
            }
            value.profiles = profiles_ptrs.empty() ? nullptr : profiles_ptrs.data();
            value.profilesCount = static_cast<uint32_t>(profiles_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFProfilesGetTitlePlayersFromMasterPlayerAccountIdsRequest {
    PFProfilesGetTitlePlayersFromMasterPlayerAccountIdsRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    std::vector<std::string> masterPlayerAccountIds_strings;
    std::vector<const char *> masterPlayerAccountIds_ptrs;
    std::string titleId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "masterPlayerAccountIds", "master_player_account_ids", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field master_player_account_ids."; return false; }
            Array source_array = field_value;
            masterPlayerAccountIds_strings.clear(); masterPlayerAccountIds_ptrs.clear();
            masterPlayerAccountIds_strings.reserve(source_array.size()); masterPlayerAccountIds_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                masterPlayerAccountIds_strings.emplace_back(String(source_array[i]).utf8().get_data());
                masterPlayerAccountIds_ptrs.push_back(masterPlayerAccountIds_strings.back().c_str());
            }
            value.masterPlayerAccountIds = masterPlayerAccountIds_ptrs.empty() ? nullptr : masterPlayerAccountIds_ptrs.data();
            value.masterPlayerAccountIdsCount = static_cast<uint32_t>(masterPlayerAccountIds_ptrs.size());
        }
        if (get_request_value(p_request, "titleId", "title_id", &field_value)) {
            titleId_string = String(field_value).utf8().get_data();
            value.titleId = titleId_string.empty() ? nullptr : titleId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFProfilesGetTitlePlayersFromMasterPlayerAccountIdsResponse {
    PFProfilesGetTitlePlayersFromMasterPlayerAccountIdsResponse value = {};
    std::string titleId_string;
    std::vector<OwnedPFEntityKeyDictionaryEntry> titlePlayerAccounts_items;
    std::vector<PFEntityKeyDictionaryEntry> titlePlayerAccounts_values;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "titleId", "title_id", &field_value)) {
            titleId_string = String(field_value).utf8().get_data();
            value.titleId = titleId_string.empty() ? nullptr : titleId_string.c_str();
        }
        if (get_request_value(p_request, "titlePlayerAccounts", "title_player_accounts", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field title_player_accounts."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            titlePlayerAccounts_items.resize(keys.size());
            titlePlayerAccounts_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!titlePlayerAccounts_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                titlePlayerAccounts_values[static_cast<size_t>(i)] = titlePlayerAccounts_items[static_cast<size_t>(i)].value;
            }
            value.titlePlayerAccounts = titlePlayerAccounts_values.empty() ? nullptr : titlePlayerAccounts_values.data();
            value.titlePlayerAccountsCount = static_cast<uint32_t>(titlePlayerAccounts_values.size());
        }
        return true;
    }
};

struct OwnedPFProfilesSetEntityProfilePolicyRequest {
    PFProfilesSetEntityProfilePolicyRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::vector<OwnedPFProfilesEntityPermissionStatement> statements_items;
    std::vector<const PFProfilesEntityPermissionStatement *> statements_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "statements", "statements", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field statements."; return false; }
            Array source_array = field_value;
            statements_items.resize(source_array.size());
            statements_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field statements."; return false; }
                if (!statements_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                statements_ptrs[static_cast<size_t>(i)] = &statements_items[static_cast<size_t>(i)].value;
            }
            value.statements = statements_ptrs.empty() ? nullptr : statements_ptrs.data();
            value.statementsCount = static_cast<uint32_t>(statements_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFProfilesSetEntityProfilePolicyResponse {
    PFProfilesSetEntityProfilePolicyResponse value = {};
    std::vector<OwnedPFProfilesEntityPermissionStatement> permissions_items;
    std::vector<const PFProfilesEntityPermissionStatement *> permissions_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "permissions", "permissions", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field permissions."; return false; }
            Array source_array = field_value;
            permissions_items.resize(source_array.size());
            permissions_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field permissions."; return false; }
                if (!permissions_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                permissions_ptrs[static_cast<size_t>(i)] = &permissions_items[static_cast<size_t>(i)].value;
            }
            value.permissions = permissions_ptrs.empty() ? nullptr : permissions_ptrs.data();
            value.permissionsCount = static_cast<uint32_t>(permissions_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFProfilesSetProfileLanguageRequest {
    PFProfilesSetProfileLanguageRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    int32_t expectedVersion_storage = {};
    std::string language_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "expectedVersion", "expected_version", &field_value)) {
            expectedVersion_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.expectedVersion = &expectedVersion_storage;
        }
        if (get_request_value(p_request, "language", "language", &field_value)) {
            language_string = String(field_value).utf8().get_data();
            value.language = language_string.empty() ? nullptr : language_string.c_str();
        }
        return true;
    }
};

struct OwnedPFProfilesSetProfileLanguageResponse {
    PFProfilesSetProfileLanguageResponse value = {};
    PFOperationTypes operationResult_storage = {};
    int32_t versionNumber_storage = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "operationResult", "operation_result", &field_value)) {
            operationResult_storage = static_cast<PFOperationTypes>(static_cast<int64_t>(field_value));
            value.operationResult = &operationResult_storage;
        }
        if (get_request_value(p_request, "versionNumber", "version_number", &field_value)) {
            versionNumber_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.versionNumber = &versionNumber_storage;
        }
        return true;
    }
};

struct OwnedPFStatisticsStatisticColumn {
    PFStatisticsStatisticColumn value = {};
    std::string name_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "aggregationMethod", "aggregation_method", &field_value)) {
            value.aggregationMethod = static_cast<PFStatisticsStatisticAggregationMethod>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "name", "name", &field_value)) {
            name_string = String(field_value).utf8().get_data();
            value.name = name_string.empty() ? nullptr : name_string.c_str();
        }
        return true;
    }
};

struct OwnedPFStatisticsStatisticsUpdateEventConfig {
    PFStatisticsStatisticsUpdateEventConfig value = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "eventType", "event_type", &field_value)) {
            value.eventType = static_cast<PFEventType>(static_cast<int64_t>(field_value));
        }
        return true;
    }
};

struct OwnedPFStatisticsStatisticsEventEmissionConfig {
    PFStatisticsStatisticsEventEmissionConfig value = {};
    OwnedPFStatisticsStatisticsUpdateEventConfig updateEventConfig_owner;
    bool updateEventConfig_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "updateEventConfig", "update_event_config", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field update_event_config."; return false; }
            if (!updateEventConfig_owner.from_dictionary(field_value, r_error)) return false;
            updateEventConfig_has_value = true;
            value.updateEventConfig = &updateEventConfig_owner.value;
        }
        return true;
    }
};

struct OwnedPFVersionConfiguration {
    PFVersionConfiguration value = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "maxQueryableVersions", "max_queryable_versions", &field_value)) {
            value.maxQueryableVersions = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "resetInterval", "reset_interval", &field_value)) {
            value.resetInterval = static_cast<PFResetInterval>(static_cast<int64_t>(field_value));
        }
        return true;
    }
};

struct OwnedPFStatisticsCreateStatisticDefinitionRequest {
    PFStatisticsCreateStatisticDefinitionRequest value = {};
    std::vector<std::string> aggregationSources_strings;
    std::vector<const char *> aggregationSources_ptrs;
    std::vector<OwnedPFStatisticsStatisticColumn> columns_items;
    std::vector<const PFStatisticsStatisticColumn *> columns_ptrs;
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    std::string entityType_string;
    OwnedPFStatisticsStatisticsEventEmissionConfig eventEmissionConfig_owner;
    bool eventEmissionConfig_has_value = false;
    std::string name_string;
    OwnedPFVersionConfiguration versionConfiguration_owner;
    bool versionConfiguration_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "aggregationSources", "aggregation_sources", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field aggregation_sources."; return false; }
            Array source_array = field_value;
            aggregationSources_strings.clear(); aggregationSources_ptrs.clear();
            aggregationSources_strings.reserve(source_array.size()); aggregationSources_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                aggregationSources_strings.emplace_back(String(source_array[i]).utf8().get_data());
                aggregationSources_ptrs.push_back(aggregationSources_strings.back().c_str());
            }
            value.aggregationSources = aggregationSources_ptrs.empty() ? nullptr : aggregationSources_ptrs.data();
            value.aggregationSourcesCount = static_cast<uint32_t>(aggregationSources_ptrs.size());
        }
        if (get_request_value(p_request, "columns", "columns", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field columns."; return false; }
            Array source_array = field_value;
            columns_items.resize(source_array.size());
            columns_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field columns."; return false; }
                if (!columns_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                columns_ptrs[static_cast<size_t>(i)] = &columns_items[static_cast<size_t>(i)].value;
            }
            value.columns = columns_ptrs.empty() ? nullptr : columns_ptrs.data();
            value.columnsCount = static_cast<uint32_t>(columns_ptrs.size());
        }
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entityType", "entity_type", &field_value)) {
            entityType_string = String(field_value).utf8().get_data();
            value.entityType = entityType_string.empty() ? nullptr : entityType_string.c_str();
        }
        if (get_request_value(p_request, "eventEmissionConfig", "event_emission_config", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field event_emission_config."; return false; }
            if (!eventEmissionConfig_owner.from_dictionary(field_value, r_error)) return false;
            eventEmissionConfig_has_value = true;
            value.eventEmissionConfig = &eventEmissionConfig_owner.value;
        }
        if (get_request_value(p_request, "name", "name", &field_value)) {
            name_string = String(field_value).utf8().get_data();
            value.name = name_string.empty() ? nullptr : name_string.c_str();
        }
        if (get_request_value(p_request, "versionConfiguration", "version_configuration", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field version_configuration."; return false; }
            if (!versionConfiguration_owner.from_dictionary(field_value, r_error)) return false;
            versionConfiguration_has_value = true;
            value.versionConfiguration = &versionConfiguration_owner.value;
        }
        return true;
    }
};

struct OwnedPFStatisticsDeleteStatisticDefinitionRequest {
    PFStatisticsDeleteStatisticDefinitionRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    std::string name_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "name", "name", &field_value)) {
            name_string = String(field_value).utf8().get_data();
            value.name = name_string.empty() ? nullptr : name_string.c_str();
        }
        return true;
    }
};

struct OwnedPFStatisticsStatisticDelete {
    PFStatisticsStatisticDelete value = {};
    std::string name_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "name", "name", &field_value)) {
            name_string = String(field_value).utf8().get_data();
            value.name = name_string.empty() ? nullptr : name_string.c_str();
        }
        return true;
    }
};

struct OwnedPFStatisticsDeleteStatisticsRequest {
    PFStatisticsDeleteStatisticsRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::vector<OwnedPFStatisticsStatisticDelete> statistics_items;
    std::vector<const PFStatisticsStatisticDelete *> statistics_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "statistics", "statistics", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field statistics."; return false; }
            Array source_array = field_value;
            statistics_items.resize(source_array.size());
            statistics_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field statistics."; return false; }
                if (!statistics_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                statistics_ptrs[static_cast<size_t>(i)] = &statistics_items[static_cast<size_t>(i)].value;
            }
            value.statistics = statistics_ptrs.empty() ? nullptr : statistics_ptrs.data();
            value.statisticsCount = static_cast<uint32_t>(statistics_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFStatisticsDeleteStatisticsResponse {
    PFStatisticsDeleteStatisticsResponse value = {};
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        return true;
    }
};

struct OwnedPFStatisticsEntityStatisticValue {
    PFStatisticsEntityStatisticValue value = {};
    std::string metadata_string;
    std::string name_string;
    std::vector<std::string> scores_strings;
    std::vector<const char *> scores_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "metadata", "metadata", &field_value)) {
            metadata_string = String(field_value).utf8().get_data();
            value.metadata = metadata_string.empty() ? nullptr : metadata_string.c_str();
        }
        if (get_request_value(p_request, "name", "name", &field_value)) {
            name_string = String(field_value).utf8().get_data();
            value.name = name_string.empty() ? nullptr : name_string.c_str();
        }
        if (get_request_value(p_request, "scores", "scores", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field scores."; return false; }
            Array source_array = field_value;
            scores_strings.clear(); scores_ptrs.clear();
            scores_strings.reserve(source_array.size()); scores_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                scores_strings.emplace_back(String(source_array[i]).utf8().get_data());
                scores_ptrs.push_back(scores_strings.back().c_str());
            }
            value.scores = scores_ptrs.empty() ? nullptr : scores_ptrs.data();
            value.scoresCount = static_cast<uint32_t>(scores_ptrs.size());
        }
        if (get_request_value(p_request, "version", "version", &field_value)) {
            value.version = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        return true;
    }
};

struct OwnedPFStatisticsEntityStatisticValueDictionaryEntry {
    PFStatisticsEntityStatisticValueDictionaryEntry value = {};
    std::string key_string;
    OwnedPFStatisticsEntityStatisticValue value_owner;
    bool value_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "key", "key", &field_value)) {
            key_string = String(field_value).utf8().get_data();
            value.key = key_string.empty() ? nullptr : key_string.c_str();
        }
        if (get_request_value(p_request, "value", "value", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field value."; return false; }
            if (!value_owner.from_dictionary(field_value, r_error)) return false;
            value_has_value = true;
            value.value = &value_owner.value;
        }
        return true;
    }
};

struct OwnedPFStatisticsEntityStatistics {
    PFStatisticsEntityStatistics value = {};
    OwnedPFEntityKey entityKey_owner;
    bool entityKey_has_value = false;
    std::vector<OwnedPFStatisticsEntityStatisticValue> statistics_items;
    std::vector<const PFStatisticsEntityStatisticValue *> statistics_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "entityKey", "entity_key", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity_key."; return false; }
            if (!entityKey_owner.from_dictionary(field_value, r_error)) return false;
            entityKey_has_value = true;
            value.entityKey = &entityKey_owner.value;
        }
        if (get_request_value(p_request, "statistics", "statistics", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field statistics."; return false; }
            Array source_array = field_value;
            statistics_items.resize(source_array.size());
            statistics_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field statistics."; return false; }
                if (!statistics_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                statistics_ptrs[static_cast<size_t>(i)] = &statistics_items[static_cast<size_t>(i)].value;
            }
            value.statistics = statistics_ptrs.empty() ? nullptr : statistics_ptrs.data();
            value.statisticsCount = static_cast<uint32_t>(statistics_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFStatisticsGetStatisticDefinitionRequest {
    PFStatisticsGetStatisticDefinitionRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    std::string name_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "name", "name", &field_value)) {
            name_string = String(field_value).utf8().get_data();
            value.name = name_string.empty() ? nullptr : name_string.c_str();
        }
        return true;
    }
};

struct OwnedPFStatisticsGetStatisticDefinitionResponse {
    PFStatisticsGetStatisticDefinitionResponse value = {};
    std::vector<std::string> aggregationDestinations_strings;
    std::vector<const char *> aggregationDestinations_ptrs;
    std::vector<std::string> aggregationSources_strings;
    std::vector<const char *> aggregationSources_ptrs;
    std::vector<OwnedPFStatisticsStatisticColumn> columns_items;
    std::vector<const PFStatisticsStatisticColumn *> columns_ptrs;
    std::string entityType_string;
    OwnedPFStatisticsStatisticsEventEmissionConfig eventEmissionConfig_owner;
    bool eventEmissionConfig_has_value = false;
    time_t lastResetTime_storage = {};
    std::vector<std::string> linkedLeaderboardNames_strings;
    std::vector<const char *> linkedLeaderboardNames_ptrs;
    std::string name_string;
    OwnedPFVersionConfiguration versionConfiguration_owner;
    bool versionConfiguration_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "aggregationDestinations", "aggregation_destinations", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field aggregation_destinations."; return false; }
            Array source_array = field_value;
            aggregationDestinations_strings.clear(); aggregationDestinations_ptrs.clear();
            aggregationDestinations_strings.reserve(source_array.size()); aggregationDestinations_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                aggregationDestinations_strings.emplace_back(String(source_array[i]).utf8().get_data());
                aggregationDestinations_ptrs.push_back(aggregationDestinations_strings.back().c_str());
            }
            value.aggregationDestinations = aggregationDestinations_ptrs.empty() ? nullptr : aggregationDestinations_ptrs.data();
            value.aggregationDestinationsCount = static_cast<uint32_t>(aggregationDestinations_ptrs.size());
        }
        if (get_request_value(p_request, "aggregationSources", "aggregation_sources", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field aggregation_sources."; return false; }
            Array source_array = field_value;
            aggregationSources_strings.clear(); aggregationSources_ptrs.clear();
            aggregationSources_strings.reserve(source_array.size()); aggregationSources_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                aggregationSources_strings.emplace_back(String(source_array[i]).utf8().get_data());
                aggregationSources_ptrs.push_back(aggregationSources_strings.back().c_str());
            }
            value.aggregationSources = aggregationSources_ptrs.empty() ? nullptr : aggregationSources_ptrs.data();
            value.aggregationSourcesCount = static_cast<uint32_t>(aggregationSources_ptrs.size());
        }
        if (get_request_value(p_request, "columns", "columns", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field columns."; return false; }
            Array source_array = field_value;
            columns_items.resize(source_array.size());
            columns_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field columns."; return false; }
                if (!columns_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                columns_ptrs[static_cast<size_t>(i)] = &columns_items[static_cast<size_t>(i)].value;
            }
            value.columns = columns_ptrs.empty() ? nullptr : columns_ptrs.data();
            value.columnsCount = static_cast<uint32_t>(columns_ptrs.size());
        }
        if (get_request_value(p_request, "created", "created", &field_value)) {
            value.created = static_cast<time_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "entityType", "entity_type", &field_value)) {
            entityType_string = String(field_value).utf8().get_data();
            value.entityType = entityType_string.empty() ? nullptr : entityType_string.c_str();
        }
        if (get_request_value(p_request, "eventEmissionConfig", "event_emission_config", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field event_emission_config."; return false; }
            if (!eventEmissionConfig_owner.from_dictionary(field_value, r_error)) return false;
            eventEmissionConfig_has_value = true;
            value.eventEmissionConfig = &eventEmissionConfig_owner.value;
        }
        if (get_request_value(p_request, "lastResetTime", "last_reset_time", &field_value)) {
            lastResetTime_storage = static_cast<time_t>(static_cast<int64_t>(field_value));
            value.lastResetTime = &lastResetTime_storage;
        }
        if (get_request_value(p_request, "linkedLeaderboardNames", "linked_leaderboard_names", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field linked_leaderboard_names."; return false; }
            Array source_array = field_value;
            linkedLeaderboardNames_strings.clear(); linkedLeaderboardNames_ptrs.clear();
            linkedLeaderboardNames_strings.reserve(source_array.size()); linkedLeaderboardNames_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                linkedLeaderboardNames_strings.emplace_back(String(source_array[i]).utf8().get_data());
                linkedLeaderboardNames_ptrs.push_back(linkedLeaderboardNames_strings.back().c_str());
            }
            value.linkedLeaderboardNames = linkedLeaderboardNames_ptrs.empty() ? nullptr : linkedLeaderboardNames_ptrs.data();
            value.linkedLeaderboardNamesCount = static_cast<uint32_t>(linkedLeaderboardNames_ptrs.size());
        }
        if (get_request_value(p_request, "name", "name", &field_value)) {
            name_string = String(field_value).utf8().get_data();
            value.name = name_string.empty() ? nullptr : name_string.c_str();
        }
        if (get_request_value(p_request, "version", "version", &field_value)) {
            value.version = static_cast<uint32_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "versionConfiguration", "version_configuration", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field version_configuration."; return false; }
            if (!versionConfiguration_owner.from_dictionary(field_value, r_error)) return false;
            versionConfiguration_has_value = true;
            value.versionConfiguration = &versionConfiguration_owner.value;
        }
        return true;
    }
};

struct OwnedPFStatisticsGetStatisticsForEntitiesRequest {
    PFStatisticsGetStatisticsForEntitiesRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    std::vector<OwnedPFEntityKey> entities_items;
    std::vector<const PFEntityKey *> entities_ptrs;
    std::vector<std::string> statisticNames_strings;
    std::vector<const char *> statisticNames_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entities", "entities", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field entities."; return false; }
            Array source_array = field_value;
            entities_items.resize(source_array.size());
            entities_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field entities."; return false; }
                if (!entities_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                entities_ptrs[static_cast<size_t>(i)] = &entities_items[static_cast<size_t>(i)].value;
            }
            value.entities = entities_ptrs.empty() ? nullptr : entities_ptrs.data();
            value.entitiesCount = static_cast<uint32_t>(entities_ptrs.size());
        }
        if (get_request_value(p_request, "statisticNames", "statistic_names", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field statistic_names."; return false; }
            Array source_array = field_value;
            statisticNames_strings.clear(); statisticNames_ptrs.clear();
            statisticNames_strings.reserve(source_array.size()); statisticNames_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                statisticNames_strings.emplace_back(String(source_array[i]).utf8().get_data());
                statisticNames_ptrs.push_back(statisticNames_strings.back().c_str());
            }
            value.statisticNames = statisticNames_ptrs.empty() ? nullptr : statisticNames_ptrs.data();
            value.statisticNamesCount = static_cast<uint32_t>(statisticNames_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFStatisticsStatisticColumnCollection {
    PFStatisticsStatisticColumnCollection value = {};
    std::vector<OwnedPFStatisticsStatisticColumn> columns_items;
    std::vector<const PFStatisticsStatisticColumn *> columns_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "columns", "columns", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field columns."; return false; }
            Array source_array = field_value;
            columns_items.resize(source_array.size());
            columns_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field columns."; return false; }
                if (!columns_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                columns_ptrs[static_cast<size_t>(i)] = &columns_items[static_cast<size_t>(i)].value;
            }
            value.columns = columns_ptrs.empty() ? nullptr : columns_ptrs.data();
            value.columnsCount = static_cast<uint32_t>(columns_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFStatisticsStatisticColumnCollectionDictionaryEntry {
    PFStatisticsStatisticColumnCollectionDictionaryEntry value = {};
    std::string key_string;
    OwnedPFStatisticsStatisticColumnCollection value_owner;
    bool value_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "key", "key", &field_value)) {
            key_string = String(field_value).utf8().get_data();
            value.key = key_string.empty() ? nullptr : key_string.c_str();
        }
        if (get_request_value(p_request, "value", "value", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field value."; return false; }
            if (!value_owner.from_dictionary(field_value, r_error)) return false;
            value_has_value = true;
            value.value = &value_owner.value;
        }
        return true;
    }
};

struct OwnedPFStatisticsGetStatisticsForEntitiesResponse {
    PFStatisticsGetStatisticsForEntitiesResponse value = {};
    std::vector<OwnedPFStatisticsStatisticColumnCollectionDictionaryEntry> columnDetails_items;
    std::vector<PFStatisticsStatisticColumnCollectionDictionaryEntry> columnDetails_values;
    std::vector<OwnedPFStatisticsEntityStatistics> entitiesStatistics_items;
    std::vector<const PFStatisticsEntityStatistics *> entitiesStatistics_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "columnDetails", "column_details", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field column_details."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            columnDetails_items.resize(keys.size());
            columnDetails_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!columnDetails_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                columnDetails_values[static_cast<size_t>(i)] = columnDetails_items[static_cast<size_t>(i)].value;
            }
            value.columnDetails = columnDetails_values.empty() ? nullptr : columnDetails_values.data();
            value.columnDetailsCount = static_cast<uint32_t>(columnDetails_values.size());
        }
        if (get_request_value(p_request, "entitiesStatistics", "entities_statistics", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field entities_statistics."; return false; }
            Array source_array = field_value;
            entitiesStatistics_items.resize(source_array.size());
            entitiesStatistics_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field entities_statistics."; return false; }
                if (!entitiesStatistics_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                entitiesStatistics_ptrs[static_cast<size_t>(i)] = &entitiesStatistics_items[static_cast<size_t>(i)].value;
            }
            value.entitiesStatistics = entitiesStatistics_ptrs.empty() ? nullptr : entitiesStatistics_ptrs.data();
            value.entitiesStatisticsCount = static_cast<uint32_t>(entitiesStatistics_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFStatisticsGetStatisticsRequest {
    PFStatisticsGetStatisticsRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::vector<std::string> statisticNames_strings;
    std::vector<const char *> statisticNames_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "statisticNames", "statistic_names", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field statistic_names."; return false; }
            Array source_array = field_value;
            statisticNames_strings.clear(); statisticNames_ptrs.clear();
            statisticNames_strings.reserve(source_array.size()); statisticNames_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                statisticNames_strings.emplace_back(String(source_array[i]).utf8().get_data());
                statisticNames_ptrs.push_back(statisticNames_strings.back().c_str());
            }
            value.statisticNames = statisticNames_ptrs.empty() ? nullptr : statisticNames_ptrs.data();
            value.statisticNamesCount = static_cast<uint32_t>(statisticNames_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFStatisticsGetStatisticsResponse {
    PFStatisticsGetStatisticsResponse value = {};
    std::vector<OwnedPFStatisticsStatisticColumnCollectionDictionaryEntry> columnDetails_items;
    std::vector<PFStatisticsStatisticColumnCollectionDictionaryEntry> columnDetails_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::vector<OwnedPFStatisticsEntityStatisticValueDictionaryEntry> statistics_items;
    std::vector<PFStatisticsEntityStatisticValueDictionaryEntry> statistics_values;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "columnDetails", "column_details", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field column_details."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            columnDetails_items.resize(keys.size());
            columnDetails_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!columnDetails_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                columnDetails_values[static_cast<size_t>(i)] = columnDetails_items[static_cast<size_t>(i)].value;
            }
            value.columnDetails = columnDetails_values.empty() ? nullptr : columnDetails_values.data();
            value.columnDetailsCount = static_cast<uint32_t>(columnDetails_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "statistics", "statistics", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field statistics."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            statistics_items.resize(keys.size());
            statistics_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!statistics_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                statistics_values[static_cast<size_t>(i)] = statistics_items[static_cast<size_t>(i)].value;
            }
            value.statistics = statistics_values.empty() ? nullptr : statistics_values.data();
            value.statisticsCount = static_cast<uint32_t>(statistics_values.size());
        }
        return true;
    }
};

struct OwnedPFStatisticsIncrementStatisticVersionRequest {
    PFStatisticsIncrementStatisticVersionRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    std::string name_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "name", "name", &field_value)) {
            name_string = String(field_value).utf8().get_data();
            value.name = name_string.empty() ? nullptr : name_string.c_str();
        }
        return true;
    }
};

struct OwnedPFStatisticsIncrementStatisticVersionResponse {
    PFStatisticsIncrementStatisticVersionResponse value = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "version", "version", &field_value)) {
            value.version = static_cast<uint32_t>(static_cast<int64_t>(field_value));
        }
        return true;
    }
};

struct OwnedPFStatisticsListStatisticDefinitionsRequest {
    PFStatisticsListStatisticDefinitionsRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    int32_t pageSize_storage = {};
    std::string skipToken_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "pageSize", "page_size", &field_value)) {
            pageSize_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.pageSize = &pageSize_storage;
        }
        if (get_request_value(p_request, "skipToken", "skip_token", &field_value)) {
            skipToken_string = String(field_value).utf8().get_data();
            value.skipToken = skipToken_string.empty() ? nullptr : skipToken_string.c_str();
        }
        return true;
    }
};

struct OwnedPFStatisticsStatisticDefinition {
    PFStatisticsStatisticDefinition value = {};
    std::vector<std::string> aggregationDestinations_strings;
    std::vector<const char *> aggregationDestinations_ptrs;
    std::vector<std::string> aggregationSources_strings;
    std::vector<const char *> aggregationSources_ptrs;
    std::vector<OwnedPFStatisticsStatisticColumn> columns_items;
    std::vector<const PFStatisticsStatisticColumn *> columns_ptrs;
    std::string entityType_string;
    OwnedPFStatisticsStatisticsEventEmissionConfig eventEmissionConfig_owner;
    bool eventEmissionConfig_has_value = false;
    time_t lastResetTime_storage = {};
    std::vector<std::string> linkedLeaderboardNames_strings;
    std::vector<const char *> linkedLeaderboardNames_ptrs;
    std::string name_string;
    OwnedPFVersionConfiguration versionConfiguration_owner;
    bool versionConfiguration_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "aggregationDestinations", "aggregation_destinations", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field aggregation_destinations."; return false; }
            Array source_array = field_value;
            aggregationDestinations_strings.clear(); aggregationDestinations_ptrs.clear();
            aggregationDestinations_strings.reserve(source_array.size()); aggregationDestinations_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                aggregationDestinations_strings.emplace_back(String(source_array[i]).utf8().get_data());
                aggregationDestinations_ptrs.push_back(aggregationDestinations_strings.back().c_str());
            }
            value.aggregationDestinations = aggregationDestinations_ptrs.empty() ? nullptr : aggregationDestinations_ptrs.data();
            value.aggregationDestinationsCount = static_cast<uint32_t>(aggregationDestinations_ptrs.size());
        }
        if (get_request_value(p_request, "aggregationSources", "aggregation_sources", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field aggregation_sources."; return false; }
            Array source_array = field_value;
            aggregationSources_strings.clear(); aggregationSources_ptrs.clear();
            aggregationSources_strings.reserve(source_array.size()); aggregationSources_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                aggregationSources_strings.emplace_back(String(source_array[i]).utf8().get_data());
                aggregationSources_ptrs.push_back(aggregationSources_strings.back().c_str());
            }
            value.aggregationSources = aggregationSources_ptrs.empty() ? nullptr : aggregationSources_ptrs.data();
            value.aggregationSourcesCount = static_cast<uint32_t>(aggregationSources_ptrs.size());
        }
        if (get_request_value(p_request, "columns", "columns", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field columns."; return false; }
            Array source_array = field_value;
            columns_items.resize(source_array.size());
            columns_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field columns."; return false; }
                if (!columns_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                columns_ptrs[static_cast<size_t>(i)] = &columns_items[static_cast<size_t>(i)].value;
            }
            value.columns = columns_ptrs.empty() ? nullptr : columns_ptrs.data();
            value.columnsCount = static_cast<uint32_t>(columns_ptrs.size());
        }
        if (get_request_value(p_request, "created", "created", &field_value)) {
            value.created = static_cast<time_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "entityType", "entity_type", &field_value)) {
            entityType_string = String(field_value).utf8().get_data();
            value.entityType = entityType_string.empty() ? nullptr : entityType_string.c_str();
        }
        if (get_request_value(p_request, "eventEmissionConfig", "event_emission_config", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field event_emission_config."; return false; }
            if (!eventEmissionConfig_owner.from_dictionary(field_value, r_error)) return false;
            eventEmissionConfig_has_value = true;
            value.eventEmissionConfig = &eventEmissionConfig_owner.value;
        }
        if (get_request_value(p_request, "lastResetTime", "last_reset_time", &field_value)) {
            lastResetTime_storage = static_cast<time_t>(static_cast<int64_t>(field_value));
            value.lastResetTime = &lastResetTime_storage;
        }
        if (get_request_value(p_request, "linkedLeaderboardNames", "linked_leaderboard_names", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field linked_leaderboard_names."; return false; }
            Array source_array = field_value;
            linkedLeaderboardNames_strings.clear(); linkedLeaderboardNames_ptrs.clear();
            linkedLeaderboardNames_strings.reserve(source_array.size()); linkedLeaderboardNames_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                linkedLeaderboardNames_strings.emplace_back(String(source_array[i]).utf8().get_data());
                linkedLeaderboardNames_ptrs.push_back(linkedLeaderboardNames_strings.back().c_str());
            }
            value.linkedLeaderboardNames = linkedLeaderboardNames_ptrs.empty() ? nullptr : linkedLeaderboardNames_ptrs.data();
            value.linkedLeaderboardNamesCount = static_cast<uint32_t>(linkedLeaderboardNames_ptrs.size());
        }
        if (get_request_value(p_request, "name", "name", &field_value)) {
            name_string = String(field_value).utf8().get_data();
            value.name = name_string.empty() ? nullptr : name_string.c_str();
        }
        if (get_request_value(p_request, "version", "version", &field_value)) {
            value.version = static_cast<uint32_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "versionConfiguration", "version_configuration", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field version_configuration."; return false; }
            if (!versionConfiguration_owner.from_dictionary(field_value, r_error)) return false;
            versionConfiguration_has_value = true;
            value.versionConfiguration = &versionConfiguration_owner.value;
        }
        return true;
    }
};

struct OwnedPFStatisticsListStatisticDefinitionsResponse {
    PFStatisticsListStatisticDefinitionsResponse value = {};
    std::string skipToken_string;
    std::vector<OwnedPFStatisticsStatisticDefinition> statisticDefinitions_items;
    std::vector<const PFStatisticsStatisticDefinition *> statisticDefinitions_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "pageSize", "page_size", &field_value)) {
            value.pageSize = static_cast<int32_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "skipToken", "skip_token", &field_value)) {
            skipToken_string = String(field_value).utf8().get_data();
            value.skipToken = skipToken_string.empty() ? nullptr : skipToken_string.c_str();
        }
        if (get_request_value(p_request, "statisticDefinitions", "statistic_definitions", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field statistic_definitions."; return false; }
            Array source_array = field_value;
            statisticDefinitions_items.resize(source_array.size());
            statisticDefinitions_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field statistic_definitions."; return false; }
                if (!statisticDefinitions_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                statisticDefinitions_ptrs[static_cast<size_t>(i)] = &statisticDefinitions_items[static_cast<size_t>(i)].value;
            }
            value.statisticDefinitions = statisticDefinitions_ptrs.empty() ? nullptr : statisticDefinitions_ptrs.data();
            value.statisticDefinitionsCount = static_cast<uint32_t>(statisticDefinitions_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFStatisticsStatisticUpdate {
    PFStatisticsStatisticUpdate value = {};
    std::string metadata_string;
    std::string name_string;
    std::vector<std::string> scores_strings;
    std::vector<const char *> scores_ptrs;
    uint32_t version_storage = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "metadata", "metadata", &field_value)) {
            metadata_string = String(field_value).utf8().get_data();
            value.metadata = metadata_string.empty() ? nullptr : metadata_string.c_str();
        }
        if (get_request_value(p_request, "name", "name", &field_value)) {
            name_string = String(field_value).utf8().get_data();
            value.name = name_string.empty() ? nullptr : name_string.c_str();
        }
        if (get_request_value(p_request, "scores", "scores", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field scores."; return false; }
            Array source_array = field_value;
            scores_strings.clear(); scores_ptrs.clear();
            scores_strings.reserve(source_array.size()); scores_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                scores_strings.emplace_back(String(source_array[i]).utf8().get_data());
                scores_ptrs.push_back(scores_strings.back().c_str());
            }
            value.scores = scores_ptrs.empty() ? nullptr : scores_ptrs.data();
            value.scoresCount = static_cast<uint32_t>(scores_ptrs.size());
        }
        if (get_request_value(p_request, "version", "version", &field_value)) {
            version_storage = static_cast<uint32_t>(static_cast<int64_t>(field_value));
            value.version = &version_storage;
        }
        return true;
    }
};

struct OwnedPFStatisticsUpdateStatisticDefinitionRequest {
    PFStatisticsUpdateStatisticDefinitionRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFStatisticsStatisticsEventEmissionConfig eventEmissionConfig_owner;
    bool eventEmissionConfig_has_value = false;
    std::string name_string;
    OwnedPFVersionConfiguration versionConfiguration_owner;
    bool versionConfiguration_has_value = false;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "eventEmissionConfig", "event_emission_config", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field event_emission_config."; return false; }
            if (!eventEmissionConfig_owner.from_dictionary(field_value, r_error)) return false;
            eventEmissionConfig_has_value = true;
            value.eventEmissionConfig = &eventEmissionConfig_owner.value;
        }
        if (get_request_value(p_request, "name", "name", &field_value)) {
            name_string = String(field_value).utf8().get_data();
            value.name = name_string.empty() ? nullptr : name_string.c_str();
        }
        if (get_request_value(p_request, "versionConfiguration", "version_configuration", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field version_configuration."; return false; }
            if (!versionConfiguration_owner.from_dictionary(field_value, r_error)) return false;
            versionConfiguration_has_value = true;
            value.versionConfiguration = &versionConfiguration_owner.value;
        }
        return true;
    }
};

struct OwnedPFStatisticsUpdateStatisticsRequest {
    PFStatisticsUpdateStatisticsRequest value = {};
    std::vector<OwnedPFStringDictionaryEntry> customTags_items;
    std::vector<PFStringDictionaryEntry> customTags_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::vector<OwnedPFStatisticsStatisticUpdate> statistics_items;
    std::vector<const PFStatisticsStatisticUpdate *> statistics_ptrs;
    std::string transactionId_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "customTags", "custom_tags", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field custom_tags."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            customTags_items.resize(keys.size());
            customTags_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!customTags_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                customTags_values[static_cast<size_t>(i)] = customTags_items[static_cast<size_t>(i)].value;
            }
            value.customTags = customTags_values.empty() ? nullptr : customTags_values.data();
            value.customTagsCount = static_cast<uint32_t>(customTags_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "statistics", "statistics", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field statistics."; return false; }
            Array source_array = field_value;
            statistics_items.resize(source_array.size());
            statistics_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field statistics."; return false; }
                if (!statistics_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                statistics_ptrs[static_cast<size_t>(i)] = &statistics_items[static_cast<size_t>(i)].value;
            }
            value.statistics = statistics_ptrs.empty() ? nullptr : statistics_ptrs.data();
            value.statisticsCount = static_cast<uint32_t>(statistics_ptrs.size());
        }
        if (get_request_value(p_request, "transactionId", "transaction_id", &field_value)) {
            transactionId_string = String(field_value).utf8().get_data();
            value.transactionId = transactionId_string.empty() ? nullptr : transactionId_string.c_str();
        }
        return true;
    }
};

struct OwnedPFStatisticsUpdateStatisticsResponse {
    PFStatisticsUpdateStatisticsResponse value = {};
    std::vector<OwnedPFStatisticsStatisticColumnCollectionDictionaryEntry> columnDetails_items;
    std::vector<PFStatisticsStatisticColumnCollectionDictionaryEntry> columnDetails_values;
    OwnedPFEntityKey entity_owner;
    bool entity_has_value = false;
    std::vector<OwnedPFStatisticsEntityStatisticValueDictionaryEntry> statistics_items;
    std::vector<PFStatisticsEntityStatisticValueDictionaryEntry> statistics_values;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "columnDetails", "column_details", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field column_details."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            columnDetails_items.resize(keys.size());
            columnDetails_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!columnDetails_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                columnDetails_values[static_cast<size_t>(i)] = columnDetails_items[static_cast<size_t>(i)].value;
            }
            value.columnDetails = columnDetails_values.empty() ? nullptr : columnDetails_values.data();
            value.columnDetailsCount = static_cast<uint32_t>(columnDetails_values.size());
        }
        if (get_request_value(p_request, "entity", "entity", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field entity."; return false; }
            if (!entity_owner.from_dictionary(field_value, r_error)) return false;
            entity_has_value = true;
            value.entity = &entity_owner.value;
        }
        if (get_request_value(p_request, "statistics", "statistics", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field statistics."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            statistics_items.resize(keys.size());
            statistics_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!statistics_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                statistics_values[static_cast<size_t>(i)] = statistics_items[static_cast<size_t>(i)].value;
            }
            value.statistics = statistics_values.empty() ? nullptr : statistics_values.data();
            value.statisticsCount = static_cast<uint32_t>(statistics_values.size());
        }
        return true;
    }
};

struct OwnedPFTitleDataManagementGetPublisherDataRequest {
    PFTitleDataManagementGetPublisherDataRequest value = {};
    std::vector<std::string> keys_strings;
    std::vector<const char *> keys_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "keys", "keys", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field keys."; return false; }
            Array source_array = field_value;
            keys_strings.clear(); keys_ptrs.clear();
            keys_strings.reserve(source_array.size()); keys_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                keys_strings.emplace_back(String(source_array[i]).utf8().get_data());
                keys_ptrs.push_back(keys_strings.back().c_str());
            }
            value.keys = keys_ptrs.empty() ? nullptr : keys_ptrs.data();
            value.keysCount = static_cast<uint32_t>(keys_ptrs.size());
        }
        return true;
    }
};

struct OwnedPFTitleDataManagementGetPublisherDataResult {
    PFTitleDataManagementGetPublisherDataResult value = {};
    std::vector<OwnedPFStringDictionaryEntry> data_items;
    std::vector<PFStringDictionaryEntry> data_values;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "data", "data", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field data."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            data_items.resize(keys.size());
            data_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!data_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                data_values[static_cast<size_t>(i)] = data_items[static_cast<size_t>(i)].value;
            }
            value.data = data_values.empty() ? nullptr : data_values.data();
            value.dataCount = static_cast<uint32_t>(data_values.size());
        }
        return true;
    }
};

struct OwnedPFTitleDataManagementGetTimeResult {
    PFTitleDataManagementGetTimeResult value = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "time", "time", &field_value)) {
            value.time = static_cast<time_t>(static_cast<int64_t>(field_value));
        }
        return true;
    }
};

struct OwnedPFTitleDataManagementGetTitleDataRequest {
    PFTitleDataManagementGetTitleDataRequest value = {};
    std::vector<std::string> keys_strings;
    std::vector<const char *> keys_ptrs;
    std::string overrideLabel_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "keys", "keys", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field keys."; return false; }
            Array source_array = field_value;
            keys_strings.clear(); keys_ptrs.clear();
            keys_strings.reserve(source_array.size()); keys_ptrs.reserve(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                keys_strings.emplace_back(String(source_array[i]).utf8().get_data());
                keys_ptrs.push_back(keys_strings.back().c_str());
            }
            value.keys = keys_ptrs.empty() ? nullptr : keys_ptrs.data();
            value.keysCount = static_cast<uint32_t>(keys_ptrs.size());
        }
        if (get_request_value(p_request, "overrideLabel", "override_label", &field_value)) {
            overrideLabel_string = String(field_value).utf8().get_data();
            value.overrideLabel = overrideLabel_string.empty() ? nullptr : overrideLabel_string.c_str();
        }
        return true;
    }
};

struct OwnedPFTitleDataManagementGetTitleDataResult {
    PFTitleDataManagementGetTitleDataResult value = {};
    std::vector<OwnedPFStringDictionaryEntry> data_items;
    std::vector<PFStringDictionaryEntry> data_values;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "data", "data", &field_value)) {
            if (field_value.get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary for field data."; return false; }
            Dictionary source_dictionary = field_value;
            Array keys = source_dictionary.keys();
            data_items.resize(keys.size());
            data_values.resize(keys.size());
            for (int64_t i = 0; i < keys.size(); ++i) {
                Dictionary entry_dictionary;
                entry_dictionary["key"] = keys[i];
                entry_dictionary["value"] = source_dictionary[keys[i]];
                if (!data_items[static_cast<size_t>(i)].from_dictionary(entry_dictionary, r_error)) return false;
                data_values[static_cast<size_t>(i)] = data_items[static_cast<size_t>(i)].value;
            }
            value.data = data_values.empty() ? nullptr : data_values.data();
            value.dataCount = static_cast<uint32_t>(data_values.size());
        }
        return true;
    }
};

struct OwnedPFTitleDataManagementGetTitleNewsRequest {
    PFTitleDataManagementGetTitleNewsRequest value = {};
    int32_t count_storage = {};

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "count", "count", &field_value)) {
            count_storage = static_cast<int32_t>(static_cast<int64_t>(field_value));
            value.count = &count_storage;
        }
        return true;
    }
};

struct OwnedPFTitleDataManagementTitleNewsItem {
    PFTitleDataManagementTitleNewsItem value = {};
    std::string body_string;
    std::string newsId_string;
    std::string title_string;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "body", "body", &field_value)) {
            body_string = String(field_value).utf8().get_data();
            value.body = body_string.empty() ? nullptr : body_string.c_str();
        }
        if (get_request_value(p_request, "newsId", "news_id", &field_value)) {
            newsId_string = String(field_value).utf8().get_data();
            value.newsId = newsId_string.empty() ? nullptr : newsId_string.c_str();
        }
        if (get_request_value(p_request, "timestamp", "timestamp", &field_value)) {
            value.timestamp = static_cast<time_t>(static_cast<int64_t>(field_value));
        }
        if (get_request_value(p_request, "title", "title", &field_value)) {
            title_string = String(field_value).utf8().get_data();
            value.title = title_string.empty() ? nullptr : title_string.c_str();
        }
        return true;
    }
};

struct OwnedPFTitleDataManagementGetTitleNewsResult {
    PFTitleDataManagementGetTitleNewsResult value = {};
    std::vector<OwnedPFTitleDataManagementTitleNewsItem> news_items;
    std::vector<const PFTitleDataManagementTitleNewsItem *> news_ptrs;

    bool from_dictionary(const Dictionary &p_request, String *r_error) {
        Variant field_value;
        if (get_request_value(p_request, "news", "news", &field_value)) {
            if (field_value.get_type() != Variant::ARRAY) { if (r_error) *r_error = "Expected Array for field news."; return false; }
            Array source_array = field_value;
            news_items.resize(source_array.size());
            news_ptrs.resize(source_array.size());
            for (int64_t i = 0; i < source_array.size(); ++i) {
                if (source_array[i].get_type() != Variant::DICTIONARY) { if (r_error) *r_error = "Expected Dictionary array item for field news."; return false; }
                if (!news_items[static_cast<size_t>(i)].from_dictionary(source_array[i], r_error)) return false;
                news_ptrs[static_cast<size_t>(i)] = &news_items[static_cast<size_t>(i)].value;
            }
            value.news = news_ptrs.empty() ? nullptr : news_ptrs.data();
            value.newsCount = static_cast<uint32_t>(news_ptrs.size());
        }
        return true;
    }
};

} }
#endif
